---
quick_id: 260625-q08
title: Fix G1 cacheKey contract + reconcile blueprint doc
date: 2026-06-25
branch: feature/q08-cachekey-contract
source: .planning/BLUEPRINT-DELTA.md (gap G1)
status: in-progress
---

# Quick Task 260625-q08 — cacheKey contract (G1) + doc reconcile

## Goal
Close gap **G1** from the blueprint delta: the registry key dropped `kind` and
`prompt`, risking app/widget collisions and inconsistent prompt-variant keying.
Fold `kind` + normalized `type` + normalized `prompt` into one opaque digest.
Then reconcile `docs/vibeappstore.md` so the blueprint stops contradicting the
built reality (delegated model, flat token budget, host/services layers, etc.).

## Task 1 — `registryKey(kind, type, prompt?)` (TDD)
- **files:** `src/registry/cacheKey.ts`, `src/registry/cacheKey.test.ts`
- **action:** Extract `normalizePart` (NFC + lowercase + trim + collapse-ws).
  Keep `cacheKey(input)` byte-identical (opaque-hash primitive — its 7 tests stay
  green). Add `RegistryKind = "app"|"widget"|"handler"` and
  `registryKey(kind, type, prompt="")` = `cacheKey([kind, normalizePart(type),
  normalizePart(prompt)].join(""))`. The U+001F unit separator survives
  normalization, so field boundaries never blur; each part is normalized on its
  own so prompt whitespace/case is equivalent.
- **verify:** new `registryKey` describe block — determinism, kind-distinctness
  (app≠widget≠handler for same slug), prompt-distinctness, prompt+type
  normalization equivalence, no field-boundary blur, 64-hex opacity, unicode-safe.
- **done:** `cacheKey.test.ts` green (old + new).

## Task 2 — migrate the 5 call sites + consuming tests
- **internal-derivation sites (MUST change — break otherwise):**
  - `src/intent/resolver.ts:43` → `registryKey("app", appType)` (+ test `resolver.test.ts:25`)
  - `src/execution/handler.ts:189` → `registryKey("handler", intent)` (+ test `handler.test.ts:134,163,407`)
  - `src/execution/widgetPrewarm.ts:61` → `registryKey("widget", widgetType)` (+ test `widgetPrewarm.test.tsx:172,205`)
  - `src/execution/widgetPrewarm.ts:133` → `registryKey("widget", widgetType, instruction)`
- **pass-through sites (self-consistent; update for fidelity):**
  - `src/ui/Marketplace.tsx:240` → `registryKey("app", target.appType, routed.instruction)`
  - `src/execution/tweakResolve.test.ts:56,80` → `registryKey("app", type, instruction)`
- **note:** `loader.test.ts` / `loaderGuardrails.test.ts` construct-and-PASS keys
  to `resolveComponent` (which takes the key as a param) — self-consistent, no
  change needed; they test tiering, not derivation.
- **verify:** full suite green (368+), `tsc --noEmit` 0, `vite build` clean (no maps).

## Task 3 — reconcile `docs/vibeappstore.md`
- delegated thin-shell as the default for unseeded apps (seeds monolithic +
  graceful fallback); flat `MAX_TOKENS=8192`; simplified theming vars
  (`--color-surface/-text/-accent`); `host/` + `services/` layers; the
  `anthropic-dangerous-direct-browser-access` header; the new cacheKey contract.
- **constraint:** devtools-hygiene hard rule intact — no banned lexicon in any
  devtools-visible surface (the doc itself is not shipped, but keep discipline).

## Quality gates
TDD throughout · IoC/DI preserved · full suite + tsc + build green BEFORE commit ·
git-flow (feature branch; merge handled separately) · hygiene gate green.
