---
milestone: "2.0"
milestone_name: "Vibe OS"
audited: "2026-06-27T04:45:40Z"
auditor: "autonomous-orchestrator (gsd-audit-milestone + gsd-integration-checker)"
status: tech_debt
scores:
  requirements: "21/21"
  phases: "5/5"
  integration: "21/21"
  flows: "5/5"
gaps: []
tech_debt:
  - phase: "14-theme-foundation"
    items:
      - "REQUIREMENTS.md traceability table checkboxes not updated: THEME-05 still shows '[ ]' and 'Pending' even though VERIFICATION.md confirms it passed. Docs staleness — not a functional gap."
      - "ThemeSelector.tsx line 20 carries a stale forward-reference comment: '// home in the AppBar for Phase 14; Phase 16 relocates this into the menu bar.' — Phase 16 is now shipped; comment should read 'relocated to MenuBar (Phase 16 complete)'."
  - phase: "15-window-manager"
    items:
      - "REQUIREMENTS.md traceability table: WIN-01..05 still show '[ ]' / 'Pending'. All verified — docs-only staleness."
      - "Phase 15 SUMMARY.md files have no 'requirements-completed' YAML field (unlike Phases 14, 16, 17). Minor frontmatter inconsistency."
      - "Architectural deviation documented (sound): windows render in-tree as React subtrees rather than separate createRoot roots. The deviation eliminates the root-leak class and is net-positive, but the REQUIREMENTS.md literal 'mounted in its own React root' (WIN-02) is technically unsatisfied. The VERIFICATION.md correctly notes the intent (concurrent, independent) is met and calls the deviation sound."
      - "Stale comment in Dock.tsx line 18: '/** Opens the launcher (Phase 17 will replace the stub). */' — Phase 17 has replaced MinimalLauncher; comment should be updated to 'Opens the SearchLauncherPanel (Phase 17, CREATE-01).'."
  - phase: "16-desktop-shell"
    items:
      - "REQUIREMENTS.md traceability: WIN-06..08 and PERF-01 still show '[ ]' / 'Pending'. All verified — docs staleness."
      - "Cosmetic: Notes window content appears 'a touch cramped'; default window sizing could be tuned. Non-blocking, noted in 16-VERIFICATION.md."
      - "index.css line 115: 'Temporary AppBar home (Phase 16 restyles…)' — ThemeSelector was already moved to MenuBar in Phase 16; comment is stale."
  - phase: "17-search-launcher"
    items:
      - "REQUIREMENTS.md traceability: CREATE-01..03 still show '[ ]' / 'Pending'. All verified — docs staleness."
      - "CSS gap (integration-checker, VERIFIED): 6 classes are applied in SearchLauncherPanel.tsx / Dock.tsx but have NO rule in src/index.css — .launcher__search, .launcher__input, .launcher__open-btn, .launcher__working, .launcher__chips, .launcher__chip (and the .dock__launch magnifier modifier). Phase 16 added MinimalLauncher CSS (.launcher, .launcher__grid, .launcher__app-btn, .launcher__close — these DO exist); Phase 17 replaced MinimalLauncher with the richer SearchLauncherPanel but never extended index.css for the new interior elements. The panel is fully FUNCTIONAL (ARIA roles, event handlers, state all wired; 0 test failures — tests query by role/label, not class), but the search input, submit button, example chips, and working indicator render with browser-default styling and no themed treatment. Affects CREATE-01 (panel visual completeness) and CREATE-02 (working-state affordance polish). Visual-only — no functional or hygiene gap."
  - phase: "18-theme-aware-generation"
    items:
      - "REQUIREMENTS.md traceability: TGEN-01..03, HYGIENE-06 still show '[ ]' / 'Pending'. All verified — docs staleness."
      - "No per-plan SUMMARY.md files created for Phase 18 (18-01 through 18-04-SUMMARY.md absent). Execution was a single atomic commit rather than plan-by-plan. Minor process deviation; all verification evidence exists in 18-VERIFICATION.md."
      - "One cosmetic code-review finding accepted (IN-02 fixture :root shim, test-quality only — produces no user-facing issue)."
      - "LIVE end-to-end visual verification (fresh produce + theme switch → re-skin) deferred to user with API key. Verified offline via captured-Haiku fixtures per the accepted 'key story is the user's to own' steer."
cross_cutting:
  - "REQUIREMENTS.md traceability table is globally stale: 17 of 21 requirements still show Pending/[ ] despite all 5 VERIFICATION.md files reporting status: passed. This is the single highest-priority documentation tech debt item — should be updated as part of gsd-complete-milestone."
  - "Build chunk size warning (3.2 MB JS / 762 KB gzipped): pre-existing Babel-standalone bundle noise. Not new to v2.0. No source maps emitted. Tracked in BLUEPRINT-DELTA.md as a known Babel-bundle cost."
  - "index.html <title> is still 'Marketplace' (pre-v2.0 name), not 'Vibe OS'. No banned tokens so hygiene-clean; minor cosmetic naming gap (integration-checker)."
dismissed_findings:
  - "integration-checker flagged WIN-03 drag clamping as 'placement-time only' (user could drag a window off-screen). DISMISSED after source verification: src/ui/useDrag.ts calls clamp(raw) INSIDE the pointermove handler (line 69) using window.innerWidth/innerHeight, not only on commit (line 98). Drag IS clamped to the viewport in real time. WIN-03 'drag (clamped)' is fully satisfied."
nyquist:
  overall: "SKIPPED"
  reason: "workflow.nyquist_validation = false (explicit config)"
---

# Milestone v2.0 "Vibe OS" — Audit Report

**Audited:** 2026-06-27
**Status: TECH DEBT** — all 21 requirements satisfied, no critical blockers. Accumulated documentation staleness and minor cosmetic items need attention before or during milestone completion.

---

## Executive Summary

All 5 phases (14–18) merged to `develop` with --no-ff. All 5 VERIFICATION.md files report `status: passed`. The 727-test suite is green, `tsc` reports 0 errors, the production build emits no source maps, and the hygiene + CSP gates pass. The primary tech debt is **documentation staleness** — REQUIREMENTS.md checkboxes and traceability table were not updated post-execution — plus **three stale source comments** with forward references to phases now completed and **one verified visual-only CSS gap** (the Phase-17 SearchLauncherPanel's interior elements have no themed CSS rules). The integration checker also flagged a drag-clamp concern that was **verified-and-dismissed** (drag IS clamped in real time in `useDrag.ts`).

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|-----------------|---------------------|-----------------|--------------|
| THEME-01 | 14 | passed | THEME-01 listed (14-05) | `[x]` Complete | **satisfied** |
| THEME-02 | 14 | passed | THEME-02 listed (14-03, 14-05) | `[x]` Complete | **satisfied** |
| THEME-03 | 14 | passed | THEME-03 listed (14-04) | `[x]` Complete | **satisfied** |
| THEME-04 | 14 | passed | THEME-04 listed (14-03, 14-04) | `[x]` Complete | **satisfied** |
| THEME-05 | 14 | passed | *(14-02 PLAN references THEME-05; no SUMMARY frontmatter)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-01 | 15 | passed | *(no requirements-completed in Phase 15 SUMMARYs)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-02 | 15 | passed | *(no requirements-completed in Phase 15 SUMMARYs)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-03 | 15 | passed | *(no requirements-completed in Phase 15 SUMMARYs)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-04 | 15 | passed | *(no requirements-completed in Phase 15 SUMMARYs)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-05 | 15 | passed | *(no requirements-completed in Phase 15 SUMMARYs)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-06 | 16 | passed | *(16-02 SUMMARY body mentions WIN-06; no frontmatter field)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-07 | 16 | passed | *(16-02 SUMMARY body mentions WIN-07; no frontmatter field)* | `[ ]` Pending | **satisfied** ¹ |
| WIN-08 | 16 | passed | WIN-08 listed (16-03, 16-04) | `[ ]` Pending | **satisfied** ¹ |
| PERF-01 | 16 | passed | PERF-01 listed (16-04) | `[ ]` Pending | **satisfied** ¹ |
| CREATE-01 | 17 | passed | CREATE-01 listed (17-04) | `[ ]` Pending | **satisfied** ¹ ² |
| CREATE-02 | 17 | passed | CREATE-02 listed (17-04) | `[ ]` Pending | **satisfied** ¹ ² |
| CREATE-03 | 17 | passed | CREATE-03 listed (17-04) | `[ ]` Pending | **satisfied** ¹ |
| TGEN-01 | 18 | passed | *(no Phase 18 SUMMARY.md files exist)* | `[ ]` Pending | **satisfied** ¹ |
| TGEN-02 | 18 | passed | *(no Phase 18 SUMMARY.md files exist)* | `[ ]` Pending | **satisfied** ¹ |
| TGEN-03 | 18 | passed | *(no Phase 18 SUMMARY.md files exist)* | `[ ]` Pending | **satisfied** ¹ |
| HYGIENE-06 | 18 | passed | *(no Phase 18 SUMMARY.md files exist)* | `[ ]` Pending | **satisfied** ¹ |

¹ **REQUIREMENTS.md checkbox/traceability table is stale** — the requirement was verified as passed in VERIFICATION.md but the docs were not updated post-execution. This is documentation tech debt, not a functional gap. All 21/21 requirements are functionally satisfied.

² **CREATE-01/02 functionally satisfied; visual-only CSS gap** — the SearchLauncherPanel's interior elements (search input, submit button, example chips, working indicator) have no `index.css` rules and render with browser defaults. Verified by the integration checker and confirmed against source. The flow works end-to-end; only visual polish is incomplete. See Tech Debt Register item 7.

**Orphaned requirements:** None — all 21 REQ-IDs appear in at least one phase VERIFICATION.md.

---

## Phase Verification Summary

| Phase | Status | Tests at Completion | Key Evidence |
|-------|--------|---------------------|--------------|
| 14 Theme Foundation | PASSED | 570 / 570 | Live browser smoke: 4-pill switch, live apply, FOUC reload, alias bridge, persistence. 0 console errors. |
| 15 Window Manager | PASSED | 600 / 600 | Viewed screenshots: Notes + Currency in glass WindowFrames; multi-window, focus, close. CR-01 drag bug fixed. |
| 16 Desktop Shell | PASSED | 636 / 636 | Viewed Aurora-vs-Noir screenshots: full re-skin, wallpaper+blobs, dock, menu bar, launcher→window. |
| 17 Search / Launcher Panel | PASSED | 669 / 669 | Viewed screenshots: SearchLauncherPanel (input + chips + grid); describe→produce→window; neutral key-missing state. |
| 18 Theme-Aware Generation | PASSED | 727 / 727 | Offline fixtures + no-regression boot smoke. colorCheck 44/44. Prompt contract mandated for all 4 produce kinds. |

**Current suite (develop HEAD):** 727 / 727 passing (83 files). `tsc` 0 errors. Build clean. No source maps. Hygiene gate 6/6 green.

---

## Cross-Phase Integration

### Phase 14 → 15 → 16 (Theme → Window → Desktop)

**VERIFIED.** `VibeThemeProvider` (Phase 14) wraps `DesktopShell` (Phase 16) in `App.tsx`. CSS custom properties are applied on `document.documentElement` via `style.setProperty`, reaching all window subtrees independently of how they're mounted (in-tree React subtrees share the document root). `WindowFrame` glass chrome references `--glass`, `--bord`, `--hi`, `--text` (Phase 14 contract). Phase 16 VERIFICATION confirms visible Aurora/Noir theme re-skin of the full screen.

### Phase 15 → 16 (Window Manager → Desktop Shell)

**VERIFIED.** `DesktopShell` consumes `useWindowManager`; `Dock` receives the `windows` array via props (IoC/DI leaf). `MenuBar` receives `activeName` (derived from focused window's title). Running-indicator dot per `WindowEntry` in dock confirmed in Phase 16 smoke.

### Phase 16 → 17 (Desktop Shell → Search / Launcher Panel)

**VERIFIED (logic).** `DesktopShell` passes `onOpenLauncher` to `Dock`; clicking the magnifier sets `launcherOpen=true`, rendering `SearchLauncherPanel`. `handleDescribe` in `DesktopShell` calls `resolveComponent(userPrompt=text)` with `registryKey("app", slug, text)` and routes the result through `useWindowManager.open()`. `MinimalLauncher` deleted (Phase 17-04 confirmed 0 imports).

**WARNING (CSS, visual-only) — confirmed by integration checker + source verification:** Six classes used by the Phase-17 panel/dock have no `index.css` rule: `.launcher__search`, `.launcher__input`, `.launcher__open-btn`, `.launcher__working`, `.launcher__chips`, `.launcher__chip`, plus the `.dock__launch` magnifier modifier. Phase 16 shipped CSS for the MinimalLauncher stub (`.launcher`, `.launcher__grid`, `.launcher__app-btn`, `.launcher__close` — present); Phase 17's richer panel added new interior structural classes that were never styled. The panel functions correctly (ARIA + events + state wired; 727/727 tests green — tests query by role/label), but the search input/button/chips/working indicator render with browser defaults. Affects CREATE-01 / CREATE-02 visual polish; no functional break.

**Stale JSDoc in Dock.tsx:** line 18 reads `/** Opens the launcher (Phase 17 will replace the stub). */` — Phase 17 has replaced the stub; comment should read `/** Opens the SearchLauncherPanel (Phase 17, CREATE-01). */`. Non-blocking tech debt.

### Phase 14 + 17 → 18 (Theme Contract + Produce Loop → Theme-Aware Generation)

**VERIFIED.** `src/execution/producer.ts` `buildPrompt`, `buildLengthPrompt`, `buildRepairPrompt` all mandate the CSS-var contract (`var(--accentA/--accentB/--text/--glass/--glass2/--bord/--hi)`). `colorCheck.ts` wired post-transpile for `kind !== "handler"` — violation feeds `TranspileError` into existing self-heal loop (≤3 retries). `sanitizeDisplayName` wired in `useWindowManager.open()` before `displayName` reaches titlebar/dock/MenuBar. 7 previously-saturated fixtures re-skinned to theme vars. Phase 18 VERIFICATION confirms 44/44 colorCheck + sanitize tests green.

### E2E Flow Verification

| Flow | Status | Evidence |
|------|--------|---------|
| Theme switch → full re-skin | PASS | Phase 16 smoke: Aurora→Noir re-skins wallpaper, blobs, glass chrome |
| Describe → produce → window on desktop | PASS | Phase 17 smoke: "a pomodoro timer with rain sounds" → WindowFrame titled "Pomodoro Timer With Rain Sounds" |
| Window lifecycle (open→focus→minimize→restore→close) | PASS | WIN-01..05 all verified: display:none minimize; appBodyCount() baseline on close; no root leak |
| Pre-installed app → desktop + dock | PASS | Phase 16 smoke: Notes opened from launcher appears in dock with running dot |
| Cache hit → instant window (no produce round-trip) | PASS | Phase 17 test: counting-transport wrapper; 1 call total across 2 describes of same text |
| Produced app name sanitization | PASS | Phase 18: sanitizeDisplayName behavioral test; "AI Weather" → "Weather" in titlebar/dock/menu |
| FOUC-safe theme reload | PASS | Phase 14 smoke: `htmlHasInlineVars=true` before React mounts with `osTheme=noir` persisted |

---

## Tech Debt Register

### Priority 1 — Documentation (low effort, high clarity)

1. **REQUIREMENTS.md stale checkboxes (17 of 21):** Update `[ ]` to `[x]` for WIN-01..05, WIN-06..08, PERF-01, CREATE-01..03, TGEN-01..03, HYGIENE-06, THEME-05. Update traceability Status column from "Pending" to "Complete" for same. → Do as part of `gsd-complete-milestone`.

2. **Phase 15 SUMMARY frontmatter missing `requirements-completed`:** Phases 14, 16, 17 have this field; Phase 15 SUMMARYs do not. Optionally add `requirements-completed: [WIN-01, WIN-02, WIN-03, WIN-04, WIN-05]` to 15-04-SUMMARY.md.

3. **Phase 18 SUMMARY.md files absent:** 18-01 through 18-04 SUMMARYs were never written; Phase 18 executed as a single atomic commit. Optionally create a combined `18-SUMMARY.md` to close the gap.

### Priority 2 — Stale Comments (low effort, dev hygiene)

4. **Dock.tsx line 18:** `/** Opens the launcher (Phase 17 will replace the stub). */` → `/** Opens the SearchLauncherPanel (Phase 17, CREATE-01). */`

5. **ThemeSelector.tsx line 20:** `// home in the AppBar for Phase 14; Phase 16 relocates this into the menu bar.` → `// relocated to MenuBar (Phase 16, WIN-07).`

6. **index.css line 115:** `/* Inline row of named-theme pills. Temporary AppBar home (Phase 16 restyles` → update to reflect that Phase 16 is complete.

### Priority 3 — Visual / Non-blocking UX

7. **SearchLauncherPanel CSS gap (CREATE-01, CREATE-02 — visual only):** Six classes used in `SearchLauncherPanel.tsx` / `Dock.tsx` have no `index.css` rule — `.launcher__search`, `.launcher__input`, `.launcher__open-btn`, `.launcher__working`, `.launcher__chips`, `.launcher__chip`, `.dock__launch`. Verified by integration checker and confirmed against source (used in JSX, absent from CSS). The panel is fully functional (727/727 green; tests query by role/label) but the search input, submit button, chips, and working indicator have browser-default styling and don't pick up the theme glass treatment. Recommend a quick CSS extension pass adding themed rules for these elements (use `var(--glass)`, `var(--bord)`, `var(--text)`, `var(--accentA)` like the sibling `.launcher__app-btn`).

8. **Window default sizing:** Notes window content "a touch cramped" (Phase 16 VERIFICATION). Default window dimensions could be tuned for better content fit. Non-blocking — no requirement specifies window sizing.

9. **Cascade placement:** Phase 15 VERIFICATION noted windows appeared "spread top-left/bottom-right rather than gently cascaded." Phase 16-01 improved this but a final polish pass could refine the cascade step.

10. **index.html `<title>`:** Still "Marketplace" (pre-v2.0 name), not "Vibe OS". Hygiene-clean (no banned token); minor naming cosmetic.

### Priority 4 — Architecture Notes (tracked, intentional)

11. **WIN-02 literal vs intent:** REQUIREMENTS.md says "each mounted in its own React root." The implementation uses in-tree React subtrees (a sound architectural deviation documented in Phase 15 VERIFICATION). Intent (concurrent, independent) is met. The REQUIREMENTS.md literal is slightly inaccurate post-deviation. Consider updating the requirement text to "each independently managed by the window manager" in the v2.0 archive copy.

12. **LIVE theme-aware generation verification deferred:** The E2E (produce a fresh app → switch theme → re-skin) requires the user's Anthropic API key. Verified offline via captured-Haiku fixtures. Consistent with the accepted "key story is the user's to own" steer from memory. Not a gap.

### Dismissed Finding (verified, not tech debt)

- **WIN-03 drag clamping:** The integration checker flagged that user drag might not be viewport-clamped (only cascade placement). **Verified false** against `src/ui/useDrag.ts`: `clamp(raw)` runs inside the `pointermove` handler (line 69, using `window.innerWidth/innerHeight`) as well as on commit (line 98). Drag is clamped to the viewport in real time. WIN-03 fully satisfied. No action needed.

---

## Nyquist Compliance

**SKIPPED** — `workflow.nyquist_validation = false` in `.planning/config.json`. No VALIDATION.md files exist for any phase. This is the configured project behavior; no action required.

---

## Cross-Cutting Constraint Status

| Constraint | Status | Evidence |
|-----------|--------|---------|
| HYGIENE-01..05 (devtools illusion, banned tokens) | PASS | Hygiene gate 6/6 green; Phase 18 extended Pitfall-11 list to all v2.0 surfaces |
| HYGIENE-06 (CI gate extended to v2.0 surfaces) | PASS | `hygiene.test.ts` covers DesktopShell, WindowFrame, Dock, MenuBar, SearchLauncherPanel, VibeThemeProvider, colorCheck, sanitizeDisplayName |
| Single Anthropic egress | PASS | colorCheck + sanitizeDisplayName never call fetch; dataBroker allowlist unchanged |
| Sourcemaps off | PASS | `dist/assets/*.js.map` absent; build clean |
| CSP allowlist (FOUC hash invariant) | PASS | CSP gate green; FOUC SHA-256 hash updated with every script change (Phase 14) |
| IoC/DI | PASS | Dock, MenuBar, SearchLauncherPanel all props-injected; colorCheck/sanitizeDisplayName are pure functions |
| TDD with captured fixtures | PASS | All phases RED→GREEN; offline captured-Haiku fixtures for delegated, weather, currency, pomodoro-timer; colorCheck 44/44 |
| Additive DB migrations | PASS | DB v2→v3 (`settings` store) additive upgrade; v2→v3 real-upgrade survival test passes |

---

## Milestone Definition of Done — Verdict

> **Transform the flat storefront into a themeable, multi-window Vibe OS desktop — apps open as draggable glass windows sharing one chrome, several at once, managed by a dock and menu bar; a dock-launched search/launcher panel replaces the flat grid and places found apps on the desktop; and a switchable, locally-persisted 4-theme system re-skins the entire OS — host chrome and every open app — at a click.**

| Capability | Delivered | Evidence |
|-----------|-----------|---------|
| 4-theme system (Aurora/Aero/Aqua/Noir) | YES | Phase 14 smoke: all 4 themes switch live, FOUC-safe reload |
| Draggable glass windows with chrome | YES | Phase 15 smoke: viewed WindowFrame with traffic-light titlebar |
| Multiple concurrent windows | YES | Phase 15: 2+ windows, independent state |
| Dock with running indicators | YES | Phase 16 smoke: running-indicator dot, focus/restore |
| Menu bar (wordmark + active-app + clock) | YES | Phase 16 smoke: "Vibe OS" + "Notes" + "18:32" |
| Themed desktop surface + animated wallpaper | YES | Phase 16 smoke: wallpaper + 4 blobs, themed |
| Search/launcher panel (describe + pre-installed) | YES | Phase 17 smoke: SearchLauncherPanel opened from dock |
| Describe → produce → window on desktop | YES | Phase 17 smoke: "pomodoro timer with rain sounds" → window |
| Theme re-skin host chrome AND generated apps | YES | Phase 14+18: prompt contract + colorCheck hook; Phase 16 full-screen re-skin observed |
| FOUC-safe persistence | YES | Phase 14: inline script before React mount |
| No root leaks on close | YES | Phase 15: appBodyCount() baseline invariant |
| CI hygiene gate extended to v2.0 surfaces | YES | Phase 18: HYGIENE-06 passed |

**All capabilities delivered. Definition of Done: MET.**

---

## Recommended Next Steps

1. **`/gsd-complete-milestone 2.0`** — archive and tag. No critical gaps; no gap-closure phases needed. (All findings are documentation or visual-polish tech debt.)
2. **During completion:** update REQUIREMENTS.md checkboxes (17 items) and traceability Status column as part of the archive commit.
3. **Post-completion (backlog quick task):** (a) add themed CSS rules for the 6 missing SearchLauncherPanel/Dock classes — the one user-visible polish item; (b) fix the 3 stale source comments (Dock.tsx, ThemeSelector.tsx, index.css); (c) optionally set `index.html` `<title>` to "Vibe OS".

---

*Report generated by `gsd-audit-milestone` orchestrator + `gsd-integration-checker` subagent.*
*Audit file: `.planning/v2.0-MILESTONE-AUDIT.md`*
