---
phase: 01-hygiene-foundation-storefront-shell
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/App.tsx
  - src/index.css
  - src/ui/Marketplace.tsx
  - src/ui/AppBar.tsx
  - src/ui/KeyDialog.tsx
  - src/ui/ThemeProvider.tsx
  - src/ui/SkeletonCard.tsx
  - src/ui/ErrorBoundary.tsx
  - src/ui/theme.test.tsx
  - src/data/appRegistry.ts
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04, HYGIENE-01]
must_haves:
  truths:
    - "A visitor sees a responsive grid of 8 fixed-width app-type cards (Weather, Calculator, Notes, Timer, Currency, Recipes, Calendar, Budget)"
    - "Clicking a card shows an inline 'Opening…' affordance for ~800ms then resets (SHELL-02 stub, no real loop)"
    - "A visitor can set, change, and clear their API key from the AppBar Account dialog, persisted under marketplace.apiKey"
    - "An invalid key (not starting with sk-ant-) shows a neutral inline format error and is not saved"
    - "A visitor can cycle theme light→dark→system from the AppBar; the choice applies data-theme on :root and persists under marketplace.theme"
    - "System theme mode tracks prefers-color-scheme via matchMedia"
  artifacts:
    - path: "src/ui/Marketplace.tsx"
      provides: "storefront grid rendering APP_REGISTRY + Opening… stub click handler"
      contains: "storefront-grid"
    - path: "src/ui/ThemeProvider.tsx"
      provides: "data-theme application + matchMedia listener for system mode"
      contains: "data-theme"
    - path: "src/ui/KeyDialog.tsx"
      provides: "set/change/clear key flows with sk-ant- validation"
      contains: "sk-ant-"
    - path: "src/ui/AppBar.tsx"
      provides: "wordmark + Account button (opens KeyDialog) + 3-way theme toggle"
      contains: "app-bar"
    - path: "src/data/appRegistry.ts"
      provides: "static APP_REGISTRY of 8 neutral app types"
      contains: "APP_REGISTRY"
  key_links:
    - from: "src/ui/KeyDialog.tsx"
      to: "localStorage marketplace.apiKey"
      via: "STORAGE_KEY_API from src/lib/storage.ts"
      pattern: "STORAGE_KEY_API|marketplace.apiKey"
    - from: "src/ui/ThemeProvider.tsx"
      to: ":root data-theme"
      via: "documentElement.setAttribute + matchMedia change listener"
      pattern: "setAttribute\\(.data-theme"
    - from: "src/App.tsx"
      to: "src/ui/ThemeProvider.tsx, AppBar, Marketplace"
      via: "component tree wiring"
      pattern: "ThemeProvider|Marketplace|AppBar"
---

<objective>
Deliver the full storefront UI slice on top of the Walking Skeleton: the storefront grid (SHELL-01), the open-app stub (SHELL-02), the API-key set/change/clear dialog (SHELL-03), and the light/dark/system theme system (SHELL-04). This replaces the minimal App.tsx skeleton with the real ThemeProvider → AppBar + Marketplace tree, and extends `index.css` with the component/layout classes. It also lands the Phase-1 stubs that Phase 2/3 need (`SkeletonCard`, `ErrorBoundary`).

Purpose: This is the user-facing vertical slice — after this plan a real visitor can browse the grid, connect/disconnect their account, and switch themes, all with neutral copy and no mechanic-revealing surface. Every visual/copy/CSS value is pinned by the UI-SPEC; this plan implements it verbatim.

Output: A fully interactive storefront (grid + AppBar + KeyDialog + theme toggle) wired into App.tsx, with `theme.test.tsx` proving theme application.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md

<interfaces>
<!-- Contracts produced by Plan 01 (already in the codebase). Use these directly — no exploration needed. -->

From src/lib/storage.ts:
- export const STORAGE_KEY_API = "marketplace.apiKey";
- export const STORAGE_KEY_THEME = "marketplace.theme";

From src/lib/logger.ts:
- export const logger: { info(msg: string, ...data: unknown[]): void; warn(...): void; error(...): void };
  // off unless localStorage.debug is truthy at load; [Marketplace] prefix when on; NEVER pass the API key in.

From src/index.css (Plan 01 shipped the variable block + base; this plan ADDS the component/layout classes):
- :root[data-theme="light"] and :root[data-theme="dark"] each define the 9 D-17 variables + --color-destructive.

App.tsx (Plan 01 minimal version) currently imports dbReady from src/registry/registry.ts and kicks init in a useEffect.
Preserve that registry-init behavior when you replace App.tsx with the full tree.
</interfaces>
</context>

<phase_goal>
**As a** visitor, **I want to** browse the storefront, connect my account with my own key, and pick a theme, **so that** the platform looks native to me and is ready to open apps.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme system — ThemeProvider, AppBar theme toggle, and theme.test.tsx</name>
  <files>src/ui/ThemeProvider.tsx, src/ui/AppBar.tsx, src/ui/theme.test.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md (§2 AppBar layout/classes/theme-toggle state ~lines 210-247; §Color Theme Application; §Copywriting Contract theme tooltips)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (D-15 default "system"; D-16 data-theme + matchMedia; D-17 9 variable names; D-18 ThemeProvider; D-19 cycle light→dark→system)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md (Pattern 5 theme application + matchMedia addEventListener ~lines 414-424; Pitfall 4 matchMedia stub; State of the Art addEventListener vs deprecated addListener)
    - src/lib/storage.ts (STORAGE_KEY_THEME)
  </read_first>
  <behavior>
    - theme.test.tsx: rendering ThemeProvider with stored theme "light" sets `document.documentElement` `data-theme="light"`
    - theme.test.tsx: switching to "dark" updates `data-theme` to "dark" and persists "dark" under marketplace.theme
    - theme.test.tsx: in "system" mode with the matchMedia stub returning matches=true (dark), `data-theme` resolves to "dark"
    - theme.test.tsx: changing the matchMedia stub and firing its 'change' listener in system mode re-applies the matching data-theme (listener wired via addEventListener('change'))
  </behavior>
  <action>
    Create `src/ui/ThemeProvider.tsx` per RESEARCH Pattern 5 (D-16/D-18/D-19): a context provider holding `mode: "light" | "dark" | "system"` (default "system", D-15) read from `localStorage` via `STORAGE_KEY_THEME`. Expose the current mode and a `cycleTheme()` that advances light→dark→system→light (D-19) and persists via `STORAGE_KEY_THEME`. Apply the resolved theme by `document.documentElement.setAttribute("data-theme", resolved)`; when `mode === "system"`, resolve via `window.matchMedia("(prefers-color-scheme: dark)").matches` and subscribe with `mq.addEventListener("change", handler)` (NOT the deprecated `addListener`), cleaning up on unmount/mode-change. Do not duplicate the FOUC script's job — ThemeProvider owns runtime switching; the inline script owns first paint.

    Add the AppBar theme-toggle portion to `src/ui/AppBar.tsx` (the file is created in Task 2; if you build AppBar here first, that is fine — coordinate so the final AppBar has both the Account button and the theme toggle). The theme toggle is a 44×44 icon button using Lucide `Sun`/`Moon`/`Monitor` for light/dark/system; the active icon gets `color: var(--color-accent-primary)`; `aria-label` communicates the NEXT action ("Switch to dark theme" etc.) per UI-SPEC §2.

    Extend `src/index.css` with `.app-bar`, `.app-bar__wordmark`, `.app-bar__controls`, `.app-bar__icon-btn` per UI-SPEC §2 (height 56px, neutral classes, 44px touch targets, focus-visible accent outline). All class names structurally neutral (D-07).

    Create `src/ui/theme.test.tsx` covering every `<behavior>` bullet. The shared `src/test/setup.ts` (Plan 01) already provides the matchMedia stub; override its `matches`/`change` behavior per test as needed.
  </action>
  <verify>
    <automated>npx vitest run src/ui/theme.test.tsx 2>&1 | tail -15; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/ThemeProvider.tsx` calls `document.documentElement.setAttribute("data-theme", …)` and uses `addEventListener("change", …)` on the matchMedia result
    - ThemeProvider default mode is `"system"` and persists via `STORAGE_KEY_THEME` (no hardcoded `"marketplace.theme"` literal — import the constant)
    - The theme toggle cycles light→dark→system→light (D-19) using Lucide Sun/Moon/Monitor
    - `npx vitest run src/ui/theme.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Theme switching applies the correct data-theme to :root for all three modes (system via matchMedia), persists the choice, and the test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Storefront grid (SHELL-01/02), AppBar Account button, and KeyDialog set/change/clear (SHELL-03)</name>
  <files>src/data/appRegistry.ts, src/ui/Marketplace.tsx, src/ui/AppBar.tsx, src/ui/KeyDialog.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md (§1 StorefrontGrid full anatomy + APP_REGISTRY 8-row table ~lines 153-207; §2 AppBar Account button; §3 KeyDialog three flows + exact copy ~lines 250-306; §Copywriting Contract all exact strings)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (D-08/D-09/D-10 grid + stub click; D-11..D-14 key UX + sk-ant- validation; D-07 neutral classes)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md (Open Question 1 APP_REGISTRY icon placement — recommend a Record<id, LucideIcon> map in the render layer; Pitfall 7 key never echoed in errors)
    - src/lib/storage.ts (STORAGE_KEY_API), src/lib/logger.ts (logger — never pass the key)
  </read_first>
  <action>
    Create `src/data/appRegistry.ts`: export the static `APP_REGISTRY: { id: string; displayName: string; description: string; icon: string }[]` with the EXACT 8 rows from UI-SPEC §1 (weather/calculator/notes/timer/currency/recipes/calendar/budget) — neutral lowercase-kebab ids, the exact displayName + description copy. Per RESEARCH Open Question 1, keep `icon` as a neutral string key in the data and map it to the Lucide component in the render layer (a `Record<id, LucideIcon>` in `Marketplace.tsx`).

    Create `src/ui/Marketplace.tsx` per UI-SPEC §1: render `.storefront-grid` with one `.app-card` `<button>` per APP_REGISTRY row (each `aria-label="{displayName} — {description}"`, icon `aria-hidden`). Card contents: 32px Lucide icon (Cloud/Calculator/NotebookPen/Timer/ArrowLeftRight/ChefHat/CalendarDays/Wallet), display name (20/600), 2-line-clamped description (14/400). Click handler is the SHELL-02 STUB (D-10): show inline "Opening…" text below the clicked card for ~800ms then reset; it MUST NOT log any banned token and may call `logger.info("Opening " + id)` (neutral). No real resolve/render.

    Create `src/ui/AppBar.tsx` (finalize, merging the theme toggle from Task 1): `<header role="banner">` with `.app-bar`; left `.app-bar__wordmark` text "Marketplace" (28/600); right `.app-bar__controls` with the Account button (Lucide `User`, opens KeyDialog) and the theme toggle. Account button `aria-label`/tooltip "Account".

    Create `src/ui/KeyDialog.tsx` per UI-SPEC §3 (D-11..D-14): a `role="dialog" aria-modal="true"` modal with the three flows — Flow A Set (no key): title "Connect your account", body "To open apps, connect your account.", `type="password"` input labeled "Access key" placeholder "Paste your access key", primary CTA "Connect"; Flow B Change (key present): title "Account", status "Account connected" with `CheckCircle2`, actions "Change key" + "Disconnect"; Flow C Disconnect confirm (inline): copy "Remove your account connection? You can reconnect at any time.", buttons "Disconnect" + "Keep connected". On save, validate format `^sk-ant-` and non-empty (D-14); on failure show the EXACT inline error "Invalid access key format. Please check and try again." and DO NOT save and NEVER echo the entered value (Pitfall 7). Persist/read/clear via `STORAGE_KEY_API`. The key is NEVER passed to `logger.*` (D-13). Escape closes; focus trap; first interactive element focused on open.

    Extend `src/index.css` with `.storefront-grid`, `.app-card`/`__icon`/`__name`/`__description`, `.key-dialog*`, `.skeleton-card*`, `.error-boundary-fallback*` classes per the exact UI-SPEC dimensions/colors. Use only the CSS variables (no hardcoded theme colors except the documented one-offs like card hover shadow rgba and focus box-shadow). All class names structurally neutral.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/data/appRegistry.ts','utf8'); ['weather','calculator','notes','timer','currency','recipes','calendar','budget'].forEach(id=>{if(!s.includes(id))throw new Error('missing app '+id)}); console.log('REGISTRY_OK')"; grep -q "sk-ant-" src/ui/KeyDialog.tsx && grep -q "STORAGE_KEY_API" src/ui/KeyDialog.tsx && echo "KEYDIALOG_OK"; grep -q "storefront-grid" src/ui/Marketplace.tsx && echo "GRID_OK"; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/appRegistry.ts` exports `APP_REGISTRY` with all 8 ids (weather, calculator, notes, timer, currency, recipes, calendar, budget)
    - `src/ui/Marketplace.tsx` renders `.storefront-grid` with one `<button>.app-card` per row and an "Opening…" stub that resets after ~800ms
    - `src/ui/KeyDialog.tsx` validates `^sk-ant-`, shows the exact neutral format-error string, and reads/writes/clears via `STORAGE_KEY_API`
    - `src/ui/KeyDialog.tsx` never passes the entered key to `logger.*` and never echoes it in any error string
    - All exact copy strings from UI-SPEC §Copywriting Contract are present verbatim; no banned token (AI/Generate/Synthesize/LLM/Mock/Fake/model) appears in any class, copy, or comment
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The storefront grid renders 8 cards with a neutral Opening… stub, the AppBar exposes Account + theme toggle, and the KeyDialog handles set/change/clear with sk-ant- validation and neutral copy.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-1 stubs (SkeletonCard, ErrorBoundary) and wire the full tree into App.tsx</name>
  <files>src/ui/SkeletonCard.tsx, src/ui/ErrorBoundary.tsx, src/App.tsx</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md (§4 SkeletonCard classes/animation/copy/a11y ~lines 309-335; §5 ErrorBoundaryFallback classes/copy ~lines 338-361; banned error copy)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (Claude's Discretion — basic ErrorBoundary class component stub; specifics — neutral "Opening…" language)
    - src/App.tsx (Plan 01 minimal version — preserve the registry-init useEffect when replacing)
  </read_first>
  <action>
    Create `src/ui/SkeletonCard.tsx` per UI-SPEC §4: a card-sized placeholder matching `.app-card` dimensions with `.skeleton-card`/`.skeleton-block--icon|name|desc`, the shimmer animation (CSS already added in Task 2, or add the `@keyframes shimmer` here in index.css), `aria-label="Loading"`, `aria-busy` on the container, and a visually-hidden `role="status"` live region announcing "Opening…". This is a STUB for Phase 3's loading state — not rendered in the live Phase-1 flow, but it must exist and compile. Banned: "Generating…/Creating…/Building…/Synthesizing…".

    Create `src/ui/ErrorBoundary.tsx` per UI-SPEC §5: a basic React class component (`componentDidCatch`/`getDerivedStateFromError`) rendering the neutral fallback — heading "Something went wrong", body "This section couldn't load. Try refreshing.", retry button "Try again". All technical errors are swallowed before display (banned: "generation failed", "model error", "Anthropic error", any mechanic-revealing string). This is the Phase-1 stub Phase 2 will rely on.

    Replace `src/App.tsx` with the full tree: `<ThemeProvider>` wrapping `<ErrorBoundary>` around `<AppBar />` + `<main><Marketplace /></main>`. PRESERVE the Plan-01 registry-init behavior — keep the `useEffect` that imports `dbReady` from `src/registry/registry.ts`, awaits it, and `logger.info("Registry initialized")`. Manage KeyDialog open/close state at the App (or AppBar) level so the Account button opens it. No banned tokens anywhere.
  </action>
  <verify>
    <automated>grep -q "ThemeProvider" src/App.tsx && grep -q "Marketplace" src/App.tsx && grep -q "ErrorBoundary" src/App.tsx && grep -q "dbReady" src/App.tsx && echo "APP_WIRED_OK"; grep -q "Something went wrong" src/ui/ErrorBoundary.tsx && echo "EB_OK"; grep -q "Loading" src/ui/SkeletonCard.tsx && echo "SKEL_OK"; npx vitest run 2>&1 | tail -10; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` renders `ThemeProvider > ErrorBoundary > (AppBar + main>Marketplace)` and still awaits `dbReady` in a useEffect (registry init preserved)
    - `src/ui/ErrorBoundary.tsx` is a class component with the exact neutral heading/body/retry copy and no mechanic-revealing strings
    - `src/ui/SkeletonCard.tsx` exists with `aria-label="Loading"` and a `role="status"` "Opening…" live region, no banned loading copy
    - `npx vitest run` exits 0 (theme.test.tsx + Plan-01 tests all green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The stubs compile, App.tsx wires the full ThemeProvider→ErrorBoundary→AppBar+Marketplace tree while preserving registry init, and the full test suite plus typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (API key) ↔ storage | The key is entered in the DOM and persisted to `localStorage`; it must never leak to logs, errors, or non-Anthropic destinations. |
| application copy/CSS ↔ devtools surface | All visible strings, class names, and `data-*` are F12-visible and must be neutral (no mechanic disclosure). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | KeyDialog input in DOM | mitigate | `type="password"` masks the key in the Elements tab (D-12); the entered value is never echoed in the format-error string (Pitfall 7). |
| T-01-08 | Information Disclosure | KeyDialog error/logging path | mitigate | The key is never passed to `logger.*` (D-13) and never placed in an `Error.message`; the neutral format error is a fixed literal. |
| T-01-09 | Input Validation | API-key format | mitigate | `^sk-ant-` + non-empty check before save (D-14, ASVS V5); invalid input rejected with a neutral inline error, not saved. |
| T-01-10 | Information Disclosure | storefront copy / CSS classes | accept (gated by Plan 04) | All copy/classes use neutral language by construction; the lexicon gate in Plan 04 enforces this across the produced files. |
</threat_model>

<verification>
- `npx vitest run` passes (theme.test.tsx + Plan-01 tests).
- `npx tsc --noEmit` passes.
- Manual (dev): `npm run dev` → grid shows 8 cards; clicking shows "Opening…" then resets; Account button opens the 3-flow dialog; invalid key shows the neutral error; valid `sk-ant-…` saves; theme toggle cycles and persists; reload keeps theme (FOUC) and key.
- No banned token in any file produced by this plan (enforced fully by Plan 04's `hygiene.test.ts`).
</verification>

<success_criteria>
- Storefront grid renders 8 neutral cards with a fixed-width responsive layout and an "Opening…" stub (SHELL-01/02).
- API key set/change/clear works with `sk-ant-` validation, neutral copy, and `marketplace.apiKey` persistence; the key never leaks to logs/errors (SHELL-03).
- Theme light/dark/system applies `data-theme` on `:root` via CSS variables, tracks `prefers-color-scheme` in system mode, and persists (SHELL-04); `theme.test.tsx` proves it.
- SkeletonCard + ErrorBoundary stubs exist for Phase 2/3.
- App.tsx wires the full tree and preserves registry init; full suite + typecheck green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hygiene-foundation-storefront-shell/01-02-SUMMARY.md`
</output>
