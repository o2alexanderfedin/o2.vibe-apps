# Requirements Archive: Vibe App Store — Milestone v2.0 "Vibe OS"

**Archived:** 2026-06-26
**Milestone Status:** ✅ SHIPPED — 21/21 requirements satisfied
**Audit:** [v2.0-MILESTONE-AUDIT.md](./v2.0-MILESTONE-AUDIT.md) — Status: `tech_debt` (no critical gaps)

> This is the archived requirements snapshot for v2.0. All requirements are marked complete based on the audit cross-reference (21/21 VERIFICATION.md `status: passed` across all 5 phases). The REQUIREMENTS.md traceability table was stale at milestone close (17 of 21 rows still showed "Pending"); this archive reflects the verified state per the audit.

---

**Defined:** 2026-06-26
**Milestone goal:** Transform the flat storefront into a themeable, multi-window **Vibe OS desktop** — apps open as draggable glass windows sharing one chrome, several at once, managed by a dock and menu bar; a **dock-launched search/launcher panel** (search/describe an app + a pre-installed apps list) replaces the flat grid and places found apps on the desktop; and a switchable, locally-persisted 4-theme system re-skins the entire OS — host chrome and every open app — at a click.

---

## v2.0 Requirements

### WIN — Windowing system

> Apps open as draggable glass windows, several at once, managed by a dock + menu bar.

- [x] **WIN-01**: An opened app renders inside a draggable **window** with a shared glass chrome and a macOS-style titlebar (traffic-light close/minimize controls + app icon + title) — the same frame for every app.
- [x] **WIN-02**: Multiple apps are open **concurrently** as independent windows, each independently managed by the window manager; the active window's app name shows in the menu bar. *(Note: implementation uses in-tree React subtrees rather than separate `createRoot` roots — a sound architectural deviation; intent met.)*
- [x] **WIN-03**: Pointer-down on a window **raises** it to front (z-order) and makes it active; dragging the titlebar moves it, **clamped** to the viewport (real-time in `pointermove` handler); new windows **cascade**-place.
- [x] **WIN-04**: A window can be **minimized** to the dock and **restored**.
- [x] **WIN-05**: A window can be **closed**, fully unmounting its app — no leaked React root, timer, or listener (close routes through the manager).
- [x] **WIN-06**: A bottom **dock** shows opened apps with a running indicator (clicking opens/restores; hover-scale) **and a search (magnifier) icon** that opens the search/launcher panel (CREATE-01).
- [x] **WIN-07**: A top **menu bar** shows the OS wordmark, the active app's name, and a live clock.
- [x] **WIN-08**: The **desktop surface** is the workspace where apps live — apps launched from the search/launcher panel are placed here (opened as windows and added to the dock); the surface carries the themed wallpaper.

### THEME — Themeable shell

> A named-theme registry of CSS-variable sets, switchable live, persisted, applied to host **and** apps.

- [x] **THEME-01**: Four built-in themes (**Aurora / Aero / Aqua / Noir**) are selectable from an always-visible switcher in the menu bar.
- [x] **THEME-02**: Switching the theme **re-skins host chrome AND every open app window live** — no reload, no remount.
- [x] **THEME-03**: The active theme **persists** across reloads and is restored on first paint with **no flash** of the wrong theme (FOUC-safe).
- [x] **THEME-04**: Themes apply as CSS custom properties on the **document root**, so the styling reaches every app window's independently-mounted subtree.
- [x] **THEME-05**: A backward-compat **alias bridge** maps prior style variables to the new theme contract so apps cached **before v2.0** still render correctly after the new variables land.

### CREATE — Search / launcher panel (find or describe → desktop)

> A dock-launched panel that unifies browse + create: search/describe an app or pick a pre-installed one; results land on the desktop.

- [x] **CREATE-01**: A **search (magnifier) icon in the dock** opens a **search/launcher panel** containing a text input + action button and a list of **pre-installed apps**.
- [x] **CREATE-02**: Submitting a description **finds-or-produces** the app via the existing resolve→produce→cache→mount loop (cache hit = instant; miss = produced) with **idle / working / result** states — the working state is a real loading affordance over **genuine** production — and the result is **placed on the desktop surface** as a window.
- [x] **CREATE-03**: Selecting a **pre-installed app** from the panel's list opens it on the desktop; launched apps appear in the dock as installed/running. No surface in the flow names the mechanic.

### TGEN — Theme-aware generation

> Produced apps reference the theme contract so they re-skin for free.

- [x] **TGEN-01**: Produced apps/widgets **reference the theme variable contract** (accents, text, glass, border, …) instead of hardcoded colors, so they re-skin automatically on theme switch.
- [x] **TGEN-02**: A **post-compile static check** detects hardcoded colors in produced code and feeds violations into the existing **compile-error self-heal loop** — no extra round-trips beyond the shipped resilience budget.
- [x] **TGEN-03**: Model-supplied app names/text rendered in chrome (titlebar, dock, menu bar) are **sanitized** so no banned-lexicon token can reach a visible surface.

### Cross-cutting acceptance constraints

- [x] **HYGIENE-06**: The CI lexicon gate is **extended to all new v2.0 surfaces/files** (window chrome, dock, menu bar, create panel) plus the sanitization of model-supplied display strings (TGEN-03).
- [x] **PERF-01**: With several windows open plus the animated wallpaper, the desktop stays responsive — **minimized windows don't composite** (`display:none`), and blur/animation **degrade** under `prefers-reduced-motion` / weak-GPU conditions.

---

## Future Requirements (deferred beyond v2.0)

- **User-created / custom themes** — built-in four only this milestone.
- **Window-position / desktop-layout persistence** — active-theme persistence ships; layout doesn't.
- **HARD-01 `<iframe sandbox>` isolation** + **SEC-01/02/03** — security deferred; windowing layer designed for contained iframe move later.
- **G2 unified `Intent` contract** — internal refactor, no user-facing value.

## Out of Scope (explicit exclusions)

- **Window resize handles** — break fixed-width generated app layouts.
- **Maximize / fullscreen** — conflicts with the partial-glass desktop aesthetic.
- **Snap / tiling / multi-desktop** — over-engineered for widget-scale apps.
- **Any application server / sync** — client-only throughout.
- **Naming the mechanic** — no AI/LLM/generate/synthesize/fake/mock in any visible or devtools surface.
- **A drag/animation/CSS-in-JS library** — zero new npm deps; hand-rolled.

---

## Traceability

| REQ-ID | Phase | Status | Evidence |
|--------|-------|--------|----------|
| THEME-01 | Phase 14 — Theme Foundation | ✅ Complete | 14-VERIFICATION.md passed; ThemeSelector 4-pill switcher in menu bar |
| THEME-02 | Phase 14 — Theme Foundation | ✅ Complete | 14-VERIFICATION.md passed; live re-skin switch-path test asserts `--text` on documentElement |
| THEME-03 | Phase 14 — Theme Foundation | ✅ Complete | 14-VERIFICATION.md passed; FOUC script + IDB settings store + localStorage |
| THEME-04 | Phase 14 — Theme Foundation | ✅ Complete | 14-VERIFICATION.md passed; CSS vars on documentElement reach all subtrees |
| THEME-05 | Phase 14 — Theme Foundation | ✅ Complete | 14-VERIFICATION.md passed; `:root` alias bridge in index.css |
| WIN-01 | Phase 15 — Window Manager | ✅ Complete | 15-VERIFICATION.md passed; WindowFrame glass chrome, traffic-light titlebar |
| WIN-02 | Phase 15 — Window Manager | ✅ Complete | 15-VERIFICATION.md passed; concurrent windows, active name in menu bar (in-tree subtrees, sound deviation) |
| WIN-03 | Phase 15 — Window Manager | ✅ Complete | 15-VERIFICATION.md passed; drag clamped in real-time (pointermove handler) |
| WIN-04 | Phase 15 — Window Manager | ✅ Complete | 15-VERIFICATION.md passed; minimize → display:none → restore |
| WIN-05 | Phase 15 — Window Manager | ✅ Complete | 15-VERIFICATION.md passed; close via manager; appBodyCount() baseline invariant |
| WIN-06 | Phase 16 — Desktop Shell | ✅ Complete | 16-VERIFICATION.md passed; dock with running indicators + magnifier icon |
| WIN-07 | Phase 16 — Desktop Shell | ✅ Complete | 16-VERIFICATION.md passed; menu bar wordmark + active-app name + live clock |
| WIN-08 | Phase 16 — Desktop Shell | ✅ Complete | 16-VERIFICATION.md passed; DesktopShell root, themed wallpaper, app placement |
| PERF-01 | Phase 16 — Desktop Shell | ✅ Complete | 16-VERIFICATION.md passed; `@media (prefers-reduced-motion)` CSS block + matchMedia marker |
| CREATE-01 | Phase 17 — Search / Launcher Panel | ✅ Complete | 17-VERIFICATION.md passed; SearchLauncherPanel with text input + pre-installed apps list |
| CREATE-02 | Phase 17 — Search / Launcher Panel | ✅ Complete | 17-VERIFICATION.md passed; describe→produce→window; cache-hit instant resolve |
| CREATE-03 | Phase 17 — Search / Launcher Panel | ✅ Complete | 17-VERIFICATION.md passed; pre-installed app opens as window, dock running indicator |
| TGEN-01 | Phase 18 — Theme-Aware Generation | ✅ Complete | 18-VERIFICATION.md passed; all prompt kinds mandate CSS-var contract |
| TGEN-02 | Phase 18 — Theme-Aware Generation | ✅ Complete | 18-VERIFICATION.md passed; colorCheck post-compile → self-heal loop (44/44 tests) |
| TGEN-03 | Phase 18 — Theme-Aware Generation | ✅ Complete | 18-VERIFICATION.md passed; sanitizeDisplayName in useWindowManager.open(); "AI Weather"→"Weather" |
| HYGIENE-06 | Phase 18 — Theme-Aware Generation | ✅ Complete | 18-VERIFICATION.md passed; hygiene gate extended to all v2.0 surfaces |

**Coverage:** 21/21 requirements satisfied across 5 phases. All cross-cutting constraints (HYGIENE-01..05, single egress, sourcemaps-off, CSP, IoC/DI, TDD, additive DB migrations, FOUC-script/CSP-hash invariant) held green on every phase exit.
