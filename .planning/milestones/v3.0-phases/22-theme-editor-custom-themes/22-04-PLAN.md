---
phase: 22-theme-editor-custom-themes
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified:
  - src/ui/ThemeSelector.tsx
  - src/ui/ThemeSelector.test.tsx
  - src/ui/MenuBar.tsx
  - src/ui/MenuBar.test.tsx
  - src/ui/DesktopShell.tsx
  - src/ui/DesktopShell.test.tsx
autonomous: true
requirements:
  - THEME-07
  - THEME-08
  - THEME-09

must_haves:
  truths:
    - "ThemeSelector renders custom theme pills from VibeThemeContext.customThemes alongside the 4 built-in pills"
    - "Clicking a custom theme pill calls setTheme('custom:<name>', vars) — re-skins host AND all frames (THEME_PUSH)"
    - "ThemeSelector provides a 'New Theme' button that triggers the ThemeEditor via onOpenThemeEditor callback"
    - "Each built-in pill has a 'Duplicate' action that opens ThemeEditor pre-filled with that theme's vars"
    - "MenuBar accepts onOpenThemeEditor prop and passes it through to ThemeSelector"
    - "DesktopShell.tsx line 842 uses themeCtx.currentVars instead of VIBE_THEMES[name] — SandboxFrame receives correct vars for custom themes"
    - "DesktopShell mounts ThemeEditor via {themeEditorState !== null && <ThemeEditor .../>} pattern, mirroring the KeyDialog pattern"
  artifacts:
    - path: "src/ui/ThemeSelector.tsx"
      provides: "Extended ThemeSelector rendering custom pills + New Theme button + Duplicate buttons"
      exports: ["ThemeSelector"]
    - path: "src/ui/MenuBar.tsx"
      provides: "Extended MenuBar with onOpenThemeEditor callback prop"
      exports: ["MenuBar", "MenuBarProps"]
    - path: "src/ui/DesktopShell.tsx"
      provides: "themeEditorState wiring, line 842 currentVars fix, ThemeEditor render"
  key_links:
    - from: "DesktopShell.tsx:842"
      to: "themeCtx.currentVars"
      via: "useContext(VibeThemeContext).currentVars"
      pattern: "currentVars"
    - from: "ThemeSelector 'custom pill' onClick"
      to: "setTheme('custom:<name>', vars)"
      via: "customThemes.get(name) to resolve vars param"
      pattern: "setTheme.*custom:"
    - from: "DesktopShell themeEditorState"
      to: "ThemeEditor component"
      via: "{themeEditorState !== null && <ThemeEditor ... />}"
      pattern: "ThemeEditor"
---

<objective>
Wire the ThemeEditor into the live application: extend ThemeSelector to show custom theme pills and expose an editor entry point, add onOpenThemeEditor to MenuBar props, fix DesktopShell line 842 to use themeCtx.currentVars (the critical SandboxFrame coupling fix), and mount ThemeEditor from DesktopShell following the KeyDialog pattern.

Purpose: Plans 22-01 through 22-03 built the contracts, utility, FOUC fix, and ThemeEditor component. This plan connects all of it to the user: custom themes appear in the menu bar, selecting them re-skins host + all frames, the editor opens from the menu bar, and SandboxFrame gets correct vars for any active theme. Without the DesktopShell:842 fix, SandboxFrame receives undefined for any "custom:*" theme — this is the highest-priority fix in the phase.

Output: All 6 files modified; full suite green; tsc 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-theme-editor-custom-themes/22-RESEARCH.md
@.planning/phases/22-theme-editor-custom-themes/22-01-SUMMARY.md
@.planning/phases/22-theme-editor-custom-themes/22-03-SUMMARY.md

<interfaces>
<!-- Key contracts verified from live codebase + prior plan outputs. -->

From src/ui/ThemeSelector.tsx (CURRENT — to be extended):
  const THEME_NAMES: ReadonlyArray<VibeThemeName> = ["aurora", "aero", "aqua", "noir"];
  const THEME_LABELS: Record<VibeThemeName, string> = { aurora: "Aurora", aero: "Aero", aqua: "Aqua", noir: "Noir" };
  export function ThemeSelector(): JSX.Element;
  // Currently renders THEME_NAMES pills, calls setTheme(name) on click
  // Imports: useVibeTheme, type VibeThemeName from "./VibeThemeProvider"

From src/ui/MenuBar.tsx (CURRENT — to be extended):
  export interface MenuBarProps {
    activeName: string;
    onOpenAccount: () => void;
  }
  export function MenuBar({ activeName, onOpenAccount }: MenuBarProps): JSX.Element;
  // Renders <ThemeSelector /> at line 47

From src/ui/DesktopShell.tsx (critical lines to change):
  Line 19: import { KeyDialog } from "./KeyDialog";
  Line 228: const [keyDialogOpen, setKeyDialogOpen] = useState(false);
  Line 841: const themeCtx = useContext(VibeThemeContext);
  Line 842: const currentThemeVars = VIBE_THEMES[themeCtx?.theme ?? "aurora"];  // MUST CHANGE
  Line 943: themeVars={currentThemeVars}  // receives the broken value for "custom:*"
  Line 972: onOpenAccount={() => setKeyDialogOpen(true)}  // MenuBar call
  Line 999: {keyDialogOpen && <KeyDialog onClose={() => setKeyDialogOpen(false)} />}  // pattern to mirror

From src/ui/VibeThemeProvider.tsx (after Plan 22-01):
  export type AnyThemeName = VibeThemeName | CustomThemeName;
  export interface VibeThemeContextValue {
    theme: AnyThemeName;
    currentVars: Record<string, string>;
    setTheme: (name: AnyThemeName, vars?: Record<string, string>) => void;
    customThemes: ReadonlyMap<string, Record<string, string>>;
    refreshCustomThemes: () => Promise<void>;
  }
  export const VIBE_THEMES: Record<VibeThemeName, Record<string, string>>;
  export const VibeThemeContext: React.Context<VibeThemeContextValue | null>;
  export function useVibeTheme(): VibeThemeContextValue;

From src/ui/ThemeEditor.tsx (after Plan 22-03):
  export interface ThemeEditorProps {
    onClose: () => void;
    initialVars?: Record<string, string>;
    editingName?: string;
  }
  export function ThemeEditor({ onClose, initialVars, editingName }: ThemeEditorProps): JSX.Element;

From src/ui/ThemeSelector.test.tsx (CURRENT — to be extended):
  // Line 71-78: asserts exactly 4 pills (Aurora, Aero, Aqua, Noir)
  // This assertion will be updated when custom pills are added (test must accept variable pill count)

From src/ui/MenuBar.test.tsx (CURRENT — to be extended):
  // Tests MenuBarProps; onOpenThemeEditor will be added as a new prop
  // Existing tests must remain green
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ThemeSelector with custom pills + Duplicate + New Theme; update MenuBar props</name>
  <files>src/ui/ThemeSelector.tsx, src/ui/ThemeSelector.test.tsx, src/ui/MenuBar.tsx, src/ui/MenuBar.test.tsx</files>

  <read_first>
  Read src/ui/ThemeSelector.tsx in full (42 lines). Read src/ui/ThemeSelector.test.tsx in full — note the assertion for exactly 4 pills at lines 71-78 that will need updating. Read src/ui/MenuBar.tsx in full (57 lines). Read src/ui/MenuBar.test.tsx in full. Read 22-RESEARCH.md §5 (ThemeSelector hook point and test implication). The ThemeSelector receives no props currently — it reads everything from context. After this task it will receive onOpenThemeEditor and onDuplicateTheme from its parent (or accept them via new props, detailed below).
  </read_first>

  <behavior>
    - Test 1: ThemeSelector with 2 custom themes in context renders 6 pills total (4 built-in + 2 custom)
    - Test 2: Clicking a custom theme pill calls setTheme("custom:myTheme", customVars) with the correct vars from customThemes map
    - Test 3: ThemeSelector renders a "New Theme" button; clicking it calls the onOpenThemeEditor callback with no initialVars (opens blank editor defaulting to Aurora vars)
    - Test 4: Each built-in pill has a "Duplicate" button; clicking "Duplicate" on the Aurora pill calls onOpenThemeEditor({ initialVars: VIBE_THEMES["aurora"] })
    - Test 5: Custom theme pills have an "Edit" button; clicking it calls onOpenThemeEditor({ initialVars: customThemeVars, editingName: "myTheme" })
    - Test 6: MenuBar renders with onOpenThemeEditor prop; the prop is passed through to ThemeSelector
    - Test 7: Existing ThemeSelector tests for 4 built-in pills (Aurora, Aero, Aqua, Noir) still pass when no custom themes are in context
  </behavior>

  <action>
  In src/ui/ThemeSelector.tsx:
  1. Add ThemeSelectorProps interface: onOpenThemeEditor: (opts?: { initialVars?: Record<string,string>; editingName?: string }) => void. Export this interface.
  2. Update ThemeSelector function signature to accept props: { onOpenThemeEditor }: ThemeSelectorProps.
  3. Add customThemes and setTheme to the useVibeTheme() destructure (already there for setTheme; add customThemes).
  4. After the 4 built-in pills, add: a section rendering custom theme pills from [...customThemes.entries()]. Each custom pill: a wrapper div containing (a) a button with className "theme-selector__pill" (same class as built-in pills) that calls setTheme(`custom:${name}` as AnyThemeName, vars) on click; (b) an "Edit" button that calls onOpenThemeEditor({ initialVars: vars, editingName: name }). Apply theme-selector__pill--active class when theme === `custom:${name}`.
  5. On each built-in pill, add a small "Duplicate" button next to the label that calls onOpenThemeEditor({ initialVars: VIBE_THEMES[name] }). The label button still calls setTheme(name) normally.
  6. After all pills, add a "New Theme" button that calls onOpenThemeEditor() with no arguments (undefined opts).
  7. Update imports to add AnyThemeName and VIBE_THEMES from ./VibeThemeProvider.

  In src/ui/ThemeSelector.test.tsx:
  - Add a default onOpenThemeEditor mock (vi.fn()) to all renderThemeSelector calls.
  - Update the "4 built-in pills" assertion: it was asserting exactly 4 buttons; update to assert the 4 built-in pill NAMES are present (not a strict button count). Alternatively, query by name and confirm the 4 exist without asserting total count.
  - Add all 7 behavior test cases above.

  In src/ui/MenuBar.tsx:
  1. Add onOpenThemeEditor: (opts?: { initialVars?: Record<string,string>; editingName?: string }) => void to MenuBarProps interface.
  2. Pass it through to <ThemeSelector onOpenThemeEditor={onOpenThemeEditor} />.

  In src/ui/MenuBar.test.tsx:
  - Update all MenuBar render calls to include a mock onOpenThemeEditor prop (vi.fn()).
  - Add a test: "MenuBar renders ThemeSelector with onOpenThemeEditor prop" — stub onOpenThemeEditor, render MenuBar, click the "New Theme" button (accessible via screen.getByRole("button", { name: /new theme/i })), assert the stub was called.
  </action>

  <verify>
    <automated>npx vitest run src/ui/ThemeSelector src/ui/MenuBar --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
  - ThemeSelector renders custom theme pills when customThemes has entries (test with 2 custom themes → 6+ pills)
  - Clicking a custom pill calls setTheme("custom:name", vars) — verified by spy in tests
  - "New Theme" button present; click calls onOpenThemeEditor()
  - "Duplicate" buttons on built-in pills call onOpenThemeEditor({ initialVars: VIBE_THEMES[name] })
  - "Edit" buttons on custom pills call onOpenThemeEditor({ initialVars: customVars, editingName: name })
  - MenuBarProps has onOpenThemeEditor prop
  - Existing MenuBar + ThemeSelector tests pass without regression
  - npx tsc --noEmit 0 errors
  </acceptance_criteria>

  <done>ThemeSelector shows custom pills + Edit/Duplicate/New Theme actions; MenuBar props updated; ThemeSelector.test.tsx + MenuBar.test.tsx all pass; tsc 0.</done>
</task>

<task type="auto">
  <name>Task 2: DesktopShell — fix line 842 currentVars, wire themeEditorState, mount ThemeEditor</name>
  <files>src/ui/DesktopShell.tsx, src/ui/DesktopShell.test.tsx</files>

  <read_first>
  Read src/ui/DesktopShell.tsx: look at lines 19 (KeyDialog import), 225-228 (keyDialogOpen state), 841-842 (critical VIBE_THEMES lookup), 943 (SandboxFrame themeVars prop), 968-972 (MenuBar render with onOpenAccount), 999 (KeyDialog conditional render). These are the exact lines to modify or mirror. Read src/ui/DesktopShell.test.tsx briefly to understand the test structure and identify any test that passes the MenuBar or checks the currentThemeVars line — those tests need updating for the new onOpenThemeEditor prop.
  </read_first>

  <action>
  In src/ui/DesktopShell.tsx:

  1. Add import { ThemeEditor } from "./ThemeEditor" alongside the existing import { KeyDialog } from "./KeyDialog".

  2. Define the ThemeEditorState type locally in the file: type ThemeEditorState = { initialVars?: Record<string,string>; editingName?: string } | null — null means the editor is closed.

  3. Add state: const [themeEditorState, setThemeEditorState] = useState<ThemeEditorState>(null), near the existing const [keyDialogOpen, setKeyDialogOpen] = useState(false) at line 228.

  4. Fix line 842: Replace:
       const currentThemeVars = VIBE_THEMES[themeCtx?.theme ?? "aurora"];
     With:
       const currentThemeVars = themeCtx?.currentVars ?? VIBE_THEMES["aurora"];
     This uses the currentVars property added to VibeThemeContextValue in Plan 22-01, which correctly resolves to the active custom theme's vars OR the active built-in's vars. The line 943 SandboxFrame prop (themeVars={currentThemeVars}) requires no change — it receives the fixed value.

  5. On the MenuBar render (around line 972): add the onOpenThemeEditor prop:
       onOpenThemeEditor={(opts) => setThemeEditorState(opts ?? {})}
     This converts the ThemeSelectorProps callback shape into the state update.

  6. After the existing {keyDialogOpen && <KeyDialog ... />} at line 999, add:
       {themeEditorState !== null && (
         <ThemeEditor
           onClose={() => setThemeEditorState(null)}
           initialVars={themeEditorState.initialVars}
           editingName={themeEditorState.editingName}
         />
       )}
     This is the exact same pattern as the KeyDialog mount.

  In src/ui/DesktopShell.test.tsx:
  1. Find any test that renders MenuBar with the old props signature and add the required onOpenThemeEditor prop mock (vi.fn()) to avoid TypeScript errors.
  2. Add a test: "custom theme vars reach SandboxFrame correctly when theme is custom:*" — pre-seed the VibeThemeContext (via a test-only provider or by having the RecordingSettingsStore return custom theme data) such that themeCtx.currentVars is a known object, then assert SandboxFrame receives that object as themeVars (verify by checking the prop or by spying on the SandboxFrame component).
  3. Add a test: "ThemeEditor opens when onOpenThemeEditor is called" — click a "New Theme" button (or call the prop directly), assert ThemeEditor is mounted in the DOM (query for an element unique to ThemeEditor, e.g. the dialog's title text or a known aria-label).
  </action>

  <verify>
    <automated>npx vitest run src/ui/DesktopShell --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
  - DesktopShell.tsx line 842: const currentThemeVars = themeCtx?.currentVars ?? VIBE_THEMES["aurora"] (no VIBE_THEMES[name] lookup — that lookup returned undefined for "custom:*")
  - DesktopShell.tsx imports ThemeEditor
  - DesktopShell.tsx has const [themeEditorState, setThemeEditorState] = useState<...>(null)
  - DesktopShell.tsx MenuBar render includes onOpenThemeEditor prop
  - DesktopShell.tsx renders ThemeEditor when themeEditorState !== null
  - ThemeEditor receives onClose, initialVars, editingName from themeEditorState
  - DesktopShell.test.tsx updated for new MenuBar prop (no TypeScript errors)
  - Test for custom theme vars reaching SandboxFrame passes
  - Test for ThemeEditor mounting passes
  - npx vitest run src/ui/DesktopShell exits with all tests passing
  - npx tsc --noEmit exits 0 (no type errors from the line 842 change or new imports)
  </acceptance_criteria>

  <done>DesktopShell:842 fixed to use themeCtx.currentVars; ThemeEditor wired via themeEditorState; MenuBar gets onOpenThemeEditor; DesktopShell.test.tsx updated; tsc 0; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeSelector custom pill onClick → setTheme | setTheme is called with "custom:<name>" and the resolved vars from customThemes — the vars come from IDB-loaded state (Plan 22-01), not directly from user input at this point |
| DesktopShell themeEditorState → ThemeEditor props | themeEditorState.initialVars comes from customThemes (IDB-loaded) or VIBE_THEMES (built-in, hardcoded) — no raw user input passes through DesktopShell to ThemeEditor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Tampering | DesktopShell:842 undefined themeVars to SandboxFrame | mitigate | Fixed in this plan: currentVars ?? VIBE_THEMES["aurora"] — SandboxFrame always receives a valid 12-var object; never receives undefined |
| T-22-12 | Information Disclosure | themeEditorState in React state | accept | themeEditorState contains only vars (CSS values) and editingName (theme name string) — no secrets, no API key, no PII |
| T-22-13 | Tampering | Custom pill setTheme with stale vars from context | accept | customThemes map is populated from IDB in refreshCustomThemes (Plan 22-01); if the map entry is absent, setTheme falls back to VIBE_THEMES["aurora"] — no crash, no wrong key broadcast |
</threat_model>

<verification>
After both tasks complete:
- npx tsc --noEmit exits 0
- npx vitest run src/ui/ThemeSelector src/ui/MenuBar src/ui/DesktopShell — all pass
- npx vitest run (full suite) — no regressions across all test files
- grep "VIBE_THEMES\[themeCtx" src/ui/DesktopShell.tsx returns 0 matches (old pattern gone)
- grep "currentVars" src/ui/DesktopShell.tsx returns ≥ 1 match (new pattern present)
- ThemeSelector.tsx exports ThemeSelectorProps (for MenuBar.tsx import)
</verification>

<success_criteria>
ThemeSelector shows custom theme pills from context, offers Duplicate on built-ins and Edit on custom themes, has a New Theme trigger — all wired through MenuBar.onOpenThemeEditor. DesktopShell line 842 fixed: SandboxFrame always receives correct themeVars for any active theme. ThemeEditor mounts from DesktopShell on demand. All tests green. tsc 0.
</success_criteria>

<output>
After completion, create .planning/phases/22-theme-editor-custom-themes/22-04-SUMMARY.md
</output>
