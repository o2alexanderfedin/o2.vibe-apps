---
phase: 21-desktop-persistence
plan: 04
type: execute
wave: 3
depends_on:
  - 21-03
files_modified:
  - src/ui/DesktopShell.test.tsx
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03

must_haves:
  truths:
    - "50 rapid geometry changes within 300ms produce exactly 1 writeRaw call for 'windowLayout' — confirmed by vi.useFakeTimers() advancing past the debounce threshold once"
    - "Restoring a seeded layout with 3 entries yields 3 open windows at the correct x/y/z/minimized values, using fresh instanceIds not matching any persisted UUID"
    - "An evicted app entry (appType not in IDB registry) produces a placeholder window — the transport is NEVER called (unusedTransport, which throws, stays silent)"
    - "The JSON persisted under 'windowLayout' parses to objects with exactly the 7 binding fields (checked via Object.keys comparison)"
    - "tsc --noEmit 0 errors; full existing test suite passes; hygiene gate green; REGISTRY_DB_VERSION unchanged at 3; npm run build produces no source maps"
  artifacts:
    - path: "src/ui/DesktopShell.test.tsx"
      provides: "debounce test, restore test, eviction test, layout shape test — covering all 5 binding success criteria"
  key_links:
    - from: "vi.useFakeTimers() + vi.advanceTimersByTime(300)"
      to: "settingsStore.rawWriteCount('windowLayout') === 1"
      via: "fake timers control the setTimeout in the save effect"
      pattern: "useFakeTimers.*advanceTimersByTime.*rawWriteCount"
    - from: "createRecordingSettingsStore().readRaw seed"
      to: "windowManager.windows has N entries at correct positions"
      via: "restore effect reads IDB-seeded layout, opens via openAt"
      pattern: "readRaw.*windowLayout.*openAt"
    - from: "unusedTransport (throws on call)"
      to: "no error thrown for evicted app restore"
      via: "pre-IDB-check shows placeholder before resolveComponent is called"
      pattern: "evicted.*placeholder.*unusedTransport"
---

<objective>
Write the automated tests that prove all 5 binding Phase 21 success criteria are satisfied. Every criterion maps to at least one test in DesktopShell.test.tsx with no subjective language.

Purpose: Phase 21 is verifiable only through automated tests because the core behaviours (debounce timing, exact geometry restoration, eviction handling, shape constraints) cannot be asserted by static code review alone. The fake-timer pattern (from MenuBar.test.tsx) and the test-kit pattern (from desktopShellTestKit.tsx) provide the infrastructure; this plan only adds the Phase 21-specific test cases.

Output: New test cases inside src/ui/DesktopShell.test.tsx; all existing tests remain green; full test suite + tsc + hygiene + build gate confirmed.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-desktop-persistence/21-CONTEXT.md
@.planning/phases/21-desktop-persistence/21-RESEARCH.md
@.planning/phases/21-desktop-persistence/21-01-SUMMARY.md
@.planning/phases/21-desktop-persistence/21-02-SUMMARY.md
@.planning/phases/21-desktop-persistence/21-03-SUMMARY.md

<interfaces>
<!-- Test infrastructure the executor needs. Extracted from live source. -->

From src/ui/desktopShellTestKit.tsx:
  // Provides: renderDesktopShell(overrides), openApp(user, appType), etc.
  // Read this file fully before writing tests — it defines how to mount DesktopShell
  // with test doubles injected via ServicesProvider.

From src/ui/MenuBar.test.tsx (lines 94-108, fake-timer pattern):
  vi.useFakeTimers();
  // ... trigger changes ...
  await act(async () => { vi.advanceTimersByTime(300); });
  expect(settingsStore.rawWriteCount("windowLayout")).toBe(1);
  vi.useRealTimers();
  // ALWAYS restore real timers in afterEach or at test end.

From src/services/testServices.ts (Plan 21-01 output):
  createRecordingSettingsStore():
    .rawWriteCount(key: string): number   // count of writeRaw calls for key
    .readRaw(key: string): Promise<string|null>  // the test double also accepts pre-seeded reads
    .writeRaw(key, value): Promise<void>  // recorded; does NOT write to real IDB

From src/registry/db.ts + src/services/testServices.ts:
  createInMemoryRegistry(): Registry   // used by createTestServices()
  // To simulate an app being IN IDB: registry.put("apps", {cacheKey, ...appRecord})
  // To simulate an app being EVICTED (not in IDB): simply omit it from the registry
  // unusedTransport (from testServices.ts): throws if called — proof that produce was NOT attempted

From src/host/layoutPersistence.ts (Plan 21-01 output):
  LAYOUT_KEY = "windowLayout"
  LayoutEntry { appType, title, icon, x, y, z, minimized }
  serializeLayout(windows: WindowEntry[]): string
  isLayoutEntry(v: unknown): v is LayoutEntry

From src/ui/useWindowManager.tsx:
  WindowEntry.instanceId — the session-scoped id minted by open()/openAt()
  // A restored instanceId must be a FRESH value (not the one in the persisted layout)
  // because the persisted layout contains NO instanceId (only appType)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debounce test (SC#2) + layout shape test (SC#5)</name>
  <files>src/ui/DesktopShell.test.tsx</files>
  <read_first>
    src/ui/DesktopShell.test.tsx — read the FULL file to understand the existing test structure, describe blocks, imports, and the desktopShellTestKit API. Find a good insertion point for the new "desktop persistence" describe block.
    src/ui/desktopShellTestKit.tsx — read to understand: how to mount DesktopShell with injected settingsStore, how to get access to windowManager from the test, how to trigger geometry changes (setGeometry or drag simulation).
    src/ui/MenuBar.test.tsx — read lines 94-108 for the exact vi.useFakeTimers() / vi.advanceTimersByTime() / vi.useRealTimers() pattern.
    src/services/testServices.ts — read createRecordingSettingsStore() to confirm rawWriteCount and the fact that writeRaw is fully in-memory (no real IDB needed).
    src/host/layoutPersistence.ts — confirm serializeLayout field list for the shape test.
  </read_first>
  <behavior>
    DEBOUNCE TEST:
    - When 50 geometry changes fire within the 300ms window, rawWriteCount("windowLayout") is 0 before time advance
    - After vi.advanceTimersByTime(300), rawWriteCount("windowLayout") is exactly 1
    - After vi.advanceTimersByTime(300) a second time with no further changes, rawWriteCount("windowLayout") is still 1

    SHAPE TEST:
    - Mount DesktopShell, open 1 window, advance past debounce
    - settingsStore.rawWrites.get("windowLayout") is defined
    - JSON.parse the last written value; the result is an array
    - The first entry's Object.keys() sorted equals ["appType","icon","minimized","title","x","y","z"] (sorted alphabetically)
    - "instanceId" is NOT in Object.keys() of the parsed entry
    - "transpiledJS" is NOT in Object.keys()
    - "id" is NOT in Object.keys()
  </behavior>
  <action>
    Add a new top-level describe block "Desktop persistence — save" to src/ui/DesktopShell.test.tsx.

    DEBOUNCE TEST: Write a test "50 rapid geometry changes produce exactly 1 debounced IDB write (SC#2)":
    1. Call vi.useFakeTimers() at the start
    2. Create a RecordingSettingsStore via createRecordingSettingsStore() and inject it via renderDesktopShell({settingsStore})
    3. Open 1 app (use the existing openApp helper or directly call windowManager.open inside act)
    4. In a loop of 50 iterations, call act(() => windowManager.setGeometry(wid, 100+i, 100+i)) where wid is the open window id
    5. Assert settingsStore.rawWriteCount("windowLayout") === 0 (debounce window not elapsed)
    6. Call await act(async () => vi.advanceTimersByTime(300))
    7. Assert settingsStore.rawWriteCount("windowLayout") === 1
    8. Call vi.useRealTimers() in cleanup (afterEach or directly)

    Note on getting windowManager access in tests: follow the pattern established in the existing test kit. If the test kit exposes windowManager via a hook or ref, use that. If not, read from the rendered component's context via renderHook or a helper. Do not add new plumbing to the source — only use what the test kit already provides or what can be obtained via screen queries on the rendered component.

    SHAPE TEST: Write a test "persisted 'windowLayout' record contains exactly the 7 required fields (SC#5)":
    1. Create RecordingSettingsStore; render DesktopShell
    2. Open 1 window; advance fake timers past 300ms
    3. Get the last written raw value: const raw = settingsStore.rawWrites.get("windowLayout")?.at(-1)
    4. Parse: const parsed = JSON.parse(raw!) as unknown[]
    5. const entry = parsed[0] as Record<string, unknown>
    6. const keys = Object.keys(entry).sort()
    7. expect(keys).toEqual(["appType", "icon", "minimized", "title", "x", "y", "z"])
    8. expect("instanceId" in entry).toBe(false)
    9. expect("transpiledJS" in entry).toBe(false)

    Cleanup: ensure vi.useRealTimers() is called after each fake-timer test. Use beforeEach/afterEach or try/finally.
  </action>
  <acceptance_criteria>
    - "50 rapid geometry changes produce exactly 1 debounced IDB write" test passes with vi.useFakeTimers()
    - rawWriteCount("windowLayout") is 0 before timer advance and 1 after (not 50, not 2)
    - "persisted record contains exactly the 7 required fields" test passes
    - Object.keys(entry).sort() deep-equals the 7-element sorted array with no extra keys
    - Both tests use the RecordingSettingsStore test double — no real IDB
    - All existing DesktopShell tests still pass
    - tsc --noEmit 0 errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.vibe-apps && npx vitest run src/ui/DesktopShell.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Debounce test and shape test pass; rawWriteCount correctly counts 1 write per drag sequence; Object.keys check confirms exactly 7 persisted fields</done>
</task>

<task type="auto">
  <name>Task 2: Restore test (SC#1) + eviction test (SC#3) + full suite gate (SC#4)</name>
  <files>src/ui/DesktopShell.test.tsx</files>
  <read_first>
    src/ui/DesktopShell.test.tsx (as modified by Task 1) — read to understand current state before adding more tests.
    src/ui/desktopShellTestKit.tsx — confirm how to inject a pre-seeded settingsStore.readRaw value to simulate a persisted layout being present at mount time. The RecordingSettingsStore.readRaw(key) returns rawCurrentMap.get(key) ?? null; to pre-seed it, the test must call writeRaw(LAYOUT_KEY, JSON.stringify(layout)) on the test double BEFORE rendering DesktopShell.
    src/services/testServices.ts — confirm createTestServices, unusedTransport (should throw if called), createInMemoryRegistry, and how to seed the registry with a cached app record so tier-3 lookup succeeds.
    src/registry/db.ts — understand the AppRecord/StoredApp type for seeding the registry (what fields does registry.put("apps", record) require for a tier-3 hit).
    src/execution/loader.ts — read lines 260-295 to understand what tier-3 returns and what fields are required in the stored record so resolveComponent succeeds without calling produce.
  </read_first>
  <behavior>
    RESTORE TEST:
    - Pre-seed settingsStore with a "windowLayout" JSON containing 3 LayoutEntry objects at known x/y/z/minimized values
    - Pre-seed the registry with cached app records for each of the 3 appTypes so tier-3 hits succeed
    - After renderDesktopShell + mount (wait for async effects to settle), windowManager.windows has length 3
    - Each window's x, y, z matches the corresponding LayoutEntry
    - Each window's minimized state matches the LayoutEntry
    - Each window's instanceId is NOT the persisted value (there is no persisted instanceId — instanceIds are freshly minted by openAt and follow the appType-N format)

    EVICTION TEST:
    - Pre-seed settingsStore with a "windowLayout" JSON containing 1 entry for an appType that has NO corresponding record in the registry
    - The test uses unusedTransport (throws if called) — if resolveComponent were called and fell through to produce, the transport would be invoked and the test would fail
    - After mount, windowManager.windows has length 1 (the placeholder window is open)
    - The window's component (retrieved from the component map) is NOT null — it is the Fallback component
    - The transport was NOT called (unusedTransport silence proves no produce attempt)

    FULL SUITE GATE (SC#4 + cross-cutting):
    - Run the full test suite after the restore and eviction tests pass
    - Confirm 5 concurrent restore attempts do not trigger a gate error (serial resolution by design — the for...of await loop allows at most 1 in-flight at a time)
    - Confirm REGISTRY_DB_VERSION is still 3 in db.ts (assert by importing the constant)
  </behavior>
  <action>
    Add a new describe block "Desktop persistence — restore" to src/ui/DesktopShell.test.tsx.

    RESTORE TEST: Write "restoring 3 persisted windows reopens them at saved positions with fresh instanceIds (SC#1)":
    1. Build 3 LayoutEntry objects: e.g., [{appType:"notes",title:"Notes",icon:"n",x:100,y:100,z:201,minimized:false}, {appType:"weather",title:"Weather",icon:"w",x:200,y:200,z:202,minimized:false}, {appType:"calc",title:"Calculator",icon:"c",x:300,y:300,z:203,minimized:true}]
    2. Create a RecordingSettingsStore; call writeRaw(LAYOUT_KEY, JSON.stringify(threeEntries)) on it BEFORE rendering to pre-seed the readRaw response
    3. Create an in-memory registry and seed it with an AppRecord for each appType/cacheKey so tier-3 resolveComponent succeeds without calling produce; use a canned transport for the component text (a minimal valid JSX component)
    4. Render DesktopShell with these injected services; wait for async effects (use waitFor from @testing-library/react)
    5. Get windowManager.windows from the rendered context
    6. expect(windows.length).toBe(3)
    7. For each window entry: expect the x, y, z, minimized to match the layout entry; expect the instanceId to match /^(notes|weather|calc)-\d+$/ (fresh, not a stored UUID)
    8. The window with minimized:true should have window.minimized === true

    EVICTION TEST: Write "evicted app on restore shows placeholder without spending API quota (SC#3)":
    1. Build 1 LayoutEntry for appType "evicted-app" (not seeded in registry)
    2. Create RecordingSettingsStore pre-seeded with that layout JSON
    3. Create services with unusedTransport and a registry that does NOT contain any record for "evicted-app"
    4. Render DesktopShell; wait for async effects
    5. expect(windows.length).toBe(1) — the placeholder window opened
    6. The test must not throw (unusedTransport silence proves resolveComponent was not called for this app)
    7. The window's component (from component Map obtained via test kit or screen assertion) renders a retry button visible in the DOM (queryAllByRole("button") includes one with retry semantics — OR simply assert windows.length === 1 and no transport error)

    FULL SUITE GATE: After Task 2's tests pass, run the full test suite:
    - `npx vitest run` — assert 0 test failures
    - `npx tsc --noEmit` — 0 errors
    - `npx vitest run src/hygiene.test.ts` — lexicon gate green
    - Assert REGISTRY_DB_VERSION: import { REGISTRY_DB_VERSION } from "../../registry/db" in the test (or grep from a shell command in verify) and assert it equals 3
  </action>
  <acceptance_criteria>
    - Restore test: after mount, 3 windows present at correct x/y/z/minimized; each instanceId is a fresh session-scoped string (format: appType + "-" + number)
    - Eviction test: 1 placeholder window present; unusedTransport was NOT called (test does not throw)
    - REGISTRY_DB_VERSION === 3 confirmed (no DB migration was introduced)
    - Full test suite passes (all existing + new Phase 21 tests): `npx vitest run` exits 0
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/hygiene.test.ts` exits 0
    - `npm run build` completes without error and produced dist/ contains no .map files (sourcemaps off)
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/ProjectsSSD/Projects/o2.vibe-apps && npx vitest run 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>All 5 binding success criteria covered by automated tests; full suite green; tsc 0 errors; hygiene gate green; REGISTRY_DB_VERSION = 3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-seeded test data → RecordingSettingsStore.readRaw | Test controls the IDB content; executor must not accidentally use real IDB in tests |
| unusedTransport boundary | If resolveComponent is ever called for an evicted app, unusedTransport throws — this is the proof mechanism for PERSIST-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Tampering | Test-seeded layout JSON | accept | Tests control the input; the isLayoutEntry / deserializeLayout path is tested separately in Plan 21-01 layoutPersistence.test.ts |
| T-21-14 | Information Disclosure | DesktopShell.test.tsx assertions on persisted JSON | accept | Test file is never shipped; assertions confirm no secret keys appear in the JSON (SC#5 shape test) |
| T-21-15 | Elevation of Privilege | unusedTransport invocation in eviction test | mitigate | If resolveComponent is called without the pre-IDB-check guard, unusedTransport throws, failing the test — CI gate proves the guard is in place and PERSIST-03 is satisfied |
| T-21-16 | Denial of Service | Parallel restore tests sharing module-level zTop / counter | accept | vitest runs each test file in its own worker; module-level state does not bleed between test files; within one file, counters increment monotonically which is correct and expected |
</threat_model>

<verification>
Final Phase 21 gate — all must be green before creating the phase SUMMARY:
- `npx vitest run src/host/layoutPersistence.test.ts` — Plan 21-01 unit tests pass
- `npx vitest run src/ui/useWindowManager.test.tsx` — Plan 21-02 openAt tests pass
- `npx vitest run src/ui/DesktopShell.test.tsx` — all Plan 21-03 and 21-04 tests pass
- `npx vitest run` — complete suite (all phases) green
- `npx tsc --noEmit` — 0 errors
- `npx vitest run src/hygiene.test.ts` — lexicon gate green (no banned tokens in new source)
- `grep "REGISTRY_DB_VERSION" src/registry/db.ts` — outputs "REGISTRY_DB_VERSION = 3" (unchanged)
- `npm run build 2>&1 | grep -i "map\|sourcemap"` — no .map files produced
</verification>

<success_criteria>
- SC#1: Restore test passes — 3 windows at correct positions, fresh instanceIds
- SC#2: Debounce test passes — 50 moves produce exactly 1 write at 300ms
- SC#3: Eviction test passes — placeholder shown, unusedTransport silent (no quota spend)
- SC#4: 5-window serial restore produces no produce-gate error (structural: for...of await loop, pre-IDB-check)
- SC#5: Shape test passes — Object.keys(entry).sort() equals exactly the 7 required fields
- Full test suite green; tsc 0 errors; hygiene gate green; no DB version bump; no sourcemaps in build
</success_criteria>

<output>
After completion, create `.planning/phases/21-desktop-persistence/21-04-SUMMARY.md`

Then create the phase completion record at `.planning/phases/21-desktop-persistence/21-PHASE-SUMMARY.md` documenting:
- All 5 success criteria confirmed green
- PERSIST-01, PERSIST-02, PERSIST-03 satisfied
- Files modified (settingsStore.ts, testServices.ts, layoutPersistence.ts, useWindowManager.tsx, DesktopShell.tsx)
- New test files (layoutPersistence.test.ts, additions to DesktopShell.test.tsx, additions to useWindowManager.test.tsx)
- REGISTRY_DB_VERSION remains 3 (no DB migration)
- Zero new runtime dependencies
</output>
