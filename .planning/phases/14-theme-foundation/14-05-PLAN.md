---
phase: 14-theme-foundation
plan: 05
type: execute
wave: 4
depends_on: [14-03, 14-02]
files_modified:
  - src/ui/ThemeSelector.tsx
  - src/ui/ThemeSelector.test.tsx
  - src/ui/AppBar.tsx
  - src/index.css
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "A user sees four theme pills (Aurora / Aero / Aqua / Noir) in the current AppBar and can click any to switch the active theme"
    - "Clicking a pill calls setTheme(name), which re-applies the named-theme variables on document.documentElement (re-skin path)"
    - "The active pill is visually marked (aria-pressed / active class)"
  artifacts:
    - path: "src/ui/ThemeSelector.tsx"
      provides: "Four neutral-named theme pills driving useVibeTheme().setTheme"
      exports: ["ThemeSelector"]
    - path: "src/ui/AppBar.tsx"
      provides: "ThemeSelector mounted in the controls area (temporary home; Phase 16 relocates it)"
      contains: "ThemeSelector"
    - path: "src/ui/ThemeSelector.test.tsx"
      provides: "Switch-path test: clicking a pill changes the active theme + the documentElement vars"
      exports: []
  key_links:
    - from: "src/ui/ThemeSelector.tsx pill onClick"
      to: "useVibeTheme().setTheme"
      via: "setTheme(name) on click"
      pattern: "setTheme\\("
    - from: "src/ui/AppBar.tsx"
      to: "ThemeSelector"
      via: "rendered inside app-bar__controls"
      pattern: "<ThemeSelector"
---

<objective>
Add a temporary four-theme switcher (`ThemeSelector` — neutral-named pills for Aurora / Aero / Aqua / Noir) to the current AppBar, wired to `useVibeTheme().setTheme`, so the theme capability is visible and testable now (THEME-01). Phase 16 relocates it to the menu bar; this is its temporary home.

Purpose: THEME-01 requires a working, visible switch path this phase. The menu bar does not exist until Phase 16, so the switcher lands in the existing AppBar (Decision 9). Clicking a pill drives the Plan-03 provider's setTheme, which re-applies the contract on documentElement — proving the live re-skin mechanism (THEME-02) end-to-end.
Output: ThemeSelector.tsx + minimal pill styling, AppBar wiring, and a switch-path test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-theme-foundation/14-CONTEXT.md
@.planning/phases/14-theme-foundation/14-PATTERNS.md

<interfaces>
<!-- The provider hook the selector consumes + the AppBar controls pattern to mirror. -->

From src/ui/VibeThemeProvider.tsx (Plan 03):
```typescript
export type VibeThemeName = "aurora" | "aero" | "aqua" | "noir";
export function useVibeTheme(): { theme: VibeThemeName; setTheme: (n: VibeThemeName) => void };
```

From src/ui/AppBar.tsx (current — the controls pattern + the THEME_META static-map idiom to mirror):
```typescript
const THEME_META: Record<ThemeMode, { Icon; nextLabel: string }> = { /* ... */ };
export function AppBar({ onOpenAccount }: AppBarProps) {
  const { mode, cycleTheme } = useTheme();   // existing light/dark toggle — KEEP intact
  return (
    <header role="banner" className="app-bar">
      <span className="app-bar__wordmark">Marketplace</span>
      <div className="app-bar__controls"> {/* ThemeSelector goes FIRST here */}
        <button ...Account... />
        <button ...cycleTheme... />          {/* vestigial, NOT removed (552 tests) */}
      </div>
    </header>
  );
}
```

ThemeSelector shape (from 14-PATTERNS.md): a `THEME_LABELS: Record<VibeThemeName,string>` map, a `THEME_NAMES` ordered array, and a `.theme-selector` / `.theme-selector__pill` / `.theme-selector__pill--active` class scheme (all neutral). Each pill: aria-pressed={theme===name}, onClick={() => setTheme(name)}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ThemeSelector + minimal pill styling, with a switch-path test</name>
  <read_first>
    - src/ui/AppBar.tsx (full file — the controls pattern + static-map idiom to mirror)
    - src/ui/VibeThemeProvider.tsx (useVibeTheme hook signature + VibeThemeName)
    - src/ui/theme.test.tsx (RTL render + act + cleanup pattern; ServicesProvider wrapping)
    - src/ui/VibeThemeProvider.test.tsx (Plan 03 — how to wrap with VibeThemeProvider + ServicesProvider + injected settingsStore double in a test)
    - src/index.css (where the `.theme-selector` pill styles go — neutral classes only)
    - .planning/phases/14-theme-foundation/14-PATTERNS.md (ThemeSelector.tsx component shape + CSS class naming rule)
  </read_first>
  <behavior>
    - Test: ThemeSelector renders exactly four pills labeled Aurora, Aero, Aqua, Noir.
    - Test: the pill matching the current theme has aria-pressed="true"; the others "false".
    - Test (switch path): clicking the "Noir" pill calls setTheme("noir") so the active theme becomes noir AND document.documentElement.style.getPropertyValue("--text") becomes noir's value ("#f5eeff") — proving the click drives the document-root re-skin.
  </behavior>
  <action>
    Create `src/ui/ThemeSelector.tsx` per the 14-PATTERNS shape: import { useVibeTheme, type VibeThemeName }; define `THEME_LABELS: Record<VibeThemeName, string>` = {aurora:"Aurora", aero:"Aero", aqua:"Aqua", noir:"Noir"} and `THEME_NAMES: ReadonlyArray<VibeThemeName>` = ["aurora","aero","aqua","noir"]; render a `<div className="theme-selector" role="group" aria-label="Color theme">` mapping THEME_NAMES to `<button>` pills (className toggles `theme-selector__pill--active`, aria-pressed={theme===name}, type="button", onClick={() => setTheme(name)}). All identifiers/classes neutral (no banned tokens).

    Add minimal `.theme-selector`, `.theme-selector__pill`, `.theme-selector__pill--active` styles to src/index.css (small inline-row of pills; the active pill visually distinguished — use the theme contract vars where reasonable, e.g. border via var(--bord), but keep it minimal since Phase 16 restyles it). Neutral comments only.

    Create `src/ui/ThemeSelector.test.tsx` covering the three <behavior> cases. Render ThemeSelector inside `<ServicesProvider services={createTestServices({ settingsStore: <recording double> })}><VibeThemeProvider>...</VibeThemeProvider></ServicesProvider>` (reuse the Plan-03 wrapping). Drive clicks with act(); assert pill labels, aria-pressed, and the documentElement --text value after the Noir click. beforeEach clears localStorage + resets documentElement.style.cssText; afterEach cleanup.
  </action>
  <verify>
    <automated>npx vitest run src/ui/ThemeSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/ThemeSelector.tsx exports ThemeSelector, renders four pills (Aurora/Aero/Aqua/Noir) driving useVibeTheme().setTheme.
    - `grep -c "setTheme(" src/ui/ThemeSelector.tsx` ≥ 1.
    - src/index.css contains `.theme-selector__pill` styling with a `--active` variant.
    - src/ui/ThemeSelector.test.tsx covers render-four-pills, aria-pressed-active, and the click→setTheme→documentElement --text switch-path case.
    - `npx vitest run src/ui/ThemeSelector.test.tsx` exits 0.
  </acceptance_criteria>
  <done>ThemeSelector renders four working pills; clicking one drives setTheme and re-skins the document root; the switch path is test-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Mount ThemeSelector in the AppBar controls (temporary home)</name>
  <read_first>
    - src/ui/AppBar.tsx (full file — add the import + render ThemeSelector first inside app-bar__controls; keep the existing useTheme/cycleTheme toggle intact)
    - src/ui/AppBar.test.tsx (if present — confirm existing AppBar assertions still hold after the additive change)
    - .planning/phases/14-theme-foundation/14-PATTERNS.md (AppBar additive insertion guidance)
  </read_first>
  <action>
    Edit `src/ui/AppBar.tsx`: import ThemeSelector and render `<ThemeSelector />` as the FIRST child inside the existing `<div className="app-bar__controls">`, before the Account and cycleTheme buttons. Add a neutral comment noting it is a temporary home relocated in Phase 16. Do NOT remove or change the existing `useTheme`/`cycleTheme` light/dark toggle — it stays vestigial (552 tests depend on it per Decision 8). This is purely additive.
  </action>
  <verify>
    <automated>npx vitest run src/ui/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<ThemeSelector" src/ui/AppBar.tsx` ≥ 1 and the import is present.
    - The existing cycleTheme/useTheme toggle is still present in AppBar.tsx (not removed).
    - `npx vitest run src/ui/` exits 0 (AppBar and all UI tests, including any existing AppBar test, stay green).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ThemeSelector is mounted in the AppBar controls area as its temporary home; the existing theme toggle is intact; all UI tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → ThemeSelector → setTheme | Pill values are from the closed THEME_NAMES array (VibeThemeName); no free-form input reaches setTheme. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Information Disclosure | ThemeSelector labels / CSS classes | mitigate | Labels (Aurora/Aero/Aqua/Noir) and the `theme-selector` class scheme are hygiene-neutral; hygiene.test.ts scans the new .tsx + .css. |
| T-14-12 | Tampering | active-pill state | accept | Pill state derives from the validated provider theme; no persisted/external value influences which pill renders active beyond the already-validated theme name. |
</threat_model>

<verification>
- `npx vitest run src/ui/ThemeSelector.test.tsx` exits 0 (switch path proven).
- `npx vitest run src/ui/` exits 0 (AppBar + all UI tests green after additive wiring).
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/hygiene.test.ts` exits 0 (ThemeSelector.tsx, .test.tsx, AppBar.tsx, index.css pill styles — all neutral).
</verification>

<success_criteria>
- Four theme pills are visible in the AppBar and switch the active theme on click (SC1 / THEME-01).
- A pill click drives setTheme → document-root re-skin (SC1 mechanism / THEME-02), proven by the switch-path test.
- The existing light/dark toggle and all UI tests stay green; tsc clean; hygiene green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-theme-foundation/14-05-SUMMARY.md`
</output>
