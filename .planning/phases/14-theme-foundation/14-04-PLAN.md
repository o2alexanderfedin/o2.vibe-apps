---
phase: 14-theme-foundation
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - index.html
autonomous: true
requirements: [THEME-03, THEME-04]
must_haves:
  truths:
    - "On a hard reload with a non-default theme persisted, the page paints the correct theme from the first frame — the FOUC script applies the named-theme variables on documentElement before React mounts, with no flash of the default"
    - "The inline FOUC script's SHA-256 source hash in the CSP meta tag matches the new script body, so the script is authorized and csp.test.ts stays green in the same commit"
  artifacts:
    - path: "index.html"
      provides: "Extended FOUC script reading marketplace.osTheme + applying VIBE_THEMES vars on documentElement; CSP script-src sha256 hash regenerated to match"
      contains: "marketplace.osTheme"
  key_links:
    - from: "index.html inline FOUC script"
      to: "document.documentElement.style"
      via: "setProperty per VIBE_THEMES var before React mounts"
      pattern: "documentElement\\.style\\.setProperty"
    - from: "index.html CSP script-src sha256 source"
      to: "the inline script body bytes"
      via: "regenerated SHA-256 base64 hash (csp.test.ts recomputes + asserts)"
      pattern: "sha256-"
---

<objective>
Extend the existing inline first-paint script in index.html to ALSO read the persisted OS theme name from localStorage and apply the matching VIBE_THEMES variables to document.documentElement synchronously, before React mounts — eliminating any flash of the default theme on reload (THEME-03, FOUC-safe). Regenerate the CSP `script-src` SHA-256 source hash to match the new script body, in the SAME commit, so csp.test.ts stays green (hard invariant).

Purpose: localStorage is the FOUC source of truth (Decision 4/5). The runtime provider (Plan 03) owns switches, but first paint must be correct before React loads — only the inline script can do that. The CSP authorizes this inline script by the exact hash of its bytes; ANY edit changes the hash, so the hash MUST be recomputed and embedded in the same commit or first paint silently breaks in production. This is a single atomic unit.
Output: An extended FOUC script in index.html with a correct regenerated CSP hash; csp.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-theme-foundation/14-CONTEXT.md
@.planning/phases/14-theme-foundation/14-PATTERNS.md

<interfaces>
<!-- The exact current script + CSP, and the hash recompute procedure. -->

From index.html (current — lines 13–24):
```html
<meta http-equiv="Content-Security-Policy"
  content="default-src 'self'; script-src 'self' 'unsafe-eval' 'sha256-N+v/OMOSGIWhW6MiaeKgpUrhYfTwftAJZBpsRoTejkc='; style-src ...; connect-src ...; ..." />
<script>
  (function () {
    var stored = localStorage.getItem('marketplace.theme');
    var theme = stored === 'light' || stored === 'dark' ? stored
      : window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', theme);
  })();
</script>
```

From src/csp.test.ts (the guard — recomputes the hash from the live file; the test does NOT change, the embedded CSP hash does):
```typescript
function inlineScriptBody(html) { return html.match(/<script>([\s\S]*?)<\/script>/)[1]; }
function sha256Source(body) { return "sha256-" + createHash("sha256").update(body, "utf8").digest("base64"); }
// asserts script-src directive contains `'${sha256Source(inlineScriptBody(html))}'`
// also asserts script-src has NO 'unsafe-inline' and DOES retain 'unsafe-eval'
```

VIBE_THEMES values (must match src/ui/VibeThemeProvider.tsx from Plan 03 EXACTLY — copy the same four-theme map verbatim into the inline script; storage key literal is 'marketplace.osTheme').

Hash recompute command (run AFTER editing the script body):
```
node -e "const c=require('node:crypto');const f=require('node:fs');const html=f.readFileSync('index.html','utf8');const body=html.match(/<script>([\s\S]*?)<\/script>/)[1];console.log('sha256-'+c.createHash('sha256').update(body,'utf8').digest('base64'))"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (ATOMIC): Extend the FOUC script + regenerate the CSP hash + csp.test.ts green</name>
  <read_first>
    - index.html (full file — the inline script at lines 17–24 and the CSP meta at lines 13–16)
    - src/csp.test.ts (full file — the hash-derivation logic at lines 26–41 and the assertions at 72–86; understand that the test recomputes from the live file)
    - src/ui/VibeThemeProvider.tsx (the VIBE_THEMES map from Plan 03 — the inline script must apply the SAME values; copy them verbatim)
    - .planning/phases/14-theme-foundation/14-PATTERNS.md (index.html extended-script shape + CSP Hash Update Procedure)
  </read_first>
  <action>
    This is ONE atomic task — the index.html script edit, the hash regeneration, and csp.test.ts passing all land in a single commit (hard invariant per Decision 5).

    (1) Edit the inline `<script>` in index.html: AFTER the existing `data-theme` setAttribute line, append a self-contained block that (a) declares a `VIBE_THEMES` object literal with the four themes and their 12 vars each — values copied VERBATIM from src/ui/VibeThemeProvider.tsx so first paint matches the runtime provider exactly; (b) reads `localStorage.getItem('marketplace.osTheme')`; (c) selects that name if it is a key of VIBE_THEMES else 'aurora'; (d) loops the selected theme's entries calling `document.documentElement.style.setProperty(k, vars[k])`. Use plain ES5-compatible function-scoped vars (the existing script style) — no imports (an inline script cannot import). Keep a neutral comment ("Named theme — apply CSS custom properties synchronously to avoid flash"). Do NOT remove or alter the existing data-theme logic.

    (2) Run the hash recompute command from <interfaces> to get the new `sha256-...` source for the edited script body.

    (3) Replace the OLD `'sha256-N+v/OMOSGIWhW6MiaeKgpUrhYfTwftAJZBpsRoTejkc='` literal in the CSP meta `content` attribute's `script-src` directive (index.html line 15) with the newly computed `'sha256-...'` value. Leave `'unsafe-eval'` in place and do NOT add `'unsafe-inline'`.

    (4) Run csp.test.ts — it must pass (it recomputes the hash from the live file and asserts the CSP contains it). Commit ALL changes together: `feat(14-04): extend FOUC script for named theme + sync CSP sha256 hash`.
  </action>
  <verify>
    <automated>npx vitest run src/csp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "marketplace.osTheme" index.html` ≥ 1 (FOUC script reads the OS theme key).
    - `grep -c "documentElement.style.setProperty" index.html` ≥ 1 (applies vars on document root pre-mount).
    - The inline script's VIBE_THEMES values match src/ui/VibeThemeProvider.tsx (e.g. noir --text "#f5eeff", aurora --accentA "#9b7cff").
    - The CSP `script-src` no longer contains the old hash `N+v/OMOSGIWhW6MiaeKgpUrhYfTwftAJZBpsRoTejkc=`; it contains the regenerated hash matching the new script body.
    - `npx vitest run src/csp.test.ts` exits 0 — both the hash-match assertion AND the "no 'unsafe-inline', retains 'unsafe-eval'" assertion pass.
    - The connect-src DATA-02 assertions in csp.test.ts still pass (connect-src untouched).
  </acceptance_criteria>
  <done>The FOUC script applies the persisted named theme on documentElement before React mounts, and the CSP sha256 hash is regenerated to authorize the new script body — all in one commit, csp.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → inline FOUC script | The persisted theme name is read pre-mount; validated against VIBE_THEMES keys before any setProperty (invalid → aurora). |
| inline script ↔ CSP | The script runs only if its byte-exact sha256 hash is in script-src — the regenerated hash is the authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | CSP hash desync | mitigate | csp.test.ts recomputes the hash from the live file and fails CI if the embedded CSP hash does not match the edited script body — desync cannot ship. |
| T-14-09 | Elevation of Privilege | inline-script authorization | mitigate | Authorization stays hash-based; the plan forbids adding 'unsafe-inline' (asserted by csp.test.ts), preserving the posture. |
| T-14-10 | Tampering | persisted osTheme value | mitigate | The FOUC script selects the stored name only if it is a key of VIBE_THEMES, else 'aurora' — no arbitrary value reaches setProperty. |
</threat_model>

<verification>
- `npx vitest run src/csp.test.ts` exits 0 (hash matches + posture preserved + connect-src intact).
- `npx vitest run src/hygiene.test.ts` exits 0 (the new inline-script comment + VIBE_THEMES literal carry no banned token; index.html is in the hygiene scan scope).
- Manual reasoning (no automated browser frame test available offline): with `marketplace.osTheme` = "noir" in localStorage, the inline script sets noir's vars on documentElement synchronously before the module script runs — first paint is noir (SC2).
</verification>

<success_criteria>
- On hard reload with a non-default theme persisted, the correct palette paints from the first frame, no flash of the default (SC2 / THEME-03).
- The CSP sha256 hash is regenerated in the same commit as the script edit; csp.test.ts is green (SC5 invariant).
- 'unsafe-inline' stays out, 'unsafe-eval' stays in; connect-src allowlist unchanged.
- Hygiene gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-theme-foundation/14-04-SUMMARY.md`
</output>
