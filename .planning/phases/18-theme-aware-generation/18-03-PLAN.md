---
phase: 18-theme-aware-generation
plan: 03
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - src/execution/producer.ts
  - src/execution/producer.test.ts
  - src/execution/producerWidget.test.ts
  - src/execution/producerDelegated.test.ts
  - src/execution/producerShell.test.ts
  - src/execution/colorCheck.ts
  - src/test/fixtures/calculator.code.txt
  - src/test/fixtures/calculator.raw.txt
  - src/test/fixtures/budget.code.txt
  - src/test/fixtures/budget.raw.txt
  - src/test/fixtures/weather.code.txt
  - src/test/fixtures/weather.raw.txt
  - src/test/fixtures/widget-data-table.raw.txt
autonomous: true
requirements:
  - TGEN-01
  - TGEN-02

must_haves:
  truths:
    - "All four UI prompt kinds (app, widget, shell, delegated) mandate the new theme var contract via var(--accentA), var(--accentB), var(--text), var(--glass), var(--glass2), var(--bord), var(--hi); the handler kind stays theming-free"
    - "buildRepairPrompt carries the same new var contract for app/widget/shell/delegated kinds (the app/widget branch at L254 is the asserted instance)"
    - "buildLengthPrompt intentionally stays theming-light — it carries NO CSS var contract (the post-compile colorCheck is the backstop); no task asserts CSS vars in buildLengthPrompt"
    - "Old vars (--color-surface, --color-text, --color-accent) are removed from all UI prompt strings"
    - "checkForHardcodedColors is called after transpile() in produceComponent for kind !== 'handler'"
    - "A saturated hex in produced code causes self-heal retry (TranspileError caught by existing loop)"
    - "Handlers (kind='handler') skip the color check — handlers produce plain TS, not React UI"
    - "EVERY fixture fed through produceComponent with a valid (non-truncated) key by an existing green test is re-skinned to theme vars / grayscale / neutral-alpha so it PASSES the new colorCheck. This is the FULL set, not just the .code.txt fixtures: calculator.code.txt, calculator.raw.txt, budget.code.txt, budget.raw.txt, weather.code.txt, weather.raw.txt, widget-data-table.raw.txt. (The .raw.txt fixtures preserve their markdown-fence / export-default / prose structure so extractCode still works — only color literals change.)"
    - "All pre-existing tests stay green (full suite >= 669), including injection.test.ts L55/L129/L156, MarketplaceFixtures.test.tsx L57/L70, and producerWidget.test.ts L104-110"
  artifacts:
    - path: "src/execution/producer.ts"
      provides: "Updated buildPrompt/buildRepairPrompt (UI kinds) + colorCheck wiring"
      contains: "var(--accentA)"
    - path: "src/execution/colorCheck.ts"
      provides: "Pure color checker (from Plan 18-01, no changes here except import in producer)"
    - path: "src/test/fixtures/calculator.code.txt"
      provides: "Calculator (.code.txt) produce-success fixture re-skinned to theme vars / grayscale / neutral-alpha (passes colorCheck)"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/calculator.raw.txt"
      provides: "Calculator (.raw.txt) produce-success fixture re-skinned (passes colorCheck); markdown fence + export-default structure preserved"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/budget.code.txt"
      provides: "Budget (.code.txt) produce-success fixture re-skinned to theme vars / grayscale / neutral-alpha (passes colorCheck)"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/budget.raw.txt"
      provides: "Budget (.raw.txt) produce-success fixture re-skinned (passes colorCheck); markdown fence + export-default structure preserved"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/weather.code.txt"
      provides: "Weather (.code.txt) produce-success fixture re-skinned (passes colorCheck)"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/weather.raw.txt"
      provides: "Weather (.raw.txt) produce-success fixture re-skinned (passes colorCheck); markdown fence + export-default structure preserved"
      contains: "var(--accentA)"
    - path: "src/test/fixtures/widget-data-table.raw.txt"
      provides: "Data-table widget produce-success fixture re-skinned (passes colorCheck)"
      contains: "var(--accentA)"
  key_links:
    - from: "src/execution/producer.ts"
      to: "src/execution/colorCheck.ts"
      via: "import { checkForHardcodedColors } from './colorCheck'"
      pattern: "checkForHardcodedColors"
    - from: "src/execution/producer.ts"
      to: "src/execution/transpile.ts"
      via: "existing import of transpile, TranspileError"
      pattern: "TranspileError"
    - from: "src/test/fixtures/calculator.code.txt"
      to: "src/execution/colorCheck.ts"
      via: "fed through produceComponent in injection.test.ts (codeFixture path); must pass colorCheck"
      pattern: "var\\(--accentA\\)"
    - from: "src/test/fixtures/calculator.raw.txt"
      to: "src/execution/colorCheck.ts"
      via: "fed through produceComponent in injection.test.ts L54 (rawFixture) and MarketplaceFixtures.test.tsx L45/L64 (rawFixture); must pass colorCheck"
      pattern: "var\\(--accentA\\)"
    - from: "src/test/fixtures/weather.raw.txt"
      to: "src/execution/colorCheck.ts"
      via: "fed through produceComponent via the weather produce path; must pass colorCheck"
      pattern: "var\\(--accentA\\)"
---

<objective>
Wire TGEN-01 (update the four UI prompt kinds to use the new theme CSS var contract) and TGEN-02 (call checkForHardcodedColors after transpile in produceComponent). Update all affected producer test files to use new var names, AND re-skin EVERY saturated-color produce-success fixture that flows through the now-gated produceComponent so they pass the new color gate.

Purpose: After this plan, every app produced by the platform will receive prompts that mandate the theme CSS variable contract. Produced code that slips in a saturated hardcoded color will be caught post-compile and fed back to the self-heal loop automatically — no new retry infrastructure needed.

Critical reconciliation: existing GREEN tests feed SATURATED-color captured fixtures through produceComponent and assert produce-SUCCESS. colorCheck is wired for ALL non-handler kinds, so EVERY such fixture must be re-skinned — not only the `.code.txt` siblings. The full set: calculator.code.txt (#667eea/#10b981/#ef4444), calculator.raw.txt (same literals, fed by MarketplaceFixtures.test.tsx L45/L64 and injection.test.ts L54), budget.code.txt (#4caf50/#f44336/rgba(76,175,80)), budget.raw.txt (same literals), weather.code.txt + weather.raw.txt (#3b82f6/#60a5fa/#fca5a5/rgba(59,130,246,…)/rgba(226,232,240,…)), and widget-data-table.raw.txt (#0066cc). The new colorCheck would throw TranspileError on any of these → self-heal re-feeds the same unchanged fixture → identical-error early-stop → ProduceError → those success assertions fail and the suite drops below 669. Task 3 re-skins ALL of them to theme vars / grayscale / neutral-alpha so they PASS the gate AND additionally prove the new theme contract end-to-end.

Output: producer.ts (updated UI prompts + colorCheck wiring), updated test files (new var names), seven re-skinned fixtures, full suite green (>= 669).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-theme-aware-generation/18-01-SUMMARY.md

# Key source files — read every line before touching
@src/execution/producer.ts
@src/execution/producer.test.ts
@src/execution/producerWidget.test.ts
@src/execution/producerDelegated.test.ts
@src/execution/producerShell.test.ts
@src/execution/colorCheck.ts
@src/execution/transpile.ts

# Fixtures reconciled in Task 3 — read each in full before editing
@src/test/fixtures/calculator.code.txt
@src/test/fixtures/calculator.raw.txt
@src/test/fixtures/budget.code.txt
@src/test/fixtures/budget.raw.txt
@src/test/fixtures/weather.code.txt
@src/test/fixtures/weather.raw.txt
@src/test/fixtures/widget-data-table.raw.txt

# Tests that feed those fixtures through produceComponent and assert success
@src/services/injection.test.ts
@src/ui/MarketplaceFixtures.test.tsx
</context>

<interfaces>
<!-- Contracts the executor must use. -->

New theme CSS variable contract (from CONTEXT.md D-1):
The vars to mandate in prompts:
- var(--accentA)      — primary brand accent
- var(--accentB)      — secondary brand accent
- var(--text)         — body text
- var(--glass)        — glass surface fill
- var(--glass2)       — secondary glass surface
- var(--bord)         — border/separator
- var(--hi)           — highlight

Gradients: from var(--accentA) to var(--accentB)

ALLOW in the prompt instruction (explicit carve-out so the model doesn't fight shadows):
- rgba(0,0,0,α) — for shadows, overlays
- rgba(255,255,255,α) — for glass highlights

Old vars being REPLACED (remove from all prompt strings):
- var(--color-surface) → replace with var(--glass) or var(--glass2)
- var(--color-text)    → replace with var(--text)
- var(--color-accent)  → replace with var(--accentA)

Handler kind: handlers produce plain TypeScript, NOT React components with styling.
The "handler" kind prompt MUST NOT receive the theme var instruction — it would be confusing and irrelevant. The existing handler prompt is theming-free by design. Leave it unchanged.

buildLengthPrompt: intentionally stays theming-LIGHT. Its app/widget branch (producer.ts L292-303) carries NO CSS var contract today and is NOT to be given one in this plan — the post-compile colorCheck is the backstop for length-retried output. Do NOT add a CSS-var assertion for buildLengthPrompt. (Only buildPrompt and buildRepairPrompt carry the var contract.)

colorCheck behavior (from Plan 18-01 — what the gate flags):
- FLAGS saturated hex (#rgb / #rrggbb / #rrggbbaa where R≠G or G≠B) and saturated rgb()/rgba() (where R≠G or G≠B).
- ALLOWS grayscale hex (#000 #fff #333 #e0e0e0 …, R===G===B), rgba(0,0,0,α), rgba(255,255,255,α), and any rgb/rgba where R===G===B.
- Alpha channel is irrelevant; only the R/G/B equality decides.

colorCheck wiring in produceComponent (producer.ts around line 460-478):
The EXISTING transpile block:
```
const transpiledJS =
  kind === "handler"
    ? transpileHandler(code, { filename: type + ".ts" })
    : transpile(code, { filename: type + ".tsx" });
// handler purity guard follows...
if (kind === "handler" && /\brequire\s*\(/.test(transpiledJS)) { throw ... }
```

After the purity guard and before `return { source: code, transpiledJS }`, add:
```
// TGEN-02: post-compile saturated color check (skipped for handlers — no React/CSS)
if (kind !== "handler") {
  checkForHardcodedColors(transpiledJS);
}
```

The TranspileError thrown by checkForHardcodedColors is caught by the EXISTING catch block (line 479-480) with no changes needed to the self-heal loop.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update buildPrompt + buildRepairPrompt (UI kinds) with new theme var contract (TGEN-01)</name>
  <files>src/execution/producer.ts</files>
  <read_first>
    - src/execution/producer.ts — full file; know every prompt branch before editing
    - src/execution/producerDelegated.test.ts — existing test for delegated prompts (these will need updating in Task 2)
    - src/execution/producerWidget.test.ts — existing test for widget prompts (these will need updating in Task 2)
    - src/execution/producerShell.test.ts — existing test for shell prompts (these will need updating in Task 2)
  </read_first>
  <behavior>
    Test assertions to check (verified by running producer tests after changes):
    - buildPrompt("weather") contains "var(--accentA)" — NOT "var(--color-surface)"
    - buildPrompt("weather") contains "var(--text)" — NOT "var(--color-text)"
    - buildPrompt("notes", "widget") contains "var(--accentA)"
    - buildPrompt("calculator", "delegated") contains "var(--accentA)" and "var(--glass)"
    - buildPrompt("calculator", "shell") contains "var(--accentA)"
    - buildPrompt("calc", "handler") does NOT contain any CSS var (handler is theming-free)
    - buildRepairPrompt("weather", code, err) contains "var(--accentA)" (app/widget branch, L254)
    - buildLengthPrompt is NOT asserted to contain CSS vars — it stays theming-light (do NOT add a var contract to it)
  </behavior>
  <action>
    Edit src/execution/producer.ts. For each of the FOUR non-handler kinds (app, widget, shell, delegated), update the theming instruction in buildPrompt and buildRepairPrompt (NOT buildLengthPrompt — see below):

    REPLACE the old CSS vars with the new theme contract. The new theming instruction for app/widget/shell/delegated prompts should read:
    "Style using the host's CSS variables: var(--accentA) and var(--accentB) for brand colors (gradients go from var(--accentA) to var(--accentB)), var(--text) for text, var(--glass) and var(--glass2) for surface fills, var(--bord) for borders, var(--hi) for highlights. For shadows and glass overlays, rgba(0,0,0,α) and rgba(255,255,255,α) are allowed — do NOT use hardcoded hex or rgb brand colors."

    The above is the required substance; adapt the phrasing to fit naturally into each prompt branch (the instruction doesn't need to be verbatim, but must name all seven vars and the gradient pattern, must explicitly allow rgba black/white shadows, and must forbid hardcoded brand colors).

    Specifically:
    - "shell" kind (line 124): replace "var(--color-surface), var(--color-text), var(--color-accent)" with the new instruction
    - "delegated" kind (line 154): replace the old vars in the STYLING paragraph — the delegated prompt has a detailed STYLING block; update that block completely
    - "widget" kind (line 168): replace "var(--color-surface), var(--color-text), var(--color-accent)"
    - "app" kind (default, line 196): replace "var(--color-surface), var(--color-text), var(--color-accent)"
    - buildRepairPrompt app/widget branch (line 254): replace old var reference with the new contract — this branch IS asserted in tests, keep it carrying the vars
    - buildLengthPrompt (lines 265-304): DO NOT add a theming/var instruction. It is intentionally theming-light; the post-compile colorCheck is the backstop for length-retried output. Leave its app/widget branch as-is.

    Handler kind: do NOT add theming instruction. Leave the handler prompts entirely unchanged.

    After editing, run:
    npx tsc --noEmit 2>&1 | head -10
    (must be clean — producer.ts is TypeScript)
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "var(--accentA)" src/execution/producer.ts</automated>
  </verify>
  <done>
    producer.ts has >= 5 occurrences of "var(--accentA)" (one per non-handler buildPrompt branch + the buildRepairPrompt app/widget branch). No occurrence of "var(--color-surface)" or "var(--color-text)" or "var(--color-accent)" remains. buildLengthPrompt carries NO CSS var contract. tsc exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire checkForHardcodedColors into produceComponent + update producer tests</name>
  <files>
    src/execution/producer.ts,
    src/execution/producer.test.ts,
    src/execution/producerWidget.test.ts,
    src/execution/producerDelegated.test.ts,
    src/execution/producerShell.test.ts
  </files>
  <read_first>
    - src/execution/producer.ts — current state after Task 1 edits
    - src/execution/colorCheck.ts — export signature (from Plan 18-01)
    - src/execution/producer.test.ts — existing prompt assertions that reference old var names
    - src/execution/producerWidget.test.ts — existing widget prompt assertions
    - src/execution/producerDelegated.test.ts — existing delegated prompt assertions (checks for "var(--color-surface)")
    - src/execution/producerShell.test.ts — existing shell prompt assertions
  </read_first>
  <behavior>
    Wiring behavior:
    - produceComponent(kind="app") with a canned transport returning "#ff0000" hex → eventually throws ProduceError after exhausting retries (because every attempt re-triggers the color check)
    - produceComponent(kind="handler") with canned transport → colorCheck is NOT called (kind==="handler" branch skips it)
    - produceComponent(kind="app") with valid code (no saturated hex) → succeeds and returns { source, transpiledJS }

    Updated test assertions (tests that currently check old var names):
    - Any test asserting "var(--color-surface)" → update to "var(--accentA)" or "var(--glass)"
    - Any test asserting "var(--color-text)" → update to "var(--text)"
    - Any test asserting "var(--color-accent)" → update to "var(--accentA)"
    - Tests that check prompt contains theming instruction should still pass after var name update
    - Do NOT add or change any assertion that requires buildLengthPrompt to contain CSS vars — it stays theming-light
  </behavior>
  <action>
    Step 1 — Wire checkForHardcodedColors in producer.ts:
    Add import at top of producer.ts: import { checkForHardcodedColors } from "./colorCheck";
    In produceComponent(), after the handler purity guard block (after the `if (kind === "handler" && /\brequire/.test...)` throw), add:
    if (kind !== "handler") { checkForHardcodedColors(transpiledJS); }
    Place this BEFORE the `return { source: code, transpiledJS }` line and INSIDE the try block so TranspileError is caught by the existing catch.

    Step 2 — Update test files:
    Run the execution + services suites to discover which tests now fail due to var name changes:
    npx vitest run src/execution/ src/services/ 2>&1 | grep -E "FAIL|AssertionError" | head -20

    For each failing assertion, update the expected string from old vars to new vars. The pattern to find-and-replace across the test files:
    - "var(--color-surface)" → "var(--glass)" (or "var(--accentA)" depending on context)
    - "var(--color-text)" → "var(--text)"
    - "var(--color-accent)" → "var(--accentA)"

    NOTE: at this step, the produce-success tests that feed SATURATED fixtures will still FAIL (injection.test.ts L55/L129/L156, MarketplaceFixtures.test.tsx L57/L70, and producerWidget.test.ts L104-110) because the fixtures haven't been re-skinned yet — that is Task 3's job. Do NOT "fix" them here by weakening the assertions; leave them failing and proceed to Task 3.

    Step 3 — Add regression tests in producer.test.ts for the color check wiring:
    a) Test that a canned response containing "#ff0000" causes produceComponent to ultimately throw ProduceError (after self-heal retries are exhausted). Use a sequenceTransport that always returns "#ff0000" code. Expect ProduceError after the identical-error early-stop.
    b) Test that kind="handler" with a canned response containing "#ff0000" does NOT trigger the color check — produceComponent("calc-handler", transport, withKey, "handler") succeeds.
    c) Test that a canned response with "rgba(0,0,0,0.3)" (valid shadow) does NOT trigger the color check — produceComponent succeeds.

    Step 4 — tsc clean:
    npx tsc --noEmit
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "checkForHardcodedColors" src/execution/producer.ts</automated>
  </verify>
  <done>
    producer.ts imports checkForHardcodedColors and calls it in produceComponent for kind !== "handler". Prompt-assertion test files (producer.test.ts, producerWidget.test.ts, producerDelegated.test.ts, producerShell.test.ts) reference new var names — no remaining references to var(--color-surface)/var(--color-text)/var(--color-accent) in those files. New colorCheck-wiring regression tests added. tsc exits 0. (The saturated-fixture produce-success tests are KNOWN-failing until Task 3 re-skins the fixtures — full-suite green is asserted in Task 3.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile EVERY saturated-color produce-success fixture with the new color gate</name>
  <files>
    src/test/fixtures/calculator.code.txt,
    src/test/fixtures/calculator.raw.txt,
    src/test/fixtures/budget.code.txt,
    src/test/fixtures/budget.raw.txt,
    src/test/fixtures/weather.code.txt,
    src/test/fixtures/weather.raw.txt,
    src/test/fixtures/widget-data-table.raw.txt
  </files>
  <read_first>
    - src/test/fixtures/calculator.code.txt — read in FULL; locate every saturated literal (enumerated below)
    - src/test/fixtures/calculator.raw.txt — read in FULL; SAME body as calculator.code.txt but wrapped in a markdown ```tsx fence with a trailing closing fence and `export default App;` (the .raw.txt structure). Same saturated literals.
    - src/test/fixtures/budget.code.txt — read in FULL; locate every saturated literal (enumerated below)
    - src/test/fixtures/budget.raw.txt — read in FULL; SAME body as budget.code.txt wrapped in a markdown ```tsx fence; same saturated literals.
    - src/test/fixtures/weather.code.txt — read in FULL; locate every saturated literal (enumerated below)
    - src/test/fixtures/weather.raw.txt — read in FULL; SAME body as weather.code.txt wrapped in a markdown ```tsx fence; same saturated literals.
    - src/test/fixtures/widget-data-table.raw.txt — read in FULL; locate the single saturated literal #0066cc
    - src/execution/colorCheck.ts — confirm exactly which literals the gate flags (saturated = R≠G or G≠B)
    - src/services/injection.test.ts — the affected assertions: L54-55 (calculator produce success via rawFixture → expects source contains "function App()" + transpiledJS contains "React.createElement"), L129 (calculator self-heal success via codeFixture → transpiledJS contains "React.createElement"), L155-157 (budget produce success via codeFixture, asserts NO global fetch + rec.calls === 1). Also note: weather is fed at L66 (codeFixture, null-key short-circuit — does NOT reach transpile, so colorCheck is irrelevant there) and L107/L110 (truncatedTransport — never reaches the fixture body), so the weather fixtures must still be re-skinned for any path that DOES reach transpile through them.
    - src/ui/MarketplaceFixtures.test.tsx — L45 + L64 feed rawFixture("calculator") through the real Marketplace open flow; L57 asserts the failure fallback is NOT present and L70 asserts interactive controls. With colorCheck live, an un-re-skinned calculator.raw.txt would throw on produce → the failure fallback WOULD appear → L57 fails. Re-skinning calculator.raw.txt fixes this.

    LOAD-BEARING CHECK (verified during planning, executor MUST re-confirm): grep the test files for the saturated color strings (#667eea, #10b981, #ef4444, #4caf50, #f44336, #6366f1, #0066cc, #3b82f6, #60a5fa, #fca5a5, etc.). No test asserts any of these specific color strings — assertions check ONLY structural markers ("function App()", "React.createElement", "```" absence), failure-fallback absence, and call counts. So replacing colors does NOT break any assertion. If, contrary to this, the executor finds a test asserting a specific saturated color string, that assertion MUST be updated to the new var/grayscale value in the same task (and called out in the SUMMARY).
  </read_first>
  <action>
    Hand-edit ALL SEVEN fixtures to replace EVERY saturated hardcoded color with a theme var, grayscale hex, or neutral-alpha rgba — so each fixture PASSES checkForHardcodedColors while remaining a valid produce-success fixture. Keep all already-grayscale literals (#e0e0e0, #ffffff, #1a1a1a, #f5f5f5, #333, #1e293b/#0f172a only if confirmed grayscale — NOTE these are saturated, see weather below; rgba(0,0,0,α), rgba(255,255,255,α), rgba(100,100,100,α)) AS-IS only when they are genuinely grayscale (R===G===B). They already pass.

    CRITICAL — .raw.txt structure preservation: The four `.raw.txt` fixtures (calculator.raw.txt, budget.raw.txt, weather.raw.txt, plus widget-data-table.raw.txt) carry a leading blank line + a markdown ```tsx opening fence, the component body, a trailing `export default App;`, and a closing ``` fence (widget-data-table.raw.txt follows its own raw structure). The platform's extractCode strips the fences to recover the component. DO NOT alter the fence lines, the `export default App;` line, the leading/trailing whitespace, or any prose/structure — change ONLY the color literals inside the body. After editing, the fence/export structure must be byte-for-byte identical to before except for the swapped color literals, so extractCode still parses cleanly. (The `.code.txt` siblings have NO fence — they are the already-stripped body; edit colors only there too.)

    Replacement convention (apply consistently across all seven fixtures):
    - primary brand color → var(--accentA)
    - secondary brand / gradient endpoint → var(--accentB)
    - success/positive accent → var(--accentA) (no separate "green" var exists in the contract)
    - danger/negative accent → var(--accentA) (no separate "red" var exists; use the accent — the fixture's job is to pass the gate, not to look semantically perfect)
    - surface fill → var(--glass) or var(--glass2)
    - border → var(--bord)
    - text → var(--text)
    - saturated rgba tints → rgba(0,0,0,α) or rgba(255,255,255,α) neutral, OR a var(--glass…) fill
    - the calculator's `:root { --color-surface: #1e1e2e; --color-accent: #667eea; }` block: replace the SATURATED VALUES with grayscale (e.g. --color-surface: #1e1e1e) and/or point them at theme vars (e.g. --color-accent: var(--accentA)). #e0e0e0 there is grayscale — keep it.

    EXACT saturated literals to replace (enumerated during planning — executor re-confirms by reading each file). The calculator.code.txt and calculator.raw.txt bodies are IDENTICAL color-wise; the budget pair is IDENTICAL; the weather pair is IDENTICAL. Apply the SAME swaps to each member of a pair so they stay in sync.

    calculator.code.txt AND calculator.raw.txt (apply identical swaps; line numbers below are for .code.txt — in .raw.txt they are offset by ~+2 for the leading blank + fence, plus the trailing `export default App;` / closing fence which must be left untouched):
    - gradient: #667eea, #764ba2 → from var(--accentA) to var(--accentB)
    - --color-surface: #1e1e2e → grayscale #1e1e1e (or var(--glass))
    - --color-accent: #667eea → var(--accentA)
    - rgba(102, 126, 234, 0.2) → rgba(255,255,255,0.1) or var(--glass2)
    - rgba(102, 126, 234, 0.4) → rgba(0,0,0,0.4) (shadow)
    - #7c8ef5 → var(--accentB)
    - #10b981 → var(--accentA)
    - #059669 → var(--accentA)
    - #ef4444 → var(--accentA)
    - #dc2626 → var(--accentA)
    - rgba(239, 68, 68, 0.3) → rgba(255,255,255,0.1)
    - rgba(239, 68, 68, 0.5) → rgba(255,255,255,0.2)
    - KEEP: #e0e0e0, rgba(0,0,0,…)

    budget.code.txt AND budget.raw.txt (apply identical swaps to both):
    - rgba(76, 175, 80, …) → rgba(255,255,255,…) neutral or var(--glass2) fill + var(--bord) border
    - #4caf50, #f44336 → var(--accentA)
    - rgba(255, 152, 0, …) → rgba(255,255,255,…) neutral or var(--glass2)/var(--bord)
    - #ff9800 → var(--accentA)
    - #f44336 (every occurrence — remaining color, progress bar, error text, delete button) → var(--accentA)
    - --color-surface: #1e1e2e → grayscale #1e1e1e (or var(--glass))
    - --color-accent: #6366f1 → var(--accentA)
    - KEEP: #e0e0e0, rgba(100,100,100,…), rgba(0,0,0,…)
    - NOTE on `rgba(var(--color-accent-rgb, 100, 100, 255), α)` expressions: the FIRST argument is `var(...)`, not a digit, so colorCheck's rgb scanner does NOT match these (it keys on a leading digit). Leave them OR replace with a var(--glass…) fill for belt-and-suspenders. Re-confirm against colorCheck.ts before deciding.
    - NOTE on `--color-accent-rgb: 99, 102, 241;` (a bare CSS custom-property value, not inside an rgb()/hex literal): colorCheck's scanners do NOT match it (no `#` prefix, not inside `rgb(`). Leave it OR neutralize to `--color-accent-rgb: 128, 128, 128;`. Re-confirm against colorCheck.ts before deciding.

    weather.code.txt AND weather.raw.txt (apply identical swaps to both — these are HEAVILY saturated; re-confirm every literal):
    - :root --color-surface: #1e293b → grayscale #1e1e1e (or var(--glass)) — #1e293b is SATURATED (R≠G≠B)
    - :root --color-accent: #3b82f6 → var(--accentA) — SATURATED
    - body gradient: linear-gradient(135deg, #1e293b 0%, #0f172a 100%) → from var(--accentA) to var(--accentB) (or grayscale #1e1e1e → #0f0f0f) — both endpoints SATURATED
    - .title gradient endpoint #60a5fa → var(--accentB) — SATURATED
    - rgba(59, 130, 246, …) (every occurrence — .unit-toggle, .unit-btn:hover, .location-input border, .detail-item bg+border, .spinner border) → rgba(255,255,255,…) neutral or var(--glass2)/var(--bord) — SATURATED
    - rgba(15, 23, 42, 0.5) (.location-input bg) → rgba(0,0,0,0.5) — SATURATED
    - rgba(226, 232, 240, …) (every occurrence — placeholder, descriptions, labels, loading) → rgba(255,255,255,…) neutral (close to white) — SATURATED
    - .error rgba(239, 68, 68, …) → rgba(255,255,255,…) neutral or var(--glass2)/var(--bord) — SATURATED
    - .error color #fca5a5 → var(--accentA) (or var(--text)) — SATURATED
    - KEEP: rgba(0,0,0,…); var(--color-text)/var(--color-accent) references in the body resolve to the re-skinned :root values (the body uses `var(--color-accent)` extensively — those are var() references, NOT literals, so they PASS the gate; only the :root literal VALUES and the inline saturated literals above need changing).

    widget-data-table.raw.txt:
    - --color-accent: #0066cc → var(--accentA)
    - KEEP everything else (#ffffff, #1a1a1a, #e0e0e0, #f5f5f5 are grayscale; rgba(0,0,0,0.02) is neutral)

    DO NOT touch the following — verified CLEAN by the checker (no saturated literals; touching them risks over-correction):
    - widget-line-chart.raw.txt, widget-stat-card.raw.txt — neutral rgba(0,0,0,…)/rgba(255,255,255,…) only, no hex; same producerWidget.test.ts L104-110 loop, must keep passing untouched.
    - timer.raw.txt — only #ccc grayscale + rgba(0,0,0,…); passes.
    - pomodoro-timer.raw.txt — no color literals; passes.
    - The inline VALID_COMPONENT / EXPORT_DEFAULT_TSX stubs in the test files — they reference a var, not a literal; pass.

    After editing, re-confirm each fixture still parses as valid TSX (the structural assertions — "function App()" present, no markdown fence leaking from .code.txt, fence intact on .raw.txt, exports intact) by running the affected tests in Step (verify).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>
    All SEVEN fixtures (calculator.code.txt, calculator.raw.txt, budget.code.txt, budget.raw.txt, weather.code.txt, weather.raw.txt, widget-data-table.raw.txt) contain ZERO saturated hardcoded colors — only theme vars, grayscale hex (R===G===B), or rgba(0,0,0,α)/rgba(255,255,255,α). Per-fixture proof: for each of the seven, `grep -noE '#[0-9a-fA-F]{3,8}' <fixture>` yields only grayscale hits (R===G===B) and `grep -noE 'rgba?\([0-9]' <fixture>` yields only rgba triples that are (0,0,0)/(255,255,255) — i.e. no saturated literal remains in ANY of the seven. The four `.raw.txt` fixtures retain their ```tsx fence + `export default App;` + closing fence structure byte-for-byte except the swapped color literals (extractCode still parses them). injection.test.ts L54-55/L129/L155-157 PASS (calculator + budget produce success again). MarketplaceFixtures.test.tsx L57/L70 PASS (calculator.raw.txt produces successfully → failure fallback NOT shown → interactive controls present). producerWidget.test.ts L104-110 PASSES (all three widget fixtures, including the re-skinned data-table, produce + transpile cleanly). Full suite >= 669 tests green. tsc exits 0. No test assertion on a specific saturated color string was broken (or, if one existed, it was updated and noted in the SUMMARY).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model response text → transpile → colorCheck | Model output flows through extract→transpile→colorCheck; colorCheck reads only the transpiled string with no side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | prompt injection via type string | accept | The type string comes from the codebase (seeded) or slugFromText (user-authored); neither path allows arbitrary injection; prompt is a string concat, not a template engine |
| T-18-06 | Information Disclosure | color check error message in self-heal prompt | accept | The error message is fed to the model (not shown to the user); it references theme variable names only — no mechanic-revealing information |
| T-18-07 | Denial of Service | colorCheck on every produce attempt | accept | colorCheck is a pure regex scan over a string of ≤2000 tokens; runtime cost is negligible compared to the model call |
</threat_model>

<verification>
1. grep -c "var(--accentA)" src/execution/producer.ts — result >= 5 (4 buildPrompt branches + buildRepairPrompt)
2. grep -c "var(--color-surface)\|var(--color-text)\|var(--color-accent)" src/execution/producer.ts — result = 0
3. grep -n "checkForHardcodedColors" src/execution/producer.ts — import line AND call site present
4. grep -n "kind !== .handler." src/execution/producer.ts — guard present before colorCheck call
5. For EACH of the seven re-skinned fixtures (calculator.code.txt, calculator.raw.txt, budget.code.txt, budget.raw.txt, weather.code.txt, weather.raw.txt, widget-data-table.raw.txt): grep -noE '#[0-9a-fA-F]{3,8}' then confirm every hit is grayscale (R===G===B), and grep -noE 'rgba?\([0-9]' then confirm every triple is rgba(0,0,0,…) or rgba(255,255,255,…) — no saturated literal remains in ANY fixture
6. For each of the four .raw.txt fixtures: confirm the leading ```tsx fence, trailing closing fence, and `export default App;` line are unchanged (only color literals differ)
7. npx vitest run — FULL suite green (>= 669 tests), including injection.test.ts, MarketplaceFixtures.test.tsx, and producerWidget.test.ts
8. npx tsc --noEmit — exits 0
9. npx vitest run src/hygiene.test.ts — hygiene gate green
10. Build clean: no source maps emitted (host build config unchanged)
</verification>

<success_criteria>
- All four UI prompt kinds carry the new theme var contract (var(--accentA) etc.) in buildPrompt; the app/widget branch of buildRepairPrompt carries it too
- buildLengthPrompt stays theming-light (no CSS var contract added; no assertion requires one)
- Handler kind has no theming instruction (unchanged)
- checkForHardcodedColors is called post-transpile for all non-handler kinds
- A saturated hex in produced code → self-heal retry → ProduceError after the identical-error early-stop (demonstrated by new test)
- Valid rgba(0,0,0,0.3) shadow → no retry (demonstrated by new test)
- EVERY saturated produce-success fixture that reaches transpile through produceComponent (calculator.code.txt, calculator.raw.txt, budget.code.txt, budget.raw.txt, weather.code.txt, weather.raw.txt, widget-data-table.raw.txt) is re-skinned to pass colorCheck while keeping its structural produce-success assertions valid; the four .raw.txt fixtures keep their fence/export structure intact so extractCode still works
- injection.test.ts L54-55/L129/L155-157, MarketplaceFixtures.test.tsx L57/L70, and producerWidget.test.ts L104-110 stay green
- All producer test files updated to new var names
- tsc 0 errors; FULL suite >= 669 tests green; hygiene gate green; no source maps

## Verification reality (honest note)
The full LIVE visual proof (produce a fresh app with the user's key → switch theme → watch it re-skin) needs an Anthropic API key the dev environment does not have. This plan verifies the theme contract OFFLINE via fixtures: the prompt-contract assertions, the flagged/clean colorCheck wiring tests, and the seven re-skinned fixtures now proving a produced app references theme vars (not saturated colors) and still compiles. The live visual re-skin is left for the user to confirm with their own key (consistent with the "user owns the key story" steer).
</success_criteria>

<output>
After completion, create .planning/phases/18-theme-aware-generation/18-03-SUMMARY.md
</output>
