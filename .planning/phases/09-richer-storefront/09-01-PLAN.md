---
phase: 09-richer-storefront
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/registry/db.ts
  - src/execution/loader.ts
  - src/ui/marketplaceUtils.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02

must_haves:
  truths:
    - "An AppRecord carries displayName, prompt, and createdAt as optional fields"
    - "A fresh-record write sets all three new fields (displayName title-cased from type/static registry, prompt = userPrompt, createdAt = Date.now())"
    - "touchRecord() carries the three new fields forward via the existing spread and never overwrites createdAt"
    - "Re-production faithfulness (ROADMAP SC2) is delivered by the EXISTING cache-key mechanism — registryKey(\"app\", type, instruction) already keys tweak/clone variants distinctly (per CONTEXT.md); the persisted `prompt` records the producing user-intent as display/inspection metadata and is NOT a new read path (no re-produce-from-prompt task is introduced this phase)"
    - "rankPopular() is a pure exported utility that is the SOLE owner of the useCount >= 1 membership filter, then sorts records by useCount desc, updatedAt desc, cacheKey asc and caps at top-N"
  artifacts:
    - path: "src/registry/db.ts"
      provides: "AppRecord with displayName?, prompt?, createdAt?"
      contains: "displayName?: string"
    - path: "src/execution/loader.ts"
      provides: "deriveDisplayName helper + fresh-record write extension"
      contains: "createdAt: Date.now()"
    - path: "src/ui/marketplaceUtils.ts"
      provides: "rankPopular exported pure sort+filter utility (owns the useCount >= 1 membership filter)"
      exports: ["rankPopular"]
  key_links:
    - from: "src/execution/loader.ts"
      to: "src/registry/db.ts"
      via: "AppRecord type import"
      pattern: "displayName.*deriveDisplayName"
    - from: "src/ui/Marketplace.tsx"
      to: "src/ui/marketplaceUtils.ts"
      via: "rankPopular import (consumed in Plan 09-03)"
      pattern: "rankPopular"
---

<objective>
Extend the app record schema with three additive optional fields (displayName, prompt, createdAt), wire them into the two loader write sites, and extract the popular-row ranking logic into a pure testable utility.

Purpose: Delivers the schema muscle for STORE-01 (faithful re-production, real names) and the sort contract for STORE-02 (popular row). All changes are additive — no DB version bump, no breaking change, old records keep working.

Re-production faithfulness clarification (ROADMAP SC2): an app already re-produces faithfully via the EXISTING cache-key mechanism — `registryKey("app", type, instruction)` keys a tweak/clone variant distinctly from its baseline (see CONTEXT.md § Producing prompt). The new persisted `prompt` field records the producing user-intent as display/inspection metadata; it is NOT a new read path and this phase introduces NO "re-produce from the stored prompt" task. The `prompt` field is write-on-fresh-record + carried-forward-on-touch only.

Output: Extended AppRecord type; extended fresh-record write; touchRecord carries fields forward; rankPopular utility module (sole owner of the useCount >= 1 filter) ready for Plan 09-03 to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-richer-storefront/09-CONTEXT.md
@.planning/phases/09-richer-storefront/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/registry/db.ts (full file — 58 lines):
- REGISTRY_DB_VERSION = 2; NO version bump in this phase.
- LruMeta: useCount?: number; updatedAt?: number
- AppRecord extends LruMeta: cacheKey, type, source, transpiledJS, [key:string]: unknown
- upgrade() only calls createObjectStore — no per-field migration needed

From src/execution/loader.ts (key write sites):
- touchRecord(): lines 46-68 — spreads the stored record then overrides cacheKey/type/useCount/updatedAt. The spread already carries any new optional fields automatically. NO signature change needed.
- Fresh-record write: lines 284-298 — explicit object literal; add displayName, prompt, createdAt here.
- resolveComponent() signature (line 173): (instanceId, appType, appCacheKey, services, userPrompt?) — userPrompt is already available at the fresh-record write site.

From src/registry/cacheKey.ts (the existing re-production mechanism — do NOT modify in this phase):
- registryKey(kind, type, prompt?) at lines 51-60 — a tweak/clone variant is already keyed distinctly from its baseline via the folded instruction. This is what makes an app "re-produce faithfully"; the new `prompt` field is metadata only.

From src/data/appRegistry.ts:
- APP_REGISTRY is an array of entries each with .id (matches appType) and .displayName (the human label, e.g. "Counter", "Weather").
- Lookup: APP_REGISTRY.find(a => a.id === appType)?.displayName

From src/services/registry.ts:
- Registry.keys(store): Promise<string[]>  — enumerate all keys
- Registry.get(store, key): Promise<StoreValue | undefined>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AppRecord schema with displayName, prompt, createdAt</name>
  <files>src/registry/db.ts</files>
  <read_first>
    src/registry/db.ts (full file — 58 lines; understand LruMeta pattern and the comment block at lines 1-9 before touching anything)
    .planning/phases/09-richer-storefront/09-PATTERNS.md (§ src/registry/db.ts — new fields block with exact JSDoc wording)
  </read_first>
  <behavior>
    - AppRecord after change declares displayName?: string with JSDoc "(Phase 9, STORE-01)"
    - AppRecord after change declares prompt?: string with JSDoc noting user-intent-only (hygiene-critical, never model system-prompt) and that it is display/inspection metadata (re-production is keyed by registryKey, not by reading this field back)
    - AppRecord after change declares createdAt?: number with JSDoc "never overwritten on touch"
    - The existing [key: string]: unknown catch-all is preserved (index signatures and named optionals coexist in TS when the named fields' types are assignable to unknown — confirm tsc passes)
    - REGISTRY_DB_VERSION stays 2 (no bump)
    - upgrade() function is unchanged
    - Comment block at lines 1-9 gains a "Phase 9 (STORE-01)" annotation noting the three additive fields follow the same no-migration pattern
  </behavior>
  <action>
    Read src/registry/db.ts. After reading, add the three optional fields to AppRecord following the exact comment/JSDoc/optional-field style of LruMeta (PATTERNS.md § db.ts). Fields go after transpiledJS and before the index signature. The prompt-field JSDoc must state it stores user intent only (never the model system-prompt) AND that it is metadata — faithful re-production comes from registryKey, not from reading this field. Update the comment block at the top (lines 1-9) to note Phase 9 additive fields (displayName/prompt/createdAt) follow the same additive-no-migration pattern as Phase 7 LRU fields. Do NOT change REGISTRY_DB_VERSION, upgrade(), or any other interface. Run `npm run test -- --reporter=verbose src/registry/registry.test.ts` to confirm existing registry tests remain green after the type change.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;&amp; echo "tsc clean"</automated>
  </verify>
  <done>
    - AppRecord type in src/registry/db.ts declares displayName?: string, prompt?: string, createdAt?: number
    - prompt JSDoc states user-intent-only + metadata (not a re-production read path)
    - REGISTRY_DB_VERSION is still 2
    - tsc --noEmit exits 0
    - npm run test -- src/registry/registry.test.ts exits 0 with all existing tests green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend loader write sites and extract rankPopular utility</name>
  <files>src/execution/loader.ts, src/ui/marketplaceUtils.ts</files>
  <read_first>
    src/execution/loader.ts (read full file — key sections: lines 1-70 for touchRecord, lines 157-305 for resolveComponent and fresh-record write)
    src/data/appRegistry.ts (scan for APP_REGISTRY array and AppRegistryEntry.displayName values — needed by deriveDisplayName lookup)
    .planning/phases/09-richer-storefront/09-PATTERNS.md (§ loader.ts — deriveDisplayName pattern, fresh-record write extension, § storagePressure sort pattern for rankPopular)
    .planning/phases/09-richer-storefront/09-CONTEXT.md (§ Decisions — displayName fallback chain, createdAt never overwritten on touch, prompt stores user intent only)
  </read_first>
  <behavior>
    loader.ts:
    - deriveDisplayName(type: string, userPrompt?: string): string — title-cases the type slug ("weather" → "Weather", "my-app" → "My App") by splitting on [-_], capitalizing first char of each word, joining with space. For a non-empty userPrompt (tweak variant), appends a short hygiene-safe suffix: first 20 chars of userPrompt, stripped to [a-zA-Z0-9 ], trimmed, wrapped in parens — e.g. "Weather (show celsius)". If the trimmed suffix is empty, returns just the base. No mechanic-lexicon tokens may appear in the result.
    - Static-registry lookup: before falling back to title-case, check APP_REGISTRY.find(a => a.id === appType)?.displayName. A seeded app gets its static label; an unseeded app gets title-case.
    - Fresh-record write (lines 284-298): add createdAt: Date.now(), displayName: deriveDisplayName(appType, userPrompt) resolved via static lookup first, prompt: userPrompt ?? undefined. Per D-01 from CONTEXT.md: prompt stores the user's intent string, never the model system-prompt. The prompt field is metadata only — no code in this phase reads it back to re-produce.
    - touchRecord(): no signature change; the existing ...record spread already carries displayName, prompt, createdAt forward. The override block only sets useCount and updatedAt — createdAt is NOT listed in the override, so it is preserved from the spread. Confirm this is the case by reading lines 46-68 before editing.
    - Import APP_REGISTRY from ../data/appRegistry at the top of loader.ts.

    marketplaceUtils.ts (NEW FILE):
    - Export rankPopular(records: Pick<AppRecord, "cacheKey" | "useCount" | "updatedAt">[], topN?: number): typeof records — this function is the SOLE owner of the useCount >= 1 membership filter for the popular row. It filters records where useCount >= 1, then sorts useCount desc then updatedAt desc then cacheKey asc, then slices to topN (default 5). No consumer (Marketplace.tsx in 09-03) re-applies the useCount filter — they pass raw records in and trust the returned array.
    - This is a pure function with zero side effects — no React, no async, no registry calls.
    - Export a titleCase(slug: string): string helper (splits on [-_], capitalises each word) for use in Marketplace.tsx popular-row fallback (Plan 09-03).
    - Both exports use only types imported from ../registry/db (AppRecord or its Pick).
  </behavior>
  <action>
    Step 1 — Read loader.ts fully, identify the import block and both write sites. Add APP_REGISTRY import from ../data/appRegistry. Implement deriveDisplayName as a module-private function (no export) above touchRecord. Extend the fresh-record object literal to include createdAt, displayName (via static lookup then deriveDisplayName), and prompt (per D-01: user intent only, metadata). Verify touchRecord is unchanged — the spread already handles the new fields.

    Step 2 — Create src/ui/marketplaceUtils.ts from scratch. Export rankPopular and titleCase. rankPopular owns the useCount >= 1 filter (filter first, then sort desc/desc/asc, then slice topN default 5) — see PATTERNS.md § sort test for the exact comparator contract. No fenced code blocks in this action.

    After both edits: run `npx tsc --noEmit` to confirm zero errors, and confirm the new utility file carries no banned mechanic lexicon.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;&amp; [ "$(grep -v '^\s*\/\/' src/ui/marketplaceUtils.ts | grep -Ec 'synthesize|synthesis|generate| AI | LLM | llm')" = "0" ] 2>&amp;&amp; npm run test -- --reporter=verbose src/execution/loader.test.ts 2>&amp;&amp; echo "loader tests green + hygiene clean"</automated>
  </verify>
  <done>
    - src/ui/marketplaceUtils.ts exists and exports rankPopular (SOLE owner of useCount >= 1 filter; sorts desc/desc/asc; slices to topN) and titleCase
    - src/execution/loader.ts fresh-record write includes createdAt, displayName, prompt (metadata only)
    - touchRecord() is unchanged (spread preserves new fields, no explicit createdAt override)
    - Hygiene grep on src/ui/marketplaceUtils.ts (non-comment lines) returns 0 banned tokens
    - tsc --noEmit exits 0
    - All existing loader tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → IndexedDB (via registry.put) | User-visible via Application → IndexedDB in DevTools; any banned mechanic lexicon stored here breaks HYGIENE-01..05 |
| userPrompt → prompt field | The input is the user's raw intent string; it must reach the record as-is (user-safe), never replaced by the model system-prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | loader.ts fresh-record write → IndexedDB `prompt` field | mitigate | Store userPrompt (user's intent only) in the `prompt` field — NEVER the output of buildPrompt() which contains the banned mechanic lexicon. Assert in hygiene.test.ts that the banned token family does not appear in any src/** surface (already runs; the new `prompt` field value is user-supplied, so it is hygiene-safe by origin). |
| T-09-02 | Information Disclosure | deriveDisplayName suffix | mitigate | Suffix derived from userPrompt is stripped to [a-zA-Z0-9 ] before appending — removes any accidental mechanic tokens a user might have typed. Keep suffix ≤ 20 chars. |
| T-09-03 | Tampering | additive schema change breaks existing records | accept | The [key: string]: unknown catch-all on AppRecord + additive optional fields means old records satisfy the interface. No migration step needed. Covered by the v1-compat test in Plan 09-02. |
</threat_model>

<verification>
After both tasks complete:
- `npx tsc --noEmit` exits 0
- `npm run test` full suite exits 0 (all existing tests green — additive only)
- `grep -n "displayName\|prompt\|createdAt" src/registry/db.ts` shows the three new optional fields
- `grep -n "rankPopular\|titleCase" src/ui/marketplaceUtils.ts` shows the exports
- `grep -n "createdAt.*Date.now\|displayName.*derive\|prompt.*userPrompt" src/execution/loader.ts` shows the fresh-record write extension
- `grep -v '^\s*//' src/ui/marketplaceUtils.ts | grep -Ec 'synthesize|synthesis|generate| AI | LLM | llm'` returns 0 (hygiene gate passes on new file, comment lines excluded)
</verification>

<success_criteria>
- AppRecord in src/registry/db.ts declares displayName?, prompt?, createdAt? as optional fields with JSDoc (prompt JSDoc states user-intent-only metadata, not a re-production read path)
- Re-production faithfulness is documented as delivered by the existing registryKey cache-key mechanism; no new read-path task introduced
- REGISTRY_DB_VERSION remains 2; no upgrade() change
- src/execution/loader.ts fresh-record write sets all three new fields; touchRecord unchanged
- src/ui/marketplaceUtils.ts exports rankPopular (sole owner of useCount >= 1 filter; sort desc/desc/asc; cap topN=5) and titleCase
- tsc --noEmit and npm run test both exit 0; marketplaceUtils.ts hygiene grep returns 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-richer-storefront/09-01-SUMMARY.md`
</output>
