---
phase: 09-richer-storefront
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/registry/registry.test.ts
  - src/ui/marketplaceUtils.test.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02

must_haves:
  truths:
    - "A v2 record written without displayName/prompt/createdAt round-trips with those fields undefined — no crash, no blank in callers"
    - "A record written with all three fields round-trips them correctly"
    - "rankPopular sorts by useCount desc, then updatedAt desc, then cacheKey asc — fully deterministic"
    - "rankPopular is the sole owner of the useCount >= 1 membership filter — a test proves it drops records with useCount < 1 so the cold-start row stays hidden without any component-side filter"
    - "rankPopular caps output at topN"
  artifacts:
    - path: "src/registry/registry.test.ts"
      provides: "v1-record compat tests for Phase 9 fields"
      contains: "Phase 9 additive migration"
    - path: "src/ui/marketplaceUtils.test.ts"
      provides: "rankPopular determinism + membership-filter tests"
      exports: []
  key_links:
    - from: "src/ui/marketplaceUtils.test.ts"
      to: "src/ui/marketplaceUtils.ts"
      via: "import { rankPopular }"
      pattern: "rankPopular"
---

<objective>
Write the two test suites that lock in the additive-schema contract and the popular-row ranking contract. Tests are written before (or alongside) the implementation they verify, following the TDD heuristic.

Purpose: Plan 09-01 implements schema + utilities; these tests prove the v1-record compat invariant (STORE-01) and the deterministic sort + membership filter (STORE-02). The cold-start-guard test specifically pins that the `useCount >= 1` membership filter lives inside `rankPopular` — the single authoritative owner Plan 09-03's Marketplace consumes without re-filtering. They run in Wave 1 alongside Plan 09-01 and will be green by the time Plan 09-03 begins.

Output: New test blocks in registry.test.ts; new test file marketplaceUtils.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-richer-storefront/09-CONTEXT.md
@.planning/phases/09-richer-storefront/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/registry/registry.test.ts (relevant section — lines 95-128):
```typescript
// Existing helper (used in tests):
function appRecord(overrides: Partial<AppRecord> & { cacheKey: string }): AppRecord {
  return {
    type: "test-app",
    source: "s",
    transpiledJS: "j",
    ...overrides,
  };
}
// ... existing v1-compat test pattern at lines 110-128 (exact code in PATTERNS.md § registry.test.ts)
```

From src/ui/marketplaceUtils.ts (created in Plan 09-01 — rankPopular owns the useCount >= 1 filter):
```typescript
export function rankPopular(
  records: Pick<AppRecord, "cacheKey" | "useCount" | "updatedAt">[],
  topN?: number,
): typeof records;  // filters useCount >= 1, sorts, slices to topN (default 5)

export function titleCase(slug: string): string;
```

From src/registry/storagePressure.test.ts (sort-test analog, lines 66-83 in PATTERNS.md):
- createInMemoryRegistry() is importable from the test infra.
- appRecord() helper is defined per-test-file with the minimal shape needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v1-record compat tests for Phase 9 fields in registry.test.ts</name>
  <files>src/registry/registry.test.ts</files>
  <read_first>
    src/registry/registry.test.ts (read fully; locate the existing v1-compat test at lines 110-128, the appRecord() helper, and the describe("registry — happy path") block to know where to append)
    .planning/phases/09-richer-storefront/09-PATTERNS.md (§ New tests in registry.test.ts — exact test code to mirror)
  </read_first>
  <behavior>
    Two new it() blocks appended inside the existing "registry — happy path" describe block (same scope as lines 110-128):

    Test A "a record missing displayName/prompt/createdAt reads back without those fields (Phase 9 additive migration)":
    - Writes a v2-style record (has useCount + updatedAt) but lacks displayName, prompt, createdAt
    - Uses put() / get() from the registry module via dynamic import (same pattern as existing compat test)
    - Asserts result?.displayName is undefined
    - Asserts result?.prompt is undefined
    - Asserts result?.createdAt is undefined
    - Asserts result?.useCount is 3 (existing fields survive)
    - Asserts result?.source is "s" (core fields survive)

    Test B "round-trips displayName, prompt, and createdAt on an AppRecord":
    - Writes an AppRecord with displayName: "Weather", prompt: "show celsius", createdAt: 99999
    - Reads it back
    - Asserts all three fields equal their written values
    - Asserts core fields (source, transpiledJS) are intact
  </behavior>
  <action>
    Read src/registry/registry.test.ts fully. Locate the "registry — happy path" describe block and find the closing brace. Append the two new it() blocks immediately before that closing brace, mirroring the exact style of the existing v1-compat test at lines 110-128 (dynamic import pattern, await dbReady, put then get, then expects). Cachekey strings must be unique from all existing test cache keys to avoid cross-test pollution ("v2-legacy" and "rich" match the PATTERNS.md example). Do NOT use vi.resetModules() inside this describe block (only the fallback tests use that pattern). Confirm no fenced code blocks are needed — the implementation detail is in PATTERNS.md.
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/registry/registry.test.ts</automated>
  </verify>
  <done>
    - Two new it() blocks exist in the "registry — happy path" describe
    - Both tests pass (npm run test exits 0 for this file)
    - Test A confirms optional fields are undefined on legacy records
    - Test B confirms all three new fields round-trip correctly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rankPopular determinism + membership-filter tests in marketplaceUtils.test.ts</name>
  <files>src/ui/marketplaceUtils.test.ts</files>
  <read_first>
    src/ui/marketplaceUtils.ts (created by Plan 09-01 Task 2 — read to know the exact exported signature before writing tests; if 09-01 hasn't run yet, write tests to the contract in 09-01's must_haves and PATTERNS.md. Confirm rankPopular owns the useCount >= 1 filter so Test 4 below targets the right function.)
    .planning/phases/09-richer-storefront/09-PATTERNS.md (§ New sort/ranking test — three test cases with exact expected outputs)
    src/registry/storagePressure.test.ts (lines 66-98 — analog test style for sort logic; note the pure-record approach with no React)
  </read_first>
  <behavior>
    New file src/ui/marketplaceUtils.test.ts with a describe("rankPopular") block containing four+ it() tests. These tests assert that rankPopular alone enforces both the ordering AND the useCount >= 1 membership filter (so the Marketplace component never needs its own filter):

    Test 1 "ranks by useCount descending":
    - Input: [{cacheKey:"b", useCount:1, updatedAt:100}, {cacheKey:"a", useCount:5, updatedAt:100}, {cacheKey:"c", useCount:3, updatedAt:100}]
    - Expected order: a (5), c (3), b (1)

    Test 2 "breaks useCount tie by updatedAt descending":
    - Input: [{cacheKey:"old", useCount:3, updatedAt:100}, {cacheKey:"new", useCount:3, updatedAt:500}]
    - Expected order: new (updatedAt 500 > 100)

    Test 3 "breaks updatedAt tie by cacheKey ascending (fully deterministic)":
    - Input: [{cacheKey:"z", useCount:2, updatedAt:200}, {cacheKey:"a", useCount:2, updatedAt:200}]
    - Expected order: a before z

    Test 4 "owns the membership filter — drops records with useCount < 1 (cold-start guard)":
    - Input: [{cacheKey:"x", useCount:0, updatedAt:100}, {cacheKey:"y", useCount:1, updatedAt:200}]
    - Expected: result.length === 1, result[0].cacheKey === "y"
    - This is the authoritative test that the useCount >= 1 filter lives in rankPopular (not in the component). When this passes, the Marketplace cold-start-hidden behavior in 09-03 follows automatically from `popularApps.length > 0`.
    - Also assert that an empty-after-filter input yields []: rankPopular([{cacheKey:"x", useCount:0, updatedAt:100}]) returns a zero-length array.

    Test 5 "caps output at topN":
    - Input: 7 records each with useCount >= 1
    - Expected: rankPopular(records, 3).length === 3

    No React imports. No async. No mocks. Pure function calls only. Test double naming: "stub" / "canned" style per PATTERNS.md shared patterns.
  </behavior>
  <action>
    Create src/ui/marketplaceUtils.test.ts. Import only { rankPopular } from "./marketplaceUtils" and describe() / it() / expect() from vitest. Each test uses a plain inline array of minimal objects matching the Pick type (cacheKey, useCount, updatedAt). No shared beforeEach — each test is fully self-contained. Test 4 must explicitly prove the useCount >= 1 filter is owned by rankPopular (drops useCount 0, and an all-zero input returns []). Run the tests once to confirm RED if marketplaceUtils.ts doesn't exist yet, then green after Plan 09-01 runs.
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/ui/marketplaceUtils.test.ts</automated>
  </verify>
  <done>
    - src/ui/marketplaceUtils.test.ts exists
    - All 5 it() blocks pass (rankPopular returns deterministically ordered results)
    - Membership-filter test (Test 4) proves rankPopular owns the useCount >= 1 filter: drops useCount < 1 and returns [] when nothing qualifies
    - topN cap passes
    - npm run test exits 0 for this file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → IndexedDB (registry.test.ts) | Tests use dynamic imports and real IndexedDB (fake-indexeddb via vitest setup); no live network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Test data for `prompt` field contains user-like strings | accept | Test strings ("show celsius") are benign user-intent strings, not model prompts. Low risk: test files are not shipped in the production bundle (no sourcemaps). |
| T-09-05 | Tampering | Cross-test cache pollution in registry.test.ts | mitigate | Use distinct cacheKey values per test ("v2-legacy", "rich") that differ from all keys used by existing tests ("legacy", "lru", "fb-key") to prevent collision. |
</threat_model>

<verification>
After both tasks complete:
- `npm run test -- src/registry/registry.test.ts` exits 0 — two new tests green, no regressions
- `npm run test -- src/ui/marketplaceUtils.test.ts` exits 0 — all sort + membership-filter tests green (Test 4 confirms rankPopular owns the useCount >= 1 filter)
- `npm run test` (full suite) exits 0 — no regressions from additive test additions
</verification>

<success_criteria>
- registry.test.ts has two new tests proving the Phase 9 additive migration: old records missing the new fields read back cleanly; new records round-trip all three fields
- marketplaceUtils.test.ts proves rankPopular is deterministic AND is the sole owner of the useCount >= 1 membership filter (drops cold-start records, returns [] when none qualify), and caps at topN
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-richer-storefront/09-02-SUMMARY.md`
</output>
