---
phase: 22-theme-editor-custom-themes
plan: 05
type: execute
wave: 4
depends_on:
  - 22-01
  - 22-02
  - 22-03
  - 22-04
files_modified:
  - src/hygiene.test.ts
autonomous: true
requirements:
  - THEME-06
  - THEME-07
  - THEME-08
  - THEME-09
  - THEME-10

must_haves:
  truths:
    - "hygiene.test.ts PHASE20_FILES includes ThemeEditor.tsx — its string literals are scanned for iframe/sandbox/isolation words"
    - "npx vitest run passes all tests (no regressions; all new ThemeEditor, contrastRatio, VibeThemeProvider, ThemeSelector, MenuBar, DesktopShell tests green)"
    - "npx tsc --noEmit exits 0 (zero TypeScript errors across the full codebase)"
    - "npx vite build exits 0 with build.sourcemap: false (no source maps; minified output)"
    - "Hygiene gate green: banned tokens absent from src/**; iframe/sandbox/isolation absent from ThemeEditor.tsx string literals"
    - "CSP gate green: csp.test.ts confirms FOUC script hash matches meta tag; frameCsp.test.ts green"
    - "All 6 binding success criteria from ROADMAP Phase 22 are satisfied (verifiable by test assertions and manual trace)"
  artifacts:
    - path: "src/hygiene.test.ts"
      provides: "Extended PHASE20_FILES array including ThemeEditor.tsx; Phase 22 coverage complete"
      contains: "ThemeEditor.tsx"
  key_links:
    - from: "src/hygiene.test.ts"
      to: "src/ui/ThemeEditor.tsx"
      via: "PHASE20_FILES array at line 245"
      pattern: "ThemeEditor\\.tsx"
---

<objective>
Extend the HYGIENE-07 gate to cover ThemeEditor.tsx (adding it to PHASE20_FILES in hygiene.test.ts), then run the full phase-wide acceptance gate: tsc 0, full vitest suite green, vite build 0 with no source maps. This plan is the final gate confirming all 5 THEME requirements and the cross-cutting v3.0 constraints are satisfied.

Purpose: HYGIENE-07 requires the lexicon gate to cover all new v3.0 surfaces including ThemeEditor.tsx. The full gate run proves all 6 binding success criteria from the ROADMAP are satisfied — this is the acceptance gate before the phase can be marked done.

Output: hygiene.test.ts updated; full suite green; build passes; phase complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-theme-editor-custom-themes/22-RESEARCH.md
@.planning/phases/22-theme-editor-custom-themes/22-04-SUMMARY.md

<interfaces>
<!-- Minimal interface context — this plan only edits one file and runs commands. -->

From src/hygiene.test.ts (current PHASE20_FILES at line 245):
  const PHASE20_FILES = [
    "src/execution/frameBridge.ts",
    "src/execution/frameMount.ts",
    "src/ui/SandboxFrame.tsx",
    "src/ui/SandboxFrame.css",
  ];

After this plan, PHASE20_FILES must include "src/ui/ThemeEditor.tsx".

The test at hygiene.test.ts line 305 checks that each file in PHASE20_FILES EXISTS (must exist, or the gate would silently scan nothing). ThemeEditor.tsx must exist before this test runs — it is created in Plan 22-03. The gate at lines 290-302 scans each file for isolation words in quoted string literals.

How the scan works (from hygiene.test.ts lines 268-302):
  - For each file in PHASE20_FILES, read lines
  - Skip lines with `import` or `from "` (module specifiers are not user copy)
  - Extract quoted string literals from remaining lines
  - Reject the literal "allow-scripts" (the sandbox attribute value — a known false positive)
  - Assert no literal contains /\b(iframe|sandbox|isolation)\b/i

Success criteria (lines 304+):
  - All files in PHASE20_FILES exist
  - No isolation-word violation found in any file's quoted string literals
  - ThemeEditor.tsx does not contain "iframe", "sandbox", or "isolation" in any string literal (verified in Plan 22-03 by construction)

Phase-wide acceptance gate commands:
  npx tsc --noEmit                            # 0 errors
  npx vitest run                              # all tests pass
  npx vite build 2>&1 | grep -v "^$"         # build succeeds; confirm no "sourcemap" in output
  npx vitest run src/hygiene.test.ts          # hygiene gate green
  npx vitest run src/csp.test.ts src/frameCsp.test.ts  # CSP hash gates green

6 binding success criteria trace (from ROADMAP Phase 22):
  SC#1 (live preview): ThemeEditor tests in 22-03 confirm :root mutation before save
  SC#2 (name+save → switcher + THEME_PUSH): broadcastTheme spy test in 22-03; ThemeSelector custom pills test in 22-04
  SC#3 (invalid color → rejected before IDB): CSS.supports rejection test in 22-03
  SC#4 (reload FOUC-free): csp.test.ts green (22-02); FOUC script branch confirmed in index.html
  SC#5 (name collision + delete auto-switch): name collision test + delete test in 22-03
  SC#6 (WCAG contrast warning): contrast warning test in 22-03
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend hygiene.test.ts PHASE20_FILES to include ThemeEditor.tsx</name>
  <files>src/hygiene.test.ts</files>

  <read_first>
  Read src/hygiene.test.ts lines 240-320 in full. Confirm the PHASE20_FILES array structure at line 245 and the existence-check test at line 305. Confirm the scan logic at lines 288-302. Confirm ThemeEditor.tsx exists at src/ui/ThemeEditor.tsx (created in Plan 22-03) before adding it — the existence test will fail if the file is absent.
  </read_first>

  <action>
  In src/hygiene.test.ts, find the PHASE20_FILES array at line 245. Add "src/ui/ThemeEditor.tsx" as the fifth entry in the array. No other changes to hygiene.test.ts are needed — the existing scan + existence-check + violation-check logic covers new entries automatically.

  Run npx vitest run src/hygiene.test.ts to confirm:
  1. ThemeEditor.tsx exists (the existence-check test passes)
  2. ThemeEditor.tsx contains no isolation-word violations in quoted string literals (the scan passes because ThemeEditor.tsx was written without "iframe", "sandbox", or "isolation" in string literals — enforced in Plan 22-03)
  3. All existing hygiene tests remain green (no regressions from the array extension)
  </action>

  <verify>
    <automated>npx vitest run src/hygiene.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - src/hygiene.test.ts PHASE20_FILES array includes "src/ui/ThemeEditor.tsx"
  - npx vitest run src/hygiene.test.ts exits with all tests passing
  - The HYGIENE-07 existence test confirms ThemeEditor.tsx is present
  - The HYGIENE-07 scan test confirms no isolation-word violations in ThemeEditor.tsx string literals
  - No regressions in other hygiene test assertions
  </acceptance_criteria>

  <done>hygiene.test.ts PHASE20_FILES extended with ThemeEditor.tsx; hygiene gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide acceptance gate — tsc + full suite + build + all cross-cutting constraints</name>
  <files></files>

  <read_first>
  No files to read — this task runs commands only. The acceptance gate commands are listed in the interfaces block above.
  </read_first>

  <action>
  Run the following commands in sequence. Each must exit cleanly before proceeding:

  Command 1 — TypeScript type check:
    npx tsc --noEmit
  Expected: exits 0, no output. If there are errors, fix them in the file reported before proceeding.

  Command 2 — Full test suite:
    npx vitest run
  Expected: all tests pass. The test count should be higher than the 827 baseline (Phase 22 adds tests in ThemeEditor.test.tsx, contrastRatio.test.ts, extended VibeThemeProvider.test.tsx, ThemeSelector.test.tsx, MenuBar.test.tsx, DesktopShell.test.tsx). Any failing test must be fixed before proceeding.

  Command 3 — Production build:
    npx vite build
  Expected: exits 0. Confirm build.sourcemap is false (check vite.config.ts — this was set in Phase 1 and must not have been changed). No "sourcemap" output files should appear in dist/.

  Command 4 — CSP hash gates:
    npx vitest run src/csp.test.ts src/frameCsp.test.ts --reporter=verbose
  Expected: all 7 csp.test.ts tests pass (including "inline script hash matches CSP meta tag"); all frameCsp.test.ts tests pass.

  Command 5 — Hygiene gate (confirmation run):
    npx vitest run src/hygiene.test.ts --reporter=verbose
  Expected: all hygiene tests pass, HYGIENE-07 gate green.

  Command 6 — Banned-token grep sanity check on new files (supplemental):
    grep -rn "synthesize\|fake\|mock\|\bAI\b\|llm\|generate\|iframe\|sandbox\|isolation" src/ui/ThemeEditor.tsx src/ui/contrastRatio.ts 2>/dev/null | grep -v "^$" | grep -v "//\|import\|from "
  Expected: no output (no banned tokens in string literals of the two new files). Import path strings and comments are acceptable per hygiene.test.ts rules.

  If any command fails, diagnose and fix before marking this task done.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run && echo "ALL GATES PASS"</automated>
  </verify>

  <acceptance_criteria>
  - npx tsc --noEmit exits 0 (no TypeScript errors in the entire codebase)
  - npx vitest run exits 0 with all tests passing (count exceeds 827 baseline)
  - npx vite build exits 0; no source maps in dist/
  - npx vitest run src/csp.test.ts src/frameCsp.test.ts — all pass (FOUC hash invariant held)
  - npx vitest run src/hygiene.test.ts — all pass (HYGIENE-07 lexicon gate covers ThemeEditor.tsx)
  - Banned-token grep on ThemeEditor.tsx and contrastRatio.ts returns no violations
  - All 6 ROADMAP Phase 22 success criteria are verifiable via test assertions (traced in interfaces block above)
  - No DB version bump in src/registry/db.ts (REGISTRY_DB_VERSION remains 3)
  - No new entries in package.json dependencies (only devDependencies changes are allowed, and only if Playwright was added for SANDBOX-05 in Phase 20)
  </acceptance_criteria>

  <done>Phase 22 acceptance gate: tsc 0, full suite green, vite build clean, CSP + hygiene gates green, all 6 success criteria satisfied. Phase 22 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hygiene gate → source files | The gate reads source files and scans string literals — it is a read-only CI check with no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-14 | Information Disclosure | Banned token in ThemeEditor string literal reaching devtools | mitigate | hygiene.test.ts PHASE20_FILES now includes ThemeEditor.tsx; any banned-token violation fails CI before merge; Plan 22-03 was written without banned tokens by construction |
| T-22-15 | Tampering | Source map in production build leaking implementation details | mitigate | build.sourcemap: false has been in vite.config.ts since Phase 1; Task 2 confirms the build produces no .map files; any accidental enablement would be caught here |
</threat_model>

<verification>
Phase 22 is complete when:
- [ ] hygiene.test.ts PHASE20_FILES has "src/ui/ThemeEditor.tsx" (Task 1)
- [ ] npx tsc --noEmit exits 0 (Task 2)
- [ ] npx vitest run passes all tests, count > 827 (Task 2)
- [ ] npx vite build exits 0, no sourcemaps (Task 2)
- [ ] npx vitest run src/csp.test.ts src/frameCsp.test.ts green (Task 2)
- [ ] npx vitest run src/hygiene.test.ts green (Task 2)
- [ ] REGISTRY_DB_VERSION unchanged (Task 2 grep confirms)
- [ ] No new runtime npm deps (Task 2 confirms)
- [ ] All 6 Phase 22 ROADMAP success criteria satisfied (traced via test coverage in interfaces block)
</verification>

<success_criteria>
HYGIENE-07 gate extended to ThemeEditor.tsx. Full tsc + vitest + vite build gate passes. All 5 THEME requirements (06-10) satisfied, verified by test suite. All v3.0 cross-cutting constraints (HYGIENE-01..07, sourcemaps-off, CSP hash invariant, additive-IDB-only, IoC/DI, zero new runtime deps) confirmed green. Phase 22 is done.
</success_criteria>

<output>
After completion, create .planning/phases/22-theme-editor-custom-themes/22-05-SUMMARY.md
</output>
