---
phase: 25-real-browser-smoke-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/frame-isolation.spec.ts
  - e2e/smoke.spec.ts
autonomous: true
requirements:
  - SMOKE-01
  - SMOKE-02
  - SMOKE-03
must_haves:
  truths:
    - "After hard reload, the Notes window has the same style.transform as before reload and Weather has .window-chrome--minimized set (both windows present in DOM)"
    - "At domcontentloaded after reload with the smoketest custom theme active, :root --text equals '#003366' and never equals '#f3f1ff' (Aurora)"
    - "After switching to Noir, page.frames()[1].__smokeThemeId is still 42 (frame was NOT reloaded) and the frame's :root --text is '#f5eeff'"
    - "npm run e2e reports 5/5 tests passed — 2 from frame-isolation.spec.ts + 3 from smoke.spec.ts"
    - "frame-isolation.spec.ts no longer uses .getByRole('button').nth(3) for the Noir pill; it uses .getByRole('button', { name: 'Noir' })"
  artifacts:
    - path: "e2e/frame-isolation.spec.ts"
      provides: "Existing Phase 20 spec with nth(3) latent defect corrected"
      contains: "getByRole(\"button\", { name: \"Noir\" })"
    - path: "e2e/smoke.spec.ts"
      provides: "Three headless Playwright smoke tests: SMOKE-01, SMOKE-02, SMOKE-03"
      exports: []
  key_links:
    - from: "smoke.spec.ts SMOKE-01"
      to: "IDB settings[windowLayout]"
      via: "page.waitForTimeout(500) after last window action then page.reload()"
      pattern: "style\\.transform"
    - from: "smoke.spec.ts SMOKE-02"
      to: ":root --text on domcontentloaded"
      via: "addInitScript (localStorage) + page.evaluate IDB seed + page.reload({ waitUntil: 'domcontentloaded' })"
      pattern: "getPropertyValue.*--text"
    - from: "smoke.spec.ts SMOKE-03"
      to: "page.frames()[1].__smokeThemeId"
      via: "getByRole button name Noir click -> THEME_PUSH -> expect.poll frame --text"
      pattern: "__smokeThemeId.*42"
---

<objective>
Close the Phase 21 and Phase 22 `human_needed` verification gaps with three deterministic, keyless Playwright smoke tests, and fix the latent `nth(3)` selector defect in the Phase 20 reference spec.

Purpose: SMOKE-01/02/03 transform three manually-verified behaviors (desktop layout persistence, FOUC-free custom theme, live frame re-skin) into CI-green automated assertions that run headless in the existing Playwright harness. Zero production-code changes.
Output: e2e/smoke.spec.ts (new, 3 test.describe blocks), e2e/frame-isolation.spec.ts (3-line nth(3) fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-real-browser-smoke-suite/25-RESEARCH.md

<interfaces>
<!-- Key verified values and selectors the executor needs. Extracted from source files per 25-RESEARCH.md. -->
<!-- No codebase exploration needed — use these directly. -->

From playwright.config.ts:
  webServer.command = "npm run build && npm run preview -- --port 4173 --strictPort"
  baseURL = "http://localhost:4173"
  fullyParallel = false
  projects = [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }]  // headless 1280x720

From e2e/frame-isolation.spec.ts (reference patterns):
  page.goto("/")
  page.getByRole("button", { name: "Open launcher" }).click()
  page.getByRole("button", { name: "Notes", exact: true }).click()
  page.frameLocator("iframe").first()          // locator API — for assertions
  page.frames()[1]!                            // Frame object — for .evaluate()
  expect.poll(() => appFrame().evaluate(...), { timeout: 10_000 }).not.toBe(before)

Verified values from VibeThemeProvider.tsx:
  Aurora --text  = "#f3f1ff"   (line 82)
  Noir   --text  = "#f5eeff"   (line 128)

Verified IDB schema from registry/db.ts:
  DB name  = "MarketplaceRegistry"
  version  = 3
  store    = "settings"
  key type = out-of-line string
  record   = { key: string; value: string }

Verified storage keys from source:
  localStorage["marketplace.osTheme"]           -- active theme name
  localStorage["vibe.customTheme.${name}"]      -- FOUC vars JSON
  IDB settings["customThemeIndex"]              -- JSON array of custom theme names
  IDB settings["custom:${name}"]               -- JSON vars for that theme

Verified selectors:
  Dock launcher   : aria-label="Open launcher"   (Dock.tsx:33)
  Window container: .window-chrome               (WindowFrame.tsx:211)
  Window title    : .window-chrome__title        (WindowFrame.tsx text child)
  Window titlebar : .window-chrome__titlebar     (WindowFrame.tsx:239, drag target)
  Minimize button : aria-label="Minimize"        (WindowFrame.tsx:289)
  Noir pill text  : "Noir"                       (ThemeSelector.tsx:10)
  Duplicate btn   : aria-label="Duplicate Noir"  (ThemeSelector.tsx:62, DIFFERENT accessible name)
  Notes input     : input[placeholder="Add a note…"]
  Weather input   : input[placeholder="Enter a location"]

Verified debounce: LAYOUT_SAVE_DEBOUNCE_MS = 300 (DesktopShell.tsx:64)
Verified minimized: .window-chrome--minimized → display:none in index.css (DesktopShellReskin.test.tsx:107-113)
Verified frame memo: SandboxFrame.tsx:108-114 — themeVars excluded from srcdoc useMemo deps (frame stable on theme change)
Verified THEME_PUSH: frameMount.ts:407-415 — broadcastTheme() posts THEME_PUSH; in-frame handler calls style.setProperty on documentElement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix frame-isolation.spec.ts nth(3) latent defect</name>
  <files>e2e/frame-isolation.spec.ts</files>
  <read_first>
    e2e/frame-isolation.spec.ts lines 55-62 — the broken nth(3) block. Read first to confirm exact line offsets before editing.
    25-RESEARCH.md "Pitfall 1" section — confirms why nth(3) is now "Aero Duplicate" after Phase 22 added Duplicate buttons, making the existing frame-isolation test intermittently or consistently fail.
  </read_first>
  <action>
    Edit e2e/frame-isolation.spec.ts. The target block (approximately lines 56-60) currently reads as a three-line chain:
    `page.getByRole("group", { name: "Color theme" }).getByRole("button").nth(3).click()`.

    Replace `.getByRole("button").nth(3)` with `.getByRole("button", { name: "Noir" })`.

    After the edit the statement is a two-line chain inside the existing `await`:
    `page.getByRole("group", { name: "Color theme" }).getByRole("button", { name: "Noir" }).click()`.

    The surrounding comment (line 55-56) and `.click()` call remain. Remove only the orphaned `.nth(3)` call. Make no other changes to this file.

    Root cause: Phase 22 added Duplicate buttons beside each pill so the Color theme group now has 9 buttons (4 pills, 4 Duplicate buttons, 1 New Theme). Index 3 = "Aero Duplicate" which opens the ThemeEditor instead of switching the theme — the poll for `--text` change then times out and the test fails. The named selector `{ name: "Noir" }` matches only the pill because the Duplicate button has a different accessible name: `"Duplicate Noir"`.

    After editing, run `npm run e2e` to confirm both frame-isolation tests pass (the build triggers automatically via the webServer block). The run must report 2 passed, 0 failed.
  </action>
  <verify>
    <automated>npm run e2e</automated>
    The output must report 2 passed, 0 failed. The file must no longer contain `.nth(3)` inside the Color theme group block. Confirm by grepping: `grep -n "nth(3)" e2e/frame-isolation.spec.ts` must return empty.
  </verify>
  <acceptance_criteria>
    - e2e/frame-isolation.spec.ts line ~59 contains `getByRole("button", { name: "Noir" })` not `.nth(3)`
    - `npm run e2e` reports 2/2 passed with 0 failures (both frame-isolation tests green)
    - `grep -n "\.nth(3)" e2e/frame-isolation.spec.ts` returns no output
  </acceptance_criteria>
  <done>
    The Phase 20 frame-isolation spec is green. The existing Playwright baseline is confirmed passing before new tests are added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create e2e/smoke.spec.ts — SMOKE-01, SMOKE-02, SMOKE-03</name>
  <files>e2e/smoke.spec.ts</files>
  <read_first>
    playwright.config.ts — confirms testDir "e2e", baseURL "http://localhost:4173", fullyParallel false, production-preview webServer.
    e2e/frame-isolation.spec.ts — reference patterns: page.goto, launcher open, frameLocator, page.frames()[1]!, appFrame().evaluate(), expect.poll.
    25-RESEARCH.md — complete implementation blueprints for SMOKE-01/02/03, verified selectors, SMOKE_CUSTOM_VARS object, IDB seed pattern, anti-patterns table.
  </read_first>
  <action>
    Create e2e/smoke.spec.ts as a new file. Import `test`, `expect`, and `Page` from `@playwright/test`.

    Declare these file-scoped constants at the top:

    `SMOKE_CUSTOM_VARS` as `Record<string, string>` containing all 12 CSS variable keys with smoke-specific values — the discriminating entry is `"--text": "#003366"` (far from Aurora `#f3f1ff`). The full 12-entry set: `--text "#003366"`, `--wall "radial-gradient(130% 110% at 18% 8%, #001122 0%, #000a15 62%)"`, `--b1 "#0066ff"`, `--b2 "#0099ff"`, `--b3 "#00ccff"`, `--b4 "#0044ff"`, `--glass "rgba(0,0,255,0.10)"`, `--glass2 "rgba(0,0,255,0.035)"`, `--bord "rgba(0,0,255,0.22)"`, `--hi "rgba(0,0,255,0.5)"`, `--accentA "#0033ff"`, `--accentB "#0088ff"`.

    `AURORA_TEXT` = `"#f3f1ff"` (verified VibeThemeProvider.tsx:82 — used as the "must NOT be present" value in SMOKE-02).

    `NOIR_TEXT` = `"#f5eeff"` (verified VibeThemeProvider.tsx:128 — expected in-frame value after Noir switch in SMOKE-03).

    Define module-level helper `windowLocator(page: Page, title: string)` that returns `page.locator('.window-chrome', { has: page.locator('.window-chrome__title', { hasText: title }) })`. This scopes to a single window by title text.

    ---

    SMOKE-01 describe block titled "SMOKE-01 — window layout persists across hard reload":

    Single test "two open windows restore at saved positions and minimized state":
    Step 1: `page.goto("/")`.
    Step 2: Open launcher via `page.getByRole("button", { name: "Open launcher" }).click()`. Open Notes: `page.getByRole("button", { name: "Notes", exact: true }).click()`. Await `expect(windowLocator(page, "Notes")).toBeVisible({ timeout: 10_000 })`.
    Step 3: Open launcher again (same click). Open Weather: `page.getByRole("button", { name: "Weather", exact: true }).click()`. Await `expect(windowLocator(page, "Weather")).toBeVisible({ timeout: 10_000 })`.
    Step 4: Drag Notes to position (200, 120). Get `notesTitlebar = windowLocator(page, "Notes").locator(".window-chrome__titlebar")`. Call `nBbox = await notesTitlebar.boundingBox()`. Then `page.mouse.move(nBbox!.x + nBbox!.width / 2, nBbox!.y + nBbox!.height / 2)`, `page.mouse.down()`, `page.mouse.move(200, 120, { steps: 10 })`, `page.mouse.up()`. The `steps: 10` is mandatory — `useDrag` accumulates deltas from pointermove events; a single-step teleport produces zero delta.
    Step 5: Drag Weather to position (500, 250). Same pattern using `windowLocator(page, "Weather").locator(".window-chrome__titlebar")` with target (500, 250).
    Step 6: Minimize Weather: `windowLocator(page, "Weather").getByRole("button", { name: "Minimize" }).click()`.
    Step 7: Capture Notes pre-reload inline transform: `notesTransform = await windowLocator(page, "Notes").evaluate((el: HTMLElement) => el.style.transform)`. Use `el.style.transform` (inline style), never `getComputedStyle` — position is set as an inline style; getComputedStyle may return matrix notation or browser-normalized form that won't match the saved string.
    Step 8: `await page.waitForTimeout(500)` — allows the 300 ms trailing debounce (LAYOUT_SAVE_DEBOUNCE_MS = 300, DesktopShell.tsx:64) to fire and IDB write to complete. A shorter wait risks the save effect being cancelled before the write occurs.
    Step 9: `await page.reload()`.
    Step 10: Assert Notes element is in DOM: `await expect(windowLocator(page, "Notes")).toBeAttached({ timeout: 10_000 })`. Do NOT use `toBeVisible()` here because minimized windows (which share the same `.window-chrome` class) have `display: none` — `toBeAttached()` is the safe DOM-presence check.
    Step 11: Read Notes transform post-reload: `notesTransformAfter = await windowLocator(page, "Notes").evaluate((el: HTMLElement) => el.style.transform)`. Assert `expect(notesTransformAfter).toBe(notesTransform)`.
    Step 12: Assert Weather attached: `await expect(windowLocator(page, "Weather")).toBeAttached({ timeout: 10_000 })`.
    Step 13: Assert Weather is minimized: `weatherMinimized = await windowLocator(page, "Weather").evaluate((el: HTMLElement) => el.classList.contains("window-chrome--minimized"))`. Assert `expect(weatherMinimized).toBe(true)`. Use the class check — `toBeVisible()` would fail because `display: none` makes Playwright report the element as not visible even though it is in the DOM.
    Step 14: Assert desktop not blank: `await expect(page.locator(".window-chrome")).toHaveCount(2)`.

    ---

    SMOKE-02 describe block titled "SMOKE-02 — custom theme on first paint, no Aurora flash":

    Single test "hard reload applies custom theme before React hydrates":
    Step 1: `await page.addInitScript((vars: Record<string, string>) => { localStorage.setItem("marketplace.osTheme", "custom:smoketest"); localStorage.setItem("vibe.customTheme.smoketest", JSON.stringify(vars)); }, SMOKE_CUSTOM_VARS)`. The `addInitScript` callback runs before every `page.goto()` and `page.reload()` in this test, ensuring the FOUC script's localStorage reads always find the custom theme.
    Step 2: `await page.goto("/")`. This triggers `openRegistry()` inside the app which creates `MarketplaceRegistry` at version 3 — the IDB database must exist before the seed step.
    Step 3: Seed IDB via `await page.evaluate(async (vars: Record<string, string>) => { ... }, SMOKE_CUSTOM_VARS)`. The evaluate body: opens `indexedDB.open("MarketplaceRegistry", 3)` with `req.onsuccess` callback. In the success handler, begin a `readwrite` transaction on `"settings"` store. Call `store.put({ key: "customThemeIndex", value: JSON.stringify(["smoketest"]) }, "customThemeIndex")` and `store.put({ key: "custom:smoketest", value: JSON.stringify(vars) }, "custom:smoketest")`. On `tx.oncomplete` close the DB and resolve the promise. Include `req.onerror` and `tx.onerror` handlers that reject with the error. Include `req.onupgradeneeded` guard that creates the `"settings"` store if `!db.objectStoreNames.contains("settings")`. Without the IDB seed, `VibeThemeProvider.refreshCustomThemes()` reads an empty `customThemeIndex` from IDB, `customThemesState` stays an empty Map, and the apply-effect falls back to Aurora after hydration (verified VibeThemeProvider.tsx:266-295). Seeding both stores is mandatory.
    Step 4: `await page.reload({ waitUntil: "domcontentloaded" })`. The `domcontentloaded` event fires after inline `<script>` elements parse but before `<script type="module">` elements (Vite production output) execute. This captures the pre-hydration window where the FOUC inline script has set `:root` vars but React has not run yet.
    Step 5: `textAtFouc = await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue("--text").trim())`.
    Step 6: `expect(textAtFouc).not.toBe(AURORA_TEXT)` — no Aurora flash on first paint (this is the key FOUC assertion).
    Step 7: `expect(textAtFouc).toBe(SMOKE_CUSTOM_VARS["--text"])` — the custom value `"#003366"` is present at first paint.
    Step 8: `await page.waitForLoadState("load")` — React hydrates.
    Step 9: `await page.waitForTimeout(500)` — `refreshCustomThemes()` reads IDB asynchronously; 500 ms buffer covers the IDB read.
    Step 10: `textAfterHydration = await page.evaluate(() => getComputedStyle(document.documentElement).getPropertyValue("--text").trim())`.
    Step 11: `expect(textAfterHydration).toBe(SMOKE_CUSTOM_VARS["--text"])` — custom theme survived React mount (the IDB seed ensured `customThemesState` is populated).

    ---

    SMOKE-03 describe block titled "SMOKE-03 — live theme switch does not reload open frame":

    Single test "switching theme re-skins the frame; in-frame state survives":
    Step 1: `await page.goto("/")`.
    Step 2: Open launcher, open Notes (same pattern as SMOKE-01 steps 2-3). Do not await `toBeVisible` on the window container yet — proceed to frameLocator wait.
    Step 3: `const frame = page.frameLocator("iframe").first()`. Await `expect(frame.locator('input[placeholder="Add a note…"]')).toBeVisible({ timeout: 30_000 })`. The 30 s timeout covers first-open Babel compilation. Wait for this before any frame evaluate calls.
    Step 4: `const appFrame = () => page.frames()[1]!`. `frames()[0]` is the main page; `[1]` is the single open app iframe. Keep it as a getter function to handle any internal re-resolution.
    Step 5: `const textBefore = await appFrame().evaluate(() => getComputedStyle(document.documentElement).getPropertyValue("--text").trim())`.
    Step 6: `await appFrame().evaluate(() => { (window as any).__smokeThemeId = 42; })`. This sets an identity marker on the frame's `window` object. The marker survives a `THEME_PUSH` postMessage (which only calls `style.setProperty` on the root) but is wiped by a reload (full document re-creation). Its post-switch survival is proof that the iframe element was NOT recreated.
    Step 7: Switch theme: `await page.getByRole("group", { name: "Color theme" }).getByRole("button", { name: "Noir" }).click()`. The named selector is mandatory — `getByRole("button", { name: "Noir" })` matches the pill whose accessible name is exactly "Noir". The Duplicate button has `aria-label="Duplicate Noir"` (a different accessible name) and clicking it would open the ThemeEditor instead of switching the theme (the exact bug fixed in Task 1 for the existing spec).
    Step 8: `await expect.poll(() => appFrame().evaluate(() => getComputedStyle(document.documentElement).getPropertyValue("--text").trim()), { timeout: 2_000 }).not.toBe(textBefore)`. Poll until the frame's `--text` changes, allowing up to 2 s for `THEME_PUSH` to propagate and the in-frame bootstrap to call `style.setProperty` on `document.documentElement`.
    Step 9: `const markerAfter = await appFrame().evaluate(() => (window as any).__smokeThemeId)`. Assert `expect(markerAfter).toBe(42)`. The marker surviving proves the iframe was not reloaded (RESKIN-01 behavior confirmed in a real browser).
    Step 10: `const textAfter = await appFrame().evaluate(() => getComputedStyle(document.documentElement).getPropertyValue("--text").trim())`. Assert `expect(textAfter).toBe(NOIR_TEXT)`. The in-frame `:root --text` must equal `"#f5eeff"` (Noir, verified VibeThemeProvider.tsx:128).

    After writing the file, run `npm run e2e`. The webServer block triggers a production build then starts vite preview. Report the total pass count — all 5 tests (2 frame-isolation + 3 smoke) must pass.
  </action>
  <verify>
    <automated>npm run e2e</automated>
    The output must report 5 passed, 0 failed. After that run: `npx vitest run` and confirm it reports 936 (or more) passed with 0 failures — the unit suite must be unaffected by adding e2e test files. Finally run `tsc --noEmit` and confirm 0 errors.
  </verify>
  <acceptance_criteria>
    - e2e/smoke.spec.ts exists with three `test.describe` blocks: "SMOKE-01 — window layout persists across hard reload", "SMOKE-02 — custom theme on first paint, no Aurora flash", "SMOKE-03 — live theme switch does not reload open frame"
    - `npm run e2e` reports 5/5 passed (2 frame-isolation + 3 smoke) with 0 failures, running headless Chromium
    - `npx vitest run` reports all existing unit tests passed (count must be >= 936) with 0 failures
    - `tsc --noEmit` exits 0 (no TypeScript errors)
    - SMOKE-01: the test asserts `notesTransformAfter === notesTransform` and `weatherMinimized === true` and `.window-chrome` count === 2
    - SMOKE-02: the test asserts `textAtFouc !== "#f3f1ff"` and `textAtFouc === "#003366"` and `textAfterHydration === "#003366"`
    - SMOKE-03: the test asserts `markerAfter === 42` and `textAfter === "#f5eeff"`
    - e2e/smoke.spec.ts contains no `.nth(3)` calls; no `"Counter"` app references; no `human_needed` annotations
    - Zero new runtime dependencies introduced (e2e/ is devDependency-scope only; @playwright/test already present)
  </acceptance_criteria>
  <done>
    All three smoke tests pass headless. The Phase 21/22 human_needed gaps are permanently closed. RESKIN-01 has real-browser proof. The vitest unit suite and tsc check are unaffected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → browser page | Playwright controls a headless Chromium instance; test code calls page.evaluate() and page.addInitScript() to seed state into the page context |
| Browser page → production preview build | The production bundle runs at http://localhost:4173; tests assert against its rendered DOM and IDB/localStorage state |

## STRIDE Threat Register

Test-only infrastructure; no ASVS controls apply. The SMOKE specs run in ephemeral Playwright browser contexts (fresh localStorage and IDB per test by default). No real API key is used; SMOKE_CUSTOM_VARS contains only CSS color strings. The IDB seed writes only theme display values, not credentials.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | page.evaluate IDB seed | accept | SMOKE_CUSTOM_VARS contains only CSS custom property strings (hex colors, gradients). No secrets written to IDB or localStorage in test code. The existing frame-isolation test already proves localStorage.getItem("marketplace.apiKey") returns null inside the frame — this invariant is preserved. |
| T-25-02 | Tampering | addInitScript localStorage seed | accept | addInitScript sets only `marketplace.osTheme` and `vibe.customTheme.smoketest` — both are non-secret display configuration. The seed is scoped to the test's browser context and does not persist beyond the test run. |
| T-25-03 | Elevation of Privilege | page.frames()[1]! evaluate | accept | The frame has opaque origin (sandbox="allow-scripts", no allow-same-origin). page.frames()[1].evaluate() is Playwright's CDP-level introspection — it does not cross the opaque-origin boundary within the running app; it is a test-harness capability. Production users cannot replicate this access path. |
</threat_model>

<verification>
Phase-level gate (run after both tasks complete):

1. `npm run e2e` — must report 5 passed, 0 failed (all headless)
2. `npx vitest run` — must report >= 936 passed, 0 failed (unit suite unchanged)
3. `tsc --noEmit` — must exit 0 (zero TypeScript errors)
4. `grep -rn "\.nth(3)" e2e/` — must return empty (no nth(3) calls in any spec)
5. `grep -rn "Counter" e2e/smoke.spec.ts` 2>/dev/null — must return empty (Counter is not in APP_REGISTRY catalog)
6. `grep -rn "human_needed" e2e/` — must return empty (no human_needed annotations in any e2e file)
</verification>

<success_criteria>
Phase 25 is complete when:
1. Playwright SMOKE-01 passes: after hard reload, Notes window `style.transform` matches pre-reload value; Weather has `.window-chrome--minimized` class; both `.window-chrome` elements present in DOM
2. Playwright SMOKE-02 passes: at `domcontentloaded` after reload, `:root --text` is `"#003366"` (custom) not `"#f3f1ff"` (Aurora); custom value persists post-hydration
3. Playwright SMOKE-03 passes: after Noir switch, `page.frames()[1].__smokeThemeId === 42` (no reload) and frame `:root --text === "#f5eeff"` (Noir)
4. All 5 e2e specs pass headless: `npm run e2e` is green in CI
5. The `nth(3)` latent defect in frame-isolation.spec.ts is patched and that spec still passes
6. Cross-cutting: zero new runtime deps; vitest suite (>= 936 tests) stays green; `tsc --noEmit` exits 0
</success_criteria>

<output>
After both tasks complete, create `.planning/phases/25-real-browser-smoke-suite/25-01-SUMMARY.md` using the summary template at @$HOME/.claude/get-shit-done/templates/summary.md.

The summary must note the nth(3) defect fix as a deviation (zero-scope correction to existing spec — policy-compliant). Include the final npm run e2e pass count and the tsc + vitest exit codes.
</output>
```
