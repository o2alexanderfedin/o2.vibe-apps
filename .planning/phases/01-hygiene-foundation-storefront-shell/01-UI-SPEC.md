---
phase: 1
slug: hygiene-foundation-storefront-shell
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-24
reviewed_at: 2026-06-24
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — plain CSS / CSS Modules only |
| Icon library | Lucide React (tree-shakeable, neutral SVG names) |
| Font | system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif |

**Source:** CONTEXT.md D-07, Claude's Discretion, design brief.

shadcn gate: not applicable. Stack is Vite + React + TypeScript with a plain-CSS mandate (CONTEXT.md D-07). No component library. No `components.json` — greenfield project. shadcn initialization skipped per explicit constraint.

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, inline chip padding |
| sm | 8px | Icon button internal padding, tight intra-card spacing |
| md | 16px | Card internal padding, dialog body padding, form field gap |
| lg | 24px | Card gap in storefront grid, AppBar horizontal padding |
| xl | 32px | Storefront grid outer padding (desktop), dialog max-content padding |
| 2xl | 48px | Page-level vertical breathing room above/below grid |
| 3xl | 64px | Reserved — not used in Phase 1 surfaces |

Exceptions:
- Touch targets for icon-only buttons (AppBar theme toggle, Account button): minimum 44px × 44px hit area regardless of visual icon size. Achieved via `padding` expansion, not changing the icon size.
- Card width: fixed at 200px (set explicitly, not derived from spacing scale) — source: CONTEXT.md D-08.

---

## Typography

Font stack applied to `:root`: `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 | 1.5 | Card descriptions, dialog body copy, form labels, inline errors |
| Label | 12px | 400 | 1.4 | Card icon captions (if needed), secondary metadata |
| Heading | 20px | 600 | 1.2 | Card display name, dialog title |
| Display | 28px | 600 | 1.15 | Platform wordmark / page title in AppBar |

Exactly 4 sizes (12, 14, 20, 28) and 2 weights (400 regular, 600 semibold). No italic, no light weight, no additional sizes in Phase 1.

---

## Color

### CSS Variable Contract (LOCKED — verbatim names required by CONTEXT.md D-17)

All 9 variable names are locked. Values are assigned here for both themes.

| CSS Variable | Light Value | Dark Value | Role / 60-30-10 split |
|---|---|---|---|
| `--color-background-primary` | `#FFFFFF` | `#0F1117` | 60% dominant — page background, dialog backdrop clear area |
| `--color-background-secondary` | `#F5F6F8` | `#1A1D27` | 60% dominant — AppBar surface, dialog surface |
| `--color-background-tertiary` | `#ECEEF2` | `#242736` | 30% secondary — card background, hover state fill |
| `--color-text-primary` | `#111318` | `#E8EAF0` | 60% — primary body text, card display name |
| `--color-text-secondary` | `#4B5160` | `#9BA3B8` | 30% — card descriptions, dialog body, labels |
| `--color-text-tertiary` | `#8891A8` | `#5C6480` | 30% — placeholder text, helper copy, disabled states |
| `--color-border-secondary` | `#D6DAE4` | `#2E3248` | 30% — card border, dialog border, input border |
| `--color-border-tertiary` | `#EDF0F5` | `#1E2133` | 30% — subtle dividers, inner section separators |
| `--color-accent-primary` | `#4F6EF7` | `#6B87FF` | 10% accent — reserved for specific elements (see below) |

**Destructive color:**

| CSS Variable | Light Value | Dark Value | Usage |
|---|---|---|---|
| `--color-destructive` | `#D93025` | `#FF6B6B` | "Disconnect" / "Remove" action text and border only |

**Accent (`--color-accent-primary`) is reserved for exactly these elements:**
1. The primary submit button in KeyDialog ("Connect account" / "Save" CTA)
2. The active/selected state indicator on the 3-way theme toggle
3. Card hover border highlight (border transitions from `--color-border-secondary` to `--color-accent-primary`)
4. Focus ring on all interactive elements (`outline` color)

Accent is NOT used for: card backgrounds, AppBar background, dialog backdrop, body text, icon fill (icons use `--color-text-secondary`).

### Theme Application

- Attribute: `data-theme="light"` or `data-theme="dark"` on `<html>` element
- Variables defined on `:root[data-theme="light"]` and `:root[data-theme="dark"]`
- System mode reads `window.matchMedia("(prefers-color-scheme: dark)")` and applies the matching theme attribute
- FOUC prevention: blocking `<script>` in `index.html` before `</head>` reads `localStorage.getItem("marketplace.theme")` and sets `data-theme` synchronously before React mounts
- Source: CONTEXT.md D-15, D-16, D-17, D-18

---

## Copywriting Contract

**Hard rule: none of the following tokens appear in any user-visible string, CSS class, `data-*` attribute, or comment visible in DevTools:**
`AI`, `Generate`, `Generating`, `Generated`, `Synthesize`, `Synthesized`, `LLM`, `Mock`, `Fake`, `Anthropic` (user-facing), `model`

| Element | Exact Copy |
|---------|-----------|
| Platform wordmark | "Marketplace" |
| AppBar Account button tooltip | "Account" |
| AppBar theme toggle tooltip (light) | "Light theme" |
| AppBar theme toggle tooltip (dark) | "Dark theme" |
| AppBar theme toggle tooltip (system) | "System theme" |
| Storefront page heading (optional) | "Apps" |
| Storefront empty state (impossible in Phase 1 — registry is static) | N/A |
| Card click stub copy (Phase 1 — no loop yet) | "Opening…" |
| KeyDialog title — no key set | "Connect your account" |
| KeyDialog body — no key set | "To open apps, connect your account." |
| KeyDialog input label | "Access key" |
| KeyDialog input placeholder | "Paste your access key" |
| KeyDialog primary CTA — set flow | "Connect" |
| KeyDialog title — key already set | "Account" |
| KeyDialog status line — key already set | "Account connected" |
| KeyDialog change action | "Change key" |
| KeyDialog clear / disconnect action | "Disconnect" |
| KeyDialog clear confirmation copy | "Remove your account connection? You can reconnect at any time." |
| KeyDialog clear confirm button | "Disconnect" |
| KeyDialog clear cancel button | "Keep connected" |
| Inline key format error | "Invalid access key format. Please check and try again." |
| KeyDialog success (key saved) | (dialog closes, no toast in Phase 1) |
| Error boundary fallback heading | "Something went wrong" |
| Error boundary fallback body | "This section couldn't load. Try refreshing." |
| Error boundary retry button | "Try again" |
| Skeleton card aria-label | "Loading" |
| Loading copy during card click stub | "Opening…" |

**Primary CTA:** "Connect" (KeyDialog set flow) — verb + implicit noun (account).

**Destructive action:** KeyDialog "Disconnect". Two-step: user clicks "Disconnect" → inline confirmation copy appears with "Disconnect" confirm button and "Keep connected" cancel button. No separate modal — inline state transition within the dialog.

---

## Component Contracts

### 1. StorefrontGrid (SHELL-01)

**File:** `src/ui/Marketplace.tsx` + companion CSS (plain CSS or CSS Module)

**CSS class naming:**
- `.storefront-grid` — the grid container
- `.app-card` — individual card
- `.app-card__icon` — icon wrapper
- `.app-card__name` — display name
- `.app-card__description` — short description text

**Layout:**
- CSS Grid: `grid-template-columns: repeat(auto-fill, minmax(200px, 200px))`; cards do not stretch — they stay 200px fixed-width (CONTEXT.md D-08)
- `justify-content: center` on the grid container to center the card pack on wide screens
- Grid gap: 24px (`lg` token) on both axes
- Outer padding: 32px horizontal + 48px vertical on desktop; 16px horizontal on mobile (below 480px breakpoint)

**Card anatomy:**
- Background: `var(--color-background-tertiary)`
- Border: 1px solid `var(--color-border-secondary)`
- Border-radius: 12px
- Padding: 20px (between `md` 16px and `lg` 24px — use 20px as a one-off for card internal feel; this is the only spacing exception)
- Box shadow: none at rest; `0 2px 12px rgba(0,0,0,0.08)` on hover (light), `0 2px 12px rgba(0,0,0,0.32)` on hover (dark)
- Hover: border color transitions to `var(--color-accent-primary)`; transition 150ms ease
- Cursor: `pointer`

**Card contents (top-to-bottom):**
1. Icon: 32px × 32px Lucide icon, color `var(--color-text-secondary)`, margin-bottom 12px
2. Display name: 20px / weight 600 / `var(--color-text-primary)`, margin-bottom 4px
3. Description: 14px / weight 400 / `var(--color-text-secondary)`, line-height 1.5, 2-line max with `overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical`

**APP_REGISTRY (static, CONTEXT.md D-09):**

| id | displayName | description | Lucide Icon |
|----|-------------|-------------|-------------|
| `weather` | Weather | Current conditions and forecast for any location. | `Cloud` |
| `calculator` | Calculator | Arithmetic, percentages, and quick conversions. | `Calculator` |
| `notes` | Notes | Capture and organize your thoughts. | `NotebookPen` |
| `timer` | Timer | Countdown and stopwatch in one. | `Timer` |
| `currency` | Currency | Live exchange rates and conversions. | `ArrowLeftRight` |
| `recipes` | Recipes | Discover and save recipes by ingredient. | `ChefHat` |
| `calendar` | Calendar | Schedule and track your upcoming events. | `CalendarDays` |
| `budget` | Budget | Track spending and savings at a glance. | `Wallet` |

**States:**
- Default: card at rest
- Hover: accent border + shadow (see above)
- Focus-visible: 2px solid `var(--color-accent-primary)` outline, 2px offset
- Active (Phase 1 stub): shows inline "Opening…" text below description for 800ms then resets (no real action yet)

**Accessibility:**
- Each card is a `<button>` element (not a `<div>`)
- `aria-label="{displayName} — {description}"` on the button
- Icon `aria-hidden="true"`

---

### 2. AppBar (SHELL-03, SHELL-04)

**File:** `src/ui/AppBar.tsx`

**CSS class naming:**
- `.app-bar` — outer nav/header element
- `.app-bar__wordmark` — platform name area
- `.app-bar__controls` — right-side icon button group
- `.app-bar__icon-btn` — individual icon button

**Layout:**
- `<header role="banner">` with `<nav>` or direct landmark
- Height: 56px
- Background: `var(--color-background-secondary)`
- Border-bottom: 1px solid `var(--color-border-secondary)`
- Horizontal padding: 24px (`lg`)
- Internal layout: `display: flex; align-items: center; justify-content: space-between`
- Left: wordmark text "Marketplace" — 28px / weight 600 / `var(--color-text-primary)`
- Right: controls group — `display: flex; align-items: center; gap: 8px`

**Controls (right to left):**
1. Account button — Lucide `User` icon, 20px, opens KeyDialog
2. Theme toggle — cycles light → dark → system; icons: `Sun` (light), `Moon` (dark), `Monitor` (system)

**Icon buttons:**
- Size: 44px × 44px touch target (`display: flex; align-items: center; justify-content: center; width: 44px; height: 44px`)
- Background: transparent at rest; `var(--color-background-tertiary)` on hover
- Border-radius: 8px
- Icon color: `var(--color-text-secondary)` at rest; `var(--color-text-primary)` on hover
- Transition: background-color 120ms ease, color 120ms ease
- Focus-visible: 2px solid `var(--color-accent-primary)` outline, 2px offset

**Theme toggle state:**
- Current theme icon is displayed (Sun / Moon / Monitor)
- The active theme icon receives `color: var(--color-accent-primary)` to signal selected state
- `aria-label` updates dynamically: "Switch to dark theme" / "Switch to system theme" / "Switch to light theme" (communicates the NEXT action, not current state)
- Cycle order: light → dark → system → light

---

### 3. KeyDialog (SHELL-03)

**File:** `src/ui/KeyDialog.tsx`

**CSS class naming:**
- `.key-dialog-overlay` — full-screen backdrop
- `.key-dialog` — dialog panel
- `.key-dialog__header` — title row
- `.key-dialog__body` — content area
- `.key-dialog__footer` — action buttons row
- `.key-dialog__input-row` — label + input wrapper
- `.key-dialog__error` — inline error message

**Layout:**
- Backdrop: full-viewport `position: fixed; inset: 0; background: rgba(0,0,0,0.4); backdrop-filter: blur(2px)`
- Dialog panel: `position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%)`
- Panel width: 400px; `max-width: calc(100vw - 32px)` on mobile
- Panel background: `var(--color-background-secondary)`
- Panel border: 1px solid `var(--color-border-secondary)`
- Panel border-radius: 16px
- Panel padding: 24px
- Close button (X icon): absolute top-right, 44×44px touch target

**Three flows — set (no key), change (has key), confirm-clear:**

**Flow A — Set (no key stored):**
- Title: "Connect your account" — 20px / weight 600
- Body: "To open apps, connect your account." — 14px / weight 400 / `--color-text-secondary`
- Input: `type="password"`, label "Access key", placeholder "Paste your access key"
- Input width: 100%, height 40px, padding 8px 12px, border 1px solid `--color-border-secondary`, border-radius 8px, background `--color-background-primary`, font 14px
- Input focus: border-color transitions to `--color-accent-primary`, box-shadow `0 0 0 3px rgba(79, 110, 247, 0.15)` (light) / `rgba(107, 135, 255, 0.2)` (dark)
- Inline error (D-14): shown below input, 12px / weight 400 / `--color-destructive`; text: "Invalid access key format. Please check and try again."
- Footer: single button row — "Connect" primary CTA right-aligned, "Cancel" text button left-aligned
- Primary button: background `--color-accent-primary`, color `#FFFFFF`, 14px / weight 600, height 36px, padding 0 16px, border-radius 8px; hover: 8% darker (achieved via `filter: brightness(0.92)`)
- Cancel button: background transparent, color `--color-text-secondary`, 14px / weight 400; hover: `--color-text-primary`

**Flow B — Change (key already stored):**
- Title: "Account" — 20px / weight 600
- Status line: "Account connected" with Lucide `CheckCircle2` icon (16px, `--color-accent-primary`) — 14px / weight 400 / `--color-text-secondary`
- Two actions side-by-side below status line:
  - "Change key" — text button, `--color-text-secondary`; clicking this replaces the status area with the Flow A input
  - "Disconnect" — text button, `--color-destructive`; clicking this transitions to Flow C (inline confirmation)
- No primary CTA in idle state; dialog is informational with actions

**Flow C — Disconnect confirmation (inline within dialog, no new modal):**
- Inline block replaces the status area:
  - Copy: "Remove your account connection? You can reconnect at any time." — 14px / `--color-text-secondary`
  - Two buttons row: "Disconnect" (color `--color-destructive`, weight 600) and "Keep connected" (text, `--color-text-secondary`)
- Clicking "Disconnect": clears `localStorage.getItem("marketplace.apiKey")`, closes dialog, resets to Flow A on next open
- Clicking "Keep connected": returns to Flow B

**Accessibility:**
- `role="dialog"` + `aria-modal="true"` + `aria-labelledby` pointing to title
- Focus trap inside dialog while open
- `Escape` key closes dialog (returns to Flow A or B without saving)
- First interactive element receives focus on open

---

### 4. SkeletonCard (GEN-05 pattern — stub established in Phase 1)

**File:** `src/ui/SkeletonCard.tsx` (created as stub; used by Phase 3 generation loading state)

**CSS class naming:**
- `.skeleton-card` — card-sized placeholder matching `.app-card` dimensions
- `.skeleton-block` — individual shimmer block
- `.skeleton-block--icon` — 32×32 icon placeholder
- `.skeleton-block--name` — heading-height placeholder (20px tall, 60% card width)
- `.skeleton-block--desc` — description placeholder (14px × 2 lines, 100% width)

**Visual:**
- Same border, border-radius, padding, size as `.app-card`
- Shimmer animation: `background: linear-gradient(90deg, var(--color-background-tertiary) 25%, var(--color-border-tertiary) 50%, var(--color-background-tertiary) 75%)`
- `background-size: 200% 100%`; `animation: shimmer 1.5s infinite linear`
- `border-radius: 6px` on each skeleton block

**Copy during "Opening…" (shown as text below the skeleton grid in Phase 3, not inside the card):**
- "Opening…" — 14px / `--color-text-tertiary` / centered
- Alternative: "Just a moment…"
- BANNED: "Generating…", "Creating…", "Building…", "Synthesizing…"

**Accessibility:**
- `aria-label="Loading"` on the skeleton card
- `aria-busy="true"` on the container region while loading
- `role="status"` on a visually hidden live region that announces "Opening…" to screen readers

---

### 5. ErrorBoundaryFallback (RESIL-01 stub established in Phase 1)

**File:** `src/ui/ErrorBoundary.tsx`

**CSS class naming:**
- `.error-boundary-fallback` — container
- `.error-boundary-fallback__heading` — error heading
- `.error-boundary-fallback__body` — descriptive copy
- `.error-boundary-fallback__retry` — retry button

**Layout:**
- Centered block, max-width 320px, padding 32px, text-align center
- Background: `var(--color-background-secondary)`
- Border: 1px solid `var(--color-border-secondary)`
- Border-radius: 12px

**Copy:**
- Heading: "Something went wrong" — 20px / weight 600 / `--color-text-primary`
- Body: "This section couldn't load. Try refreshing." — 14px / weight 400 / `--color-text-secondary`
- Retry button: "Try again" — same style as primary button but with `--color-background-tertiary` background and `--color-text-primary` text (secondary action style)

**Banned copy in any error output:** "generation failed", "model error", "Anthropic error", "AI error", any technical error string that reveals the mechanic. All technical errors are swallowed before display — only the neutral fallback copy is shown.

---

## CSS Variable Contract

Full declaration block to be placed in `src/index.css` or `src/styles/variables.css`:

```css
:root[data-theme="light"] {
  --color-background-primary:   #FFFFFF;
  --color-background-secondary: #F5F6F8;
  --color-background-tertiary:  #ECEEF2;
  --color-text-primary:         #111318;
  --color-text-secondary:       #4B5160;
  --color-text-tertiary:        #8891A8;
  --color-border-secondary:     #D6DAE4;
  --color-border-tertiary:      #EDF0F5;
  --color-accent-primary:       #4F6EF7;
  --color-destructive:          #D93025;
}

:root[data-theme="dark"] {
  --color-background-primary:   #0F1117;
  --color-background-secondary: #1A1D27;
  --color-background-tertiary:  #242736;
  --color-text-primary:         #E8EAF0;
  --color-text-secondary:       #9BA3B8;
  --color-text-tertiary:        #5C6480;
  --color-border-secondary:     #2E3248;
  --color-border-tertiary:      #1E2133;
  --color-accent-primary:       #6B87FF;
  --color-destructive:          #FF6B6B;
}
```

**Contrast compliance (WCAG AA):**
- Light: `--color-text-primary` (#111318) on `--color-background-primary` (#FFFFFF) = 19.8:1 (AAA)
- Light: `--color-text-secondary` (#4B5160) on `--color-background-primary` (#FFFFFF) = 7.1:1 (AAA)
- Light: `--color-text-secondary` (#4B5160) on `--color-background-tertiary` (#ECEEF2) = 5.2:1 (AA)
- Dark: `--color-text-primary` (#E8EAF0) on `--color-background-primary` (#0F1117) = 16.4:1 (AAA)
- Dark: `--color-text-secondary` (#9BA3B8) on `--color-background-secondary` (#1A1D27) = 5.6:1 (AA)
- Light: `--color-accent-primary` (#4F6EF7) on white — for text use only in focused outlines; accent is never used for body text on colored backgrounds

---

## FOUC Prevention Script

This exact script (verbatim) belongs inside `index.html` as an inline `<script>` tag placed immediately before `</head>`:

```html
<script>
  (function () {
    var stored = localStorage.getItem('marketplace.theme');
    var theme = stored === 'light' || stored === 'dark' ? stored
      : window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', theme);
  })();
</script>
```

This runs synchronously before React mounts, preventing any flash of wrong theme. Source: CONTEXT.md D-18.

---

## Hygiene CSS Class Denylist

The following class name patterns are banned from the entire Phase 1 codebase. The hygiene-check CI gate (CONTEXT.md D-38..41) must scan for these:

| Pattern | Reason |
|---------|--------|
| `.ai-*` | Exposes AI mechanic |
| `.generated-*` | Exposes on-demand generation |
| `.synthesize-*` | Banned token |
| `.llm-*` | Exposes model |
| `.mock-*` | Exposes fake data |
| `.fake-*` | Exposes fake data |
| `data-generated` | Exposes mechanic via attribute |
| `data-ai-*` | Exposes AI mechanic |

Allowed structural class patterns: `.app-shell`, `.storefront-grid`, `.app-card`, `.app-bar`, `.key-dialog`, `.skeleton-card`, `.widget-frame`, `.error-boundary-fallback`

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party | none | not applicable |

No component library or registry is used. All components are authored as plain CSS + React. No `npx shadcn view` gate required.

---

## Pre-Population Traceability

| Decision | Source |
|----------|--------|
| No component library, plain CSS | CONTEXT.md D-07, design brief |
| Lucide React icons | CONTEXT.md Claude's Discretion, design brief |
| System-ui font stack | design brief — no Google Fonts |
| CSS variable names (all 9) | CONTEXT.md D-17 — LOCKED |
| `data-theme` on `<html>` | CONTEXT.md D-16 |
| FOUC blocking script | CONTEXT.md D-18 |
| Theme values: "light" / "dark" / "system" | CONTEXT.md D-15 |
| Cycle order light → dark → system | CONTEXT.md D-19 |
| Key stored as `marketplace.apiKey` | CONTEXT.md D-11 |
| Key framed as "Connect your account" | CONTEXT.md D-11 |
| Input `type="password"` | CONTEXT.md D-12 |
| Three KeyDialog flows | CONTEXT.md D-12 |
| Inline format error on save | CONTEXT.md D-14 |
| Fixed-width 200px cards | CONTEXT.md D-08 |
| APP_REGISTRY 6-8 app types | CONTEXT.md D-09 |
| DB name "MarketplaceRegistry" | CONTEXT.md D-20 |
| "Opening…" loading copy | CONTEXT.md D-10, design brief (GEN-05 pattern) |
| Banned tokens in copy/classes | CONTEXT.md D-39, design brief hygiene constraints |
| Color values (hex) | Claude's Discretion — D-17 names locked, values free |
| Typography sizes and weights | Claude's Discretion — no upstream constraint |
| Spacing scale | Claude's Discretion — standard 4px-multiple scale |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
