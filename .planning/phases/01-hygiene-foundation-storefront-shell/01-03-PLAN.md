---
phase: 01-hygiene-foundation-storefront-shell
plan: 03
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/registry/cacheKey.ts
  - src/registry/cacheKey.test.ts
  - src/host/modelClient.ts
  - src/host/modelClient.test.ts
autonomous: true
requirements: [LOOP-02, HYGIENE-05]
must_haves:
  truths:
    - "cacheKey(input) returns a deterministic 64-char lowercase hex SHA-256 over the normalized input"
    - "Normalization makes 'Weather ', 'weather', and 'WEATHER ' produce the identical key (NFC→lower→trim→collapse)"
    - "Unicode/emoji input does not throw and produces a valid 64-hex key"
    - "Distinct inputs (weather vs calculator) produce different keys with no readable type slug in the output"
    - "buildHeaders(apiKey) returns the 4 mandatory Anthropic headers and the key never appears in any log or error"
  artifacts:
    - path: "src/registry/cacheKey.ts"
      provides: "async SHA-256 opaque cache-key derivation over normalized input"
      exports: ["cacheKey"]
    - path: "src/host/modelClient.ts"
      provides: "single Anthropic egress stub — header assembly + constants + origin-assert seam (no fetch)"
      exports: ["buildHeaders", "ANTHROPIC_API_BASE", "ANTHROPIC_MODEL", "assertAnthropicTarget"]
  key_links:
    - from: "src/registry/cacheKey.ts"
      to: "crypto.subtle.digest"
      via: "SHA-256 over TextEncoder(normalized)"
      pattern: "crypto\\.subtle\\.digest\\(.SHA-256"
    - from: "src/host/modelClient.ts"
      to: "api.anthropic.com headers"
      via: "buildHeaders returns x-api-key + anthropic-version + dangerous-direct-browser-access"
      pattern: "anthropic-dangerous-direct-browser-access"
---

<objective>
Land the two pure foundation modules that have defined input→output contracts and benefit most from TDD: the opaque SHA-256 cache-key derivation (LOOP-02) and the single Anthropic egress stub (HYGIENE-05). Neither touches the DOM. The cache-key function is the keying primitive every later phase uses before any registry read/write; the egress stub locks the correct header set and single-chokepoint shape for Phase 3 without making any network call.

Purpose: These are the highest-determinism units in Phase 1 — exact input→output behavior is specified in the decisions, so they are written test-first (RED→GREEN). The cache-key test must run under the Node environment (jsdom breaks `crypto.subtle.digest`).

Output: `cacheKey.ts` + `modelClient.ts` with passing tests, including the key-never-leaks assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md

<interfaces>
<!-- This plan CREATES these contracts. Downstream phases consume them. -->

src/registry/cacheKey.ts:
- export async function cacheKey(input: string): Promise<string>;  // 64-char lowercase hex

src/host/modelClient.ts:
- export const ANTHROPIC_API_BASE = "https://api.anthropic.com";
- export const ANTHROPIC_MODEL = "claude-haiku-4-5-20251001";
- export function buildHeaders(apiKey: string): Record<string, string>;  // 4 headers, key passed at call time
- export function assertAnthropicTarget(url: string): void;  // Phase 1 stub seam; Phase 3 enforces

Test infra from Plan 01 (src/test/setup.ts) is loaded by the jsdom default env.
cacheKey.test.ts MUST override to the node env per-file (see Task 1).
</interfaces>
</context>

<phase_goal>
**As a** platform, **I want to** derive opaque, normalized, collision-resistant cache keys and assemble the exact Anthropic request headers at a single egress chokepoint, **so that** later phases can cache and call the model without leaking the mechanic or the user's key.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cacheKey — opaque SHA-256 over normalized input (RED→GREEN, node env)</name>
  <files>src/registry/cacheKey.ts, src/registry/cacheKey.test.ts</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (D-24 signature; D-25 normalization steps; D-26 hashing; D-27 no-btoa/no-slug-prefix; D-28 required tests)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md (Pattern 1 cacheKey code ~lines 320-330; Pitfall 2 crypto.subtle in jsdom + node-env pragma; Anti-Patterns btoa/slug-prefix; verified-live note)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-VALIDATION.md (LOOP-02 command + node pragma requirement)
  </read_first>
  <behavior>
    - Determinism: cacheKey("weather") called twice returns the identical string
    - Normalization equivalence: cacheKey("Weather "), cacheKey("weather"), cacheKey("WEATHER ") all equal each other (NFC→lower→trim→collapse, D-25)
    - Internal whitespace collapse: cacheKey("a   b") equals cacheKey("a b")
    - Opacity/format: output matches /^[0-9a-f]{64}$/ and contains no readable type slug (e.g. the substring "weather" never appears in the hex output)
    - Unicode safety: cacheKey("weather ☀️ 天气") resolves to a valid 64-hex string and does NOT throw
    - No collision on minimal difference: cacheKey("weather") !== cacheKey("calculator")
  </behavior>
  <action>
    FIRST create `src/registry/cacheKey.test.ts` whose VERY FIRST LINE is the pragma `// @vitest-environment node` (Pitfall 2 — jsdom's key-shim makes `crypto.subtle.digest` throw a TypeError; the function is pure so the node env is correct and verified working). Write all `<behavior>` cases. Run it and confirm it FAILS (RED — no implementation yet).

    THEN create `src/registry/cacheKey.ts` per RESEARCH Pattern 1 (D-24..D-27): `export async function cacheKey(input: string): Promise<string>` that normalizes `input.normalize("NFC").toLowerCase().trim().replace(/\s+/g, " ")` (D-25, exact order), encodes via `new TextEncoder().encode(normalized)`, hashes with `await crypto.subtle.digest("SHA-256", bytes)`, and maps the resulting `Uint8Array` to a lowercase hex string with `.toString(16).padStart(2,"0")`. NEVER use `btoa`/base64; NEVER prepend the type slug to the output — the slug is part of the hashed INPUT only (D-27). Run the test and confirm it PASSES (GREEN).

    Produce the two atomic commits the TDD cycle requires: `test(01-03): add failing cacheKey tests` then `feat(01-03): implement opaque cacheKey`.
  </action>
  <verify>
    <automated>head -1 src/registry/cacheKey.test.ts | grep -q "@vitest-environment node" && echo "PRAGMA_OK"; grep -q 'crypto.subtle.digest("SHA-256"' src/registry/cacheKey.ts && echo "DIGEST_OK"; ! grep -q "btoa" src/registry/cacheKey.ts && echo "NO_BTOA_OK"; npx vitest run src/registry/cacheKey.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `src/registry/cacheKey.test.ts` first line is `// @vitest-environment node`
    - `src/registry/cacheKey.ts` contains `crypto.subtle.digest("SHA-256"` and does NOT contain `btoa`
    - `cacheKey` normalizes in the exact D-25 order (NFC → toLowerCase → trim → collapse whitespace) before hashing
    - Output is a 64-char lowercase hex string with no readable slug prefix
    - `npx vitest run src/registry/cacheKey.test.ts` exits 0 with all 6 behaviors green
  </acceptance_criteria>
  <done>cacheKey is deterministic, normalization-stable, 64-hex opaque, emoji-safe, collision-distinct, and its node-env test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: modelClient — Anthropic egress stub with key-never-leaks guarantee (RED→GREEN)</name>
  <files>src/host/modelClient.ts, src/host/modelClient.test.ts</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (D-34 stub-only/no-fetch; D-35 exports + exact 4 headers; D-36 origin assert seam; D-37 key never stored module-level/never logged; D-13)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md (Anthropic egress stub code example ~lines 603-625; mandatory dangerous-direct-browser-access header note; Pitfall 7 key leaking into log/error)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-VALIDATION.md (HYGIENE-05 command)
  </read_first>
  <behavior>
    - buildHeaders("sk-ant-test123") returns an object with exactly the 4 keys: "content-type"="application/json", "x-api-key"="sk-ant-test123", "anthropic-version"="2023-06-01", "anthropic-dangerous-direct-browser-access"="true"
    - The module exports ANTHROPIC_API_BASE === "https://api.anthropic.com" and ANTHROPIC_MODEL === "claude-haiku-4-5-20251001"
    - Key safety: calling buildHeaders does NOT trigger any console.* call; no module-level variable retains the key after the call (the function is the only thing that references it)
    - assertAnthropicTarget exists and, in Phase 1, is a no-op stub seam (does not throw) — Phase 3 will enforce the origin check
  </behavior>
  <action>
    FIRST create `src/host/modelClient.test.ts` covering every `<behavior>` bullet. For the key-safety assertion, spy on all `console.*` methods and assert none fire during `buildHeaders`; assert the returned object has exactly the 4 expected entries with the exact values. Run it → RED.

    THEN create `src/host/modelClient.ts` per RESEARCH "Anthropic egress stub" (D-34..D-37): export `ANTHROPIC_API_BASE = "https://api.anthropic.com"`, `ANTHROPIC_MODEL = "claude-haiku-4-5-20251001"`, and `buildHeaders(apiKey: string): Record<string,string>` returning the exact 4-header object (the `anthropic-dangerous-direct-browser-access: "true"` header is MANDATORY for browser→Anthropic CORS and is locked now for Phase 3). The key is received as a parameter at call time and NEVER stored module-level and NEVER logged (D-37). Export `assertAnthropicTarget(url: string): void` as a Phase-1 no-op seam with a NEUTRAL comment that Phase 3 will throw when `new URL(url).origin !== ANTHROPIC_API_BASE` — do NOT make any fetch call in Phase 1 (D-34). Run the test → GREEN.

    Commits: `test(01-03): add failing modelClient header tests` then `feat(01-03): implement Anthropic egress header stub`.
  </action>
  <verify>
    <automated>grep -q "anthropic-dangerous-direct-browser-access" src/host/modelClient.ts && grep -q '"anthropic-version": "2023-06-01"' src/host/modelClient.ts && echo "HEADERS_OK"; grep -q "claude-haiku-4-5-20251001" src/host/modelClient.ts && echo "MODEL_OK"; ! grep -qE "fetch\(|axios" src/host/modelClient.ts && echo "NO_FETCH_OK"; npx vitest run src/host/modelClient.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `src/host/modelClient.ts` `buildHeaders` returns exactly `content-type`, `x-api-key`, `anthropic-version` (= "2023-06-01"), `anthropic-dangerous-direct-browser-access` (= "true")
    - Exports `ANTHROPIC_API_BASE = "https://api.anthropic.com"` and `ANTHROPIC_MODEL = "claude-haiku-4-5-20251001"`
    - The module makes NO `fetch`/network call (grep finds no `fetch(`) — it is a header-assembly stub only (D-34)
    - The API key is never logged and never stored module-level; the test proves no `console.*` fires during `buildHeaders`
    - `npx vitest run src/host/modelClient.test.ts` exits 0
  </acceptance_criteria>
  <done>The single egress stub assembles the exact mandatory headers, exposes the base/model constants and the origin-assert seam, makes no network call, and guarantees the key never leaks — proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application ↔ Anthropic network edge | `modelClient.ts` is the only module that names the external host; in Phase 1 it must not perform I/O. |
| cache-key input ↔ devtools-visible output | The 64-hex key must be opaque — no readable type slug — so the registry surface never narrates which app it caches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information Disclosure | cache-key output (slug leak) | mitigate | SHA-256 hex with the slug hashed INTO the input, never prepended to the output (D-27); test asserts the slug substring never appears in the key. |
| T-01-12 | Information Disclosure | API key in headers/logs | mitigate | Key passed at call time, never stored module-level, never logged (D-37); test asserts no `console.*` fires during `buildHeaders`. |
| T-01-13 | Information Disclosure | premature/unintended egress | mitigate | No `fetch` in Phase 1 (D-34); `assertAnthropicTarget` seam reserved to enforce the `api.anthropic.com` origin in Phase 3. |
| T-01-14 | Cryptography misuse | hand-rolled hashing | mitigate | Native `crypto.subtle.digest("SHA-256")` only — never a JS hash impl (ASVS V6, D-26). |
</threat_model>

<verification>
- `npx vitest run src/registry/cacheKey.test.ts src/host/modelClient.test.ts` passes (cacheKey under node env).
- `npx tsc --noEmit` passes.
- `modelClient.ts` contains no `fetch`/network call; the key never appears in a log or error.
</verification>

<success_criteria>
- `cacheKey` produces deterministic, normalization-stable, 64-hex opaque, emoji-safe, collision-distinct keys (LOOP-02); test green under the node env.
- `modelClient.ts` assembles the exact 4 mandatory headers + base/model constants + origin-assert seam, makes no network call, and never leaks the key (HYGIENE-05); test green.
- Both modules are pure, DOM-free, and ready for Phase 2/3 consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hygiene-foundation-storefront-shell/01-03-SUMMARY.md`
</output>
