---
phase: 01-hygiene-foundation-storefront-shell
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/hygiene.test.ts
autonomous: true
requirements: [HYGIENE-01, HYGIENE-02, HYGIENE-03]
must_haves:
  truths:
    - "A Vitest test scans src/** plus index.html and fails (exit 1) if any banned mechanic-revealing token appears in a devtools-visible context"
    - "The gate passes (green) on the complete Phase 1 source produced by Plans 01-03"
    - "The literal 'synthesize/synthesized/synthesis' appears nowhere in the scanned source"
    - "The gate excludes itself and node_modules/dist to avoid self-matching its own regex literals"
  artifacts:
    - path: "src/hygiene.test.ts"
      provides: "lexicon-grep CI gate scanning the source tree for banned tokens"
      contains: "synthesi"
  key_links:
    - from: "src/hygiene.test.ts"
      to: "src/** + index.html"
      via: "fs walk + per-file regex scan"
      pattern: "readdirSync|readFileSync"
---

<objective>
Implement the CI lexicon-grep hygiene gate (HYGIENE-03) as a Vitest test that statically scans the entire Phase 1 source surface and fails any merge introducing banned, mechanic-revealing tokens. It runs LAST in the phase because it must scan everything Plans 01-03 produced — and it must pass green on that source, proving HYGIENE-01 (no devtools-visible mechanic disclosure) and HYGIENE-02 (the "synthesize" family appears nowhere) hold for the real codebase.

Purpose: This gate is the enforcement mechanism for the entire "apps just exist" illusion. Combined with `sourcemap: false` (Plan 01), it ensures no symbol, store name, CSS class, `data-*`, comment, or visible string narrates the on-demand mechanic. It is wired into `npm run test` so it runs on every future change.

Output: `src/hygiene.test.ts` that scans `src/**` + `index.html`, passes on Phase 1 source, and is self-verifying (a deliberately-injected banned token would make it red).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md
@.planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md
</context>

<phase_goal>
**As a** maintainer, **I want to** automatically fail any change that introduces a mechanic-revealing token into a devtools-visible surface, **so that** the "apps just exist" illusion can never be eroded by an accidental edit.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Lexicon hygiene gate — scan src/** + index.html, fail on banned tokens, pass on Phase 1 source</name>
  <files>src/hygiene.test.ts</files>
  <read_first>
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-CONTEXT.md (D-38 vitest-preferred; D-39 banned token set; D-40 generate exception + scan targets; D-41 wired into npm run test; D-42 fs.readFileSync + regex)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-RESEARCH.md (Pattern 6 lexicon gate code ~lines 430-447; Pitfall 6 self-match/false-positive avoidance + word boundaries; Open Question 2 scan scope incl. index.html; Assumption A3 generate* may be banned repo-wide in Phase 1)
    - .planning/phases/01-hygiene-foundation-storefront-shell/01-UI-SPEC.md (§Hygiene CSS Class Denylist; §Copywriting Contract banned-token list)
  </read_first>
  <action>
    Create `src/hygiene.test.ts` per RESEARCH Pattern 6 (D-38..D-42). Use Node `fs.readdirSync`/`statSync`/`readFileSync` to recursively walk `src/**` and additionally read `index.html` at the repo root (per Open Question 2 — `index.html` carries the CSP/FOUC/title and is the only other authored devtools-visible surface). EXCLUDE `node_modules`, `dist`, and the gate file itself (`hygiene.test.ts`) so the test does not match its own regex literals (Pitfall 6).

    Banned token regex set (D-39, with the word boundaries from Pitfall 6):
    - `/synthesi[sz]/i` — synthesize/synthesized/synthesis, safe to match anywhere (HYGIENE-02)
    - `/\bfake\b/i`
    - `/\bmock\b/i`
    - `/\bAI\b/` — case-SENSITIVE exact word (matches "AI" but not "air"/"maintain")
    - `/\bllm\b/i`
    - `/\bgenerat(e|ed|ing)\b/i` — per Assumption A3, Phase 1 ships NO internal `generate*` identifier, so ban the `generate*` family across all scanned `src/**` + `index.html` for Phase 1. Add a clear NEUTRAL comment in the test stating that Phase 2 (which may introduce an internal `generate*` name) must relax this to the D-40 context-aware carve-out (CSS class names, `data-*` values, DOM/console string literals, comments only). This is the one precision judgment call — call it out explicitly in the test comment.

    For each scanned file, for each banned regex, if a match is found, collect `{file, token, lineNumber, snippet}`. The test asserts the collected violations array is EMPTY; on failure it reports every violation with file+line so a developer can fix it. Wrap the whole scan in a single `it("contains no mechanic-revealing tokens", …)` (or split per-category) inside a `describe`. The gate runs as part of `npm run test` (already aliased to `vitest run` in Plan 01) per D-41 — no separate script needed, though you may also confirm a `"hygiene": "vitest run src/hygiene.test.ts"` script is harmless to add.

    Run the gate against the real Phase 1 tree. It MUST pass green. If it flags a legitimate Phase-1 string (e.g. a benign substring), prefer FIXING the source to be neutral over loosening the regex — but never weaken a word boundary in a way that would let a real leak through. Note: the banned-token list in UI-SPEC §Copywriting Contract and §Hygiene CSS Class Denylist must all be covered.
  </action>
  <verify>
    <automated>grep -q "synthesi" src/hygiene.test.ts && grep -qE "readdirSync|readFileSync" src/hygiene.test.ts && echo "GATE_PRESENT"; grep -q "hygiene.test.ts" src/hygiene.test.ts && echo "SELF_EXCLUDE_OK"; npx vitest run src/hygiene.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/hygiene.test.ts` walks `src/**` (via `readdirSync`/`statSync`) and reads `index.html`, excluding `node_modules`, `dist`, and itself
    - The banned set includes `/synthesi[sz]/i`, `/\bfake\b/i`, `/\bmock\b/i`, `/\bAI\b/` (case-sensitive), `/\bllm\b/i`, and `/\bgenerat(e|ed|ing)\b/i`
    - A NEUTRAL comment documents the Phase-1 repo-wide `generate*` ban and the Phase-2 relaxation to the D-40 carve-out
    - `npx vitest run src/hygiene.test.ts` exits 0 (passes on the real Phase 1 source)
    - `npx vitest run` (full suite) exits 0 — the gate is wired into `npm run test`
  </acceptance_criteria>
  <done>The lexicon gate scans the full Phase 1 source surface, fails on any banned token, self-excludes to avoid false positives, and passes green on the actual Phase 1 codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored source ↔ devtools-visible surface | Every identifier, comment, CSS class, `data-*`, and string literal can surface in devtools (or via a slip in sourcemaps); the gate is the static-analysis guard that no banned token crosses into that surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | mechanic-revealing token in source | mitigate | Static lexicon gate (`src/hygiene.test.ts`) fails the build on any banned token in `src/**` + `index.html` (HYGIENE-03); wired into `npm run test` so it runs on every change (D-41). |
| T-01-16 | Information Disclosure | "synthesize" family specifically | mitigate | `/synthesi[sz]/i` matches anywhere including comments (HYGIENE-02); combined with `sourcemap:false` (Plan 01) the token can never reach a shipped surface. |
| T-01-17 | False-negative (gate too loose) | word-boundary/self-match errors | mitigate | Word boundaries per Pitfall 6 (`\bAI\b` case-sensitive, `\bmock\b`, `\bfake\b`); gate excludes itself and node_modules to avoid self-matching its own regex literals (no false positives that would tempt loosening). |
</threat_model>

<verification>
- `npx vitest run src/hygiene.test.ts` passes on the Phase 1 source.
- `npx vitest run` (full suite incl. all Plan 01-03 tests + the gate) passes.
- A manual sanity check: temporarily adding a banned token (e.g. `// synthesize`) to any `src/` file makes the gate go red, then removing it restores green. (Do not commit the temporary token.)
</verification>

<success_criteria>
- The lexicon-grep gate exists as a Vitest test, scans `src/**` + `index.html`, and fails on the full D-39 banned-token set with correct word boundaries (HYGIENE-03).
- The gate passes green on the complete Phase 1 codebase, proving no devtools-visible surface narrates the mechanic (HYGIENE-01) and the "synthesize" family appears nowhere (HYGIENE-02).
- The gate self-excludes to avoid false positives and is wired into `npm run test`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hygiene-foundation-storefront-shell/01-04-SUMMARY.md`
</output>
