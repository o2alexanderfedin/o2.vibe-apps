---
phase: "12-network-data-path"
plan: "04"
type: execute
wave: 2
depends_on:
  - "12-01"
  - "12-03"
files_modified:
  - src/apps/seeds.ts
  - src/apps/weatherHandlers.ts
  - src/apps/currencyHandlers.ts
  - src/execution/handler.ts
autonomous: true
requirements:
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Weather app renders deterministically from SEEDED_SOURCES without any model call"
    - "Currency app renders deterministically from SEEDED_SOURCES without any model call"
    - "Weather 'search' action handler is seeded — calls fetchData('weather-geocode',...) then fetchData('weather-forecast',...) and returns {data:{state:{place,tempC,condition,status:'ready'}}}"
    - "Currency 'load' action handler is seeded — calls fetchData('fx-latest',...) and returns {data:{state:{base,rates,status:'ready'}}}"
    - "Seeded handler short-circuit in resolveHandlerJS fires BEFORE registry lookup and BEFORE model call"
    - "On fetch error either handler returns {data:{state:{...prev, status:'error'}}} — state update keeps prior values except status"
    - "Weather initialState fields (query, place, tempC, condition, status) satisfy Phase 11 deriveStateSchema so fetched-data merge is allowed"
    - "Currency initialState fields (base, rates, status) satisfy Phase 11 deriveStateSchema so fetched-data merge is allowed"
    - "Neutral loading/error copy: 'Loading conditions…' / 'Couldn't load conditions' / 'Enter a location' for Weather; 'Loading rates…' / 'Couldn't load rates' for Currency"
    - "No banned mechanic tokens in any new source string, view markup, or comment"
  artifacts:
    - path: "src/apps/seeds.ts"
      provides: "Weather and Currency delegated module source strings in SEEDED_SOURCES"
      contains: "weather"
    - path: "src/apps/weatherHandlers.ts"
      provides: "WEATHER_HANDLER_SOURCES ReadonlyMap with the seeded search handler source"
      exports: ["WEATHER_HANDLER_SOURCES"]
    - path: "src/apps/currencyHandlers.ts"
      provides: "CURRENCY_HANDLER_SOURCES ReadonlyMap with the seeded load handler source"
      exports: ["CURRENCY_HANDLER_SOURCES"]
    - path: "src/execution/handler.ts"
      provides: "Seeded-handler short-circuit in resolveHandlerJS using SEEDED_HANDLER_SOURCES"
      contains: "SEEDED_HANDLER_SOURCES"
  key_links:
    - from: "src/execution/handler.ts"
      to: "src/apps/weatherHandlers.ts"
      via: "SEEDED_HANDLER_SOURCES.get(intent) in resolveHandlerJS before registry lookup"
      pattern: "SEEDED_HANDLER_SOURCES\\.get"
    - from: "src/apps/weatherHandlers.ts"
      to: "src/data/dataBroker.ts"
      via: "handler source calls fetchData('weather-geocode',...) and fetchData('weather-forecast',...)"
      pattern: "weather-geocode|weather-forecast"
    - from: "src/apps/currencyHandlers.ts"
      to: "src/data/dataBroker.ts"
      via: "handler source calls fetchData('fx-latest',...)"
      pattern: "fx-latest"
    - from: "src/apps/seeds.ts"
      to: "src/execution/delegated.tsx"
      via: "SEEDED_SOURCES feeds instantiateDelegated via the loader seeded path"
      pattern: "SEEDED_SOURCES"
---

<objective>
Add Weather and Currency as seeded delegated modules (behavior-free view + initialState + actionSpec) and ship deterministic seeded handlers for their primary actions. The seeded handler lookup short-circuits resolveHandlerJS before the registry and before any model call — making both flagship network apps fully deterministic and testable offline.

Purpose: DATA-03 requires Weather and Currency to show real data with neutral states. The seeded-handler path makes them work without any Anthropic call, mirroring how SEEDED_SOURCES avoids app production. DATA-04 TTL cache is exercised by Plan 05 tests; the seeded handler is what calls fetchData.

Output: src/apps/seeds.ts (extended), src/apps/weatherHandlers.ts (new), src/apps/currencyHandlers.ts (new), src/execution/handler.ts (seeded-handler short-circuit added to resolveHandlerJS).
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-network-data-path/12-CONTEXT.md
@.planning/phases/12-network-data-path/12-PATTERNS.md
@.planning/phases/12-network-data-path/12-01-SUMMARY.md
@.planning/phases/12-network-data-path/12-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/execution/delegated.tsx (lines 39-46) — DelegatedModule contract seeded sources must satisfy:
```typescript
export type DelegatedState = Record<string, unknown>;
export interface DelegatedModule {
  initialState: DelegatedState;         // complete state shape + initial values
  view: (state: DelegatedState) => ReactNode;  // pure markup, data-action attrs, NO handlers
  actionSpec: string;                   // what each action does to the state shape
}
```

From src/execution/delegated.tsx (lines 136-146) — buildActionIntent (the exact intent string format):
```typescript
export function buildActionIntent(appType: string, actionSpec: string, action: string): string {
  return (
    `${appType} action '${action}': ${actionSpec} ` +
    `The handler input is { state, payload } where payload is the action string '${action}'. ` +
    `Return { data: { state } } with the SAME state shape and ALWAYS a valid state.`
  );
}
```
CRITICAL: The seeded handler map key MUST be the exact string buildActionIntent produces.
The key is deterministic given (appType, actionSpec, action) — the actionSpec you put in seeds.ts
is part of the lookup key. Keep actionSpec strings short and stable.

From src/execution/handler.ts (lines 186-229) — resolveHandlerJS (WHERE to insert the short-circuit):
```typescript
async function resolveHandlerJS(intent: string, services: Services): Promise<string> {
  const key = await registryKey("handler", intent);

  // ADD: seeded-handler short-circuit HERE — before registry lookup, before model call
  // const seededJS = SEEDED_HANDLER_SOURCES.get(intent);
  // if (seededJS) return seededJS;

  const stored = await services.registry.get("handlers", key);
  // ... rest of cache → model path ...
}
```

From src/execution/transpile.ts — transpileHandler for the seeded-handler short-circuit:
```typescript
// transpileHandler(source, { filename }) → transpiledJS string (used by executeHandler)
import { transpileHandler } from "./transpile";
```

From src/apps/seeds.ts (lines 1-7) — SEEDED_SOURCES Map pattern:
```typescript
export const SEEDED_SOURCES: ReadonlyMap<string, string> = new Map([
  ["counter", `...TSX source...`],
  ["notes", `...TSX source...`],
]);
```

From .planning/phases/12-network-data-path/12-CONTEXT.md specifics — VERIFIED live API response shapes:
  geocode response: { results:[{ name, latitude, longitude, country, country_code, admin1, timezone }], generationtime_ms }
  forecast response: { latitude, longitude, elevation, timezone, current_units:{temperature_2m:"°C",...}, current:{ time, interval, temperature_2m, weather_code, wind_speed_10m } }
  fx response: { amount, base, date, rates:{ EUR, GBP, JPY } }

Weather initialState (CONTEXT.md DATA-03): { query:"", place:"", tempC:null, condition:"", status:"idle" }
Currency initialState (CONTEXT.md DATA-03): { base:"USD", rates:null, status:"idle" }

Phase 11 deriveStateSchema (src/execution/stateSchema.ts) derives a Zod schema from initialState.
Fields present in initialState are accepted on merge; fields absent from initialState are EXTRA KEYS
(allowed per RELY-01 "extra keys allowed"). So tempC:null in initialState allows a number on merge.
rates:null in initialState allows an object on merge. Status:"idle" (string) allows any string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Weather + Currency delegated modules to seeds.ts</name>
  <files>src/apps/seeds.ts</files>
  <read_first>
    - src/apps/seeds.ts (full file — 108 lines) — read before editing; understand the existing ReadonlyMap pattern and how counter/notes source strings are structured
    - src/execution/delegated.tsx (lines 39-46, 136-146) — DelegatedModule contract (initialState, view, actionSpec) and buildActionIntent format; the actionSpec text written here becomes part of the seeded handler map key
    - src/execution/stateSchema.ts — deriveStateSchema to confirm that null-typed fields in initialState allow updates (they do — null matches any type in the lenient schema)
    - .planning/phases/12-network-data-path/12-CONTEXT.md DATA-03 — initialState shapes; neutral copy strings; aria-busy loading pattern; no banned mechanic tokens
    - .planning/phases/12-network-data-path/12-PATTERNS.md seeds.ts section — delegated module shape to follow (NOT a monolithic App function)
    - src/ui/SkeletonCard.tsx (lines 4-15) — aria-busy="true" loading pattern to replicate in view markup
  </read_first>
  <behavior>
    - SEEDED_SOURCES.get("weather") returns a non-empty string that, when passed to instantiateDelegated, produces a DelegatedModule with initialState.status === "idle", initialState.query === "", initialState.tempC === null
    - SEEDED_SOURCES.get("currency") returns a non-empty string that, when passed to instantiateDelegated, produces a DelegatedModule with initialState.status === "idle", initialState.base === "USD", initialState.rates === null
    - Weather view({...initialState, status:"loading"}) renders markup with aria-busy="true" and text "Loading conditions…"
    - Weather view({...initialState, status:"error"}) renders markup with text "Couldn't load conditions" and a data-action="search" retry element
    - Weather view({...initialState}) with status "idle" renders markup with "Enter a location" placeholder and a data-action="search" element
    - Weather view({query:"London", place:"London, GB", tempC:12, condition:"Partly cloudy", status:"ready"}) renders the place name and temperature
    - Currency view({...initialState, status:"loading"}) renders "Loading rates…" with aria-busy="true"
    - Currency view({base:"USD", rates:{EUR:0.93,GBP:0.79}, status:"ready"}) renders rate information
    - actionSpec for weather is a short stable string that includes the state shape fields and the "search" action description
    - actionSpec for currency is a short stable string that includes the state shape fields and the "load" action description
  </behavior>
  <action>
    Edit src/apps/seeds.ts. Add two new entries to the SEEDED_SOURCES Map: "weather" and "currency". Both are delegated module source strings (NOT monolithic App components) — they export initialState, view, and actionSpec using module.exports = { initialState, view, actionSpec } pattern (so instantiateDelegated can resolve them from mod.exports).

    Weather source string must export:
      initialState = { query: "", place: "", tempC: null, condition: "", status: "idle" }
      actionSpec = "State: {query:string, place:string, tempC:number|null, condition:string, status:string}. search: use query to fetch weather; updates place, tempC, condition, status."
      view(state) — pure markup function returning React.createElement calls (not JSX — this is a runtime string, Babel transpiles it via the loader). The view must handle status branches: idle shows an input bound to data-action="search"; loading shows aria-busy="true" on a container and "Loading conditions…" span with role="status"; ready shows place + tempC + condition; error shows "Couldn't load conditions" and a data-action="search" retry button saying "Try again". All interactive elements carry data-action="search", NO onClick handlers. Neutral copy only — no mechanic tokens.

    Currency source string must export:
      initialState = { base: "USD", rates: null, status: "idle" }
      actionSpec = "State: {base:string, rates:object|null, status:string}. load: fetch exchange rates for base; updates rates, status."
      view(state) — pure markup. idle: show base selector with data-action="load" button; loading: aria-busy="true" + "Loading rates…" with role="status"; ready: render rates object entries as a list; error: "Couldn't load rates" + data-action="load" retry "Try again". Neutral copy only.

    IMPORTANT: The actionSpec text written here is used verbatim in buildActionIntent to form the handler lookup key. Keep it concise and stable — whitespace differences produce different keys. Write it as a single-line string with no embedded newlines.

    Both source strings use React.createElement calls (no JSX angle brackets) because they run through Babel-standalone at runtime but the seeds file itself is TypeScript compiled by Vite — keep the source strings as plain JS with React.createElement so they work whether or not the runtime transpiler processes them. Actually: they DO go through the normal transpile path in the loader (seededSource → transpile()) so JSX is fine inside the string — the string content is transpiled at runtime. Use JSX inside the source strings for readability.

    No banned mechanic tokens ("synthesize", "generate", "produce", "compile", "on-demand") in any string literal, view copy, or actionSpec.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SEEDED_SOURCES Map has 4 entries (counter, notes, weather, currency); tsc --noEmit exits 0; weather entry exports initialState with tempC:null and status:"idle"; currency entry exports initialState with rates:null</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seeded handler sources for Weather and Currency</name>
  <files>src/apps/weatherHandlers.ts, src/apps/currencyHandlers.ts</files>
  <read_first>
    - src/apps/seeds.ts (after Task 1 edit) — read the exact actionSpec strings written for weather and currency; these strings appear verbatim in buildActionIntent and determine the map keys
    - src/execution/delegated.tsx (lines 136-146) — buildActionIntent exact format; mentally construct the key for weather "search" and currency "load" actions using the actionSpec from seeds.ts
    - .planning/phases/12-network-data-path/12-PATTERNS.md weatherHandlers.ts section — WEATHER_HANDLER_SOURCES ReadonlyMap shape, the handler source format, and how SEEDED_HANDLER_SOURCES aggregates them
    - .planning/phases/12-network-data-path/12-CONTEXT.md specifics — VERIFIED live API response shapes (geocode: results[0].latitude, results[0].longitude; forecast: current.temperature_2m, current.weather_code; fx: rates object); neutral error state {data:{state:{...input.state, status:"error"}}}
    - src/execution/handler.ts (lines 101-149) — executeHandler context: the handler source in these maps will be executed with fetchData in scope (as of Plan 03); the handler function receives `input` = {state, payload}
  </read_first>
  <behavior>
    Weather handler source for the "search" action, when executed with:
      - input = {state: {query:"London", place:"", tempC:null, condition:"", status:"idle"}, payload:"search"}
      - fetchData("weather-geocode", {name:"London", count:1, language:"en", format:"json"}) → {data:{results:[{name:"London",latitude:51.5,longitude:-0.1,country:"United Kingdom"}]}}
      - fetchData("weather-forecast", {latitude:51.5, longitude:-0.1, current:"temperature_2m,weather_code,wind_speed_10m"}) → {data:{current:{temperature_2m:12.3, weather_code:3, wind_speed_10m:15.2}}}
      → resolves to {data:{state:{query:"London", place:"London, United Kingdom", tempC:12, condition:"Overcast", status:"ready"}}}

    Weather handler on geocode error or empty results:
      - fetchData returns {error:"..."} or {data:{results:[]}}
      → resolves to {data:{state:{...input.state, status:"error"}}}

    Weather handler on forecast error:
      - geocode succeeds; forecast fetchData returns {error:"..."}
      → resolves to {data:{state:{...input.state, status:"error"}}}

    Currency handler source for the "load" action, when executed with:
      - input = {state:{base:"USD", rates:null, status:"idle"}, payload:"load"}
      - fetchData("fx-latest", {base:"USD"}) → {data:{base:"USD", date:"2026-06-26", rates:{EUR:0.93,GBP:0.79,JPY:160.0}}}
      → resolves to {data:{state:{base:"USD", rates:{EUR:0.93,GBP:0.79,JPY:160.0}, status:"ready"}}}

    Currency handler on fetch error:
      → resolves to {data:{state:{...input.state, status:"error"}}}

    WMO weather code mapping (for the condition string): code 0→"Clear sky", 1→"Mainly clear", 2→"Partly cloudy", 3→"Overcast", 45-48→"Foggy", 51-55→"Drizzle", 61-65→"Rain", 71-75→"Snow", 80-82→"Rain showers", 95→"Thunderstorm", fallback→"Conditions unavailable"
  </behavior>
  <action>
    Create src/apps/weatherHandlers.ts. This file exports WEATHER_HANDLER_SOURCES as ReadonlyMap. The map has ONE entry. The key must be the exact string buildActionIntent("weather", weatherActionSpec, "search") produces — where weatherActionSpec is the exact actionSpec string from seeds.ts (read it first). Manually construct the key: `weather action 'search': ${actionSpec} The handler input is { state, payload } where payload is the action string 'search'. Return { data: { state } } with the SAME state shape and ALWAYS a valid state.`

    The value is a handler source string (TypeScript-flavored JS, will be transpiled by transpileHandler) containing an async function handler(input) that: (1) reads input.state.query; if empty return {data:{state:{...input.state}}} unchanged; (2) set status loading if needed — actually skip an intermediate loading state since we can't async-update it; (3) call fetchData("weather-geocode", {name: input.state.query, count:1, language:"en", format:"json"}); if error or results empty → return {data:{state:{...input.state, status:"error"}}}; (4) extract results[0]: latitude, longitude, name, country; (5) call fetchData("weather-forecast", {latitude, longitude, current:"temperature_2m,weather_code,wind_speed_10m"}); if error → return {data:{state:{...input.state, status:"error"}}}; (6) extract current.temperature_2m (round to integer), current.weather_code; map weather_code to a condition string using the WMO mapping (inline small switch/if-else: 0→"Clear sky", 1→"Mainly clear", 2→"Partly cloudy", 3→"Overcast", codes 45-48→"Foggy", 51-55→"Drizzle", 61-65→"Rain", 71-75→"Snow", 80-82→"Rain showers", 95→"Thunderstorm", else→"Conditions unavailable"); (7) return {data:{state:{...input.state, place: name+", "+country, tempC: Math.round(temperature_2m), condition, status:"ready"}}}.

    Create src/apps/currencyHandlers.ts. Exports CURRENCY_HANDLER_SOURCES as ReadonlyMap with ONE entry. Key is buildActionIntent("currency", currencyActionSpec, "load") — construct it the same way from the actual actionSpec in seeds.ts. Value handler source: async function handler(input) that: (1) reads input.state.base (default "USD"); (2) calls fetchData("fx-latest", {base: input.state.base}); (3) if error or no rates → return {data:{state:{...input.state, status:"error"}}}; (4) return {data:{state:{base: input.state.base, rates: result.data.rates, status:"ready"}}}.

    IMPORTANT: Do NOT use the import/export syntax inside the handler source strings — they run in a constrained new Function scope. Use function declarations and return values only. fetchData is available as a parameter in scope (injected by Plan 03). No banned tokens in source strings or file comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>src/apps/weatherHandlers.ts and src/apps/currencyHandlers.ts exist; each exports a ReadonlyMap with one entry; tsc --noEmit exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Seeded-handler short-circuit in resolveHandlerJS</name>
  <files>src/execution/handler.ts</files>
  <read_first>
    - src/execution/handler.ts (lines 186-229) — resolveHandlerJS function: insert the short-circuit at the very top of the function body, BEFORE const key = await registryKey(...)
    - src/execution/loader.ts (lines 252-263) — SEEDED_SOURCES short-circuit pattern to mirror exactly (check before cache, no model call, return the JS string)
    - src/apps/weatherHandlers.ts (just created) — WEATHER_HANDLER_SOURCES export name
    - src/apps/currencyHandlers.ts (just created) — CURRENCY_HANDLER_SOURCES export name
    - src/execution/transpile.ts — transpileHandler import (already imported in handler.ts line 40)
    - .planning/phases/12-network-data-path/12-PATTERNS.md weatherHandlers.ts section — SEEDED_HANDLER_SOURCES aggregation map and resolveHandlerJS insertion point
  </read_first>
  <action>
    Edit src/execution/handler.ts. Add the following changes:

    1. At the top of the file, add imports for WEATHER_HANDLER_SOURCES from "../apps/weatherHandlers" and CURRENCY_HANDLER_SOURCES from "../apps/currencyHandlers".

    2. After the imports, define a module-level constant: const SEEDED_HANDLER_SOURCES: ReadonlyMap<string, string> = new Map([...WEATHER_HANDLER_SOURCES, ...CURRENCY_HANDLER_SOURCES]); This aggregates all seeded handler source strings. If adding more seeded handlers in future, they are added to this spread.

    3. Inside resolveHandlerJS, at the very beginning of the function body (before const key = await registryKey(...)), insert: const seededSource = SEEDED_HANDLER_SOURCES.get(intent); if (seededSource) { logger.info("Handler: seeded handler hit"); return transpileHandler(seededSource, { filename: "seeded-handler.ts" }); }

    The short-circuit returns the transpiled JS string directly — the same type resolveHandlerJS already returns. No registry write needed (seeded handlers are stateless and always available). The produceGate.tryAcquire() call is NEVER reached for seeded intents. No model call.

    Do NOT change any other part of handler.ts. The DENIED_GLOBALS, executeHandler signature (already updated in Plan 03), runHandler, and executeHandlerSource are unchanged.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>npm test exits 0 (all prior tests pass); SEEDED_HANDLER_SOURCES is defined in handler.ts; resolveHandlerJS has the short-circuit before registryKey; tsc --noEmit exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DelegatedShell → seeded handler source | The handler source string is host-authored, not model-produced; it is still executed in the same constrained new Function scope |
| seeded handler source → fetchData | Handler calls fetchData(sourceId, params); the broker guards origin and param filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-A | Spoofing | Seeded handler intent key collisions | mitigate | Keys are the full buildActionIntent string (includes appType + actionSpec + action); collision requires matching all three fields exactly; actionSpec is controlled in seeds.ts |
| T-12-04-B | Info Disclosure | Neutral error copy in view markup | mitigate | Error copy "Couldn't load conditions" / "Try again" and "Couldn't load rates" are data-framed; no mechanic tokens, no status codes, no provider names in view strings |
| T-12-04-C | Tampering | Handler source calling fetchData with unlisted sourceId | mitigate | Broker rejects unknown sourceId with {error} (Plan 01 guard); seeded handler sources only call "weather-geocode", "weather-forecast", "fx-latest" — all in the manifest |
| T-12-04-D | Info Disclosure | WMO weather code as internal mechanic detail | accept | WMO codes are a data classification standard, not an app mechanic; mapping them to human copy is appropriate data presentation, not mechanic exposure |
| T-12-04-E | Denial of Service | Seeded handler blocking the action indefinitely | mitigate | DelegatedShell sets busy=action before the call and clears in finally{} (delegated.tsx:181-199); a hanging seeded handler is bounded by the broker's fetch timeout and limiter |
</threat_model>

<verification>
After all three tasks complete:
- npx tsc --noEmit exits 0
- npm test exits 0 (all prior tests pass)
- SEEDED_SOURCES.get("weather") and SEEDED_SOURCES.get("currency") return non-empty strings
- SEEDED_HANDLER_SOURCES in handler.ts has entries for both weather search and currency load intent strings
- grep "status.*idle\|tempC.*null\|rates.*null" src/apps/seeds.ts — confirms initialState fields
- grep "weather-geocode\|weather-forecast" src/apps/weatherHandlers.ts — confirms sourceId usage
- grep "fx-latest" src/apps/currencyHandlers.ts — confirms sourceId usage
- grep "synthesize\|synthesis\|synthesized\|generate\|generated\|on-demand" src/apps/seeds.ts src/apps/weatherHandlers.ts src/apps/currencyHandlers.ts — no matches
- grep "Couldn't load conditions\|Loading conditions\|Enter a location" src/apps/seeds.ts — confirms neutral weather copy
</verification>

<success_criteria>
- seeds.ts: SEEDED_SOURCES has 4 entries; weather and currency export initialState/view/actionSpec; Phase 11 schema accepts all data fields (tempC, condition, place, rates all in initialState)
- weatherHandlers.ts: WEATHER_HANDLER_SOURCES with one key matching buildActionIntent("weather", weatherActionSpec, "search"); handler makes two sequential fetchData calls; error paths return status:"error"
- currencyHandlers.ts: CURRENCY_HANDLER_SOURCES with one key matching buildActionIntent("currency", currencyActionSpec, "load"); handler maps fx response.rates to state.rates; error path returns status:"error"
- handler.ts: SEEDED_HANDLER_SOURCES aggregates both maps; resolveHandlerJS short-circuit fires before registryKey; logger.info "seeded handler hit" on match
- npm test: 0 regressions; tsc --noEmit: 0 errors; hygiene: 0 banned tokens
</success_criteria>

<output>
After completion, create .planning/phases/12-network-data-path/12-04-SUMMARY.md
</output>
