---
phase: "12-network-data-path"
plan: "05"
type: tdd
wave: 3
depends_on:
  - "12-01"
  - "12-02"
  - "12-03"
  - "12-04"
files_modified:
  - src/data/dataBroker.test.ts
  - src/apps/handlers.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "TTL cache HIT: two broker calls with same sourceId+params within TTL → fetchFn called exactly once (stub clock)"
    - "TTL cache MISS: two broker calls after clock advanced past TTL → fetchFn called twice (stub clock)"
    - "Allowlist rejection: unknown sourceId → {error} with neutral copy; fetchFn never called"
    - "Param injection guard: extra params not in allowedParams are dropped; URL query string contains only allowed keys"
    - "Non-2xx response: broker returns neutral {error}; no throw"
    - "CORS/network throw: broker catches and returns neutral {error}; no rethrow"
    - "Weather seeded handler integration: canned geocode + forecast fixtures → state.tempC, state.place, state.condition, state.status='ready'"
    - "Currency seeded handler integration: canned fx fixture → state.rates, state.status='ready'"
    - "No-broker fallback: services.fetchDataBroker absent → bound fetchData returns {error} neutral copy; runHandler does not throw"
    - "Handler scope: a handler body that references fetch directly gets undefined (DENIED_GLOBALS enforcement)"
    - "All new tests run offline — no real network, no real IndexedDB (canned transport + in-memory registry + cannedBroker/stub clock)"
  artifacts:
    - path: "src/data/dataBroker.test.ts"
      provides: "Unit tests for DataFetchBroker: TTL hit/miss (stub clock), allowlist rejection, param injection guard, non-2xx, network throw"
      exports: []
    - path: "src/apps/handlers.test.ts"
      provides: "Integration tests: weather + currency seeded handlers with real-shape fixtures → correct state fields; no-broker fallback"
      exports: []
  key_links:
    - from: "src/data/dataBroker.test.ts"
      to: "src/data/dataBroker.ts"
      via: "createDataBroker({clock: stubClock, fetchFn: cannedFetch, ttlCache: stubCache})"
      pattern: "createDataBroker"
    - from: "src/apps/handlers.test.ts"
      to: "src/execution/handler.ts"
      via: "executeHandlerSource(weatherHandlerSource, input) with cannedBroker injected via services"
      pattern: "executeHandlerSource|runHandler"
    - from: "src/apps/handlers.test.ts"
      to: "src/services/testServices.ts"
      via: "createTestServices({ fetchDataBroker: cannedBroker(fixture) })"
      pattern: "cannedBroker"
---

<objective>
Write the full test suite for Phase 12: broker unit tests (TTL hit/miss, allowlist rejection, param injection guard, HTTP error, network throw), plus handler integration tests with real-shape API fixtures for Weather and Currency (verifying the handler maps geocode + forecast → correct state fields, and fx → correct state fields). All tests run offline with no real network.

Purpose: Phase 12 acceptance requires tsc 0 errors, all prior 422 tests green, and the new DATA-01..04 test coverage. This plan delivers the tests that gate phase completion.

Output: src/data/dataBroker.test.ts (new), src/apps/handlers.test.ts (new).
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-network-data-path/12-CONTEXT.md
@.planning/phases/12-network-data-path/12-PATTERNS.md
@.planning/phases/12-network-data-path/12-01-SUMMARY.md
@.planning/phases/12-network-data-path/12-03-SUMMARY.md
@.planning/phases/12-network-data-path/12-04-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/execution/handler.test.ts (lines 75-117) — test structure pattern:
```typescript
describe("<unit> — <requirement>", () => {
  it("<behavior>", async () => {
    // Arrange: inject canned/stub doubles, never real network or IndexedDB
    const services = createTestServices({ fetchDataBroker: cannedBroker({ data: FIXTURE }) });
    // Act + Assert
  });
});
```

From src/host/clock.ts — createStubClock pattern for TTL tests:
```typescript
const clock = createStubClock(0);
// clock.now() starts at 0
// clock.sleep(70_000) advances .current by 70_000
```

From src/data/dataBroker.ts (Plan 01) — createDataBroker DI options:
```typescript
createDataBroker({ clock?: Clock; limiter?: TokenBucket; ttlCache?: TtlCache; fetchFn?: typeof fetch })
```

From src/services/testServices.ts (Plan 03) — test helpers:
```typescript
cannedBroker(response: {data?: unknown; error?: string}): DataFetchBroker
unusedBroker: DataFetchBroker
createTestServices(overrides?: TestServicesOverrides): Services
```

From src/execution/handler.ts (Plan 03) — executeHandlerSource (test escape hatch):
```typescript
// Accepts (source, input) — the no-op fetchData stub is bound internally (Plan 03)
export async function executeHandlerSource(source: string, input: unknown): Promise<HandlerResult>
// For fetchData-testing, use runHandler with services that carry a real cannedBroker.
```

VERIFIED live API response shapes (from CONTEXT.md specifics — use these EXACT shapes in fixtures):
  geocode fixture: { results:[{ name:"London", latitude:51.5085, longitude:-0.1257, country:"United Kingdom", country_code:"GB", admin1:"England", timezone:"Europe/London" }], generationtime_ms:0.5 }
  forecast fixture: { latitude:51.5, longitude:-0.125, elevation:11.0, timezone:"Europe/London", current_units:{ time:"iso8601", interval:"seconds", temperature_2m:"°C", weather_code:"wmo code", wind_speed_10m:"km/h" }, current:{ time:"2026-06-26T12:00", interval:900, temperature_2m:18.3, weather_code:2, wind_speed_10m:14.4 } }
  fx fixture: { amount:1.0, base:"USD", date:"2026-06-26", rates:{ EUR:0.9280, GBP:0.7863, JPY:159.42 } }

From src/apps/weatherHandlers.ts (Plan 04) — WEATHER_HANDLER_SOURCES ReadonlyMap:
  import { WEATHER_HANDLER_SOURCES } from "../apps/weatherHandlers";
  const weatherSource = [...WEATHER_HANDLER_SOURCES.values()][0]; // the one entry
From src/apps/currencyHandlers.ts (Plan 04) — CURRENCY_HANDLER_SOURCES ReadonlyMap:
  import { CURRENCY_HANDLER_SOURCES } from "../apps/currencyHandlers";
  const currencySource = [...CURRENCY_HANDLER_SOURCES.values()][0];
</interfaces>
</context>

<feature>
  <name>DataFetchBroker unit tests + handler integration tests (DATA-01..04)</name>
  <files>src/data/dataBroker.test.ts, src/apps/handlers.test.ts</files>
  <behavior>
    Broker unit tests (src/data/dataBroker.test.ts):
    1. TTL CACHE HIT — Two calls to broker.fetch("weather-geocode", {name:"London"}) within TTL: stub clock stays at 0; verify fetchFn call count is 1 on second call (cache serves the second). Assert result.data equals the fixture.
    2. TTL CACHE MISS — Set clock to 0; first call fetches and caches; advance clock past TTL (600_001 ms via clock.sleep); second call must re-fetch. Verify fetchFn call count is 2.
    3. ALLOWLIST REJECTION — broker.fetch("unknown-source", {name:"x"}): result.error is defined; fetchFn call count is 0.
    4. PARAM INJECTION GUARD — broker.fetch("weather-geocode", {name:"London", injectedKey:"bad", count:1}): inspect the URL the cannedFetch received; assert query string does NOT contain "injectedKey"; DOES contain "name=London"; DOES contain "count=1". (allowedParams for weather-geocode: ["name","count","language","format"])
    5. NON-2XX RESPONSE — cannedFetch returns Response with ok:false and status:404: result.error is defined; result.data is undefined.
    6. NETWORK THROW — cannedFetch throws new Error("Network failure"): broker.fetch resolves (not rejects) with {error} neutral copy.

    Handler integration tests (src/apps/handlers.test.ts):
    7. WEATHER HANDLER — Real geocode+forecast fixture via a two-call cannedBroker sequence: first call ("weather-geocode") returns geocode fixture; second call ("weather-forecast") returns forecast fixture. Run the weather seeded handler source (from WEATHER_HANDLER_SOURCES) via runHandler with a services bundle carrying this sequential cannedBroker. Assert result.data.state.place === "London, United Kingdom"; result.data.state.tempC === 18 (Math.round(18.3)); result.data.state.condition === "Partly cloudy" (weather_code 2); result.data.state.status === "ready".
    8. CURRENCY HANDLER — fx fixture via cannedBroker: run currency seeded handler source via runHandler with fx fixture broker. Assert result.data.state.rates.EUR === 0.9280; result.data.state.status === "ready"; result.data.state.base === "USD".
    9. NO-BROKER FALLBACK — createTestServices() with no fetchDataBroker override (defaults to undefined): runHandler with a weather intent string → result.data.state.status === "error" (the bound fetchData stub returns {error}, handler propagates it to state.status). Assert result is {data:{state:{...}}} with status "error" — NOT a thrown rejection.
    10. FETCH BYPASS — executeHandlerSource with source "async function handler(input){return {data:{fetchIsUndefined: typeof fetch === 'undefined', xhrIsUndefined: typeof XMLHttpRequest === 'undefined'}};}": result.data.fetchIsUndefined === true; result.data.xhrIsUndefined === true. (Proves DENIED_GLOBALS still shadow both after Plan 03 changes.)

    All tests: no real network (cannedFetch / cannedBroker / stub clock); no real IndexedDB (createInMemoryRegistry or unusedTransport); imports from vitest.
  </behavior>
  <implementation>
    For the two-call sequential cannedBroker in test 7: build a stateful test double that returns the geocode fixture on the first call and the forecast fixture on the second. Pattern:

    let callCount = 0;
    const sequentialBroker: DataFetchBroker = {
      fetch: (_sourceId, _params) => {
        callCount++;
        if (callCount === 1) return Promise.resolve({ data: GEOCODE_FIXTURE });
        return Promise.resolve({ data: FORECAST_FIXTURE });
      }
    };

    For broker unit test TTL/fetchFn stubbing: build the broker with opts.fetchFn as a jest/vitest mock function (vi.fn()) that returns a Response-like object: { ok: true, json: () => Promise.resolve(FIXTURE) }. This avoids needing a real Response constructor. Check call count with mockFn.mock.calls.length.

    For the URL inspection in test 4 (param injection guard): the cannedFetch receives the full URL string as its first argument — capture it in the mock. Assert url.includes("name=London"), url.includes("count=1"), !url.includes("injectedKey").

    For test 5 (non-2xx): cannedFetch returns { ok: false, status: 404, json: () => Promise.resolve({}) }.

    Import paths: dataBroker from "../data/dataBroker"; TtlCache from "../host/ttlCache"; createStubClock from "../host/clock"; createTestServices, cannedBroker from "../services/testServices"; runHandler, executeHandlerSource from "../execution/handler"; WEATHER_HANDLER_SOURCES from "../apps/weatherHandlers"; CURRENCY_HANDLER_SOURCES from "../apps/currencyHandlers"; { describe, it, expect, vi, beforeEach } from "vitest".
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scope → production code | Tests exercise the real broker/handler code with injected doubles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-A | Tampering | Tests inadvertently using real network | mitigate | All tests use vi.fn() or cannedBroker / stub clock; no globalThis.fetch is called; the broker's fetchFn is always injected in tests |
| T-12-05-B | Info Disclosure | Test file comments naming banned tokens | accept | Test-only files are not shipped in the production bundle; developer comments may use technical terms as long as the banned mechanic tokens (synthesize/generate/produce/compile etc.) are not used even in test files |
</threat_model>

<verification>
After both test files are written and all tests pass:
- npm test exits 0; output shows ≥422 + 10 new tests passing
- npx tsc --noEmit exits 0
- npm test -- --reporter=verbose src/data/dataBroker.test.ts 2>&1 shows all 6 broker tests green
- npm test -- --reporter=verbose src/apps/handlers.test.ts 2>&1 shows all 4 handler tests green (weather integration, currency integration, no-broker fallback, fetch bypass)
- No test makes a real network call (grep "globalThis.fetch\|window.fetch" src/data/dataBroker.test.ts src/apps/handlers.test.ts returns no matches — only injected fetchFn mocks)
</verification>

<success_criteria>
- src/data/dataBroker.test.ts: 6 test cases covering TTL hit/miss, allowlist rejection, param injection guard, non-2xx, network throw — all green
- src/apps/handlers.test.ts: 4 test cases covering weather integration (geocode+forecast fixtures → correct state), currency integration (fx fixture → correct state), no-broker fallback (status="error" not throw), fetch bypass (typeof fetch === undefined in handler scope) — all green
- npm test: total count ≥ 432 (422 prior + at least 10 new); exit 0
- tsc --noEmit: 0 errors
- No real network calls in any new test
- Hygiene: no banned mechanic tokens in test file comments or string literals
</success_criteria>

<output>
After completion, create .planning/phases/12-network-data-path/12-05-SUMMARY.md
</output>
