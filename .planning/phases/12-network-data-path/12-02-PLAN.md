---
phase: "12-network-data-path"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/csp.test.ts
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "index.html connect-src lists exactly: 'self' https://api.anthropic.com https://api.open-meteo.com https://geocoding-api.open-meteo.com https://api.frankfurter.dev"
    - "connect-src never contains a wildcard (*)"
    - "csp.test.ts asserts all four origins individually and asserts the absence of *"
    - "The existing inline-script hash guard tests still pass after the edit"
  artifacts:
    - path: "index.html"
      provides: "Widened connect-src with the 3 new data-path origins"
      contains: "https://api.frankfurter.dev"
    - path: "src/csp.test.ts"
      provides: "DATA-02 connect-src allowlist assertions (5 new it() cases)"
      exports: []
  key_links:
    - from: "src/csp.test.ts"
      to: "index.html"
      via: "readFileSync(INDEX_HTML) + connectSrcDirective() helper"
      pattern: "connectSrcDirective"
---

<objective>
Widen the Content Security Policy connect-src directive in index.html to include the three allowlisted data-path origins, then add a dedicated test block in csp.test.ts that asserts each origin is present and that no wildcard (*) is present.

Purpose: DATA-02 requires the CSP to be widened to exactly the finite allowlist — never *. The test block is the enforcement gate that keeps the CSP honest through future edits.

Output: Modified index.html (updated connect-src), modified src/csp.test.ts (new connectSrcDirective helper + 5 assertions). This plan is independent of Plan 01 and can run in Wave 1 in parallel.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-network-data-path/12-CONTEXT.md
@.planning/phases/12-network-data-path/12-PATTERNS.md

<interfaces>
<!-- Key context the executor needs. Extracted from codebase. -->

Current index.html line 15 (full CSP meta content attribute value):
  default-src 'self'; script-src 'self' 'unsafe-eval' 'sha256-N+v/OMOSGIWhW6MiaeKgpUrhYfTwftAJZBpsRoTejkc='; style-src 'self' 'unsafe-inline'; connect-src 'self' https://api.anthropic.com; img-src 'self' data:; font-src 'self';

Only the connect-src directive changes — all other directives are untouched.

Target connect-src value (exact string, from CONTEXT.md DATA-02):
  connect-src 'self' https://api.anthropic.com https://api.open-meteo.com https://geocoding-api.open-meteo.com https://api.frankfurter.dev

Existing csp.test.ts helpers (lines 26-55):
  function readIndexHtml(): string { return readFileSync(INDEX_HTML, "utf8"); }
  function inlineScriptBody(html: string): string { ... }
  function sha256Source(body: string): string { ... }
  function scriptSrcDirective(html: string): string {
    // splits on ";", trims, finds part starting with "script-src"
  }

Existing test structure (lines 57-71): two it() cases inside describe("CSP inline-script hash guard (CR-01)", ...)

The sha256 hash in script-src MUST NOT change — only connect-src changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen connect-src in index.html to the four allowlisted origins</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file — 31 lines) — read the complete CSP meta content string at line 15 before editing; need the exact current string to reconstruct with only connect-src changed
    - .planning/phases/12-network-data-path/12-CONTEXT.md decisions DATA-02 — exact target origins: 'self' https://api.anthropic.com https://api.open-meteo.com https://geocoding-api.open-meteo.com https://api.frankfurter.dev
    - .planning/phases/12-network-data-path/12-PATTERNS.md index.html section — the before/after connect-src strings
  </read_first>
  <action>
    Edit index.html. Change ONLY the connect-src directive inside the content attribute of the Content-Security-Policy meta tag. The current value is "connect-src 'self' https://api.anthropic.com". The new value must be "connect-src 'self' https://api.anthropic.com https://api.open-meteo.com https://geocoding-api.open-meteo.com https://api.frankfurter.dev". All other directives (default-src, script-src with its sha256 hash, style-src, img-src, font-src) remain byte-for-byte identical. CRITICAL: the sha256 hash in script-src must not change — it is computed from the inline script body and any change will break the existing CSP hash guard test. Use a surgical string replacement on connect-src only.
  </action>
  <verify>
    <automated>grep "api.frankfurter.dev" index.html && grep "connect-src" index.html | grep -v "\*"</automated>
  </verify>
  <done>index.html connect-src contains https://api.frankfurter.dev and does not contain *; the inline script sha256 hash in script-src is unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add connectSrcDirective helper and DATA-02 assertion block to csp.test.ts</name>
  <files>src/csp.test.ts</files>
  <read_first>
    - src/csp.test.ts (full file — 73 lines) — read the entire file; understand the existing helper pattern (scriptSrcDirective) and the existing describe block structure; the new helper mirrors scriptSrcDirective exactly
    - index.html (after Task 1 edit) — confirm the connect-src directive text the helper will parse
    - .planning/phases/12-network-data-path/12-PATTERNS.md csp.test.ts section — connectSrcDirective helper implementation and the 5 it() cases for DATA-02
  </read_first>
  <behavior>
    - connectSrcDirective(html) returns the full connect-src directive string from the CSP meta tag
    - connectSrcDirective throws when no CSP meta tag is found
    - connectSrcDirective throws when no connect-src directive is found
    - "contains the Anthropic platform origin" test passes: directive.toContain("https://api.anthropic.com")
    - "contains the forecast API origin" test passes: directive.toContain("https://api.open-meteo.com")
    - "contains the geocoding API origin" test passes: directive.toContain("https://geocoding-api.open-meteo.com")
    - "contains the FX rate origin" test passes: directive.toContain("https://api.frankfurter.dev")
    - "does not contain a wildcard origin" test passes: not.toContain("*")
  </behavior>
  <action>
    Edit src/csp.test.ts. Add a helper function connectSrcDirective(html: string): string after the existing scriptSrcDirective function. It parses the CSP meta content attribute (same regex as scriptSrcDirective), splits on ";", trims, and finds the part starting with "connect-src". If the meta tag or the connect-src part is not found, throw with a descriptive error (mirror scriptSrcDirective error messages). Then append a new describe block "CSP connect-src allowlist (DATA-02)" with exactly 5 it() cases: (1) contains https://api.anthropic.com, (2) contains https://api.open-meteo.com, (3) contains https://geocoding-api.open-meteo.com, (4) contains https://api.frankfurter.dev, (5) does not contain "*". Each it() calls connectSrcDirective(readIndexHtml()). Do NOT modify the existing describe block or any existing it() cases. Place the new describe block after the existing one.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/csp.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>npm test passes; the new describe block "CSP connect-src allowlist (DATA-02)" has 5 green it() cases; existing 2 hash-guard tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → external data APIs | CSP connect-src is the browser-enforced egress gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-A | Tampering | connect-src directive creep | mitigate | csp.test.ts DATA-02 block asserts each required origin individually and asserts absence of wildcard; any future edit that adds * or removes an origin fails CI |
| T-12-02-B | Info Disclosure | Overly broad connect-src | mitigate | Never use *; only the four exact origins in CONTEXT.md DATA-02 are listed; the test gates this invariant |
| T-12-02-C | Tampering | script-src hash invalidation during CSP edit | mitigate | Only connect-src is modified; existing hash guard test immediately catches any accidental script-src change |
</threat_model>

<verification>
After both tasks complete:
- npm test exits 0 — all prior 422 tests pass, plus 5 new DATA-02 assertions
- grep "connect-src" index.html outputs exactly one line containing all four origins and no *
- grep "api.frankfurter.dev" index.html returns a match
- grep "api.open-meteo.com" index.html returns a match
- grep "geocoding-api.open-meteo.com" index.html returns a match
- The sha256 hash in script-src is identical to the pre-edit value: sha256-N+v/OMOSGIWhW6MiaeKgpUrhYfTwftAJZBpsRoTejkc=
</verification>

<success_criteria>
- index.html connect-src: exactly "'self' https://api.anthropic.com https://api.open-meteo.com https://geocoding-api.open-meteo.com https://api.frankfurter.dev" — no * anywhere
- csp.test.ts: connectSrcDirective helper added; describe("CSP connect-src allowlist (DATA-02)") with 5 it() cases; existing tests untouched
- npm test: all tests pass (including existing 2 + 5 new = 7 CSP tests)
- tsc --noEmit: 0 errors
</success_criteria>

<output>
After completion, create .planning/phases/12-network-data-path/12-02-SUMMARY.md
</output>
