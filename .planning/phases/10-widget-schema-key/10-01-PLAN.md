---
phase: "10-widget-schema-key"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/registry/db.ts
  - src/execution/widgetPrewarm.ts
autonomous: true
requirements:
  - WIDGET-07

must_haves:
  truths:
    - "WidgetRecord is an explicit interface extending LruMeta (not a Record type alias)"
    - "HandlerRecord is an explicit interface extending LruMeta (not a Record type alias)"
    - "Both interfaces expose named required fields matching the actual runtime write shapes"
    - "Widget write sites include useCount and updatedAt on first write for LRU parity"
    - "tsc --noEmit exits 0 with no errors after the type change"
    - "The full test suite stays green at >=393 tests after this plan"
  artifacts:
    - path: "src/registry/db.ts"
      provides: "WidgetRecord and HandlerRecord interface definitions"
      contains: "export interface WidgetRecord extends LruMeta"
    - path: "src/execution/widgetPrewarm.ts"
      provides: "Widget write sites with LRU fields"
      contains: "useCount: 0"
  key_links:
    - from: "src/registry/db.ts"
      to: "src/services/registry.ts"
      via: "RegistrySchema references WidgetRecord/HandlerRecord"
      pattern: "WidgetRecord|HandlerRecord"
    - from: "src/execution/widgetPrewarm.ts"
      to: "src/registry/db.ts"
      via: "registry.put uses typed WidgetRecord fields"
      pattern: "useCount: 0"
---

<objective>
Replace the placeholder `WidgetRecord` and `HandlerRecord` type aliases in `src/registry/db.ts` with explicit interfaces extending `LruMeta`, mirroring the `AppRecord` pattern. Add LRU write parity (`useCount: 0, updatedAt: Date.now()`) to both widget write sites in `src/execution/widgetPrewarm.ts`. Verify `tsc --noEmit` exits 0 after the tightening.

Purpose: Closes WIDGET-07 — gives the `widgets` and `handlers` registry stores real typed schemas so the type system enforces the actual write contract, removing the placeholder looseness that would hide type-shape bugs when widget composition activates in Phase 13.

Output: Updated `src/registry/db.ts` with two new interface declarations; updated `src/execution/widgetPrewarm.ts` with additive LRU fields at both write sites; clean TypeScript compilation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-widget-schema-key/10-CONTEXT.md
@.planning/phases/10-widget-schema-key/10-PATTERNS.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/registry/db.ts (current state — lines to REPLACE):
```typescript
// CURRENT (lines 56-57) — replace both:
export type WidgetRecord = Record<string, unknown> & LruMeta;
export type HandlerRecord = Record<string, unknown> & LruMeta;
```

From src/registry/db.ts — AppRecord analog (lines 37-55) — follow this interface shape:
```typescript
export interface AppRecord extends LruMeta {
  cacheKey: string;
  type: string;
  source: string;
  transpiledJS: string;
  displayName?: string;
  prompt?: string;
  createdAt?: number;
  [key: string]: unknown; // allow forward-compat extra fields
}
```

From src/registry/db.ts — RegistrySchema (lines 59-63) — remains unchanged:
```typescript
export interface RegistrySchema extends DBSchema {
  apps: { key: string; value: AppRecord };
  widgets: { key: string; value: WidgetRecord };
  handlers: { key: string; value: HandlerRecord };
}
```

From src/registry/db.ts — LruMeta (lines 26-31):
```typescript
export interface LruMeta {
  useCount?: number;
  updatedAt?: number;
}
```

From src/execution/widgetPrewarm.ts — write site 1 (lines 99-103) — BEFORE:
```typescript
await services.registry.put(
  "widgets",
  { cacheKey: key, type: widgetType, source, transpiledJS },
  key,
);
```

From src/execution/widgetPrewarm.ts — write site 2 (lines 156-160) — BEFORE:
```typescript
await services.registry.put(
  "widgets",
  { cacheKey: key, type: widgetType, source, transpiledJS },
  key,
);
```

From src/execution/handler.ts (lines 214-225) — LRU write analog:
```typescript
await services.registry.put(
  "handlers",
  {
    cacheKey: key,
    intent,
    source: produced.source,
    transpiledJS: produced.transpiledJS,
    useCount: 0,
    updatedAt: Date.now(),
  },
  key,
);
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace WidgetRecord and HandlerRecord with explicit interfaces in db.ts</name>
  <files>src/registry/db.ts</files>
  <read_first>
    - src/registry/db.ts (full file — read the existing LruMeta, AppRecord, WidgetRecord, HandlerRecord, RegistrySchema definitions)
    - .planning/phases/10-widget-schema-key/10-PATTERNS.md (section "src/registry/db.ts — Replace WidgetRecord and HandlerRecord placeholders" for exact target interface definitions)
    - .planning/phases/10-widget-schema-key/10-CONTEXT.md (decisions section for field list: widgets need cacheKey/type/source/transpiledJS; handlers need cacheKey/intent/source/transpiledJS)
  </read_first>
  <action>
    In src/registry/db.ts, replace lines 56-57 (the two placeholder type aliases) with explicit interface declarations that mirror the AppRecord pattern.

    WidgetRecord target fields (per CONTEXT.md D-01 decisions, WIDGET-07):
    - cacheKey: string (required)
    - type: string (required)
    - source: string (required)
    - transpiledJS: string (required)
    - [key: string]: unknown (forward-compat catch-all — no mode field, single instantiation path)
    - extends LruMeta (not & LruMeta — use interface extends, not type alias intersection)

    HandlerRecord target fields (per CONTEXT.md D-01 decisions, WIDGET-07):
    - cacheKey: string (required)
    - intent: string (required — not type, mirrors handler write shape in handler.ts:214)
    - source: string (required)
    - transpiledJS: string (required)
    - [key: string]: unknown (forward-compat catch-all)
    - extends LruMeta

    Rules:
    - Use "export interface X extends LruMeta" (not "export type X = ... & LruMeta")
    - All four named fields are required (no optional markers) — they are always written at the identity write sites
    - Place the [key: string]: unknown catch-all last
    - Do not add displayName/prompt/createdAt to widget or handler records — those are app-only (STORE-01)
    - Update the comment block above these two lines to reference Phase 10 (WIDGET-07)
    - RegistrySchema (lines 59-63) references these by name — no change needed there; tsc will validate
    - Do NOT alter LruMeta, AppRecord, RegistrySchema, openRegistry, or REGISTRY_DB_VERSION

    After editing, the file must not introduce the token "synthesize/synthesized/synthesis" in any comment (devtools hygiene hard rule).
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && grep -n "export interface WidgetRecord extends LruMeta" src/registry/db.ts && grep -n "export interface HandlerRecord extends LruMeta" src/registry/db.ts && echo "Interfaces present"</automated>
  </verify>
  <done>
    - src/registry/db.ts contains "export interface WidgetRecord extends LruMeta" with cacheKey/type/source/transpiledJS required fields and [key:string]:unknown catch-all
    - src/registry/db.ts contains "export interface HandlerRecord extends LruMeta" with cacheKey/intent/source/transpiledJS required fields and [key:string]:unknown catch-all
    - The old "export type WidgetRecord = Record..." and "export type HandlerRecord = Record..." lines are gone
    - No other existing definitions in db.ts are altered
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add LRU write parity to both widget write sites in widgetPrewarm.ts; verify tsc clean</name>
  <files>src/execution/widgetPrewarm.ts</files>
  <read_first>
    - src/execution/widgetPrewarm.ts (read full file; locate both registry.put calls at lines ~99-103 and ~156-160)
    - src/execution/handler.ts lines 211-225 (LRU write analog — the exact pattern to replicate)
    - src/registry/db.ts (read the WidgetRecord interface just written in Task 1 — confirm the field names before updating write literals)
    - .planning/phases/10-widget-schema-key/10-PATTERNS.md (section "src/execution/widgetPrewarm.ts — Add LRU fields to widget write sites" for exact before/after)
  </read_first>
  <action>
    In src/execution/widgetPrewarm.ts, add LRU bookkeeping fields to BOTH widget registry.put write sites (per CONTEXT.md decision WIDGET-07d).

    Write site 1 — resolveWidget function (~lines 99-103):
    Extend the object literal to include "useCount: 0" and "updatedAt: Date.now()" after transpiledJS.
    The result literal: { cacheKey: key, type: widgetType, source, transpiledJS, useCount: 0, updatedAt: Date.now() }

    Write site 2 — resolveWidgetTweak function (~lines 156-160):
    Same additive change: add "useCount: 0" and "updatedAt: Date.now()" after transpiledJS.
    The result literal: { cacheKey: key, type: widgetType, source, transpiledJS, useCount: 0, updatedAt: Date.now() }

    Rules:
    - This is purely additive — do not alter any other logic in widgetPrewarm.ts
    - No createdAt field on widgets (not in the decided schema per CONTEXT.md)
    - useCount: 0 (no hits yet on first write); updatedAt: Date.now() (epoch ms of write instant)
    - These fields match the LruMeta optional fields, so old records that lack them remain valid
    - Do NOT touch registryKey calls, the SeededSources map, the resolveWidget return path, or any other logic
    - Do NOT introduce the token "synthesize/synthesized/synthesis" in any comment

    After both write sites are updated, run tsc to verify the entire codebase compiles cleanly:
    - Command: npx tsc --noEmit
    - Expected: exits 0 with no errors
    - If tsc surfaces errors at any site that relied on the old Record<string,unknown> looseness, fix those sites (they are bugs to correct, not reasons to revert the type tightening). Likely sites: any code that reads a WidgetRecord field as unknown without narrowing, or any put() call that passes an object incompatible with the new explicit interface.

    Then run the full test suite to confirm no regression:
    - Command: npm test
    - Expected: exits 0, >=393 tests passing
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && grep -c "useCount: 0" src/execution/widgetPrewarm.ts && npx tsc --noEmit && echo "tsc clean"</automated>
  </verify>
  <done>
    - src/execution/widgetPrewarm.ts has exactly 2 occurrences of "useCount: 0" (one in resolveWidget, one in resolveWidgetTweak)
    - src/execution/widgetPrewarm.ts has exactly 2 occurrences of "updatedAt: Date.now()" at the widget write sites
    - npx tsc --noEmit exits 0 (no TypeScript errors anywhere in the project)
    - npm test exits 0 with >=393 tests passing
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan makes only internal type-annotation and LRU-field changes. No new inputs, no new outputs, no new network calls, no new UI surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | — | src/registry/db.ts, src/execution/widgetPrewarm.ts | accept | No new threats: this is a pure internal typing change (interface declarations replacing type aliases) and an additive write-literal change (two LRU fields). No new trust boundaries, no new inputs accepted, no new outputs emitted, no network paths changed. The devtools-hygiene hard rule (no "synthesize" token) is enforced by the action directives and the hygiene gate in the acceptance criteria. |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript clean: `npx tsc --noEmit` exits 0
2. WidgetRecord interface: `grep -n "export interface WidgetRecord extends LruMeta" src/registry/db.ts` returns a match
3. HandlerRecord interface: `grep -n "export interface HandlerRecord extends LruMeta" src/registry/db.ts` returns a match
4. Old aliases gone: `grep -n "Record<string, unknown> & LruMeta" src/registry/db.ts` returns no match
5. LRU write parity: `grep -c "useCount: 0" src/execution/widgetPrewarm.ts` returns 2
6. Full suite: `npm test` exits 0 with >=393 tests passing
7. Hygiene gate: `npm test -- --reporter=verbose 2>&1 | grep hygiene` shows hygiene tests green
8. Build clean: `npm run build` completes without error and sourcemaps are absent from dist/
</verification>

<success_criteria>
- src/registry/db.ts replaces both placeholder type aliases with explicit "export interface X extends LruMeta" declarations with the correct named required fields
- src/execution/widgetPrewarm.ts adds useCount: 0 and updatedAt: Date.now() at both widget write sites (resolveWidget and resolveWidgetTweak)
- npx tsc --noEmit exits 0 (no TypeScript errors)
- npm test exits 0 with >=393 tests passing, no regression
- No devtools-hygiene-banned token ("synthesize"/"synthesized"/"synthesis") appears in any modified or added comment or source surface
</success_criteria>

<output>
After completion, create `.planning/phases/10-widget-schema-key/10-01-SUMMARY.md`
</output>
