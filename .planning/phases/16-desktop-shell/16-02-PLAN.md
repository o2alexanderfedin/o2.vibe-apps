---
phase: 16-desktop-shell
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/ui/Dock.tsx
  - src/ui/Dock.test.tsx
  - src/ui/MenuBar.tsx
  - src/ui/MenuBar.test.tsx
  - src/ui/MinimalLauncher.tsx
  - src/ui/MinimalLauncher.test.tsx
  - src/ui/iconForApp.tsx
  - src/index.css
autonomous: true
requirements: [WIN-06, WIN-07]
must_haves:
  truths:
    - "The dock shows one icon button per open window plus a magnifier (launcher) button"
    - "Each running window's dock icon shows a running-indicator dot"
    - "Clicking a non-minimized window's dock icon calls onFocus(id); clicking a minimized window's dock icon calls onRestore(id)"
    - "Clicking the dock magnifier calls onOpenLauncher"
    - "The menu bar shows the OS wordmark, the active window's app name, the relocated 4-theme switcher, an account control, and a live HH:MM clock"
    - "Clicking the menu-bar account control calls onOpenAccount (KeyDialog gate, SHELL-03)"
    - "The minimal launcher lists the pre-installed apps; clicking one calls onOpen(appId, displayName) then onClose"
  artifacts:
    - path: "src/ui/Dock.tsx"
      provides: "Dock leaf component (props-injected windows/onFocus/onRestore/onOpenLauncher)"
      contains: "onOpenLauncher"
    - path: "src/ui/MenuBar.tsx"
      provides: "MenuBar leaf component (activeName + onOpenAccount + relocated ThemeSelector + live clock)"
      contains: "menu-bar"
    - path: "src/ui/MinimalLauncher.tsx"
      provides: "Minimal app-list launcher (Phase 17 replaces this stub)"
      contains: "onOpen"
    - path: "src/ui/iconForApp.tsx"
      provides: "Shared neutral icon-key → glyph mapping reused by dock/launcher"
      contains: "ICONS"
    - path: "src/index.css"
      provides: ".dock / .menu-bar / .launcher glass styles + running-dot + hover-scale"
      contains: ".dock"
  key_links:
    - from: "src/ui/Dock.tsx"
      to: "onFocus / onRestore"
      via: "dock icon click → minimized ? onRestore(id) : onFocus(id)"
      pattern: "onRestore|onFocus"
    - from: "src/ui/MenuBar.tsx"
      to: "src/ui/ThemeSelector.tsx"
      via: "renders the relocated <ThemeSelector />"
      pattern: "ThemeSelector"
    - from: "src/ui/MenuBar.tsx"
      to: "onOpenAccount"
      via: "account button onClick → onOpenAccount (KeyDialog)"
      pattern: "onOpenAccount"
    - from: "src/ui/MinimalLauncher.tsx"
      to: "onOpen"
      via: "app button onClick → onOpen(app.id, app.displayName); onClose()"
      pattern: "onOpen"
---

<objective>
Build the three desktop chrome leaf components — `Dock`, `MenuBar`, `MinimalLauncher` — as pure IoC/DI components that accept injected callbacks and data (no direct `useWindowManager()` inside them), plus a shared `iconForApp` glyph helper, and their `.dock` / `.menu-bar` / `.launcher` glass CSS. These are the WIN-06 (dock) and WIN-07 (menu bar) deliverables and the minimal launcher stub that keeps the desktop usable this phase (CONTEXT decision 4; Phase 17 replaces it).

Purpose: Each component is independently testable offline with substituted callbacks (matching the WindowFrame props-injection pattern). Wiring them onto the desktop root happens in plan 16-03. Building them first, fully tested in isolation, de-risks the root rewire.

Output: New `Dock.tsx`, `MenuBar.tsx`, `MinimalLauncher.tsx`, `iconForApp.tsx` + their `.test.tsx` files, and new `.dock`/`.menu-bar`/`.launcher` rules appended to `src/index.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-desktop-shell/16-CONTEXT.md
@.planning/phases/16-desktop-shell/16-PATTERNS.md

<interfaces>
<!-- WindowEntry (src/ui/useWindowManager.tsx) — Dock consumes this array: -->
export interface WindowEntry {
  id: string; instanceId: string; appType: string;
  title: string; icon: string; x: number; y: number; z: number; minimized: boolean;
}

<!-- ICONS map currently inline in Marketplace.tsx (lines 35-44) — EXTRACT to iconForApp.tsx: -->
const ICONS: Record<string, LucideIcon> = {
  cloud: Cloud, calculator: Calculator, notes: NotebookPen, timer: Timer,
  currency: ArrowLeftRight, recipes: ChefHat, calendar: CalendarDays, budget: Wallet,
};
<!-- WindowEntry.icon currently holds the appType string (Marketplace passes icon: appType).
     APP_REGISTRY entries carry a SEPARATE `icon` key (e.g. "cloud"). For dock glyphs, map the
     window's appType → APP_REGISTRY entry → its icon key → glyph; fall back to Cloud. -->

<!-- APP_REGISTRY (src/data/appRegistry.ts): array of { id, displayName, description, icon } -->

<!-- ThemeSelector (src/ui/ThemeSelector.tsx) — relocate here; it reads useVibeTheme() internally,
     so MenuBar must be rendered inside a VibeThemeProvider in tests (mirror ThemeSelector.test.tsx). -->

<!-- KeyDialog gate: App.tsx today passes onOpenAccount to AppBar; MenuBar takes the same callback. -->

<!-- Existing reusable button class: .app-bar__icon-btn (src/index.css ~lines 80-109) with focus-visible. -->

<!-- Design references (design/VibeOS.dc.html):
  menu-bar line 34: height 40px glass; dock line 134: bottom-center glass radius 22px;
  dock icon line 136: 52px, hover scale(1.22) translateY(-7px), transform-origin bottom;
  running dot line 138: 4px circle, background var(--text), opacity .7 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iconForApp shared glyph helper (extract ICONS)</name>
  <read_first>
    - src/ui/Marketplace.tsx (lines 1-44 — the lucide imports + ICONS map to extract)
    - src/data/appRegistry.ts (APP_REGISTRY shape + icon keys)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (MinimalLauncher section — ICONS reuse)
  </read_first>
  <action>
    Create `src/ui/iconForApp.tsx`. Move the `ICONS: Record<string, LucideIcon>` map (cloud/calculator/notes/timer/currency/recipes/calendar/budget → lucide glyphs) out of `Marketplace.tsx` into this module and export it. Export a helper `iconForAppType(appType: string): LucideIcon` that looks up `APP_REGISTRY.find(a => a.id === appType)?.icon`, maps it through `ICONS`, and falls back to `Cloud` for unknown types. Keep all names neutral (no banned tokens). Do NOT yet edit Marketplace to import it (that file is rewired in plan 16-03) — duplicate-free is fine because 16-03 owns Marketplace; here just create the shared module so Dock and MinimalLauncher can import it. Re-export `ICONS` so the launcher can use the same map directly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/iconForApp.tsx` exists and exports `ICONS` and `iconForAppType`
    - `grep -c "export" src/ui/iconForApp.tsx` returns >= 2
    - `iconForAppType("weather")` resolves to a defined LucideIcon (cloud); `iconForAppType("unknown")` resolves to Cloud (fallback)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A shared neutral icon helper exists and is importable by Dock and MinimalLauncher; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dock component (WIN-06) — running indicators, hover-scale, focus/restore, magnifier</name>
  <read_first>
    - src/ui/iconForApp.tsx (created in Task 1 — the glyph helper)
    - src/ui/useWindowManager.tsx (WindowEntry shape; focus/restore semantics; minimized flag)
    - src/ui/WindowFrame.tsx (the props-injection / IoC pattern to mirror — NO useWindowManager inside the leaf)
    - design/VibeOS.dc.html (dock container line 134, icon line 136, running dot line 138)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (Dock.tsx section, lines ~171-224)
  </read_first>
  <behavior>
    - Test 1: Given two WindowEntry windows + the launcher, the dock renders one icon button per window plus a magnifier button (assert button count and an accessible magnifier label e.g. "Open launcher").
    - Test 2: Each window's dock icon contains a running-indicator dot element (class `dock__running-dot`).
    - Test 3: Clicking a NON-minimized window's dock icon calls onFocus with that window's id (not onRestore).
    - Test 4: Clicking a MINIMIZED window's dock icon calls onRestore with that window's id (not onFocus).
    - Test 5: Clicking the magnifier button calls onOpenLauncher.
  </behavior>
  <action>
    Create `src/ui/Dock.tsx`. Define `DockProps { windows: WindowEntry[]; onFocus: (id:string)=>void; onRestore: (id:string)=>void; onOpenLauncher: ()=>void; }`. Render a `<nav className="dock" aria-label="Open apps">` (or role-appropriate landmark) containing: a magnifier/search button (`Search` lucide glyph, `aria-label="Open launcher"`, className `dock__icon dock__launch`, `onClick={onOpenLauncher}`), then one `<button className="dock__icon">` per window with: the glyph from `iconForAppType(entry.appType)`, `aria-label={entry.title}`, a child `<span className="dock__running-dot" aria-hidden="true" />`, and `onClick={() => entry.minimized ? onRestore(entry.id) : onFocus(entry.id)}`. Inject ALL behavior via props — NO `useWindowManager()` inside Dock (IoC, matching WindowFrame). Keep all copy neutral. Write `src/ui/Dock.test.tsx` covering the five behaviors using substituted vi.fn() callbacks.
  </action>
  <verify>
    <automated>npx vitest run src/ui/Dock.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/Dock.test.tsx` exits 0 with all five behaviors asserted
    - `grep -c "useWindowManager" src/ui/Dock.tsx` returns 0 (IoC: no direct manager access)
    - `grep -c "onOpenLauncher" src/ui/Dock.tsx` returns >= 1
    - `grep -c "dock__running-dot" src/ui/Dock.tsx` returns >= 1
    - Click-minimized→onRestore and click-active→onFocus assertions both pass
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dock renders a per-window icon (with running dot) + a magnifier; clicks route to onFocus/onRestore by minimized state; magnifier calls onOpenLauncher; component is pure props-injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MenuBar (WIN-07) + MinimalLauncher (launcher stub) + dock/menu-bar/launcher CSS</name>
  <read_first>
    - src/ui/AppBar.tsx (analog top-bar layout + Account button + the ThemeSelector it currently hosts)
    - src/ui/ThemeSelector.tsx (the switcher being relocated; reads useVibeTheme())
    - src/ui/ThemeSelector.test.tsx (the renderWithServices wrapper pattern for VibeThemeProvider + ServicesProvider)
    - src/ui/KeyDialog.tsx (onClose-only props — the account gate target)
    - src/ui/iconForApp.tsx (ICONS for the launcher grid)
    - src/data/appRegistry.ts (APP_REGISTRY for the launcher list)
    - src/index.css (lines ~57-111 .app-bar + .app-bar__icon-btn; lines ~687-779 desktop/window-chrome block — append below)
    - design/VibeOS.dc.html (menu-bar line 34; dock line 134; clock pattern line 193)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (MenuBar section ~110-167, MinimalLauncher ~227-282, CSS section ~392-502)
  </read_first>
  <behavior>
    - MenuBar Test 1: renders the OS wordmark text (e.g. "Vibe OS") and, when activeName is a string, renders that active-app name; when activeName is null, renders no active-app name node.
    - MenuBar Test 2: renders the relocated ThemeSelector (four pills Aurora/Aero/Aqua/Noir) — assert the group is present (rendered inside VibeThemeProvider + ServicesProvider in the test).
    - MenuBar Test 3: renders an account control (aria-label e.g. "Account"); clicking it calls onOpenAccount.
    - MenuBar Test 4: renders a clock node whose text matches /^\d{2}:\d{2}$/ (HH:MM); using fake timers, advancing time updates the clock text (or at minimum the clock initializes from a controllable now). Clean up the interval on unmount (no leaked timer).
    - MinimalLauncher Test 1: renders one button per APP_REGISTRY entry (assert count == APP_REGISTRY.length) each labeled by its displayName.
    - MinimalLauncher Test 2: clicking an app button calls onOpen(app.id, app.displayName) AND then onClose (open-then-close ordering).
    - MinimalLauncher Test 3: the overlay/backdrop click and the explicit close control both call onClose; clicking inside the panel does NOT call onClose (stopPropagation).
  </behavior>
  <action>
    (a) Create `src/ui/MenuBar.tsx` with `MenuBarProps { activeName: string | null; onOpenAccount: () => void; }`. Render `<header role="banner" className="menu-bar">` with a left group (`menu-bar__left`: a `menu-bar__wordmark` span "Vibe OS" + a conditional `menu-bar__active-app` span showing activeName when non-null) and a right group (`menu-bar__right`): the relocated `<ThemeSelector />`, an account `<button className="app-bar__icon-btn" aria-label="Account" onClick={onOpenAccount}>` (User glyph), and a `<span className="menu-bar__clock" aria-live="off">` driven by a `setInterval`-in-`useEffect` that updates HH:MM and is cleared on unmount. Add a module-local `formatClock(d: Date): string` using `toLocaleTimeString([], { hour: "2-digit", minute: "2-digit", hour12: false })`. Keep all copy neutral.
    (b) Create `src/ui/MinimalLauncher.tsx` with `MinimalLauncherProps { onOpen: (appType: string, displayName: string) => void; onClose: () => void; }`. Render an overlay div (`launcher-overlay`, `onClick={onClose}`) wrapping an inner `<div className="launcher" role="dialog" aria-label="Open an app" onClick={e => e.stopPropagation()}>` with a neutral close control and a `launcher__grid` of `APP_REGISTRY.map` buttons, each using `ICONS[app.icon] ?? Cloud`, labeled by `app.displayName`, `onClick={() => { onOpen(app.id, app.displayName); onClose(); }}`. (Phase 17 replaces this stub — keep it small and neutral.)
    (c) In `src/index.css`, append the `.menu-bar` (top 40px glass per design line 34, `z-index: 9000`, `color: var(--text)`), `.dock` (bottom-center glass radius 22px per line 134, `z-index: 9000`), `.dock__icon` (52px, hover `transform: scale(1.22) translateY(-7px)`, `transition: transform .2s cubic-bezier(.2,.8,.2,1)`, `transform-origin: bottom`), `.dock__running-dot` (4px circle, `background: var(--text)`, opacity .7), `.menu-bar__left/__right/__wordmark/__active-app/__clock`, and `.launcher-overlay`/`.launcher`/`.launcher__grid`/launcher button rules (reuse the glass formula `var(--glass)`/`var(--bord)`/`backdrop-filter: blur(...)`). Reference theme vars (`--glass`, `--bord`, `--text`, `--hi`) so the chrome re-skins with the theme. All class names neutral.
    (d) Write `src/ui/MenuBar.test.tsx` and `src/ui/MinimalLauncher.test.tsx` covering the behaviors above. For MenuBar, wrap in `ServicesProvider` + `VibeThemeProvider` (mirror ThemeSelector.test.tsx) so ThemeSelector resolves. Use `vi.useFakeTimers()` for the clock test and restore real timers + assert the interval is cleared on unmount.
  </action>
  <verify>
    <automated>npx vitest run src/ui/MenuBar.test.tsx src/ui/MinimalLauncher.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/MenuBar.test.tsx src/ui/MinimalLauncher.test.tsx` exits 0
    - `grep -c "ThemeSelector" src/ui/MenuBar.tsx` returns >= 1 (switcher relocated)
    - `grep -c "onOpenAccount" src/ui/MenuBar.tsx` returns >= 1
    - `grep -c "clearInterval" src/ui/MenuBar.tsx` returns >= 1 (timer cleanup)
    - MenuBar clock node text matches /^\d{2}:\d{2}$/ in the test
    - MinimalLauncher renders exactly APP_REGISTRY.length app buttons; click calls onOpen then onClose
    - `grep -Ec "\.dock\b|\.menu-bar\b|\.launcher\b" src/index.css` returns >= 3 (all three surfaces styled)
    - `grep -c "scale(1.22)" src/index.css` returns >= 1 (hover-scale)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MenuBar shows wordmark + active-app name + relocated theme switcher + account control + live HH:MM clock (timer cleaned up on unmount); MinimalLauncher lists pre-installed apps and opens-then-closes; .dock/.menu-bar/.launcher glass styles (with running dot + hover-scale, theme-var-referenced) are in index.css.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/Dock.test.tsx src/ui/MenuBar.test.tsx src/ui/MinimalLauncher.test.tsx` all green.
- `npx vitest run src/hygiene.test.ts` green — new files Dock.tsx/MenuBar.tsx/MinimalLauncher.tsx/iconForApp.tsx and new CSS classes (.dock/.menu-bar/.launcher/.dock__running-dot) carry NO banned tokens (synthesi*/AI/llm/generate/fake/mock). The hygiene gate auto-scans all src/** so the new files are covered by construction.
- `npx vitest run` (full suite) green — no regression (Dock/MenuBar/MinimalLauncher are additive; no existing file is modified except index.css append).
- `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Dock (WIN-06): per-window icon + running dot + hover-scale; click → focus/restore by minimized state; magnifier → onOpenLauncher.
- MenuBar (WIN-07): wordmark + active-app name + relocated 4-theme switcher + account control + live clock.
- MinimalLauncher: lists pre-installed apps; clicking opens then closes.
- All three are pure IoC/DI (no useWindowManager inside); all tested offline; hygiene + tsc green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-desktop-shell/16-02-SUMMARY.md`
</output>
