---
phase: 16-desktop-shell
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - src/ui/DesktopShell.tsx
  - src/ui/DesktopShell.test.tsx
  - src/ui/Marketplace.tsx
  - src/App.tsx
  - src/ui/AppBar.tsx
  - src/index.css
autonomous: true
requirements: [WIN-08]
must_haves:
  truths:
    - "DesktopShell is the root UI — the flat storefront grid is gone as the root"
    - "The desktop renders a themed wallpaper layer (background var(--wall)) behind the windows"
    - "Four animated blob layers (--b1..--b4, vibeFloat, blur, mix-blend-mode:screen) render behind the windows"
    - "Layer order back→front is wallpaper → blobs → windows (isolation:isolate) → dock + menu bar"
    - "Opening an app from the launcher mints a WindowFrame on the desktop and adds a running entry to the dock"
    - "Clicking a dock icon focuses/restores the corresponding window (completes WIN-04 restore UI)"
    - "The menu bar active-app name reflects the front-most (highest-z, non-minimized) window"
    - "The KeyDialog (SHELL-03) is reachable via the menu-bar account control"
    - "The contextual ⋮ modification (MOD) still works inside a window"
  artifacts:
    - path: "src/ui/DesktopShell.tsx"
      provides: "Root desktop layout: wallpaper + blobs + windows + dock + menu bar + launcher + KeyDialog; owns the open flow"
      contains: "desktop-shell"
    - path: "src/index.css"
      provides: ".desktop-shell wallpaper + .desktop-shell__blob layers + vibeFloat keyframes"
      contains: "vibeFloat"
    - path: "src/App.tsx"
      provides: "DesktopShell mounted as root (replaces AppBar + Marketplace storefront)"
      contains: "DesktopShell"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/DesktopShell.tsx"
      via: "renders <DesktopShell /> inside ServicesProvider/VibeThemeProvider"
      pattern: "DesktopShell"
    - from: "src/ui/DesktopShell.tsx"
      to: "src/ui/Dock.tsx"
      via: "passes windows + focus/restore + onOpenLauncher"
      pattern: "<Dock"
    - from: "src/ui/DesktopShell.tsx"
      to: "src/ui/MenuBar.tsx"
      via: "passes activeName (front-most window) + onOpenAccount"
      pattern: "<MenuBar"
    - from: "src/ui/DesktopShell.tsx"
      to: "src/ui/MinimalLauncher.tsx"
      via: "launcherOpen state gates the launcher; onOpen → handleOpen"
      pattern: "MinimalLauncher"
    - from: "src/ui/DesktopShell.tsx"
      to: "src/ui/WindowFrame.tsx"
      via: "maps windowManager.windows → WindowFrame per entry (open flow ported from Marketplace)"
      pattern: "WindowFrame"
---

<objective>
Assemble `DesktopShell` as the new root UI (WIN-08): a themed animated wallpaper + blob layer behind the windows, with the dock, menu bar, and minimal launcher wired together over the windowed open flow ported from `Marketplace`. Rewire `App.tsx` to mount `DesktopShell` as root, strip the storefront grid from `Marketplace` (and the relocated `ThemeSelector` from `AppBar`), and prove the integrated desktop with end-to-end tests.

Purpose: This is the visual payoff — the flat white storefront becomes the Vibe OS desktop. It composes the leaf components (16-02) and the chrome fixes (16-01) into a working desktop, porting the proven `handleOpen`/`handleClose`/`handleModify` flow so windowing, MOD, and key-dialog access are preserved with zero regression.

Output: New `DesktopShell.tsx` + `DesktopShell.test.tsx`; modified `App.tsx` (DesktopShell root), `Marketplace.tsx` (storefront grid removed), `AppBar.tsx` (ThemeSelector relocated out), `index.css` (wallpaper + blob layers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-desktop-shell/16-CONTEXT.md
@.planning/phases/16-desktop-shell/16-PATTERNS.md

<interfaces>
<!-- The open flow to PORT verbatim from src/ui/Marketplace.tsx:
  - handleOpen (lines 185-263): mints window first, resolves component, isOpenByInstance guard (Pitfall 9),
    storeComponent, fallback variants (auth/throttle/generic).
  - handleClose (lines 156-173): evictLiveComponent + windowManager.close + drop body/position.
  - storeComponent (178-183), handleModify (279-337: remove/clone/tweak), handleOpenRef (267-268).
  - State (128-147): openingId, components Map, positions Map, keyDialogOpen, timeoutRef.
  - The <div className="desktop"> window map (433-464) → WindowFrame per entry. -->

<!-- DesktopShell wraps its OWN WindowManagerProvider (mirror Marketplace lines 117-123) so it stays testable: -->
export function DesktopShell() {
  return (<WindowManagerProvider><DesktopShellInner /></WindowManagerProvider>);
}

<!-- Active window derivation: highest-z non-minimized window is the active one (for MenuBar activeName):
  const activeWindow = [...windowManager.windows].filter(w => !w.minimized).sort((a,b)=>b.z-a.z)[0] ?? null; -->

<!-- Dock/MenuBar/MinimalLauncher props (from plan 16-02):
  DockProps { windows; onFocus; onRestore; onOpenLauncher }
  MenuBarProps { activeName: string|null; onOpenAccount: () => void }
  MinimalLauncherProps { onOpen: (appType, displayName) => void; onClose: () => void } -->

<!-- App.tsx CURRENT root (to replace):
  ThemeProvider > VibeThemeProvider > ErrorBoundary > WindowManagerProvider > AppBar + main[Marketplace] + KeyDialog
  NEW root: ThemeProvider > VibeThemeProvider > ErrorBoundary > ServicesProvider(createServices()) > DesktopShell
  NOTE: confirm where createServices() / ServicesProvider currently lives in the tree (App today does NOT wrap
  ServicesProvider — check main.tsx / entry). Preserve the existing Services wiring; DesktopShell consumes useServices(). -->

<!-- Layer order + CSS (design/VibeOS.dc.html lines 15-35; 16-PATTERNS.md CSS section ~392-502):
  .desktop-shell { position:fixed; inset:0; background:var(--wall); overflow:hidden; }
  .desktop-shell__blob { position:absolute; border-radius:50%; filter:blur(60px); opacity:.6;
    mix-blend-mode:screen; pointer-events:none; } + per-blob position/size/var(--b1..--b4)/vibeFloat timing.
  @keyframes vibeFloat { 0%,100%{translate(0,0) scale(1)} 33%{translate(7%,-9%) scale(1.14)} 66%{translate(-8%,6%) scale(.92)} }
  Existing .desktop (lines 689-695): z-index:100; isolation:isolate. Blobs sit at lower z (e.g. 0/1); dock+menu-bar z 9000. -->
</interfaces>

<dependency_context>
- Pitfall 3 (z-index stacking): blobs and wallpaper are LOWER-z siblings of the `.desktop` window container (which keeps `isolation:isolate`); dock/menu-bar at z 9000 above. No transform/filter on the `.desktop` window container.
- Pitfall 8 (root leak): the close path MUST route through the ported handleClose → windowManager.close + evictLiveComponent. Do not bypass it. MarketplaceWindows.test.tsx's zero-leak assertions are the bar; mirror them.
- Pitfall 9 (mid-flight close): keep the isOpenByInstance guard from the ported handleOpen.
- Pitfall 12 (focus): keep WindowFrame's onFocus-on-pointerdown (raise) separate from input focus — do not add any focus() call in DesktopShell.
</dependency_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DesktopShell root layout + ported open flow + leaf wiring</name>
  <read_first>
    - src/ui/Marketplace.tsx (the FULL open flow: imports, state, handleOpen/handleClose/storeComponent/handleModify, the WindowFrame map, makeFallback — this is the source to port)
    - src/ui/Dock.tsx, src/ui/MenuBar.tsx, src/ui/MinimalLauncher.tsx (the leaf props from plan 16-02)
    - src/ui/useWindowManager.tsx (WindowManagerProvider wrapper pattern + windows/focus/restore/close)
    - src/ui/WindowFrame.tsx (props to pass per entry)
    - src/ui/KeyDialog.tsx (onClose-only — the account gate)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (DesktopShell section ~26-107: provider wrapper, layer order, active-window derivation, state pattern, error-handling/useCallback shared patterns)
  </read_first>
  <action>
    Create `src/ui/DesktopShell.tsx` exporting `DesktopShell()` that wraps its OWN `WindowManagerProvider` around `DesktopShellInner()` (mirror Marketplace's self-contained provider so DesktopShell stays testable standalone). In `DesktopShellInner`: consume `useServices()` + `useWindowManager()`; PORT the proven open flow from Marketplace verbatim — `handleOpen` (mint-window-first + isOpenByInstance guard + storeComponent + auth/throttle/generic fallbacks via makeFallback), `handleClose` (evictLiveComponent + windowManager.close + drop body/position maps), `storeComponent`, `handleModify` (remove/clone/tweak), `handleOpenRef`, and the `openingId`/`components`/`positions`/`keyDialogOpen`/`timeoutRef` state. Add `launcherOpen` state. Render the layer stack in back→front order inside a `<div className="desktop-shell">`: the wallpaper is the `.desktop-shell` background (CSS, Task 2); four `<div className="desktop-shell__blob desktop-shell__blob--N" aria-hidden="true" />`; the existing `<div className="desktop">` mapping `windowManager.windows` → `<WindowFrame>` per entry (same props wiring as Marketplace, including onMove/onModify/onFocus/onMinimize/onClose); then `<MenuBar activeName={activeWindow?.title ?? null} onOpenAccount={() => setKeyDialogOpen(true)} />`; `<Dock windows={windowManager.windows} onFocus={windowManager.focus} onRestore={windowManager.restore} onOpenLauncher={() => setLauncherOpen(true)} />`; `{launcherOpen && <MinimalLauncher onOpen={(t,n)=>{ void handleOpen(t,n); }} onClose={() => setLauncherOpen(false)} />}`; `{keyDialogOpen && <KeyDialog onClose={() => setKeyDialogOpen(false)} />}`. Derive `activeWindow` as the highest-z non-minimized window. Move `makeFallback` + the fallback content components (FailedAppContent/NeedsAuthContent/ThrottledAppContent) into DesktopShell (or import them if Marketplace re-exports — prefer porting so Marketplace can be slimmed in Task 3). Keep ALL copy neutral (no banned tokens).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/DesktopShell.tsx` exists; `grep -c "desktop-shell" src/ui/DesktopShell.tsx` returns >= 1
    - `grep -Ec "<Dock|<MenuBar|MinimalLauncher" src/ui/DesktopShell.tsx` returns >= 3 (all three leaves wired)
    - `grep -c "desktop-shell__blob" src/ui/DesktopShell.tsx` returns >= 4 (four blob layers)
    - `grep -c "isOpenByInstance" src/ui/DesktopShell.tsx` returns >= 1 (Pitfall 9 guard ported)
    - `grep -c "WindowManagerProvider" src/ui/DesktopShell.tsx` returns >= 1 (self-contained provider)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>DesktopShell renders the full layer stack (wallpaper/blobs/windows/dock/menu-bar) with the ported open flow, all leaf components wired via injected callbacks, and the active-window name feeding the menu bar; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wallpaper + animated blob CSS (layer order, isolation, mix-blend)</name>
  <read_first>
    - src/index.css (lines ~687-779 desktop/window-chrome block; the .dock/.menu-bar appended in plan 16-02)
    - src/ui/VibeThemeProvider.tsx (VIBE_THEMES — confirms --wall and --b1..--b4 exist per theme)
    - design/VibeOS.dc.html (blob positions/sizes/animation timing line 19+423; layer order 15-35)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (index.css section ~392-502: wallpaper, blob keyframes, blob base + per-blob rules, z-layering)
  </read_first>
  <action>
    Append to `src/index.css`: `.desktop-shell { position:fixed; inset:0; background:var(--wall); overflow:hidden; }`; a `@keyframes vibeFloat` (0%/100% identity, 33% translate(7%,-9%) scale(1.14), 66% translate(-8%,6%) scale(.92)); a `.desktop-shell__blob` base rule (`position:absolute; border-radius:50%; filter:blur(60px); opacity:.6; mix-blend-mode:screen; pointer-events:none; z-index:0;`); and four `.desktop-shell__blob--1..4` rules with the design's positions/sizes and `background: radial-gradient(circle, var(--bN) 0%, transparent 68%)` plus staggered `animation: vibeFloat <dur>s ease-in-out infinite <delay>s`. Confirm the existing `.desktop` window container keeps `z-index:100; isolation:isolate` (above blobs, below the z-9000 dock/menu-bar) and carries NO transform/filter (Pitfall 3). Verify the blobs render BEHIND the windows by z-order (blob z 0, windows z 100). Do NOT add a `transition` on `:root`/custom properties (Pitfall 7 — theme switch must not cascade-restyle; the reduced-motion + transition strategy is plan 16-04). All class names neutral.
  </action>
  <verify>
    <automated>grep -c "vibeFloat" src/index.css && grep -c "desktop-shell__blob" src/index.css && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vibeFloat" src/index.css` returns >= 2 (keyframes def + at least one animation use)
    - `grep -c "desktop-shell__blob--" src/index.css` returns >= 4 (four positioned blobs)
    - `grep -E "\.desktop-shell\s*\{[^}]*var\(--wall\)" src/index.css` matches OR a `.desktop-shell` rule referencing `var(--wall)` exists (wallpaper bound to theme var)
    - `grep -c "mix-blend-mode" src/index.css` returns >= 1
    - `grep -E "\.desktop\b[^_]" src/index.css` still shows `isolation: isolate` retained for the window container
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The wallpaper layer binds to var(--wall); four animated blobs (var(--b1..--b4), vibeFloat, blur, mix-blend screen) render behind the windows at lower z; the window container keeps isolation:isolate above blobs and below the dock/menu-bar.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire App.tsx to DesktopShell root; strip storefront grid + relocated ThemeSelector; integration tests</name>
  <read_first>
    - src/App.tsx (current root tree; confirm ServicesProvider/createServices location — check src/main.tsx if App does not wrap it)
    - src/main.tsx (entry — to confirm where ServicesProvider is mounted today)
    - src/ui/Marketplace.tsx (the storefront-grid JSX to remove lines ~366-427; keep what other tests need or migrate ownership)
    - src/ui/AppBar.tsx (remove the <ThemeSelector /> import + element; preserve onOpenAccount + the light/dark toggle the 600 tests depend on)
    - src/ui/MarketplaceWindows.test.tsx (the integration-test patterns: pointer-capture stubs, openApp helper, frameByTitle, appBodyCount zero-leak, cascade/focus/minimize/MOD assertions — MIRROR these for DesktopShell)
    - src/ui/ThemeSelector.test.tsx, src/ui/AppBar tests, any Marketplace*.test.tsx (to know what must stay green after the grid removal)
  </read_first>
  <action>
    (a) `src/App.tsx`: replace the `AppBar + main[Marketplace] + KeyDialog` subtree with `<DesktopShell />`. DesktopShell owns its OWN WindowManagerProvider + KeyDialog, so remove App's outer WindowManagerProvider and App-level keyDialog state if they become unused. Preserve `ThemeProvider > VibeThemeProvider > ErrorBoundary` and the existing Services wiring (do NOT drop ServicesProvider — keep whichever layer currently provides it so useServices() resolves for DesktopShell). Keep the `dbReady` init effect.
    (b) `src/ui/Marketplace.tsx`: remove the storefront-grid sections (the main `.storefront-grid` and the "Your most-opened" popular section) as the ROOT surface per CONTEXT decision 1. The window-open machinery now lives in DesktopShell (Task 1). Reduce Marketplace to whatever existing tests still require — if Marketplace.test.tsx and friends test the open flow, EITHER keep a slim Marketplace that re-uses the ported flow OR update those tests to target DesktopShell. Prefer: keep Marketplace exporting a thin wrapper or remove it and re-point its tests to DesktopShell — read the tests first and choose the path that keeps the full suite green with the least churn. Do not delete coverage; migrate it.
    (c) `src/ui/AppBar.tsx`: remove the `import { ThemeSelector }` and the `<ThemeSelector />` element (it now lives in MenuBar). Keep the Account button (onOpenAccount) and the existing light/dark/system cycle toggle intact (600 tests depend on them). If AppBar is no longer rendered anywhere after the root rewire, leave the file intact (still exported) so its tests pass, but ensure no dangling ThemeSelector import remains.
    (d) Create `src/ui/DesktopShell.test.tsx` mirroring MarketplaceWindows.test.tsx: install the pointer-capture stubs; render `<ServicesProvider services={createTestServices(...)}><DesktopShell/></ServicesProvider>`; assert (1) the desktop-shell + blobs render, (2) opening via the launcher (open the MinimalLauncher through the dock magnifier, click an app) mints a WindowFrame AND adds a dock entry with a running dot, (3) a dock icon click focuses (raises z) the window; minimizing then clicking the dock icon restores it (WIN-04 completion), (4) closing leaves zero leaked app bodies, (5) the menu-bar account control opens the KeyDialog, (6) the menu-bar active-app name reflects the front window, (7) the ⋮ MOD "remove" still closes a window. Reuse canned transport + in-memory registry (no network, offline).
  </action>
  <verify>
    <automated>npx vitest run src/ui/DesktopShell.test.tsx && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DesktopShell" src/App.tsx` returns >= 1; `grep -c "storefront-grid" src/App.tsx` returns 0
    - `grep -c "ThemeSelector" src/ui/AppBar.tsx` returns 0 (relocated out)
    - `grep -c "storefront-grid" src/ui/Marketplace.tsx` returns 0 (grid removed as root)
    - `npx vitest run src/ui/DesktopShell.test.tsx` exits 0 with the seven integration behaviors
    - DesktopShell test asserts: dock entry appears on open, dock-click focus/restore works, close leaves zero `.app-shell` leaks, menu-bar account opens KeyDialog, active-app name reflects front window, ⋮ remove closes a window
    - `npx vitest run` (full suite) exits 0 — all ~600 existing tests green (no regression)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>DesktopShell is the root; the flat storefront grid is gone; the relocated ThemeSelector is out of AppBar; the full suite + tsc are green; integration tests prove launch→window→dock→focus/restore→close→MOD→KeyDialog end-to-end offline.</done>
</task>

</tasks>

<verification>
- `npx vitest run` (full suite) green — zero regression across the ~600 existing tests after root rewire and storefront-grid removal.
- `npx vitest run src/ui/DesktopShell.test.tsx` green (the seven integration behaviors).
- `npx vitest run src/hygiene.test.ts` green — DesktopShell.tsx + new CSS (.desktop-shell, .desktop-shell__blob, vibeFloat) carry no banned tokens; the gate auto-scans all src/**.
- `npx tsc --noEmit` exits 0.
- `npm run build` succeeds with `build.sourcemap: false` (already set in vite.config.ts — do not flip).
</verification>

<success_criteria>
- DesktopShell is the root UI (WIN-08): wallpaper + blobs behind windows; dock + menu bar over them; flat storefront grid gone.
- Launching opens a window + a running dock entry; dock click focuses/restores (completes WIN-04 restore UI).
- Menu-bar active-app name tracks the front window; KeyDialog reachable; ⋮ MOD still works.
- Zero-leak close preserved (Pitfall 8); mid-flight-close guard preserved (Pitfall 9); z-order/isolation correct (Pitfall 3).
- Full suite + tsc + hygiene + build all green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-desktop-shell/16-03-SUMMARY.md`
</output>
