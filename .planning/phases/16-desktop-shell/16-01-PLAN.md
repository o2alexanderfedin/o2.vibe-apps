---
phase: 16-desktop-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/AppShell.tsx
  - src/ui/WindowFrame.tsx
  - src/ui/useWindowManager.tsx
  - src/index.css
autonomous: true
requirements: [WIN-08]
must_haves:
  truths:
    - "A windowed app shows no redundant inner × close button (the traffic-light is the only close)"
    - "A window titlebar shows the app icon then the title, grouped and centered between the traffic-lights and the right edge"
    - "AppShell used standalone (not in a window) still renders its inner × close button"
    - "New windows cascade-offset gently so they do not spread fully across the viewport"
  artifacts:
    - path: "src/ui/AppShell.tsx"
      provides: "hideClose prop that suppresses the inner × when true"
      contains: "hideClose"
    - path: "src/ui/WindowFrame.tsx"
      provides: "title-group markup (icon + title centered) + hideClose pass-through to AppShell"
      contains: "window-chrome__title-group"
    - path: "src/index.css"
      provides: "centered title-group grid layout for the titlebar"
      contains: "window-chrome__title-group"
  key_links:
    - from: "src/ui/WindowFrame.tsx"
      to: "src/ui/AppShell.tsx"
      via: "hideClose prop passed into AppShell"
      pattern: "hideClose"
    - from: "src/ui/WindowFrame.tsx WindowBody"
      to: "AppShell"
      via: "WindowBody forwards hideClose so framed apps suppress the inner ×"
      pattern: "hideClose"
---

<objective>
Land the Phase-15 window-chrome cosmetic fixes (CONTEXT decision 8) as the foundational, lowest-regression slice of Phase 16: an `AppShell` `hideClose` prop that suppresses the redundant inner × when an app is wrapped in a `WindowFrame`, a centered icon+title group in the titlebar (icon first, then title), and a gentler window-open cascade offset.

Purpose: The traffic-light close must be the single authoritative close inside a window, and the titlebar must match the design (centered icon+title) before the DesktopShell root lands. These are the highest single-file regression risks in the phase (`AppShell`/`WindowFrame` are covered by ~600 existing tests), so they ship first and in isolation behind passing tests.

Output: Modified `AppShell.tsx` (new `hideClose` prop), `WindowFrame.tsx` (title-group + `hideClose` pass-through), `useWindowManager.tsx` (cascade tune), `index.css` (titlebar grid). No new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-desktop-shell/16-CONTEXT.md
@.planning/phases/16-desktop-shell/16-PATTERNS.md

<interfaces>
<!-- Current AppShell props (src/ui/AppShell.tsx) — add hideClose here -->
export interface AppShellProps {
  displayName: string;
  onClose: () => void;
  onModify?: (instruction: string) => void;
  children: ReactNode;
}
<!-- The inner × button to guard (src/ui/AppShell.tsx ~lines 64-72): aria-label `Close ${displayName}` -->

<!-- Current WindowFrame WindowBody (src/ui/WindowFrame.tsx) renders:
  <AppShell displayName={title} onClose={onClose} onModify={onModify}> ... </AppShell>
  Titlebar markup order today: traffic-lights → <span class="window-chrome__title"> → <span class="window-chrome__icon">
  WindowFrameProps already carries: id, instanceId, title, icon, x, y, z, minimized, Component, onClose, onMinimize, onFocus, onMove, onModify -->

<!-- Cascade constant (src/ui/useWindowManager.tsx): CASCADE_OFFSET = 28, first window at (80,80), clamped to viewport -->

<!-- Existing titlebar CSS (src/index.css ~lines 720-769):
  .window-chrome__titlebar { display:flex; align-items:center; gap:8px; padding:8px 12px; cursor:grab; user-select:none; }
  .window-chrome__title { flex:1; font-size:13px; font-weight:600; color:var(--text,...); overflow:hidden; text-overflow:ellipsis; white-space:nowrap; } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppShell hideClose prop (suppress inner × when framed)</name>
  <read_first>
    - src/ui/AppShell.tsx (the component + AppShellProps interface + the × button render)
    - src/ui/AppShell.test.tsx (if present) and any test importing AppShell, to mirror render-wrapper conventions
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (AppShell.tsx section — hideClose pattern, lines ~325-347)
  </read_first>
  <behavior>
    - Test 1: AppShell rendered WITHOUT hideClose still renders a button whose aria-label is `Close {displayName}` (default/standalone behavior — no regression).
    - Test 2: AppShell rendered WITH hideClose={true} renders NO `Close {displayName}` button (the × is suppressed).
    - Test 3: AppShell with hideClose={true} STILL renders the ⋮ "App options" button (only the close is suppressed, the contextual prompt stays).
  </behavior>
  <action>
    Add an optional `hideClose?: boolean` field to `AppShellProps` (documented: "When true, the inner × close button is not rendered — the surrounding chrome's close is authoritative"). Destructure `hideClose` in the `AppShell` function signature. Wrap the existing × `<button>` (the one with aria-label `Close ${displayName}`) in a `{!hideClose && (...)}` guard so it is not rendered when `hideClose` is true. Leave the ⋮ "App options" button and ContextualPrompt wiring untouched. Do NOT change `onClose` semantics — `onClose` is still required and still used by the ⋮-prompt "remove" path is unaffected (it routes through onModify, not onClose). Add the test cases to a new or existing `src/ui/AppShell.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/AppShell.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/AppShell.test.tsx` exits 0
    - `grep -c "hideClose" src/ui/AppShell.tsx` returns >= 2 (interface field + render guard)
    - Test asserting hideClose={true} hides the `Close` button passes; test asserting default renders it passes; test asserting ⋮ still renders under hideClose passes
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AppShell exposes hideClose; the inner × is suppressed only when hideClose is true; standalone AppShell behavior is unchanged; the ⋮ prompt is unaffected; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WindowFrame title-group (centered icon+title) + hideClose pass-through</name>
  <read_first>
    - src/ui/WindowFrame.tsx (WindowBody + WindowFrame + titlebar markup + WindowBodyProps)
    - src/ui/WindowFrame.test.tsx (existing assertions on titlebar/title/traffic-lights — must stay green)
    - src/ui/MarketplaceWindows.test.tsx (frameByTitle reads `.window-chrome__title` textContent — must keep working)
    - src/index.css (lines ~720-769 — .window-chrome__titlebar / .window-chrome__title)
    - design/VibeOS.dc.html (window-chrome titlebar layout reference)
    - .planning/phases/16-desktop-shell/16-PATTERNS.md (WindowFrame.tsx section, lines ~286-321)
  </read_first>
  <behavior>
    - Test 1: A rendered WindowFrame's titlebar contains an element with class `window-chrome__title-group` that wraps BOTH the icon (`window-chrome__icon`) and the title (`window-chrome__title`), with the icon element appearing before the title element in DOM order.
    - Test 2: `.window-chrome__title` still carries the title text (so frameByTitle in MarketplaceWindows.test.tsx keeps resolving) — the title span is preserved, only regrouped.
    - Test 3: WindowFrame passes hideClose={true} into its AppShell — rendering a WindowFrame with a resolved Component produces NO inner `Close {title}` button inside the body (only the traffic-light Close remains).
  </behavior>
  <action>
    In `WindowFrame.tsx`: restructure the titlebar so the `window-chrome__icon` span and the `window-chrome__title` span are siblings inside a new wrapper `<div className="window-chrome__title-group">` placed between the `window-chrome__traffic-lights` group and the right edge — ICON FIRST, then TITLE (matching the design). Keep `aria-hidden="true"` on the icon span and keep the title span's class and text content intact so `frameByTitle()` selectors and existing WindowFrame.test.tsx assertions still pass. Add `hideClose` to `WindowBodyProps` and pass `hideClose={true}` from WindowFrame into WindowBody, then forward it onto `<AppShell ... hideClose={hideClose} />` (always true when rendered inside a WindowFrame — the traffic-light close is authoritative). Update the WindowBody memo comparator only if needed (hideClose is constant per frame, so it need not enter the comparator). Add the three test cases to `src/ui/WindowFrame.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/WindowFrame.test.tsx src/ui/MarketplaceWindows.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/WindowFrame.test.tsx src/ui/MarketplaceWindows.test.tsx` exits 0
    - `grep -c "window-chrome__title-group" src/ui/WindowFrame.tsx` returns >= 1
    - `grep -c "hideClose" src/ui/WindowFrame.tsx` returns >= 2 (WindowBodyProps + AppShell pass-through)
    - In a rendered WindowFrame, querying `.window-chrome__title-group .window-chrome__icon` and `.window-chrome__title-group .window-chrome__title` both resolve, icon precedes title
    - A rendered framed app shows exactly ONE element matching aria-label "Close" (the traffic-light), zero matching `Close {title}` (the AppShell inner ×)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Titlebar groups icon+title (icon first) for centering; the title text is preserved for selectors; WindowFrame forwards hideClose so framed apps render no inner ×; WindowFrame.test + MarketplaceWindows tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Titlebar centering CSS + gentler cascade offset</name>
  <read_first>
    - src/index.css (lines ~687-779 — the Phase-15 desktop + window-chrome block)
    - src/ui/useWindowManager.tsx (cascadePlace + CASCADE_OFFSET + DEFAULT_W/H)
    - src/ui/useWindowManager.test.tsx (existing cascade assertions — must stay green; check exact offset expectations)
    - src/ui/MarketplaceWindows.test.tsx (the cascade test asserts secondPos.x/y > firstPos.x/y — keep that invariant true)
    - design/VibeOS.dc.html (titlebar centering reference)
  </read_first>
  <action>
    In `src/index.css`, change `.window-chrome__titlebar` from `display:flex` to `display:grid; grid-template-columns: auto 1fr auto; align-items:center;` so the traffic-lights sit left, the title-group centers in the `1fr` column, and a trailing spacer (or empty `auto`) balances the right. Add a `.window-chrome__title-group { display:flex; align-items:center; justify-content:center; gap:6px; overflow:hidden; min-width:0; }` rule. Update `.window-chrome__title` to drop `flex:1` (the grid column now owns centering) while keeping the ellipsis/nowrap truncation and `color: var(--text, ...)`. Ensure `.window-chrome__icon` (if no rule exists) gets a small flex-shrink:0 so it does not collapse. In `useWindowManager.tsx`, tune the cascade so windows offset gently and predictably: keep `CASCADE_OFFSET` modest (e.g. 28 → confirm it reads from the last entry, offsets both axes, and clamps to viewport) and confirm the first window starts at a comfortable inset. If the existing offset already satisfies "gentle offset, stays within viewport, second window down-and-right of first," make only the minimal CSS change and leave the constant; do NOT break the `useWindowManager.test.tsx` cascade expectations — read them first and keep the assertions true (adjust the test ONLY if the design genuinely requires a different offset, and update both together).
  </action>
  <verify>
    <automated>npx vitest run src/ui/useWindowManager.test.tsx src/ui/MarketplaceWindows.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "window-chrome__title-group" src/index.css` returns >= 1
    - `grep -E "grid-template-columns:\s*auto 1fr auto" src/index.css` matches (titlebar uses the 3-column grid)
    - `npx vitest run src/ui/useWindowManager.test.tsx src/ui/MarketplaceWindows.test.tsx` exits 0 (cascade invariant preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The titlebar centers the icon+title group via a 3-column grid; the title still truncates; the cascade offsets new windows gently within the viewport without regressing the window-manager or windowed-open tests.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/AppShell.test.tsx src/ui/WindowFrame.test.tsx src/ui/useWindowManager.test.tsx src/ui/MarketplaceWindows.test.tsx` all green.
- `npx vitest run` (full suite) green — no regression across the ~600 existing tests.
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/hygiene.test.ts` green (no new banned tokens introduced; all new identifiers/classes neutral: `hideClose`, `window-chrome__title-group`).
</verification>

<success_criteria>
- A framed app renders NO redundant inner × (only the traffic-light close).
- Standalone AppShell still renders its inner × (default behavior preserved).
- The titlebar shows icon-then-title, grouped and centered.
- New windows cascade gently and stay within the viewport.
- Full suite + tsc + hygiene all green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-desktop-shell/16-01-SUMMARY.md`
</output>
