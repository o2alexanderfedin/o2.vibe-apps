---
phase: 16-desktop-shell
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - src/index.css
  - src/ui/DesktopShell.tsx
  - src/ui/DesktopShellReskin.test.tsx
  - src/ui/reducedMotion.test.tsx
  - src/hygiene.test.ts
autonomous: false
requirements: [PERF-01, WIN-08]
must_haves:
  truths:
    - "Under prefers-reduced-motion the blob animation is disabled and the blur is reduced (CSS media query) and a matchMedia-driven class/state reflects it for tests"
    - "Switching theme from one named theme to another visibly changes the wallpaper/chrome — the resolved CSS var (--wall / --text) on documentElement DIFFERS between two themes"
    - "Minimized windows are display:none (no compositing) — confirmed by an assertion"
    - "The hygiene gate explicitly covers the new Phase-16 source files"
    - "A live viewed-screenshot smoke confirms the desktop re-skins between at least two themes (orchestrator-run)"
  artifacts:
    - path: "src/index.css"
      provides: "@media (prefers-reduced-motion: reduce) degrade for blobs"
      contains: "prefers-reduced-motion"
    - path: "src/ui/DesktopShell.tsx"
      provides: "matchMedia-driven reduced-motion state/class injectable for tests"
      contains: "matchMedia"
    - path: "src/ui/DesktopShellReskin.test.tsx"
      provides: "theme re-skin acceptance — documentElement var differs across two themes"
      contains: "--wall"
    - path: "src/ui/reducedMotion.test.tsx"
      provides: "prefers-reduced-motion degrade test (PERF-01)"
      contains: "prefers-reduced-motion"
    - path: "src/hygiene.test.ts"
      provides: "explicit coverage assertion for Phase-16 new files"
      contains: "DesktopShell"
  key_links:
    - from: "src/ui/DesktopShell.tsx"
      to: "window.matchMedia('(prefers-reduced-motion: reduce)')"
      via: "reduced-motion state toggles a root class the CSS + tests can read"
      pattern: "prefers-reduced-motion|matchMedia"
    - from: "src/ui/DesktopShellReskin.test.tsx"
      to: "documentElement.style --wall/--text"
      via: "switch theme → assert resolved var changed"
      pattern: "getPropertyValue"
---

<objective>
Land PERF-01's concrete deliverable (the `prefers-reduced-motion` degrade for the blob layer) and make the phase headline — theme switching visibly re-skins the whole desktop — explicitly testable, then extend the hygiene gate to assert coverage of the new Phase-16 surfaces, and finish with the orchestrator-run live viewed-screenshot smoke per theme.

Purpose: The "no colors yet" gap from Phases 14-15 closes here with a hard, automated acceptance: a theme switch must change the wallpaper/chrome CSS vars on documentElement. PERF-01 ships its real, testable degrade (reduced-motion driven through an injectable/mocked matchMedia) rather than the deferred full GPU frame-timing detection. The final visual proof is a viewed screenshot the orchestrator captures.

Output: `@media (prefers-reduced-motion: reduce)` CSS + a matchMedia-driven reduced-motion state in DesktopShell; `DesktopShellReskin.test.tsx` (re-skin acceptance); `reducedMotion.test.tsx` (PERF-01); a hygiene-gate coverage assertion; and a human-verify checkpoint for the per-theme screenshot smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-desktop-shell/16-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Theme var contract (src/ui/VibeThemeProvider.tsx VIBE_THEMES): each theme sets --wall, --text, --b1..--b4,
  --glass, --glass2, --bord, --hi, --accentA, --accentB on document.documentElement via style.setProperty.
  Distinct values per theme — e.g. noir --text "#f5eeff" vs aurora --text "#f3f1ff"; --wall radial-gradients differ. -->

<!-- setTheme path: useVibeTheme().setTheme(name) → applyVibeTheme → documentElement.style.setProperty(...).
  Existing ThemeSelector.test.tsx proves: click Noir → documentElement.style.getPropertyValue("--text") === "#f5eeff". -->

<!-- Reduced-motion: window.matchMedia('(prefers-reduced-motion: reduce)'). In jsdom, matchMedia is not
  implemented by default — tests MUST stub window.matchMedia (return { matches, addEventListener, removeEventListener }).
  DesktopShell should read it via a small seam so a test can drive matches=true/false. -->

<!-- Existing CSS reduced-motion stub (16-PATTERNS.md ~414):
  @media (prefers-reduced-motion: reduce) { .desktop-shell__blob { animation: none !important; filter: blur(80px); } } -->

<!-- hygiene.test.ts (src/hygiene.test.ts): walks src/** + index.html; auto-covers new files. Add an EXPLICIT
  assertion that the Phase-16 files are in the scanned set so a future path regression is caught. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prefers-reduced-motion degrade (PERF-01) — CSS + matchMedia-driven state</name>
  <files>src/index.css, src/ui/DesktopShell.tsx, src/ui/reducedMotion.test.tsx</files>
  <read_first>
    - src/index.css (the .desktop-shell__blob rules + vibeFloat from plan 16-03)
    - src/ui/DesktopShell.tsx (where to add the reduced-motion seam/state)
    - .planning/research/PITFALLS.md (Pitfall 4 — reduced-motion + blur reduction; Pitfall 7 — no full-page restyle on theme switch)
    - .planning/phases/16-desktop-shell/16-CONTEXT.md (decision 6 — prefers-reduced-motion is the concrete deliverable; GPU frame-timing deferred)
    - src/ui/MarketplaceWindows.test.tsx (jsdom pointer-capture stub pattern — mirror for matchMedia stubbing)
  </read_first>
  <behavior>
    - Test 1: With window.matchMedia stubbed to return matches=true for '(prefers-reduced-motion: reduce)', DesktopShell renders the desktop-shell root carrying a reduced-motion marker (e.g. class `desktop-shell--reduced-motion` or data attribute) that tests can assert.
    - Test 2: With matchMedia stubbed to matches=false, the reduced-motion marker is ABSENT (normal animated path).
    - Test 3: The CSS contains an `@media (prefers-reduced-motion: reduce)` block that sets `animation: none` on `.desktop-shell__blob` (degrade is declared in CSS, the media-query path that needs no JS).
  </behavior>
  <action>
    (a) In `src/index.css`, add `@media (prefers-reduced-motion: reduce) { .desktop-shell__blob { animation: none !important; filter: blur(80px); } }` — the CSS degrade per Pitfall 4 (disable animation, the larger blur keeps the ambient look without per-frame animated compositing). Optionally also reduce `backdrop-filter` blur radius under the same query if it reads cheaply.
    (b) In `src/ui/DesktopShell.tsx`, add a small reduced-motion seam: read `window.matchMedia('(prefers-reduced-motion: reduce)')` in an effect (guarded for environments where matchMedia is undefined), store `.matches` in state, subscribe to its change event, and apply a `desktop-shell--reduced-motion` class (or `data-reduced-motion`) on the `.desktop-shell` root when true. This gives tests a JS-observable signal driven by a mockable matchMedia AND lets the JS path (e.g. future blob count reduction) hook in. Clean up the listener on unmount. Keep the CSS media query as the primary degrade; the JS marker is the testable hook.
    (c) Write `src/ui/reducedMotion.test.tsx` covering the three behaviors. Stub `window.matchMedia` (returning `{ matches, media, addEventListener, removeEventListener, addListener, removeListener, dispatchEvent, onchange }`) — drive matches true/false. For Test 3, read `src/index.css` from disk (mirror hygiene.test.ts's readFileSync approach) and assert the `prefers-reduced-motion` + `animation: none` block is present.
  </action>
  <verify>
    <automated>npx vitest run src/ui/reducedMotion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/reducedMotion.test.tsx` exits 0 with all three behaviors
    - `grep -c "prefers-reduced-motion" src/index.css` returns >= 1
    - `grep -E "@media \(prefers-reduced-motion: reduce\)" src/index.css` matches and the block sets `animation: none` on `.desktop-shell__blob`
    - `grep -c "matchMedia" src/ui/DesktopShell.tsx` returns >= 1
    - `grep -c "removeEventListener\|removeListener" src/ui/DesktopShell.tsx` returns >= 1 (listener cleanup)
    - matches=true → reduced-motion marker present; matches=false → absent (both asserted)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The blob layer degrades under prefers-reduced-motion via the CSS media query (animation off, blur adjusted); DesktopShell exposes a matchMedia-driven, test-mockable reduced-motion marker with a cleaned-up listener; PERF-01's concrete deliverable is proven offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme re-skin acceptance (phase headline) + minimized display:none + hygiene coverage</name>
  <files>src/ui/DesktopShellReskin.test.tsx, src/hygiene.test.ts</files>
  <read_first>
    - src/ui/ThemeSelector.test.tsx (the proven click-Noir → documentElement --text === "#f5eeff" assertion to extend across themes)
    - src/ui/VibeThemeProvider.tsx (VIBE_THEMES per-theme --wall/--text values — pick two themes with provably distinct --wall and --text)
    - src/ui/DesktopShell.tsx (renders within VibeThemeProvider; the MenuBar's ThemeSelector drives setTheme)
    - src/ui/MarketplaceWindows.test.tsx (minimize test pattern: minimized frame gets .window-chrome--minimized; CSS sets display:none)
    - src/index.css (.window-chrome--minimized { display:none } — confirm present)
    - src/hygiene.test.ts (the scan()/walk() structure to add an explicit Phase-16 coverage assertion)
  </read_first>
  <behavior>
    - Test 1 (re-skin headline): Render DesktopShell inside VibeThemeProvider + ServicesProvider. Read documentElement's resolved `--wall` (and `--text`) for theme A (e.g. aurora). Switch theme to B (e.g. noir) by clicking the menu-bar theme pill (or calling setTheme via a test harness). Assert `documentElement.style.getPropertyValue("--wall")` for B DIFFERS from A, AND `--text` for B DIFFERS from A. This proves the wallpaper/chrome re-skins live (THEME-02 at the desktop level — the phase headline).
    - Test 2 (PERF-01 minimized): Open a window, minimize it, assert the frame carries `window-chrome--minimized` AND that the CSS rule `.window-chrome--minimized { display:none }` exists (read index.css from disk) — minimized windows do not composite.
    - Test 3 (hygiene coverage): The hygiene gate's scanned file set includes the Phase-16 source files (DesktopShell.tsx, Dock.tsx, MenuBar.tsx, MinimalLauncher.tsx, iconForApp.tsx).
  </behavior>
  <action>
    (a) Create `src/ui/DesktopShellReskin.test.tsx`. Render `<ServicesProvider services={createTestServices()}><VibeThemeProvider><DesktopShell/></VibeThemeProvider></ServicesProvider>` (install the pointer-capture stubs as in MarketplaceWindows.test.tsx). Clear localStorage + documentElement.style in beforeEach. Capture `--wall` and `--text` after the default theme applies; click a different theme pill in the menu bar (via getByRole within the menu-bar theme group) — or, if reaching the pill through DesktopShell is brittle, drive setTheme through a thin VibeThemeProvider consumer harness — and assert both vars CHANGED. Also assert the new `--wall`/`--text` equal the target theme's verbatim VIBE_THEMES values (proving the correct theme applied, not just any change).
    (b) In the same file (or DesktopShellReskin.test.tsx), add the minimized display:none acceptance: open a window through the launcher, minimize it, assert `.window-chrome--minimized` on the frame; read `src/index.css` and assert it contains `.window-chrome--minimized` with `display: none`.
    (c) In `src/hygiene.test.ts`, add a focused `it(...)` that runs `walk(SRC_DIR)` and asserts the returned set includes `src/ui/DesktopShell.tsx`, `src/ui/Dock.tsx`, `src/ui/MenuBar.tsx`, `src/ui/MinimalLauncher.tsx`, `src/ui/iconForApp.tsx` (so a future path regression that stops scanning these new surfaces is caught). Do NOT weaken the banned-token set.
  </action>
  <verify>
    <automated>npx vitest run src/ui/DesktopShellReskin.test.tsx src/hygiene.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/DesktopShellReskin.test.tsx src/hygiene.test.ts` exits 0
    - Re-skin test asserts BOTH `--wall` and `--text` on documentElement DIFFER between two named themes AND match the target theme's VIBE_THEMES values
    - Minimized test asserts `.window-chrome--minimized` on the frame AND `display: none` present in index.css for that class
    - `grep -c "DesktopShell" src/hygiene.test.ts` returns >= 1 (explicit Phase-16 coverage assertion added)
    - hygiene gate still green (no banned tokens; banned set unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The theme re-skin headline is proven by an automated assertion (wallpaper/chrome vars change across two themes and match the target contract); minimized windows are display:none (no compositing); the hygiene gate explicitly covers the new Phase-16 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live viewed-screenshot smoke per theme (Aurora vs Noir)</name>
  <files>(verification only — no source files modified)</files>
  <what-built>
    The full Vibe OS desktop is now the root: themed animated wallpaper + blobs behind windows, a bottom dock (running indicators + magnifier + hover-scale), a top menu bar (wordmark + active-app name + 4-theme switcher + live clock), and the minimal launcher. Theme switching re-skins the wallpaper + chrome live (proven automatically in Task 2). The automated suite, tsc, hygiene gate, and a clean no-sourcemap build are all green.
  </what-built>
  <action>
    The ORCHESTRATOR (not the executor's automated tests) performs this visual smoke. Run the dev server, view the desktop in two themes (Aurora and Noir), capture and VIEW a screenshot of each, and confirm the wallpaper + chrome visibly re-skin between them. See `<how-to-verify>` for the exact steps. This is a manual/checkpoint gate — the executor pauses here and the orchestrator captures the screenshots.
  </action>
  <how-to-verify>
    The ORCHESTRATOR performs this visual smoke (not the executor's automated tests):
    1. Run the dev server (e.g. `npm run dev`) and open the app in a browser.
    2. With the Aurora theme active, capture a screenshot of the desktop (wallpaper + blobs + dock + menu bar; open 1-2 apps so windows + dock running-indicators are visible). VIEW the image — confirm the purple/aurora wallpaper, animated blobs, glass dock, and menu bar render correctly.
    3. Click the Noir theme pill in the menu bar. Capture and VIEW a second screenshot — confirm the wallpaper + blobs + dock + menu bar + open window chrome all visibly re-skinned to the Noir palette (magenta/teal on near-black). The two screenshots must look clearly different across wallpaper AND chrome.
    4. Confirm via F12 that no devtools-visible surface (dock tooltips, window titles, menu-bar text, class names) contains a banned token (synthesi*/AI/llm/generate/fake/mock).
    5. Open F12 → Sources and confirm no source maps are served (build hygiene; production build only).
  </how-to-verify>
  <verify>Orchestrator-run visual smoke: two viewed screenshots (Aurora, Noir) that look clearly different across wallpaper + chrome.</verify>
  <acceptance_criteria>
    - Two viewed screenshots (Aurora and Noir) show a clearly different wallpaper AND chrome — the desktop re-skins end to end (the phase headline, visually confirmed).
    - The dock shows running-indicator dots for open apps + the magnifier; hover-scale animates.
    - The menu bar shows wordmark + active-app name + theme switcher + a live clock.
    - No banned token visible in any devtools surface; no source maps served in the production build.
  </acceptance_criteria>
  <done>The orchestrator has viewed Aurora and Noir desktop screenshots, confirmed they differ across wallpaper + chrome, and confirmed no banned tokens and no served source maps.</done>
  <resume-signal>Type "approved" once both theme screenshots are viewed and confirm the desktop re-skins, or describe the visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/ui/reducedMotion.test.tsx src/ui/DesktopShellReskin.test.tsx src/hygiene.test.ts` all green.
- `npx vitest run` (full suite) green — zero regression.
- `npx tsc --noEmit` exits 0.
- `npm run build` succeeds with `build.sourcemap: false`.
- Live viewed-screenshot smoke (orchestrator): Aurora vs Noir desktops are visibly different across wallpaper + chrome.
</verification>

<success_criteria>
- PERF-01: prefers-reduced-motion disables blob animation + reduces blur (CSS) with a matchMedia-driven, test-mockable JS marker; minimized windows are display:none.
- Theme re-skin headline: an automated test proves --wall and --text on documentElement change (and match the target theme contract) across two themes.
- Hygiene gate explicitly covers the new Phase-16 surfaces.
- The orchestrator-run viewed-screenshot smoke confirms the desktop visibly re-skins between Aurora and Noir.
- Full suite + tsc + hygiene + build all green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-desktop-shell/16-04-SUMMARY.md`
</output>
