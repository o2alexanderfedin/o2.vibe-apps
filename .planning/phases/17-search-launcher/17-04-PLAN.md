---
phase: 17-search-launcher
plan: 04
type: execute
wave: 4
depends_on:
  - 17-03
files_modified:
  - src/ui/MinimalLauncher.tsx
  - src/ui/MinimalLauncher.test.tsx
autonomous: true
requirements:
  - CREATE-01
  - CREATE-02
  - CREATE-03
must_haves:
  truths:
    - "npm run test exits 0 with all tests passing (≥ 636 existing + Phase 17 new tests)"
    - "npx tsc --noEmit exits 0"
    - "npm run build exits 0 with no source maps in dist/"
    - "hygiene gate passes: no banned tokens in any .ts/.tsx/.css/.html source file"
    - "MinimalLauncher.tsx is deleted; no production code imports it"
    - "MinimalLauncher.test.tsx is deleted (tests migrated to SearchLauncherPanel.test.tsx in Plan 01)"
    - "SearchLauncherPanel.tsx is in the hygiene gate scan (walk includes it automatically)"
    - "Focus-not-stolen test passes (in SearchLauncherPanel.test.tsx from Plan 01)"
    - "Cache-hit-instant test passes (in SearchLauncherPanel.integration.test.tsx from Plan 03)"
  artifacts:
    - path: "src/ui/MinimalLauncher.tsx"
      provides: "DELETED — superseded by SearchLauncherPanel.tsx"
    - path: "src/ui/MinimalLauncher.test.tsx"
      provides: "DELETED — tests migrated to SearchLauncherPanel.test.tsx"
  key_links:
    - from: "npm run test"
      to: "full suite ≥ 636 tests green"
      via: "vitest run"
      pattern: "Tests passed"
    - from: "npm run build"
      to: "dist/ with no .map files"
      via: "vite build"
      pattern: "build complete"
---

<objective>
Delete MinimalLauncher.tsx and its test file, then run the full verification suite to confirm Phase 17 is complete with no regressions.

Purpose: MinimalLauncher is now superseded by SearchLauncherPanel. Deleting it removes a dangling file that DesktopShell no longer imports, prevents the hygiene gate explicit-list from referencing a deleted file, and keeps the codebase clean. The full suite run (npm run test + tsc + build) is the final acceptance gate for Phase 17.

Output: MinimalLauncher.tsx and MinimalLauncher.test.tsx deleted; full suite confirmed green.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-search-launcher/17-CONTEXT.md
@.planning/phases/17-search-launcher/17-03-SUMMARY.md

Key source files to read before implementing:
@src/ui/DesktopShell.tsx (verify MinimalLauncher is NOT imported — Plan 02 removed it)
@src/hygiene.test.ts (verify MinimalLauncher.tsx is NOT in the explicit list — Plan 02 removed it)
</context>

<interfaces>
<!-- No new interfaces. This plan is deletion + final verification. -->

Pre-deletion checklist (executor must verify ALL before deleting):
```
grep -rn "MinimalLauncher" src/ --include="*.ts" --include="*.tsx"
```
Expected result: ZERO references in any .ts or .tsx file outside of:
  - src/ui/MinimalLauncher.tsx itself
  - src/ui/MinimalLauncher.test.tsx itself
If any OTHER file still imports MinimalLauncher, stop and fix that import first.

Build verification target — dist/ must NOT contain .map files:
```
npm run build && ls dist/*.map 2>/dev/null && echo "SOURCE MAPS FOUND — FAIL" || echo "no source maps — OK"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion audit and delete MinimalLauncher files</name>
  <files>src/ui/MinimalLauncher.tsx, src/ui/MinimalLauncher.test.tsx</files>

  <read_first>
    - src/ui/DesktopShell.tsx (confirm line "MinimalLauncher" does NOT appear — Plan 02 removed it)
    - src/hygiene.test.ts (confirm "MinimalLauncher.tsx" does NOT appear in the explicit list — Plan 02 removed it)
  </read_first>

  <action>
    STEP 1 — Pre-deletion reference audit:
    Run: grep -rn "MinimalLauncher" /Volumes/Unitek-B/Projects/o2.vibe-apps/src/ --include="*.ts" --include="*.tsx"

    Expected output: only lines FROM MinimalLauncher.tsx and MinimalLauncher.test.tsx themselves.
    If any other file still imports MinimalLauncher, STOP — fix that import before proceeding.
    (DesktopShell.tsx was fixed in Plan 02; desktopShellTestKit.tsx never imported MinimalLauncher.)

    STEP 2 — Delete both files:
    Use the Bash tool to delete both files:
    rm /Volumes/Unitek-B/Projects/o2.vibe-apps/src/ui/MinimalLauncher.tsx
    rm /Volumes/Unitek-B/Projects/o2.vibe-apps/src/ui/MinimalLauncher.test.tsx

    STEP 3 — Verify deletion:
    Run: ls /Volumes/Unitek-B/Projects/o2.vibe-apps/src/ui/MinimalLauncher* 2>&1
    Expected: "No such file or directory"

    STEP 4 — Confirm hygiene.test.ts references: the test `it("explicitly covers the Phase-16 and Phase-17 desktop-shell source files...")` must NOT list MinimalLauncher.tsx (Plan 02 removed it). The test should now PASS because MinimalLauncher.tsx is deleted — if it still listed the file, the test would fail with "file not found in scanned set". Verify by inspecting hygiene.test.ts lines 161–170.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && ls src/ui/MinimalLauncher* 2>&1; grep -rn "MinimalLauncher" src/ --include="*.ts" --include="*.tsx" | grep -v "MinimalLauncher" | wc -l</automated>
    First command: "No such file or directory" (both files gone).
    Second command: 0 (no remaining references in non-MinimalLauncher files).
  </verify>

  <acceptance_criteria>
    - `ls src/ui/MinimalLauncher.tsx 2>&1` returns "No such file or directory"
    - `ls src/ui/MinimalLauncher.test.tsx 2>&1` returns "No such file or directory"
    - `grep -rn "MinimalLauncher" src/ --include="*.ts" --include="*.tsx"` returns 0 lines
    - `grep '"src/ui/MinimalLauncher.tsx"' src/hygiene.test.ts` returns 0 lines (was removed in Plan 02)
  </acceptance_criteria>

  <done>Both MinimalLauncher files deleted; zero remaining references in source; hygiene.test.ts does not reference the deleted file.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification — test + tsc + build + hygiene</name>
  <files></files>

  <read_first>
    No files to read — this task runs verification commands only.
  </read_first>

  <action>
    Run the full verification suite in order. Each step must pass before proceeding.

    STEP 1 — Full test suite:
    cd /Volumes/Unitek-B/Projects/o2.vibe-apps && npm run test 2>&1 | tail -30

    Expected: All tests pass. Look for "Tests passed" or equivalent. Count must be ≥ 636 (Phase 17 adds ~20 new tests: ~12 panel unit tests + ~4 integration tests + ~4 launcherUtils tests). No test may say "fail" or "error".

    If any test fails:
    - A SearchLauncherPanel unit test failure: the component implementation in Plan 01 is wrong — fix SearchLauncherPanel.tsx
    - A DesktopShell integration test failure: Plan 02 wiring is wrong — fix DesktopShell.tsx
    - An integration test failure: fixture or load.ts is wrong — fix in Plan 03 files
    - A hygiene test failure: banned token leaked into a new source file — identify with the scan and fix the file
    DO NOT modify test files to make them pass; fix the implementation.

    STEP 2 — TypeScript:
    npx tsc --noEmit 2>&1

    Expected: 0 errors (empty output or just "Done."). If TypeScript errors appear, fix the source files — do NOT add `// @ts-ignore` or `any` casts unless the error is in an existing test file that previously had them.

    STEP 3 — Production build:
    npm run build 2>&1 | tail -20

    Expected: build completes without errors. Check that dist/ contains NO .map files (vite.config.ts sets `build.sourcemap: false`). Run: `ls dist/*.map 2>/dev/null | wc -l` → must be 0.

    STEP 4 — Hygiene gate spot-check:
    npx vitest run src/hygiene.test.ts 2>&1 | tail -10

    Expected: All 3 hygiene tests pass. The walk-based scan covers SearchLauncherPanel.tsx automatically.
    The explicit Pitfall-11 list now includes SearchLauncherPanel.tsx (added in Plan 02).
    No banned tokens appear in any of the Phase 17 source files.

    STEP 5 — Focus-not-stolen assertion:
    npx vitest run src/ui/SearchLauncherPanel.test.tsx -t "focus" 2>&1 | tail -10
    Expected: passes (the test asserting document.activeElement !== input on mount).

    STEP 6 — Cache-hit assertion:
    npx vitest run src/ui/SearchLauncherPanel.integration.test.tsx -t "cache" 2>&1 | tail -10
    Expected: passes (the test asserting transport call count === 1 after two describe calls).

    If ALL steps pass, Phase 17 is done. Document the final test count in the SUMMARY.
  </action>

  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && npm run test 2>&1 | grep -E "Tests passed|passed|failed|error" | tail -5</automated>
    Must show tests passed, zero failed, zero errors.
  </verify>

  <acceptance_criteria>
    - `npm run test` exits 0; test count ≥ 636; zero failures; zero errors
    - `npx tsc --noEmit` exits 0 (zero TypeScript errors)
    - `npm run build` exits 0; `ls dist/*.map 2>/dev/null | wc -l` returns 0 (no source maps)
    - `npx vitest run src/hygiene.test.ts` exits 0; all 3 hygiene tests pass
    - `grep -rn "SearchLauncherPanel" src/ui/DesktopShell.tsx` returns ≥ 2 lines (import + JSX)
    - `grep -rn "MinimalLauncher" src/` returns 0 lines
    - The focus-not-stolen test passes AND is non-empty: `npx vitest run src/ui/SearchLauncherPanel.test.tsx -t "focus"` exits 0 and its output shows at least 1 test run (NOT "no test found"). Plan 01 guarantees the focus test title contains "focus".
    - The cache-hit test passes AND is non-empty: `npx vitest run src/ui/SearchLauncherPanel.integration.test.tsx -t "cache"` exits 0 and shows ≥ 1 test run. Plan 03's TEST TITLE MANDATE guarantees Test 2's title contains "cache".
    - The describe→produce test passes AND is non-empty: `npx vitest run src/ui/SearchLauncherPanel.integration.test.tsx -t "transport"` exits 0 and shows ≥ 1 test run. Plan 03's TEST TITLE MANDATE guarantees Test 1's title contains "transport".
    - NOTE: a `-t` filter that matches zero tests makes vitest exit 0 with "no test files found" / "0 tests" — a FALSE pass. Each of the three `-t` checks above MUST also assert the run count ≥ 1 (e.g. grep the output for a non-zero "Tests" line), so a renamed/missing test fails loudly here. The full-suite green check (line above) is the primary gate; these `-t` checks are confirmation that the three named behaviors specifically exist and pass.
  </acceptance_criteria>

  <done>Full suite green (≥ 636 tests); tsc clean; build produces no source maps; hygiene gate passes; MinimalLauncher fully removed; Phase 17 acceptance criteria satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deleted file → no dangling imports | Deletion is safe only after confirming zero imports remain (audit in Task 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Tampering | deletion of MinimalLauncher | accept | Pre-deletion audit confirms zero imports; git history preserves the file if rollback is needed |
| T-17-11 | Information Disclosure | dist/ source maps | mitigate | `npm run build` verified to produce zero .map files; build.sourcemap: false is already set in vite.config.ts |
</threat_model>

<verification>
The verification IS the task. Run:
```
cd /Volumes/Unitek-B/Projects/o2.vibe-apps
npm run test && npx tsc --noEmit && npm run build
```
All three must exit 0. Phase 17 is complete when this compound command succeeds.
</verification>

<success_criteria>
Phase 17 is done when ALL of the following are simultaneously true:
1. npm run test passes with ≥ 636 tests, 0 failures
2. npx tsc --noEmit passes with 0 errors
3. npm run build completes with 0 source maps in dist/
4. hygiene.test.ts passes (3/3 tests green, SearchLauncherPanel.tsx in explicit list)
5. MinimalLauncher.tsx and MinimalLauncher.test.tsx are deleted from src/
6. SearchLauncherPanel is the launcher rendered by DesktopShell
7. Focus-not-stolen test passes (panel does not steal input focus from open windows)
8. Cache-hit-instant test passes (no extra transport call on repeated same-text describe)
9. Describe→produce path tested offline with captured pomodoro-timer fixture
</success_criteria>

<output>
After completion, create `.planning/phases/17-search-launcher/17-04-SUMMARY.md` summarizing:
- Final test count (total passing)
- Files deleted (MinimalLauncher.tsx, MinimalLauncher.test.tsx)
- All Phase 17 acceptance criteria confirmed satisfied
- CREATE-01, CREATE-02, CREATE-03 requirements closed

Also update .planning/ROADMAP.md:
- Phase 17 Plans: "4 plans"
- Plan list:
  - [x] 17-01-PLAN.md — slugFromText utility + SearchLauncherPanel component + unit tests
  - [x] 17-02-PLAN.md — DesktopShell integration: wire describe→produce + hygiene gate update
  - [x] 17-03-PLAN.md — Fixture + describe→produce integration tests + cache-hit test
  - [x] 17-04-PLAN.md — Final verification: delete MinimalLauncher, full suite green
</output>
