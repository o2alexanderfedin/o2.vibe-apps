---
phase: 11-reliability-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - src/execution/delegatedValidation.test.tsx
  - src/execution/delegatedNoOp.test.tsx
autonomous: true
requirements:
  - RELY-01
  - RELY-02
  - RELY-03

must_haves:
  truths:
    - "A wrong-typed known field (e.g. display becomes a number) keeps prior state — proven by test asserting the display renders unchanged after a corrupt handler response"
    - "Extra unknown keys in the handler response merge without error — proven by test asserting known fields are intact after merge"
    - "A valid partial update (only some known fields) merges — proven by test asserting updated field changed and unmentioned field kept"
    - "An action with no produced handler is a no-op — no throw, no state change, button re-enables"
    - "An unknown/unhandled action (handler returns {error}) is a no-op — no throw, no state change, button re-enables"
    - "A handler that throws is a no-op — no throw visible to the user, no state change"
    - "Validation reject causes ZERO new transport calls beyond the original produce — asserted by spying the injected transport"
    - "No mechanic-revealing string or banned token appears in any new test file or assertion message"
  artifacts:
    - path: "src/execution/delegatedValidation.test.tsx"
      provides: "Tests for keep-prior (wrong type), extra-keys (passthrough), valid-partial (merge), and zero-transport-call assertion"
      exports: []
    - path: "src/execution/delegatedNoOp.test.tsx"
      provides: "Tests for no-op paths: no handler, unknown action, {error}, throw"
      exports: []
  key_links:
    - from: "delegatedValidation.test.tsx"
      to: "src/execution/stateSchema.ts (deriveStateSchema)"
      via: "tests exercise DelegatedShell which calls deriveStateSchema internally"
      pattern: "deriveStateSchema"
    - from: "delegatedNoOp.test.tsx"
      to: "src/execution/delegated.tsx (DelegatedShell)"
      via: "renders DelegatedShell with a spy transport; asserts no state change + 0 extra transport calls"
      pattern: "runHandler.*unusedTransport\|spyTransport"
---

<objective>
Add the complete test suite proving RELY-01 (keep-prior on bad shape), RELY-02 (no-op on unknown action), and RELY-03 (zero extra round-trips on validation reject). Tests use real captured-Haiku fixtures and injected services (IoC/DI). No production code changes — tests validate what Plan 01 wired.

Purpose: Without tests, the validation wired in Plan 01 is invisible to CI. These tests lock the four no-op paths (RELY-02) and the three validation outcomes (wrong-type/keep-prior, extra-keys/merge, valid-partial/merge) and prove that a validation reject never triggers an additional model call (RELY-03 zero-round-trip guarantee).

Output:
- `src/execution/delegatedValidation.test.tsx` — validation outcome tests (keep-prior, extra-keys, valid-partial, zero-transport)
- `src/execution/delegatedNoOp.test.tsx` — no-op path tests (no handler, unknown action, {error}, throw)
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/PROJECT.md
@/Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/ROADMAP.md
@/Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/STATE.md
@/Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/phases/11-reliability-hardening/11-01-SUMMARY.md

<interfaces>
<!-- Key contracts for test authorship. Extracted from codebase. -->

From src/execution/delegated.tsx (post-Plan-01 state):
```typescript
export type DelegatedState = Record<string, unknown>;

export interface DelegatedModule {
  initialState: DelegatedState;
  view: (state: DelegatedState) => ReactNode;
  actionSpec: string;
}

// Factory: derives schema from module.initialState once, passes to DelegatedShell:
export function makeDelegatedComponent(
  appType: string,
  module: DelegatedModule,
  runHandler: RunHandler,  // (intent: string, input: unknown) => Promise<HandlerResult>
): ComponentType

// Shell — the component under test (rendered via makeDelegatedComponent):
export function DelegatedShell({ appType, module, runHandler, stateSchema }: DelegatedShellProps): React.ReactElement
```

From src/execution/instantiate.ts (RunHandler type):
```typescript
export type RunHandler = (intent: string, input: unknown) => Promise<HandlerResult>;
```

From src/execution/handler.ts:
```typescript
export interface HandlerResult {
  data?: unknown;
  error?: string;
}
export const NEUTRAL_HANDLER_ERROR = "This operation could not be completed.";
```

From src/services/testServices.ts:
```typescript
export function createTestServices(overrides?: TestServicesOverrides): Services;
export function createInMemoryRegistry(): Registry;
export const unusedTransport: TransportFn;  // throws if called
export function cannedTransport(text: string): TransportFn;

// For counting transport calls:
// const calls: string[] = [];
// const spyTransport: TransportFn = (url, init) => { calls.push(url); return ... };
```

Fixtures directory: `src/test/fixtures/`
- `delegated-calculator.code.txt` — real JSX delegated module (initialState: { display: "0", expr: "" })
- `delegated-calc-reducer.code.txt` — real reducer handler (routes by payload; returns { data: { state } } or { error })
- `handler-calc-key.code.txt` — real key-append handler
- `handler-calc-equals.code.txt` — real equals handler

Pattern for loading fixtures (from existing tests):
```typescript
const FIXTURE_DIR = join(dirname(fileURLToPath(import.meta.url)), "..", "test", "fixtures");
const read = (f: string): string => readFileSync(join(FIXTURE_DIR, f), "utf8");
```

Pattern for rendering DelegatedShell (from delegated.test.tsx):
```typescript
const mod = instantiateDelegated(transpile(MODULE, { filename: "calc.tsx" }));
const App = makeDelegatedComponent("calculator", mod, routedRunHandler);
render(createElement(App));
```

Pattern for a runHandler that returns a canned result without a transport:
```typescript
const runHandler = (_intent: string, _input: unknown) =>
  Promise.resolve<HandlerResult>({ data: { state: { display: 42, expr: "" } } });
```

Pattern for a runHandler that returns {error}:
```typescript
const runHandler = (_intent: string, _input: unknown) =>
  Promise.resolve<HandlerResult>({ error: "unknown action" });
```

Pattern for a runHandler that throws:
```typescript
const runHandler = (_intent: string, _input: unknown): Promise<HandlerResult> => {
  throw new Error("handler threw");
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write validation outcome tests (keep-prior, extra-keys, valid-partial, zero-transport)</name>
  <files>src/execution/delegatedValidation.test.tsx</files>
  <read_first>
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/execution/delegated.tsx — post-Plan-01 state; imports, DelegatedModule, makeDelegatedComponent signature
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/execution/delegated.test.tsx — existing test patterns (fixture loading, renderDelegatedCalc, routedRunHandler pattern)
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/execution/delegatedReal.test.tsx — real fixture usage pattern
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/services/testServices.ts — createTestServices, unusedTransport, cannedTransport
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/test/fixtures/delegated-calculator.code.txt — initialState shape: { display: "0", expr: "" }
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/test/fixtures/handler-calc-key.code.txt — real handler shape reference
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/phases/11-reliability-hardening/11-CONTEXT.md — specifics block for exact assertion targets
  </read_first>
  <behavior>
    Test 1 — wrong-typed known field → keep prior state:
    - Render DelegatedShell with a runHandler that returns `{ data: { state: { display: 42, expr: "" } } }` (display is number, initialState has display as string "0")
    - Click a `data-action` button
    - Assert the display element still shows "0" (prior state), not "42" or blank
    - Assert the component did not unmount or throw (still renders)

    Test 2 — extra unknown keys → merge succeeds, known fields intact:
    - Render DelegatedShell with a runHandler that returns `{ data: { state: { display: "5", expr: "5", unknownKey: true } } }`
    - Click a button
    - Assert display shows "5" (known field updated)
    - Assert the component still renders (no crash from extra key)

    Test 3 — valid partial update → merges:
    - Render DelegatedShell with a runHandler that returns `{ data: { state: { display: "9" } } }` (only one of two known fields)
    - Click a button
    - Assert display shows "9" (updated field)
    - Assert expr field is still the prior value (empty string or "0" — unchanged by the partial merge)

    Test 4 — zero extra transport calls on validation reject:
    - Set up a spy transport that counts calls: `let transportCalls = 0; const spy: TransportFn = () => { transportCalls++; return Promise.resolve(...); }`
    - Provide a runHandler (NOT the full services stack) that returns the corrupt response — the runHandler is injected directly, NOT going through transport
    - Alternatively: use `createTestServices` with the spy transport and a canned handler in the registry so a cache-miss triggers the transport; after the corrupt response is returned, assert `transportCalls === 0` additional calls (only the initial produce)
    - The assertion: `expect(transportCalls).toBe(0)` (no additional model round-trip beyond the one that returned the corrupt response)

    For Tests 1-3, the simplest approach: construct the runHandler as a direct function returning a Promise (no transport involved at all — proving that a canned bad response triggers validation, not a network call). Transport call count is provably 0 because no transport is wired.

    For Test 4 (explicit zero-transport assertion): provide a runHandler that counts its own invocations and a spy that would be called if any re-produce happened. Since the direct-runHandler approach does not use a transport, assert `spyTransport` was never called — demonstrate the pattern clearly in the test.
  </behavior>
  <action>
    Create `src/execution/delegatedValidation.test.tsx`.

    Use the same import pattern as `delegated.test.tsx` and `delegatedReal.test.tsx`:
    - `readFileSync` + `fileURLToPath` + `dirname` + `join` for fixtures
    - `afterEach(() => cleanup())` after all imports
    - Named `describe` blocks, `it` assertions, `waitFor` for async state updates

    Load the real `delegated-calculator.code.txt` fixture for the module (so tests use a real Haiku-produced initialState `{ display: "0", expr: "" }`). Transpile it with `transpile(MODULE_SOURCE, { filename: "calc.tsx" })` and instantiate with `instantiateDelegated`.

    For each test, inject a custom `runHandler` directly (not via createTestServices + transport) so:
    1. No transport is invoked
    2. The runHandler returns an exact canned HandlerResult for the test scenario
    3. Transport call count is provably 0

    The test for zero transport calls should make this explicit:
    - Build a spy: `let extraCalls = 0; const spyTransport: TransportFn = () => { extraCalls++; return Promise.reject(new Error("unexpected")); }`
    - The injected `runHandler` returns the canned result WITHOUT using the spy
    - After clicking and state settling: `expect(extraCalls).toBe(0)` — proving no transport was invoked by the validation path

    Click interaction: use `userEvent.setup()` and `user.click()` on a `data-action` element. Use `waitFor` to assert post-click state.

    Identify the display element by `container.querySelector('[style*="text-align: right"]')` OR by reading the data-testid if the fixture has one. Check `delegated-calculator.code.txt` for the actual markup — the display element has `style` with `text-align: right`. Use the same selector pattern as `delegatedReal.test.tsx`.

    All assertion messages and comments must be mechanic-neutral (no banned tokens).

    File header comment: brief, neutral — explain what the tests assert without narrating the generation mechanic.
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && npm test -- --reporter=verbose 2>&1 | grep -E "delegatedValidation|PASS|FAIL|✓|✗|×" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/execution/delegatedValidation.test.tsx` exists
    - The file imports from `"zod/mini"` — NO; the test file does NOT need to import zod directly. It tests behavior via DelegatedShell renders
    - The file imports from `"./delegated"` (makeDelegatedComponent, instantiateDelegated) and `"./transpile"` and `"./handler"` (executeHandlerSource) as needed
    - Test "wrong-typed known field → keep prior": after a corrupt display (number instead of string), the display element text is still "0" (prior), not "42"
    - Test "extra unknown keys → merge ok": display shows "5" after the update; component still renders
    - Test "valid partial → merges": display shows "9"; other known field expr is unchanged
    - Test "zero extra transport calls": `extraCalls === 0` after clicking with a corrupt runHandler response
    - `npm test` exits 0 — all 399+ tests including these 4 new ones pass
    - No banned token (synthesize/synthesized/synthesis) in the test file
  </acceptance_criteria>
  <done>Four validation outcome tests written and passing; zero-transport assertion explicit; npm test green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write no-op path tests (no handler, unknown action, {error}, throw)</name>
  <files>src/execution/delegatedNoOp.test.tsx</files>
  <read_first>
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/execution/delegated.tsx — post-Plan-01 state; DelegatedShell onClick (lines 170-193); the existing no-op paths (missing data-action → early return; handler returns {error} → next undefined → no merge; throw → caught → state unchanged)
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/execution/delegated.test.tsx — patterns for rendering and clicking
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/src/services/testServices.ts — unusedTransport (useful as spy proving no transport call)
    - /Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/phases/11-reliability-hardening/11-CONTEXT.md — RELY-02 decisions (four no-op paths to cover)
  </read_first>
  <behavior>
    Test A — action with no produced handler (runHandler never called for the action):
    - Inject a runHandler that returns `{ error: "no handler" }` for the clicked action
    - Click a data-action button
    - Assert: state does not change (display still "0"); component still renders; busy state cleared (button re-enables)

    Test B — unknown/unhandled action (handler returns { error } with no { data }):
    - Inject a runHandler returning `{ error: "unhandled action" }` (no data field)
    - Click a data-action button
    - Assert: next is undefined (since res.data?.state is undefined when there is no data field); no setState called; display stays "0"; component still renders

    Test C — handler { error } path (same as B but explicit label for coverage clarity):
    - Named separately to document the { error } contract: when a handler returns { error }, res.data is undefined, next is undefined, the existing guard `if (next && typeof next === "object")` prevents any merge
    - Assert: state unchanged, no throw, app renders

    Test D — handler throw/timeout (runHandler itself throws):
    - Inject a runHandler that throws: `(_intent: string, _input: unknown) => { throw new Error("timeout"); }`
    - Click a data-action button
    - Assert: the outer catch in DelegatedShell catches the throw; state is unchanged (display still "0"); component still renders (no unmount); `waitFor` settles without error

    All four: assert `data-busy` or `aria-busy` attribute is cleared after the action settles (busy state resets via the `finally` block). Use `waitFor(() => expect(button).not.toHaveAttribute("data-busy"))` or equivalent to confirm the finally block ran.

    None of these paths involve transport calls — the runHandler is injected directly. Assert by using `unusedTransport` from testServices and verifying no error was thrown from it (if it were called, the test would fail from its throw).
  </behavior>
  <action>
    Create `src/execution/delegatedNoOp.test.tsx`.

    Use the same import/fixture pattern as the other delegated test files. Load `delegated-calculator.code.txt` as the module (real Haiku fixture, initialState `{ display: "0", expr: "" }`). Transpile and instantiate via `transpile` + `instantiateDelegated`.

    For each test, wrap the injected `runHandler` in a function matching the `RunHandler` signature: `(intent: string, input: unknown) => Promise<HandlerResult>`.

    For Test D (handler throw), note: the DelegatedShell wraps the handler call in `try { ... } catch (err) { logger.error(...); }` — the throw should be caught and the component should NOT unmount. Use `waitFor` with a short timeout to confirm the display text is still "0" after the click and that the test does not throw at the React level.

    For the busy-state assertion: after clicking, use `waitFor(() => { expect(container.querySelector('[data-busy]')).toBeNull(); })` to confirm the finally block ran and busy was cleared. Alternatively assert `aria-busy` is gone.

    The module's `data-action` buttons are in the calculator fixture. Pick a button with a stable action name (e.g. `[data-action="clear"]` or `[data-action="1"]`). Use `container.querySelector('[data-action="1"]')` to click, consistent with `delegatedReal.test.tsx`.

    All assertion messages and comments must be mechanic-neutral (no banned tokens). The no-op concept is expressed in test names as "no state change" or "does nothing" — never narrating the mechanic.

    Organize as one `describe` block with four `it` cases (A, B, C, D) named descriptively (e.g. "handler returns {error}: state is unchanged and app stays responsive").
  </action>
  <verify>
    <automated>cd /Volumes/Unitek-B/Projects/o2.vibe-apps && npm test -- --reporter=verbose 2>&1 | grep -E "delegatedNoOp|PASS|FAIL|✓|✗|×" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/execution/delegatedNoOp.test.tsx` exists
    - Four `it` cases present covering the four no-op paths (A: no handler / returns {error}; B: unknown action / {error} no data; C: explicit {error} with no state field; D: handler throw)
    - Each case asserts display text is still "0" (prior state unchanged) after the click
    - Each case asserts the component still renders (no unmount/crash)
    - Each case asserts busy state cleared (aria-busy gone or data-busy attribute removed) via waitFor
    - `npm test` exits 0 — all 399+ + 4 validation tests + 4 no-op tests pass (≥407 total)
    - No banned token in the test file
    - `npm run typecheck` exits 0 (tsc clean with the new test files)
  </acceptance_criteria>
  <done>Four no-op path tests written and passing; busy state cleared assertion present; full suite green at ≥407 tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test doubles → DelegatedShell | Tests inject controlled runHandler functions instead of real transport; proves the validation boundary without network |
| fixture source → test instantiation | Fixture files are committed source (trusted); instantiated through the same path as production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Repudiation | Test coverage gaps | mitigate | These tests lock the four no-op paths and three validation outcomes, preventing silent regression on the keep-prior guarantee |
| T-11-06 | Information Disclosure | Test assertion messages | mitigate | All assertion messages and test descriptions are mechanic-neutral; no banned token family in test file (hygiene gate covers src/**) |
| T-11-07 | Denial of Service | handler throw in test D | accept | DelegatedShell already wraps handler in try/catch; the throw test verifies the catch path works and the component recovers — no action needed beyond existing code |
</threat_model>

<verification>
After both tasks complete:

1. `npm run typecheck` exits 0 (includes new test files)
2. `npm test` exits 0 with all tests passing (≥407: 399 existing + 4 validation + 4 no-op)
3. `npm run build` exits 0; no `.map` files in `dist/`
4. `grep -r "synthesize\|synthesized\|synthesis" src/ | grep -v "node_modules"` returns empty
5. `grep "delegatedValidation\|delegatedNoOp" src/` — both test files present
6. Transport spy asserts: the zero-extra-transport-call test passes (extraCalls === 0)
7. Busy-state assertion: each no-op test confirms aria-busy/data-busy is cleared after the action
</verification>

<success_criteria>
- Eight new tests (4 validation + 4 no-op) pass and are in the full suite
- Keep-prior behavior proven by test: wrong-typed known field → display still "0"
- No-op behavior proven by test: all four no-op paths do not change state and do not crash
- Zero-extra-round-trips proven by test: spy transport count is 0 after validation reject
- tsc 0 errors, full test suite green (≥407 tests), build clean (no sourcemaps)
- Devtools hygiene gate green (no banned token in any new test file)
- RELY-01, RELY-02, RELY-03 all have test coverage
</success_criteria>

<output>
After completion, create `/Volumes/Unitek-B/Projects/o2.vibe-apps/.planning/phases/11-reliability-hardening/11-02-SUMMARY.md`
</output>
