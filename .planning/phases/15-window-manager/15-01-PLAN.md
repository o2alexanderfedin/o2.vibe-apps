---
phase: 15-window-manager
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ui/useDrag.ts
  - src/ui/useDrag.test.tsx
autonomous: true
requirements: [WIN-03]
must_haves:
  truths:
    - "Pointerdown on the drag handle captures the pointer so drag tracks across any subtree"
    - "Drag writes position imperatively via requestAnimationFrame, not React state, during move"
    - "Final position commits to React state exactly once on pointerup"
    - "The dragged element is clamped to the viewport (cannot leave the visible area)"
    - "Dragging the handle does not start text selection or steal input focus from the window body"
  artifacts:
    - path: "src/ui/useDrag.ts"
      provides: "useDrag hook — pointer-capture drag with rAF imperative writes + viewport clamp"
      contains: "export function useDrag"
    - path: "src/ui/useDrag.test.tsx"
      provides: "RED-first tests: pointer capture, clamp, rAF, focus-not-stolen, no-preventDefault-on-body"
      contains: "setPointerCapture"
  key_links:
    - from: "useDrag.handlePointerDown"
      to: "drag handle element"
      via: "e.currentTarget.setPointerCapture(e.pointerId)"
      pattern: "setPointerCapture"
    - from: "pointermove handler"
      to: "elementRef.current.style"
      via: "requestAnimationFrame imperative transform/left-top write"
      pattern: "requestAnimationFrame"
    - from: "pointerup handler"
      to: "onCommit(x, y)"
      via: "single committed-position callback"
      pattern: "onCommit"
---

<objective>
Build `useDrag` — the hand-rolled pointer-capture drag hook that moves a window frame element. This is the lowest-level, self-contained piece of the window manager: it has no codebase analog and no dependency on the rest of Phase 15, so it ships first and in parallel with `useWindowManager`.

The hook must defend Research Pitfalls 1 (pointer capture lost), 2 (setState thrash → use rAF + imperative writes, commit to state only on pointerup), and 12 (focus stealing — preventDefault only on the handle, never the body). It clamps the moved element to the viewport so windows stop at screen edges (WIN-03).

Purpose: A window can be dragged by its titlebar cleanly across the desktop and across another window's content area with no sticking, no text-selection, no per-frame React reconciliation, and clamped at viewport edges.
Output: `src/ui/useDrag.ts` (hook, ~60 lines) + `src/ui/useDrag.test.tsx` (RED-first behavior tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-window-manager/15-CONTEXT.md
@.planning/phases/15-window-manager/15-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The hook signature to implement, from 15-PATTERNS.md lines 182-201. -->
<!-- elementRef points at the WindowFrame outer div (the thing that moves). -->
<!-- onCommit fires once on pointerup with the final clamped (x, y). -->

export interface UseDragOptions {
  elementRef: React.RefObject<HTMLElement>;   // element to move (window frame div)
  initialX: number;                            // initial position from WindowEntry
  initialY: number;
  onCommit: (x: number, y: number) => void;    // called with final clamped position on pointerup
}

export function useDrag(options: UseDragOptions): {
  handlePointerDown: (e: React.PointerEvent) => void;
};

<!-- No existing codebase pattern uses setPointerCapture/requestAnimationFrame. -->
<!-- The authoritative implementation reference is PITFALLS.md Pitfall 1 (lines 13-34) -->
<!-- and Pitfall 2 (lines 39-67). The ref+effect imperative pattern analog is -->
<!-- ContextualPrompt.tsx lines 39-44 (useRef + imperative DOM call). -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing behavior tests for useDrag</name>
  <files>src/ui/useDrag.test.tsx</files>
  <read_first>
    - src/ui/useDrag.test.tsx (does not exist yet — create it)
    - .planning/research/PITFALLS.md (Pitfalls 1, 2, 12 — the authoritative behavior spec)
    - .planning/phases/15-window-manager/15-PATTERNS.md (useDrag section, lines 148-209; hook signature lines 182-201)
    - src/ui/ContextualPrompt.tsx (useRef + useEffect imperative DOM analog, lines 39-44)
    - src/execution/mount.test.tsx (vitest + jsdom style: describe/it, document.createElement, afterEach cleanup)
  </read_first>
  <behavior>
    - Test "captures the pointer on handle pointerdown": render a host component that uses useDrag with an elementRef on a window div; fire a pointerdown on the handle; assert the handle element's setPointerCapture was called with the event's pointerId (spy on Element.prototype.setPointerCapture).
    - Test "writes position imperatively without React state during move": after pointerdown, fire pointermove; assert the moved element's inline style (style.transform OR style.left/style.top) changed; assert onCommit was NOT called during move.
    - Test "commits final position to state exactly once on pointerup": pointerdown → pointermove → pointerup; assert onCommit called exactly once with numeric (x, y).
    - Test "clamps to viewport edges": drag far past the right/bottom edge (large pointermove deltas); assert the committed x ≤ window.innerWidth - elementWidth and y ≤ window.innerHeight - elementHeight, and x ≥ 0, y ≥ 0.
    - Test "does not preventDefault on the window body": assert handlePointerDown calls preventDefault on the handle event, but a pointerdown dispatched on the body content does NOT have its default prevented (focus-not-stolen guard, Pitfall 12).
    - Test "releases pointer capture on pointerup": spy on Element.prototype.releasePointerCapture; assert it is called with the pointerId on pointerup.
  </behavior>
  <action>
    Create src/ui/useDrag.test.tsx with a vitest describe block "useDrag — pointer-capture drag". Use jsdom: build a real host React component (via @testing-library/react render) that calls useDrag and spreads handlePointerDown onto a `.titlebar-handle` element nested in a `.window-chrome` element with the elementRef on the outer div. Drive pointer events with fireEvent.pointerDown/pointerMove/pointerUp (or userEvent.pointer). Spy on Element.prototype.setPointerCapture and releasePointerCapture with vi.spyOn. Stub elementRef element's getBoundingClientRect to return a fixed width/height (e.g. 320x240) so the clamp math is deterministic, and set window.innerWidth/innerHeight via Object.defineProperty for the clamp assertions. Mock requestAnimationFrame to run synchronously (vi.stubGlobal or a flush helper) so the imperative style write is observable in the test tick. All test-double names neutral (handle, win, host) — no banned tokens. Run the suite; ALL new tests MUST FAIL (useDrag.ts does not exist). Commit: `test(15-01): add failing useDrag pointer-capture + clamp tests`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/useDrag.test.tsx 2>&1 | grep -Eq "Cannot find module|fail|FAIL|No test files? found|useDrag.ts" &amp;&amp; echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/useDrag.test.tsx exists and contains the string `setPointerCapture`
    - src/ui/useDrag.test.tsx contains the string `onCommit`
    - src/ui/useDrag.test.tsx contains a clamp assertion referencing `innerWidth` or `innerHeight`
    - `npx vitest run src/ui/useDrag.test.tsx` exits non-zero (RED — implementation absent)
    - No banned token (`synthesi*`, `AI`, `llm`, `generate`, `fake`, `mock`) appears in the test file
  </acceptance_criteria>
  <done>useDrag.test.tsx exists with 6 behavior tests covering pointer capture, imperative-move, single-commit, viewport clamp, no-body-preventDefault, and capture-release; the suite fails because useDrag.ts is not implemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement useDrag to pass the tests</name>
  <files>src/ui/useDrag.ts</files>
  <read_first>
    - src/ui/useDrag.test.tsx (the RED spec written in Task 1 — implement against it)
    - .planning/research/PITFALLS.md (Pitfall 1 lines 21-25 for setPointerCapture; Pitfall 2 lines 46-58 for rAF + imperative write + commit-on-up)
    - .planning/phases/15-window-manager/15-PATTERNS.md (lines 162-209: rafId ref, hook signature, pointer-capture pattern)
    - src/ui/ContextualPrompt.tsx (useRef pattern, lines 39-44)
  </read_first>
  <action>
    Create src/ui/useDrag.ts exporting `interface UseDragOptions` (elementRef, initialX, initialY, onCommit) and `function useDrag(options)` returning `{ handlePointerDown }`. Implementation per CONTEXT Decision 2 and PITFALLS Pitfalls 1/2/12:
    - Hold a `rafId = useRef<number>(0)` and refs for the drag-start pointer coords, the element's start x/y, and the live x/y.
    - `handlePointerDown(e)`: call `e.preventDefault()` (handle only — never the body), `e.currentTarget.setPointerCapture(e.pointerId)`, record start pointer + start position, toggle a `.desktop--dragging` class on the desktop root (query `document.querySelector(".desktop")` and add the class; remove on pointerup) for the user-select:none surface guard, and attach `pointermove`/`pointerup`/`pointercancel` listeners to `e.currentTarget` (the captured handle), NOT to window.
    - On `pointermove`: compute deltas, compute clamped x/y (clamp so the element stays fully in [0, innerWidth-width] × [0, innerHeight-height] using elementRef.current.getBoundingClientRect for width/height), `cancelAnimationFrame(rafId.current)` then `rafId.current = requestAnimationFrame(...)` writing `elementRef.current.style.transform = "translate(" + x + "px," + y + "px)"` (or style.left/top). Do NOT call setState/onCommit here.
    - On `pointerup`/`pointercancel`: `cancelAnimationFrame(rafId.current)`, `e.currentTarget.releasePointerCapture(e.pointerId)`, remove the `.desktop--dragging` class, detach the listeners, and call `onCommit(clampedX, clampedY)` exactly once.
    - Wrap `handlePointerDown` in `useCallback` with stable deps (elementRef, onCommit, initialX, initialY) per the useCallback convention in 15-PATTERNS.md.
    Use only neutral identifiers. No banned tokens, including comments. Run the suite to GREEN. Then run the full suite + tsc. Commit: `feat(15-01): implement useDrag pointer-capture drag with viewport clamp`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/useDrag.test.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/useDrag.ts contains `export function useDrag`
    - src/ui/useDrag.ts contains `setPointerCapture` and `releasePointerCapture`
    - src/ui/useDrag.ts contains `requestAnimationFrame`
    - `npx vitest run src/ui/useDrag.test.tsx` exits 0 (all GREEN)
    - `npx tsc --noEmit` exits 0
    - `grep -RInE 'synthesi|[^a-z]AI[^a-z]|llm|generate|fake|mock' src/ui/useDrag.ts` returns no match (hygiene)
  </acceptance_criteria>
  <done>useDrag.ts implements pointer-capture drag with rAF imperative writes, viewport clamp, single onCommit on pointerup, preventDefault only on the handle; all 6 tests pass; tsc clean; hygiene clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer events → DOM style writes | User-driven pointer input drives imperative style mutation on the window element; no untrusted code crosses here (host code only) |
| drag handle → captured subtree | Pointer capture routes events across the generated-app subtree boundary, but useDrag only writes geometry, never reads app content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of Service | pointermove handler | mitigate | rAF-coalesced single style write per frame (Pitfall 2) prevents per-event reconciliation thrash that would jank the page |
| T-15-02 | Tampering | window position | accept | Drag only mutates host-owned geometry style; clamp bounds the value to the viewport; no persistence in Phase 15 so no stored-state tampering surface |
| T-15-03 | Information Disclosure | pointer capture across app subtree | accept | Capture routes pointer events to the host handle but useDrag reads no app DOM/content; the generated app is the user's own code (v1 threat model) |
</threat_model>

<verification>
- `npx vitest run src/ui/useDrag.test.tsx` — all useDrag tests pass.
- `npx vitest run` — full suite still ≥ 570 tests, all green (useDrag is additive; no existing file modified).
- `npx tsc --noEmit` — 0 errors.
- Hygiene: no banned token in useDrag.ts or useDrag.test.tsx.
</verification>

<success_criteria>
- useDrag captures the pointer on handle pointerdown and releases on pointerup.
- Position is written imperatively via rAF during move; React state (onCommit) fires once on pointerup.
- The moved element is clamped to the viewport on every committed position.
- preventDefault is called only on the handle, never the window body (focus survives).
- Full suite green, tsc clean, hygiene clean.
</success_criteria>

<output>
After completion, create `.planning/phases/15-window-manager/15-01-SUMMARY.md`.
</output>
