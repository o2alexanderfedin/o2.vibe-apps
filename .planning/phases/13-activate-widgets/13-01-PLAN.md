---
phase: 13-activate-widgets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/execution/instantiate.ts
  - src/execution/delegated.tsx
  - src/execution/loader.ts
  - src/execution/widgetPrewarm.ts
  - src/execution/delegatedWidgets.test.tsx
  - src/execution/widgetPrewarm.test.tsx
autonomous: true
requirements:
  - WIDGET-06
note: >
  Authored + executed inline by the orchestrator after the planning subagent hit the
  account weekly usage limit. Single plan; design fully specified in 13-CONTEXT.md.

must_haves:
  truths:
    - "instantiateDelegated injects useWidget into the delegated module scope (NULL_USE_WIDGET default)"
    - "loader prewarms a delegated app's @widget deps and threads makeUseWidget into instantiateDelegated"
    - "prewarmWidgets enforces MAX_WIDGET_DEPTH; over-deep types are isolated (absent from the map)"
    - "a delegated app with no @widget mounts byte-identically (backward compat)"
    - "a throwing widget is isolated by its WidgetErrorBoundary; the parent app survives"
---

# Phase 13 Plan 01: Activate Widget Composition (WIDGET-06)

Wire the dormant widget machinery into the delegated render path. Three threading points
(`instantiateDelegated` useWidget param, loader `instantiateDelegatedWithWidgets` prewarm+inject,
used at both delegated sites) + a composition-depth cap in `prewarmWidgets`. Isolation reused
as-is (`wrapWidget` = WidgetShell + WidgetErrorBoundary). See 13-CONTEXT.md for the full design.

## Tasks
1. Export `NULL_USE_WIDGET` (instantiate.ts).
2. Thread `useWidget` into `instantiateDelegated`'s `new Function` params (delegated.tsx).
3. Add `instantiateDelegatedWithWidgets` (prewarm → inject → mount); use at both delegated sites (loader.ts).
4. Add `MAX_WIDGET_DEPTH` (8) depth cap to `prewarmWidgets` (widgetPrewarm.ts).
5. Tests: `delegatedWidgets.test.tsx` (e2e render-through-loader, throwing-widget isolation, no-widget backward compat) + depth-cap test in `widgetPrewarm.test.tsx`.

## Verification
tsc 0; full suite green (≥548 + new); build clean (no sourcemaps); hygiene+csp green; browser
no-regression smoke (Weather/Currency still fetch real data).
