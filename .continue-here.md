# Continue Here — Vibe App Store

**Updated:** 2026-06-26
**Status:** **v1.1 "Real & Robust" SHIPPED** (all 5 phases merged, milestone audited + archived + tagged `v1.1`, pushed). Clean checkpoint — nothing in flight.

## Position
- Branch: `develop` (synced to origin; clean except untracked `design/`). Latest: `66824aa`.
- Tags: `v0.1.0`, `v1.0`, **`v1.1`** (all pushed).
- **552 tests** green · `tsc` 0 · build clean (no source maps) · hygiene + CSP 9/9.
- `develop` was NOT merged to `main` — the release to main is a deliberate, user-owned step (not done).

## THE NEXT STEP (after `/clear`)
```
/gsd-new-milestone        (start the next milestone)
```
or `/gsd-quick <task>` for ad-hoc work. Strong candidate for the next milestone: the **deferred Anthropic-key / `<iframe sandbox>` security story** (HARD-01) — see memory [[key-exfil-deprioritized-v11]].

## What shipped this session — v1.1 (Phases 9–13)
| Phase | Delivered | Merge |
|---|---|---|
| 9 Richer Storefront (STORE-01/02) | Persist displayName/prompt/createdAt (additive); real card names; "Your most-opened" row. Browser UAT 9/9. | `7dd8b43` |
| 10 Widget Schema & Key (WIDGET-07/08) | Typed WidgetRecord/HandlerRecord; registryKey everywhere + collision audit. | `3b83cf7` |
| 11 Reliability Hardening (RELY-01/02/03) | Lenient zod/mini validate-at-merge (keep-prior); no-op unknown actions; zero extra round-trips. | `8e10317` |
| 12 Network-Data Path (DATA-01..04) | Host-brokered fetchData keyless allowlist + CSP + TTL; **real Weather + Currency, live-CORS-verified**; 2 smoke-found bugs fixed. | `de9ce2b` |
| 13 Activate Widget Composition (WIDGET-06) | useWidget wired into delegated scope; @widget sub-widgets isolated + depth-capped; backward-compatible. | `ab4f105` |

Milestone archive commit: `66824aa`.

## How it was run (process notes)
- Autonomous loop per phase: focused **codebase scout** → decision-rich **CONTEXT.md** (`--skip-research`) → **plan delegate** (await explicit `PLANS_READY`) → **execute+review delegate** → **independent gate verification** (tsc/test/build/hygiene) → **live browser smoke** for user-visible deliverables → `--no-ff` merge to develop + push.
- **Phase 9 lesson:** don't dispatch execute before the planning delegate fully terminates (planner overstepped into impl on the shared branch). Fixed from Phase 10 on — always wait for `PLANS_READY`.
- **Phase 12 lesson (reaffirmed):** the live browser smoke caught TWO integration bugs that 542 unit tests passed over (seeded-delegated instantiation routing; DelegatedShell `data-field` input capture). Both got regression tests. Smoke the real seeded→loader→render path, not just modules in isolation. [[verify-ui-visually]]
- **Phase 13 was implemented INLINE** by the orchestrator: the planning subagent hit the **account weekly usage limit** (resets Jun 29 7pm PT). Design was pre-resolved in 13-CONTEXT.md; no gate skipped. Budget for quota on future large autonomous runs.

## Key new architecture seams (for future work)
- **DelegatedShell** (`src/execution/delegated.tsx`): validates the merge via a lenient `zod/mini` schema from `initialState` (`src/execution/stateSchema.ts`); captures `[data-field]` inputs into the handler's `state`; `instantiateDelegated(js, useWidget)` injects the widget accessor.
- **Network data:** `src/data/dataBroker.ts` + `sourceManifest.ts` + `src/host/ttlCache.ts`; CSP `connect-src` = self + api.anthropic.com + open-meteo (x2) + api.frankfurter.dev (never `*`). Seeded delegated apps are flagged via `SEEDED_DELEGATED` in `src/apps/seeds.ts`.
- **Widgets:** `prewarmWidgets` has `MAX_WIDGET_DEPTH=8`; delegated apps get `useWidget` via loader `instantiateDelegatedWithWidgets`.
- New prod dep: `zod` (^4.4.3), imported as `zod/mini`.

## Standing conventions
- git-flow: `main`/`develop` + `.githooks/` (blocks direct develop/main commits). Feature branches → `--no-ff` merge to develop; merges/pushes use `--no-verify` AFTER independent full-suite verification. Tracking/handoff docs commit directly to develop with `--no-verify`.
- IoC/DI everywhere; TDD with real captured-Haiku fixtures; devtools-hygiene gate bans the mechanic lexicon across `src/**` + index.html; verify generated/record-driven UI **visually**.

## Resume
`/gsd-progress` for status, or `/clear` then `/gsd-new-milestone`. Memory: `~/.claude/projects/-Volumes-Unitek-B-Projects-o2-vibe-apps/memory/` (see [[milestone-v11-shipped]]).
