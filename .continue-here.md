# Continue Here — Vibe App Store

**Updated:** 2026-06-26
**Status:** **v1.0 MVP shipped + closed** (archived, tagged `v1.0`); **v1.1 "Real & Robust" milestone initialized** (research + requirements + roadmap done). Clean checkpoint — nothing in flight.

## Position
- Branch: `develop` (synced to origin; clean except untracked `design/`). Latest: `51fa459`.
- Tests **378** · `tsc` 0 · build clean (no source maps) · hygiene gate green. *(Milestone setup since then is docs-only — no code changed.)*

## THE NEXT STEP (after `/clear`)
```
/gsd-plan-phase 9          (or /gsd-discuss-phase 9 to clarify approach first)
```
**Phase 9 — Richer Storefront** (STORE-01 persist displayName/prompt/createdAt; STORE-02 "popular" row from `useCount`). First, lowest-risk phase. `/clear` first — phase planning wants a fresh context.

## What happened this session (recent → older)
1. **v1.1 "Real & Robust" milestone INITIALIZED** (merged `51fa459`). Scope (user picked all 4): network-data path, reliability hardening, richer storefront, activate widgets.
   - **Research:** 4 parallel agents + synthesis → `.planning/research/{STACK-v1.1,FEATURES,ARCHITECTURE,PITFALLS-v1.1,SUMMARY}.md`. Key calls: one new dep `zod/mini` (validate-at-merge); keyless CORS APIs **Open-Meteo + Frankfurter** for data; **host-brokered `fetchData(sourceId,params)`** (host builds URL from a curated allowlist; raw `fetch` stays shadowed; CSP widened to a finite keyless-origin set, never `*`).
   - **Requirements** (`REQUIREMENTS.md`): 12 reqs — **DATA-01..04, RELY-01..03, STORE-01..02, WIDGET-06..08** (continues v1.0 families).
   - **Roadmap** (`ROADMAP.md`): **Phases 9–13**, continuing from v1.0's Phase 8. Build order: **9 Storefront → 10 Widget Schema/Key → 11 Reliability → 12 Network-Data → 13 Widget Activation**. Hard constraints: **RELY(11) before DATA(12)**; **WIDGET-typing(10) before WIDGET-activation(13)**.
   - **⚠ USER STEER (recorded):** "Do not worry about Anthropic key shipped — I have ideas to apply after this milestone." → Phase 12 keeps the host-brokered keyless allowlist (needed for CORS) but treats it as a **feature necessity, not a key-exfil security gate**; don't gold-plate key-exfil defenses. See `REQUIREMENTS.md` DATA scope note + memory [[key-exfil-deprioritized-v11]].
2. **v1.0 MVP milestone CLOSED** (merged `6557182`, tag `v1.0` pushed): archived ROADMAP/REQUIREMENTS/audit → `milestones/v1.0-*`, `MILESTONES.md`, `RETROSPECTIVE.md`, PROJECT.md evolution (reqs → Validated), v1.0 phase dirs → `milestones/v1.0-phases/`.
3. **Quick task q08 — G1 cacheKey contract** (merged `c68a2b7`): `registryKey(kind, type, prompt?)` folds kind+prompt into the opaque key (app/widget can't collide; tweak variants key distinctly). `cacheKey(input)` kept as the primitive. Also reconciled `docs/vibeappstore.md` to built reality. **+ `.planning/BLUEPRINT-DELTA.md`** (the blueprint-vs-built gap-check that started it all).

## Key context
- **v1.1 = "make the marketplace real & robust"**: network apps actually fetch data, produced behavior is correct more often, storefront has depth, widget composition activates.
- Network-data design crux: **host-brokered allowlist** (NOT un-shadowing `fetch`). Reliability: **validate-at-merge** against `initialState` as schema, keep-prior on mismatch, no runtime-error round-trips.
- Widgets are **"activate, don't build"** — machinery exists (WIDGET-01..05) but dormant; `instantiateDelegated` injects no `useWidget` (the activation gap, Phase 13 = highest regression risk).
- git-flow: `main`/`develop` + `.githooks/` (blocks direct develop/main commits). Feature/chore branches → `--no-ff` merge to develop; merges/pushes use `--no-verify` AFTER independent full-suite verification. Pause-work handoffs commit directly to develop with `--no-verify`.
- Standing: IoC/DI everywhere; TDD with real captured-Haiku fixtures; devtools-hygiene gate bans the mechanic lexicon across `src/**`; verify generated UI **visually** ([[verify-ui-visually]]).

## Resume
`/gsd-progress` for status, or `/clear` then `/gsd-plan-phase 9`. Memory: `~/.claude/projects/-Volumes-Unitek-B-Projects-o2-vibe-apps/memory/`.
